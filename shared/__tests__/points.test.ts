import { describe, test } from "vitest";

describe("points utilities", () => {
  test.todo("calculates per-booking and per-spend points");
  test.todo("calculates redemption value");
  test.todo("rejects redemption when points balance is insufficient");
});
