import { describe, test } from "vitest";

describe("reference utilities", () => {
  test.todo("generates booking references with daily sequence");
  test.todo("generates member numbers with five-digit padding");
  test.todo("generates store order references");
});
