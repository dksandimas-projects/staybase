import { describe, test } from "vitest";

describe("pricing utilities", () => {
  test.todo("calculates base rate times nights");
  test.todo("applies breakfast, member, Senior/PWD, voucher, and points adjustments in the approved order");
  test.todo("never returns a negative total");
});
