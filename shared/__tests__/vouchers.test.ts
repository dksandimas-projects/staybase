import { describe, test } from "vitest";

describe("voucher utilities", () => {
  test.todo("rejects inactive, expired, capped, and room-type mismatched vouchers");
  test.todo("accepts unlimited vouchers and all-room vouchers");
  test.todo("calculates flat and percent discounts with a zero floor");
});
