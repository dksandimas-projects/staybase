# Env files
.env
.env.local
guest-app/.env
admin-app/.env
api/.env

# Build outputs
dist/
node_modules/
.vercel/
*.tsbuildinfo

# Legacy (renamed to project/)
.project/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
.env*

# Git worktrees checked out inside the repo (see scripts/docs-audit.mjs SKIP_DIRS).
# Without this a stray `git add -A` commits another branch's entire checkout as an
# embedded repository — which happened on 2026-08-02.
.worktrees/
