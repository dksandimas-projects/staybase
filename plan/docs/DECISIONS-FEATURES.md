# Feature & Product Decisions
> Requires: CLAUDE.md

Resolved product, feature scope, business rules, compliance, and UX decisions. Load this when building a specific feature to understand what was decided and why.

For architecture and stack decisions see `plan/docs/DECISIONS-ARCH.md`.

---

| # | Decision |
|---|---|
| 1 | Room photos: architectural renders for now — managed via Room Management and Settings |
| 2 | Payment flow Phase 1: manual screenshot upload — no payment gateway |
| 3 | Payment gateway Phase 2: PayMongo (deferred) |
| 4 | Admin accounts: DK creates initial; owner gets admin; front desk accounts by admin only |
| 5 | About page: Mission, vision, hotel story only — no team/owner section |
| 6 | Add to Calendar included on booking confirmation page |
| 7 | BIR receipts: NOT generated — system generates booking confirmation receipts only |
| 8 | Corporate rates: NOT shown on marketing page — inquiry-based or via `/corporate/book` |
| 9 | Corporate booking: `/corporate/book` reuses 4-step flow with flat rate (public) or access code (negotiated) |
| 10 | Guest intercom: QR-based, browser chat, no app, no login — quick requests configurable in Settings |
| 11 | Booking statuses: Pending → Payment Uploaded → Payment Confirmed → Confirmed → Checked In → Checked Out → Cancelled |
| 12 | Discounts: Senior Citizen 20% + PWD 20% — OSCA-mandated; guest uploads OSCA Card / PWD ID photo at booking Step 3; staff verifies or rejects in booking drawer; rejection restores full `totalPrice`, triggers discount-rejected email instructing guest to pay full amount at check-in |
| 13 | Promo vouchers: guest enters code at Step 3 of booking flow — admins manage campaigns on the Rates page; front desk sees applied voucher outcomes in booking operations |
| 13b | **Discount stacking order** (when multiple discounts apply on the same booking): **1.** Senior/PWD (20% subtotal) → **2.** Voucher (flat or percent, applied to post–Senior/PWD subtotal) → **3.** Spark Rewards member discount (percent, applied to post–voucher subtotal). Member discount only applies when the guest is signed in AND `settings/rewardsConfig.memberDiscountEnabled === true`. Implemented in `shared/utils/pricing.ts` `calculateBookingTotal()` — applied in the listed order, not cumulatively on the original subtotal. UI shows each discount as a separate line in the price summary. |
| 14 | Walk-in bookings: front desk creates manually from dashboard — source field captures origin |
| 15 | Housekeeping status: tracked per room as clean / dirty / in-progress — toggleable from dashboard |
| 16 | Intercom notification sound: Web Audio API only — no extra library — hosted in Firebase Storage |
| 17 | Intercom notification sound plays on every incoming guest message — not just the first per conversation |
| 18 | Intercom security model: physical QR gate + room ID from URL + name prompt — no login required |
| 19 | Reports: occupancy + revenue + bookings by source — Recharts — exportable PDF/CSV/XLSX |
| 63 | Data backup: single "Download Full Backup" button (admin-only) generates one multi-sheet XLSX covering all 8 operational collections — Bookings, Payments, Members, Store Orders, Store Catalog, Breakfast Selections, Vouchers, Corporate Inquiries; all queries run in parallel client-side via SheetJS |
| 73 | Additional onsite payments tracked in `bookings/{id}/payments` subcollection — append-only audit trail; outstanding balance computed client-side; covers discount rejections, points reversals, and Pay at Hotel confirmations; no editing or deletion allowed |
| 64 | DOT/RA 11862 compliance: guest registry fields (nationality, ID type + number, DOB, gender, address) collected at physical check-in via the Guest Registration Form PDF — not required at online booking step |
| 65 | DOT/RA 11862 compliance: guest registry records must be retained minimum 6 months — erasure requests cannot apply to this data within that window |
| 66 | RA 11862 (Anti-Trafficking): unaccompanied minor warning shown as informational banner in booking detail drawer — front desk must verify guardian presence at check-in; system does not block the booking |
| 67 | Guest ID photo captured by front desk at check-in via upload in booking detail drawer — stored in Firebase Storage (staff-only), `guestIdPhotoUrl` on booking document, embedded in Guest Registration Form PDF; if not yet uploaded, PDF shows blank placeholder box |
| 68 | Reports page organized into two tabs: Performance (occupancy, bookings by source) and Sales (all revenue streams consolidated) |
| 69 | Sales Report consolidates all three revenue streams — room bookings, breakfast add-ons, store orders — into one view with summary cards, charts, detail tables, printable PDF, and multi-sheet XLSX export |
| 70 | Sales Report PDF uses html2canvas to capture Recharts SVG charts + jsPDF autoTable for data tables — generated client-side, no API route needed |
| 71 | Sales XLSX export is multi-sheet: Summary, Bookings, Breakfast, Store Orders — one file covers all revenue data for the selected period |
| 72 | Breakfast kitchen prep report and store low-stock alerts remain as separate operational tools — their revenue figures appear in the Sales tab, not duplicated |
| 20 | Website content editable from Settings (homepage, about, corporate sections) — no full CMS |
| 21 | `isCorporate` and `corporateCode` set server-side — never trusted from client |
| 22 | Corporate codes and vouchers validated server-side via API route |
| 23 | Booking reference format: `{config.bookingRefPrefix}-YYYYMMDD-NNN` — generated server-side |
| 24 | Domain: `sparkinnbohol.com` — DK purchases as part of project |
| 25 | Data Privacy Act of 2012 (RA 10173) compliance required — hotel processes guest PII |
| 26 | Data retention: indefinitely — guests may request erasure via email to hotel |
| 27 | Privacy Policy and Terms of Service: dedicated `/privacy` and `/terms` pages on guest site — linked from footer, booking form, and emails where applicable |
| 28 | Consent checkbox required at booking Step 2 — links to `/privacy` and `/terms`, blocks submission if unchecked |
| 29 | Hotel owner/admin serves as Data Protection Officer (DPO) |
| 30 | Data breach notification: NPC within 72 hours if breach affects guest PII |
| 31 | Legal content (privacy policy body, cancellation policy, house rules) editable at runtime from Settings |
| 32 | Spark Essentials store accessible only via room QR scan — not from public website |
| 33 | Store orders linked to room ID and active booking (null if no active booking found) |
| 34 | Store payment methods: CoD, Add to Bill, GCash — independent of booking payment methods |
| 35 | "Add to Bill" = note for front desk to collect at checkout — does not auto-update booking totalPrice |
| 36 | Store stock: null = unlimited, 0 = out of stock, n = tracked quantity |
| 37 | Stock decremented on order confirmed (not placed) — restored if cancelled before confirmed |
| 38 | Store order status flow: Placed → Confirmed → Out for Delivery → Delivered; Cancelled is terminal at any point |
| 39 | Store item deleted with existing orders: soft-delete only (isActive: false) — never hard delete |
| 40 | Store order ref format: SO-YYYYMMDD-NNN — generated server-side |
| 41 | Spark Rewards Phase 1: auth, member profile, booking history, configurable points earning, configurable member discount, SR-XXXXX member card |
| 42 | Spark Rewards Phase 2: points redemption, tier system, tier-based perks — TBD |
| 43 | Guest auth: Google Sign-In + email/password — separate from admin auth, same Firebase project |
| 44 | Guest registration available post-booking (Step 4 prompt) and standalone at `/rewards` |
| 45 | Past anonymous bookings linked to member account by email match on registration |
| 46 | Points redemption is Phase 1 but admin-only — staff applies it manually from the booking detail drawer; guests cannot redeem online; redemption rate is configurable in Settings |
| 47 | Early check-in request: always available to members — not configurable on/off; sends tagged intercom message or email to front desk |
| 48 | Manual points adjustment by staff requires a reason — always logged to points history |
| 49 | Member account deletion triggers data erasure per RA 10173 right to erasure |
| 50 | `guests/` collection = staff only; `members/` collection = guest loyalty members |
| 51 | Breakfast add-on: per person per night — rate × numGuests × numNights |
| 52 | Breakfast shown at Step 1 as "Room Only" vs "Room + Breakfast" combined rate — not as a separate add-on at Step 3 |
| 53 | Breakfast rate locked at booking time — stored as `breakfastRate` on booking document |
| 54 | Silog selection: per guest per day — each guest can choose a different silog each morning |
| 55 | Silog menu fully configurable from Settings — hotel can add/remove/disable items |
| 56 | Silog selections entered by front desk in booking detail drawer at check-in — not by guest online |
| 57 | Silog choices appear on the guest registration form PDF — guests fill in at check-in |
| 58 | Daily kitchen prep report: counts of each silog needed for a given morning — printable |
| 59 | Breakfast globally enable/disable from Settings — hides option from booking flow when off |
| 60 | Homepage Services section (Tour Packages, Car Rentals) — display only, CTA links to Contact Us — no booking or pricing |
| 61 | Homepage Spark Rewards section — shows Join CTA for non-members, Welcome back for logged-in members |
| 62 | Services and Spark Rewards homepage sections editable from Settings → Website Content — sections hidden when disabled |
| 74 | Email acknowledgment: the booking submitted email acts as an acknowledgment/receipt submission, warning the guest that their booking and payment are under manual review and a final confirmation email will follow after verification |
| 75 | Breakfast pricing model: add-on only — booking flow shows "Room Only" vs "Room + Breakfast" toggle at Step 1, with `breakfastRate` charged per-person-per-night on top of the room rate. No `includedInRoomRate` field on `breakfastConfig`. The current implementation already handles the use case. If a future hotel client needs "breakfast always included" as a per-room differentiator, add it as a scoped feature then. *(Per audit W1.7, 2026-06-15)* |
| 76 | Contact form on `/contact` is in scope for Phase 1 — wire the existing form to a real `/api/contact` endpoint. Match the corporate inquiry pattern: Zod schema (strict), honeypot (`_hp`), Turnstile verification, rate-limit (5/min per IP), new `contactInquiries/{id}` Firestore collection, new `contactInquiryEmail` template sent to `settings/hotelConfig.supportEmail`. The current `setTimeout`-based fake success in `ContactPage.tsx` is removed. Full spec in `plan/features/CONTACT-INQUIRIES.md`. **Implementation deferred to Phase 1 (with the other 5 SEV-1s).** *(Per audit W1.8, 2026-06-15)* |
| 77 | `payment-confirmed` is a real state — set automatically by `handleAddPayment` when the running payment total ≥ `totalPrice` (in the same transaction that records the payment). Admin's "Confirm Payment & Booking" button then flips `payment-confirmed → confirmed`. Closes the gap where the state machine listed 7 statuses but the runtime only transitioned through 5. *(Per audit W1.1, 2026-06-15)* |
| 78 | Room block date range semantics — store as structured fields: `blockedFrom: Timestamp`, `blockedTo: Timestamp`, plus the existing `blockReason: string`. The booking creation transaction iterates active block ranges and rejects overlaps. The current `addRoomBlock` strings the date range into `blockReason` (lossy, dead data) — that implementation is replaced. Migration: backfill `blockedFrom`/`blockedTo` from existing string-encoded `blockReason` values. *(Per audit W1.2, 2026-06-15)* |
| 79 | `isCorporate` is server-authoritative — never trusted from the client. Client sends `corporateCode` only. Server looks up `corporateCodes/{code}` inside the booking transaction, validates `isActive` + not-expired + `usageCount < usageCap`, applies `ratePerRoomType` if valid, and sets `isCorporate: true` + `companyName` server-side. Invalid/missing code falls back to standard/corporate rate. Increment `usageCount` in the same transaction (currently never written). *(Per audit W1.3, 2026-06-15)* |
| 80 | Store stock is decremented on `confirmed`, not on `placed` (matches `DECISIONS-FEATURES.md #37` and resolves the contradiction with `STORE-MANAGEMENT.md`). Changes: (a) `handleCreateStoreOrder` no longer decrements stock; (b) new `handleConfirmStoreOrder` API decrements inside a transaction; (c) admin "Confirm" action routes through the new endpoint; (d) `handleCancelStoreOrder` only restores stock that was decremented at confirmation. Backfill: existing `placed` orders in production are migrated. *(Per audit W1.5, 2026-06-15)* |
| 81 | Vouchers live in Rates page, not Settings page. Rates is admin-only; Settings is admin-only. Vouchers need front-desk access for walk-in redemptions — front desk can apply voucher codes during walk-in creation (validated server-side), but campaign management (create, disable, view codes) is admin-only. The `SETTINGS.md` section count is updated to drop the Vouchers entry; the Rates page already owns the CRUD. *(Per audit W1.6, 2026-06-15; corrected per audit FD-03, 2026-07-17)* |
| 82 | Booking Confirmation Receipt PDF is in scope for Phase 1 — add `printBookingReceiptPDF(booking)` next to `printRegistrationPDF` in `admin-app/src/pages/BookingsPage.tsx`. PDF includes: booking ref, guest info, room, dates, nights, guests, rate, applied discounts (senior/PWD, voucher, member), payment ledger with Total Collected + Outstanding Balance, hotel branding header. Reused as email attachment in the `booking-confirmed` email template. Closes the 1.7 SEV-1 finding. *(Per audit W1.9, 2026-06-15)* |
| 83 | Check-in reminder cron adds an idempotency marker — `reminderSentAt: Timestamp` written to the booking in the same transaction that sends the email. Cron query filters `where("reminderSentAt", "==", null)` so retries do not double-send. Closes the at-least-once delivery risk. *(Per audit W1.10, 2026-06-15)* |
| 84 | Firestore `bookings.checkIn` and `bookings.checkOut` are always stored as `Timestamp.fromDate(jsDate)`, never as raw `Date` or ISO string. Read paths use `data.checkIn.toDate()`. Remove the dead `adminDb.doc(...).firestore.valueType` ternary at `bookings.ts:281` and the `parseDateString` helper. The `TYPES.md §Booking` `checkIn`/`checkOut: Timestamp` declaration becomes accurate. *(Per audit W1.11, 2026-06-15)* |
| 85 | `AdminContext.members` is sourced from a real `onSnapshot(collection(db, "members"), …)` listener — not a `useState` mock with one hardcoded fake entry. The Members page becomes functional: list, detail drawer, manual points adjustment (debit/credit), pointsHistory append, suspend/activate via real API. Closes 1.2 SEV-1 #5, 1.3 SEV-2 #2d, 1.5 SEV-1 (Members). *(Per audit W1.12, 2026-06-15)* |
| 86 | No developer's personal name in any default. The seeded `settings/hotelConfig` payment-method defaults `accountName` field (e.g. `"BDO: 001234567890 (Daniel Sandimas)"`) is removed. Replaced with empty string or `config.legalName + " — " + config.supportEmail`. This is a hard-rule violation per `GOTCHAS.md §Never log PII` and `WHITE-LABEL.md`. **Implemented in `86bb50b` (Phase 11.6 Batch 1)** *(Per audit W1.13, 2026-06-15)* |
| 87 | Honeypot inputs are always inside the `<form>` element, hidden via CSS (absolute position + opacity 0). The current `CorporateStaysPage.tsx:567-578` honeypot lives outside the form (works by accident because of React-controlled state) — moved inside to match the booking-flow pattern. **Implemented in `56bf8d4` (regression test; implementation already correct)** *(Per audit W1.14, 2026-06-15)* |
| 88 | Housekeeping cycle order: `clean → dirty → in-progress → clean` (per `DASHBOARD-OVERVIEW.md` spec). Current `AdminContext.tsx:452-459` cycles `clean → in-progress → dirty → clean` — wrong middle state. Fix the cycle and surface all 3 states (`clean | dirty | in-progress`) in the dashboard housekeeping button. **Implemented in `7266c34` (Phase 11.6 Batch 1)** *(Per audit W1.15, 2026-06-15)* |
| 89 | Overlapping bookings by the same email are **allowed** — repeat guests can book multiple rooms (or back-to-back stays) in overlapping date ranges. The room conflict check prevents the same room from being double-booked; cross-room overlap is fine. Spec was silent; chosen default is the more permissive option. *(Per audit W2.1, 2026-06-15)* |
| 90 | Member discount server-side enforcement: client sends nothing member-related in the body. If the request includes an `Authorization: Bearer <firebaseIdToken>` header, the server verifies it, looks up the member by `authUser.uid`, and applies the 3rd stacking discount step per `DECISIONS-FEATURES.md #13b`. If no header, `memberId` is `null` and no member discount. Any client-supplied `memberDiscount` / `memberDiscountPct` field is rejected by the Zod schema (strict mode). *(Per audit W2.2, 2026-06-15)* |
| 91 | Past-booking linkage beyond email is **deferred to Phase 2**. `linkBookingsByEmail` runs at registration only. A future "claim past stays" UI lets the member manually add past bookings not caught by email match. *(Per audit W2.3, 2026-06-15)* |
| 92 | Early check-in request when the member has multiple upcoming bookings: pick the **first confirmed or checked-in booking by `checkIn` ascending**. If only one upcoming booking exists, skip the picker. If none, show an error "no upcoming booking — book a stay first". The flow is implemented as a small picker modal (1-3 booking cards) when count > 1. **Implemented in `2d41ed7` (Phase 11.6 Batch 2)** *(Per audit W2.4, 2026-06-15)* |
| 93 | No member-registration welcome email for Phase 1. The "Welcome Gift" marketing copy on `RewardsLandingPage.tsx:281-292` is removed (or reworded to "Welcome to the program" without a tangible promise). A transactional welcome email can be added in a future phase. **Implemented in `6796fc6` (Phase 11.6 Batch 2)** *(Per audit W2.5, 2026-06-15)* |
| 94 | Multiple concurrent calls in admin inbox: **second wins**. Accepting a new call writes `status: "ended", endedAt: serverTimestamp()` to the old call doc (if it was active) before accepting the new one. The previous guest's UI sees the call end naturally via the snapshot listener. **Implemented in `4f6aa92` (Phase 11.6 Batch 1)** *(Per audit W2.6, 2026-06-15)* |
| 95 | Intercom thread auto-archive on checkout: `handleCheckoutBooking` sets `intercoms/{roomNumber}.resolved: true` in the same transaction that flips the booking status. Staff can reopen from the admin Inbox by clicking the resolved thread and toggling `resolved: false`. **Implemented in `e787594` (Phase 11.6 Batch 1)** *(Per audit W2.7, 2026-06-15)* |
| 96 | Cancellation messages are rendered as a distinct visual state: greyed-out card with a "Cancelled" header in both the guest's chat history and the admin Inbox. The `isCancelledOrder: true` flag is set on the message doc by the cancellation handler. **Implemented in `bc8e01f` (Phase 11.6 Batch 1)** *(Per audit W2.8, 2026-06-15)* |
| 97 | Notification sound mute: persisted in `localStorage` per-staff (key: `notif_sound_muted`). A `Bell` / `BellOff` icon button in the inbox header toggles. `playNotificationSound` early-returns when muted. Per-staff Firestore setting is overkill for Phase 1. *(Per audit W2.9, 2026-06-15)* |
| 98 | `calls/{roomId}` retention: **delete after 30s grace** from when both sides have observed `status: "ended"`. A `setTimeout` in `stopGuestCallResources` (guest) and `cleanupAdminCall` (admin) issues `deleteDoc(callRef)` after the grace period. The `calls` collection is bounded; no doc accumulates forever. **Implemented in `67beb4c` (Phase 11.6 Batch 1)** *(Per audit W2.10, 2026-06-15)* |
| 99 | LOU (Letter of Undertaking) for corporate chargeback bookings: **no file upload in Phase 1**. The current `CorporateBookingPage.tsx:1230-1255` upload widget is replaced with a UI note "our accounts team will email you within 24 hours to request your LOU". The booking drawer gets an `louReceived: boolean` toggle (default `false`) so staff can track receipt manually. Spec was silent on the LOU flow; deferring is safe. **Implemented in `f3054b1` (Phase 11.6 Batch 2)** *(Per audit W2.11, 2026-06-15)* |
| 100 | Corporate bookings never accept promo vouchers. The `CorporateBookingPage.tsx:200` `voucherDiscount: 0` hardcode is correct. Negotiated rates (per W2.13) are already a discount; stacking voucher on top would be double-dipping. *(Per audit W2.12, 2026-06-15)* |
| 101 | Negotiated corporate rate model: **flat rate per room type** via `corporateCodes/{code}.ratePerRoomType: Record<roomType, rate>`. The UI label is "Negotiated rate applied" — the previous `(X% additional discount applied)` wording is removed. If `ratePerRoomType[roomType]` is undefined, fall back to `room.corporateRate` and log a warning. **Implemented in `c8282ed` (Phase 11.6 Batch 2)** *(Per audit W2.13, 2026-06-15)* |
| 102 | Converted-inquiry bookings get a `linkedInquiryId?: string` field on the booking document (add to `shared/types/index.ts §Booking`). The original inquiry gets a back-reference `convertedBookingId: string`. Both fields are set in the same transaction when staff clicks "Convert to Booking". **Implemented in `551760c` (Phase 11.6 Batch 2)** *(Per audit W2.14, 2026-06-15)* |
| 103 | Booking source for a converted inquiry is **`"corporate"`** (not `"walk-in"`). The channel is what matters for revenue reporting. `BookingSource` enum already has `walk-in | online | corporate | facebook` per `TYPES.md` — `"corporate"` is the right pick. *(Per audit W2.15, 2026-06-15)* |
| 104 | **Email template extensions (Wave 4 #4)** — 7 new transactional email templates are in scope, raising the total from 8 to 15. New templates: `voucher-issued` (guest, when admin creates a voucher with a guest email), `store-order-placed` / `-confirmed` / `-out-for-delivery` / `-delivered` / `-cancelled` (guest, fire on each store order status transition), `staff-new-booking` (admin, fire on new online booking), `staff-new-payment` (admin, fire on payment proof upload). Closes the "guest misses order update after closing the intercom tab" gap and the "staff miss new booking when logged out" gap. Full spec in `plan/features/EMAIL-AUDIT-EXTENSIONS.md`. **Implementation deferred to Phase 1.5.** *(Per audit W4.4, 2026-06-15)* |
| 105 | **Wave 3 — UI/UX spec closures (12 questions, all approved 2026-06-15)** — answers 12 minor spec gaps so the polish work in Phase 11.6 has a clear target. Items: booking payment methods live in Rates (#105a), Spark Rewards tab is admin-only with explicit role guard (#105b), Room Types migrates from localStorage to `settings/hotelConfig.roomTypes` (#105c), reports backup button role guard is page-level (#105d), Reports Performance tab swaps "Avg Length of Stay" for "Avg Occupancy" + "Busiest Room Type" per `REPORTS.md` spec (#105e), AboutPage "Brand Promise" banner kept + `STATIC-PAGES.md` updated (#105f), CorporateStaysPage "Integration Process" + "Retreat CTA" sections kept + `STATIC-PAGES.md` updated (#105g), PrivacyPage + TermsPage include global `<Navbar />` for consistency (#105h), PrivacyPage section 3 heading renamed to "How Long We Keep It" per spec wording (#105i), new `config.rewardsName: "Spark Rewards"` for white-label (#105j), new `config.termsLastUpdated` field (#105k), 404 page renders a tiny `<p>v{VERSION}</p>` (resolves the spec contradiction) (#105l). *(Per audit W3.1–W3.12, 2026-06-15)* |
| 106 | **Wave 4 — Infrastructure closures (8 remaining questions, all approved 2026-06-15)** — answers the spec/build questions for the cross-cutting work. Items: corporate rate lookup fails soft to `room.corporateRate` with a console warning if `ratePerRoomType[roomType]` is undefined (#106a), `index.html` static OG meta is templated at Vite build time via a small `transformIndexHtml` plugin reading `hotel.config.ts` (#106b), `WHITE-LABEL.md` schema is updated to match the actual `HotelConfig` fields used in `hotel.config.ts` (#106c), Storage rule for `store-orders/{roomNumber}/payment-proof/` uses `roomNumber` (matching the existing client upload path) (#106e), CORS `Allow-Credentials: true` is removed; explicit allowlist from `config.domain` + `config.adminDomain` + localhost dev origins (#106f), `prompt("Enter cancellation reason:")` is replaced with a small input field in the existing cancellation drawer (#106g), Privacy + Terms pages keep the `Republic of the Philippines` hardcoded copy in legally-relevant sections but expose `config.applicableLaw` for the rest (#106h), `Spark Inn Hotel Corp` hardcoded `accountName` fallback becomes `config.legalName` (#106i). *(Per audit W4.1–W4.9, 2026-06-15)* |
| 107 | **Admin app gets a responsive mobile layout (Phase 11.7).** The admin app was built desktop-first for an assumed minimum of 768px tablet width — below 768px the fixed 240px sidebar consumes the entire viewport, the header overflows, tables force horizontal scrolling, and drawers get clipped. Phase 11.7 adds a three-mode responsive sidebar (mobile slide-in / tablet icon-only / desktop full), a compact sticky mobile header, full-screen bottom-sheet drawers with sticky action footers, a `DataTable` mobile card view via a new `renderMobileCard` prop, and per-page rules for the 11 existing pages. Replaces all `alert()` / `confirm()` / `prompt()` calls with inline forms + toast (extends #106g to the whole admin app). No PWA, no offline, no install prompt (per `DECISIONS-ARCH.md #47`). Estimate: ~6 dev days. Full spec: `plan/features/ADMIN-MOBILE.md`. *Identified 2026-06-17 during mobile UX assessment.* **Implemented 2026-06-18 on `dev` at v0.90.0** — branch `feature/phase-11.7-admin-mobile`, 9 commits, 9 new test files (94 tests, 342/342 total green), build + typecheck clean. P0 foundations + Drawer/Modal/Toast + DataTable mobile card view + Bookings filter/cleanup + Intercom single-pane + Settings tab bar + focus trap are all shipped. P3 manual QA matrix (18 screens × 6 breakpoints) + real-device testing deferred to post-staging. |
| 108 | **Booking payment methods are a fully dynamic, admin-managed list** (Settings → Payment Methods, per `plan/features/SETTINGS.md §2`). Each method owns its `method` key, `label`, `accountName`, `accountNumber`, `qrUrl`, and `isEnabled` flag. QR images are uploaded to Firebase Storage at `assets/payment-methods/{method}/{filename}` (public read, staff write — new `match /assets/payment-methods/{method}/{fileName}` rule). "Pay at Hotel" is just another method (`method: "pay-at-hotel"`, `isEnabled: true/false`); the previous standalone `payAtHotelEnabled` global toggle was removed in favour of the per-method `isEnabled` flag. The schema is open (`method: string`) so admins can add custom methods, but the UI surfaces two policies: (a) a persistent callout lists the supported methods (`SUPPORTED_PAYMENT_METHODS` in `shared/constants`); (b) `UNSUPPORTED_PAYMENT_METHODS` (currently just `pesonet`) triggers a two-step save confirm. The schema is not hard-blocked, so future business changes don't require a code deploy — the friction is policy, not enforcement. The `Booking.paymentMethod` type was already a string union (`"pay-at-hotel" | "gcash" | "paypal" | string`), so the booking record itself is unchanged — only the admin's edit surface + the guest booking page render from the dynamic list. Migration: pre-feature docs that carry `bookingPaymentMethods[]` (with the old `accountInfo` field) are auto-reshaped on the first `AdminContext` snapshot into the new `paymentMethods[]` shape; the legacy key is left in place on the doc since `setDoc(..., { merge: true })` cannot remove fields. *(Per feature request, 2026-07-01, implemented in `feature/payment-methods`.)* |
| 109 | **Protected payment methods** — entries whose keys are in `PROTECTED_PAYMENT_METHODS` in `shared/constants` cannot be deleted from the admin Payment Methods tab. Current protected keys are `["pay-at-hotel", "add-to-bill"]`. Defense in depth: the row in `SettingsPage.tsx` hides Delete and shows a blue "Required" pill, and `AdminContext.deletePaymentMethod` blocks direct calls. A one-shot backfill appends missing protected methods to existing `paymentMethods[]`. `pay-at-hotel` is protected because walk-in creation, corporate inquiry conversion, and booking step-3 fallback default to it. `add-to-bill` is protected because booking checkout folios and reports derive billed store charges from `storeOrders.paymentMethod === "add-to-bill"`. |
| 110 | **In-room store payment methods are managed only from Settings → Payment Methods** — `settings/hotelConfig.paymentMethods[]` is now the single source for booking, store, and corporate payment visibility. The prior `settings/storeConfig.useBookingPaymentMethods` toggle and `settings/storeConfig.paymentMethods[]` fields are legacy and ignored by checkout. `cod` and `add-to-bill` are backfilled into `paymentMethods[]` as store-only methods (`isEnabled: false`, `showInStore: true`, `showInCorporate: false`). `getEffectiveStorePaymentMethods(hotelConfig.paymentMethods)` filters by `showInStore !== false`, excludes `pay-at-hotel`, and is called by both guest checkout and `/api/store/create-order` inside the Firestore transaction. Any non-`cod`/non-`add-to-bill` method requires a payment proof screenshot. Rationale: hotels should not configure GCash/Maya/PayPal details on two tabs, and Add to Bill belongs in the same list as a protected folio method. *(Supersedes the earlier opt-in toggle model from #110; finalized 2026-07-03.)* |
| 111 | **Each payment method owns three independent surface visibility switches** — `isEnabled` (regular booking), plus optional `showInStore` and `showInCorporate` booleans. The Settings → Payment Methods row renders three inline pill toggles per method (Booking, Store, Corp). Missing `showInStore` / `showInCorporate` values are read permissively for custom legacy methods; built-ins are normalized to sensible defaults (`pay-at-hotel` store-hidden, `cod` / `add-to-bill` store-visible and booking/corporate-hidden). `CorporateBookingPage.tsx` adds `showInCorporate !== false` to its existing `gcash`/`maya`/`bank` allowlist. The Zod schema marks both new fields optional. Rationale: one global on/off switch is too coarse for hotels that want, e.g., PayPal on the booking site but not in the in-room store. *(Per feature request, 2026-07-03, implemented in `feature/per-method-surface-toggles`.)* |
| 112 | **Google Sign-In is authentication only; Spark Rewards enrollment requires explicit consent.** The `/signin` Google button never creates or enrolls `members/{uid}` by itself. The `/signup` Google path first requires the Privacy Policy / Terms checkbox, then calls `registerCurrentMember()` as the explicit enrollment step. Signed-in non-members can join from Rewards/Profile surfaces through the same helper, and registration failures are surfaced inline instead of being swallowed. This closes the RA 10173 consent asymmetry from GA-07 while preserving Google as a sign-in provider. *(Per `AUDIT-GUEST-APP-2026-07-07.md` GA-07, implemented in `fix/audit-ga-sev2`.)* |
| 113 | **Homepage featured room cards do not expose live per-room operational status.** The homepage cards are type-driven marketing cards sourced from `settings/websiteContent.homepage.featuredTypeValues` and `settings/hotelConfig.roomTypes[]`; they must not label an occupied physical room as "Blocked" or leak live operational state. Date-aware availability remains in the booking flow and `/api/rooms/availability`. *(Per `AUDIT-GUEST-APP-2026-07-07.md` GA-13, implemented in `fix/audit-ga-sev3`.)* |
| 114 | **Self-service cross-provider auth linking is deferred to Phase 2.** Phase 1 handles Firebase `auth/account-exists-with-different-credential` with provider-conflict copy that tells guests to use their existing sign-in method first. The full `linkWithPopup(googleProvider)` prompt and booking-email mismatch prompt remain Phase 2 work because they need additional UX/state around pending credentials and account ownership. *(Per `AUDIT-GUEST-APP-2026-07-07.md` GA-11, implemented in `fix/audit-ga-sev3`.)* |
| 115 | **BIR/VAT scope decision** — The system does not generate or print official BIR-registered receipts or sales invoices. Official receipt (OR) issuance remains a manual or external process handled by the hotel front desk. However, to assist the hotel's accountant in monthly filings, the system calculates and exports the tax breakdown (12% VAT, VATable Sales, VAT-Exempt Sales for OSCA-mandated Senior/PWD bookings, and VAT Amount) client-side in the reports and XLSX exports. *(Per finance audit FIN-06, resolved 2026-07-11)* |
| 116 | **Direct-paid store orders join the shared collections ledger on delivery.** The staff-only delivery transition atomically marks the order delivered and appends one deterministic tender under that order. COD is recorded as Cash; configured direct methods retain their method key. Add to Bill creates no store tender because it remains an unsettled booking-folio charge. Store tenders stay outside `bookings/{id}/payments` so they reconcile store revenue and Daily Close without incorrectly reducing the room balance. Store revenue is recognized on `deliveredAt` (legacy fallback: `createdAt`). *(Per Finance Lifecycle Audit FL-05, resolved 2026-07-13.)* |
| 117 | **Early departure retains the contracted booking total.** Checkout still shortens `checkOut` and `numNights` to release inventory accurately, but it does not issue an automatic refund or reduce `totalPrice`. The locked breakdown is rebuilt around the consumed nights/add-ons, preserves the booking's deductions, and adds an explicit “Early departure — original total retained” adjustment so receipts remain mathematically transparent. Any discretionary refund uses the existing admin refund ledger. *(Per Finance Lifecycle Audit FL-10, approved and resolved 2026-07-13.)* |
| 118 | **Spark Rewards points require a fully settled folio and cover net booking spend only.** Eligible spend is the locked `booking.totalPrice` (room and breakfast after booking-level deductions); incidental charges and store orders do not earn points. Checkout snapshots the charge-inclusive folio and stamps any unpaid balance. A settled member folio earns immediately; an unpaid folio locks a pending award that the final `/api/bookings/add-payment` transaction consumes exactly once. Checkout remains allowed with a balance as a front-desk judgment call. *(Per Finance Lifecycle Audit FL-11, approved and resolved 2026-07-13.)* |
| 119 | **Production environment split carry-over:** The production environment database starts as a clean slate for bookings and transactions (no bookings or related collections data are carried over from staging). Only the active staff accounts are recreated and pre-provisioned on the production project. *(Approved and resolved 2026-07-14.)* |
| 120 | **Notification Center (Phase 12) ships as a persisted `notifications` Firestore collection, not a session-only ring buffer.** The admin app already **rings** for 5 event types (booking / payment / message / arrival / departure) via `AdminContext.playSynthNotification`, but the ring is ephemeral — no record of what happened if staff look away. Owner chose Option B: a `notifications/{notificationId}` collection written **server-side via the Admin SDK** from the existing API routes (booking create / add-payment / confirm / check-in / check-out / store order placed). One doc per event; never a guest write. Per-staff read trail via a `readBy: Record<uid, timestamp>` map; the Firestore rule restricts client updates to the `readBy` field only. Guest chat messages are **not** persisted (no server route for guest sends; rejected B2) — they remain live-derived from the existing `intercoms` listener (B1, decided 2026-07-15). Build must include: bell + panel reachable from every admin page, 30-day retention cron (`/api/notifications/prune`, `0 3 * * *`), per-staff read-state, deep links, and respect for the existing sound mute setting (Decision #97 — visual-only mode). Out of scope: browser push (needs FCM — off-stack), per-event-type mute granularity. Full spec: `plan/features/NOTIFICATION-CENTER.md`. *Resolved 2026-07-15.* |
| 121 | **Purpose of stay is required at physical check-in and defaults to `Leisure`.** Add the field to the admin booking drawer's guest-registration form and the server-authoritative check-in readiness gate. The control opens with `Leisure` selected while allowing staff to choose another supported purpose before saving. Store the selected value with the guest registration and include it in the registration PDF. This is an owner-required operational field, not a DOT minimum-particular requirement. *(Approved 2026-07-23.)* |
| 122 | **"Confirm with balance" lets staff transition a `payment-uploaded` booking to `confirmed` when the partial payment is intentional and the rest will be collected at check-in (CWB-01..05, approved 2026-07-23).** Reuses the existing `hotelConfig.unpaidCheckoutApprovalThreshold` (default 5,000) as the same "four-eyes" gate the unpaid-checkout flow uses — `front-desk` may confirm with a balance up to the threshold, above it requires an authenticated `admin` claim. The reason is free text, 500-char max (mirrors `unpaidCheckoutReason`). A new `booking-confirmed-with-balance` email is fired with the balance and reason so the guest arrives knowing what to settle. Server stamps four fields atomically inside the transaction: `confirmedWithBalance`, `confirmedWithBalanceReason`, `confirmedWithBalanceAt`, `confirmedWithBalanceBy` — all nullable on existing bookings (no migration). A balance-owed indicator renders in the booking drawer through `checked-out` while `confirmedWithBalance > 0` and any unpaid balance remains; it auto-hides at ₱0. The form renders a **persistent threshold info banner** (not a hidden tooltip) so the staff is *guided/reminded* by the limit before they submit, and the submit button is disabled when the current balance is above the threshold AND the operator is `front-desk`. Scope is MVP (~250 server + ~300 client lines) — no auto-collection, no partial-settlement flows. Full spec: `plan/features/BOOKINGS-MANAGEMENT.md §Confirm with Balance (CWB)`. |
| 123 | **Multi-booking picker for the public `/my-booking` page when the email-alone path returns more than one booking (MBP-01..04, shipped 2026-07-24).** The previous email-alone path returned only the most-recent booking (RA 10173 privacy choice). Loosen to a privacy-preserving list: when the email matches >1 booking, the endpoint returns up to 10 most-recent under a new `kind: "list"` response shape. Two auto-detected modes: **single-name mode** (all matches share the same trimmed lowercased `guestName` — the same person) renders the full row including name; **multi-name mode** (mixed names — likely a shared email) omits the name. The picker never discloses which mode fired. Click → existing strict `ref + email` lookup (no new auth, no PII widening). Strict `ref + (email | token)` paths unchanged and now explicitly return `kind: "single"` for backward-compat. Cap at 10 via `limit(11)` (11th = "more exist" sentinel, no second query). Sort: `checkIn` desc with `createdAt` desc tiebreaker. No new collection, no schema change, no composite index at 14 rooms (FLR-03 trigger fires for the scale-out path). **Implementation:** `guest-app/server/handlers/bookings.ts` — email-alone branch changed from `limit(50) + pick-top-by-createdAt` to `limit(11) + sort-by-checkIn-desc-then-createdAt-desc`; 1 match keeps the existing single-booking enriched flow; 2+ matches return `{ kind: "list", bookings: entries, moreExist }` where each entry is `{ id, bookingRef, checkIn, checkOut, numNights, roomType, status, guestName? }` (`guestName` only in single-name mode). The single-booking `enrichAndRespond` wrapper now sets `kind: "single"`. `guest-app/src/pages/BookingLookupPage.tsx` — new `pickerResults` state + `PickerEntry` type; result area cycles search form → picker (when `kind: "list"`) → single-booking card (after a row click); "10 most recent — contact the front desk for older stays" footer when `moreExist` is true. `guest-app/tests/api/bookings-lookup-list.test.ts` — 8 new tests pin the contract: 1-match → single, 2-match same name → single-name list, 2-match mixed names → multi-name list, mixed-case same name resolves to single-name, 11+ matches → `moreExist: true` with 10 entries, `checkIn`/`createdAt` sort order, `ref+email` returns single (picker unreachable from strict paths), 0 matches → 404. Updated `bookings-lookup.test.ts` (the "email alone with several bookings" test was checking the old auto-pick behavior; now scoped to 1 match). Spec: `plan/features/BOOKING-LOOKUP.md §Multi-Booking Picker`. |
| 124 | **Unify the payment reference number onto the payment ledger (approved 2026-07-24).** The previous two-field design — a guest-entered `Booking.paymentReferenceNumber` at booking time plus a staff-entered `OnsitePayment.transactionReference` on each ledger entry — was confusing (the staff rarely trusts the guest's claim anyway, since they always look up the actual GCash ref / bank trace in the wallet app at verify time) and surfaced contradictory copy in the dashboard + reports. Drop the top-level `Booking.paymentReferenceNumber` field entirely; the canonical reference is now exclusively the `transactionReference` on the relevant `bookings/{id}/payments/{paymentId}` entry. Guests no longer type a ref at booking time on either `/book` or `/corporate/book`; staff populate it on **Verify & Record Payment** / **Record Payment** (server-enforced via the `requireReferenceNumber` flag on the payment method). The admin booking drawer surfaces a read-only "Reference" cell sourced from the latest ledger entry via a small `getLatestPaymentReference(booking)` helper (defined in `BookingsPage.tsx` and `BookingDrawerWorkspace.tsx`). The Firestore rules' staff update allowlist for `bookings/{id}` no longer carries `paymentReferenceNumber`; the `bookings/{id}/payments/{paymentId}` ledger is already append-only at the rules level, so the canonical reference can never drift. Old bookings that still carry the field render unchanged; no migration. Backwards-compatible — neither a new collection nor a new index is needed. Full spec: `plan/features/BOOKINGS-MANAGEMENT.md §Payment Reference Semantics`. |
| 125 | **Use `heic-to@1.5.2` (LGPL-3.0) for HEIC client-side conversion in the admin guest ID upload (HSD-01 + HSD-02..05, decided 2026-07-24).** Library eval compared three candidates against Vite 6 + Node 20 + React 19: `heic2any@0.0.4` is **unmaintained** (`alexcorvi/heic2any#63`), `heic-convert@2.1.0` is maintained but wraps a semi-stale `heic-decode`, and `heic-to@1.5.2` (LGPL-3.0, last publish ~1 month ago) is the actively maintained successor that tracks libheif 1.20.2. All three ship a ~500-720 KB gzipped libheif WASM blob — the original "<200 KB" budget in the HSD spec was wishful, no realistic HEIC library gets close. **720 KB gzipped is the floor**, paid only when a HEIC file is detected via dynamic `import()`. LGPL-3.0 is acceptable for a CDN-loaded browser library (the combined-work requirement doesn't apply via dynamic import; no source modifications are made to the lib). **Implementation (HSD-02..05) shipped 2026-07-24 on `feat/heic-support`:** `handleGuestIdUpload` branches on `HEIC_INPUT_MIME_TYPES = { image/heic, image/heif }` BEFORE the allowlist + compress path; the branch dynamic-imports `heic-to`, converts to JPEG at quality 0.92 via `heicTo({ blob, type: "image/jpeg", quality: 0.92 })`, wraps the result in `new File([converted], "id.jpg", { type: "image/jpeg" })`, and feeds it into the standard compress + upload flow. The conversion uses Web Workers internally (per the library README) so the UI thread is not blocked. JPEG/PNG/WebP uploads never pay the lazy-chunk cost. **Bundle delta measured 2026-07-24:** initial bundle +0.59 KB gzipped (just the import statement); lazy chunk 734.59 KB gzipped (`dist/assets/heic-to-*.js`), loaded only on HEIC detection and cached after first use. The picker `accept` attribute is `image/jpeg,image/png,image/webp,image/heic,image/heif` so iOS Safari surfaces HEIC photos in the "Recents" list. Conversion errors (corrupt HEIC, oversized, WASM init failure) fall through to a user-facing toast with a "re-capture or convert manually" next step; the existing 5s decode-timeout in the registration PDF generator remains as defense-in-depth. Branch: `feat/heic-support`. Spec: `plan/features/BOOKINGS-MANAGEMENT.md §Guest ID upload`. Regression: `admin-app/src/__tests__/heic-support.test.ts` (9 tests pin the dynamic-import + allowlist + accept + copy + package-wiring contract); updated `admin-app/src/__tests__/pdf-generation-repair.test.ts` to assert the new HEIC-precedes-allowlist order. |
| 127 | **Walk-in modal mirrors the guest `/book` page: collect first + last name as separate fields, drop the `split(" ")` kludge (WSN-01..02, approved 2026-07-25).** The walk-in modal (and the `/calendar` "Create Calendar Booking" modal, which shares the same `addWalkinBooking` entry point) used to collect a single `guestName` string ("Guest Full Name", placeholder "Maria Santos"). On submit, the client did `firstName = booking.guestName.split(" ")[0] || "Guest"` and `lastName = booking.guestName.split(" ").slice(1).join(" ") || "Walkin"`. That kludge silently produced `lastName: "Walkin"` for any single-name guest (e.g. "Cher"), mangled compound names ("Maria Teresa Santos" → firstName="Maria", lastName="Teresa Santos"), and reversed "Last, First" inputs. The `WalkinGuestDetailsSchema` server schema was already correct (`firstName` + `lastName` required) — the modal was the only outlier. Decision: **mirror the guest `/book` page** — two input fields (first + last, both `required`, `autoComplete="given-name"`/`"family-name"`, mobile-stacking flex layout that keeps the Phase 11.7 single-column-on-mobile rule intact), `addWalkinBooking` accepts `{ firstName, lastName }` directly via `Omit<Booking, …, "guestName">` (the input no longer carries a `guestName` field), and the context refuses empty inputs before the network. The booking doc still stores `guestName = \`${firstName} ${lastName}\`` server-side (same logic the guest `/book` flow has always used). No schema change, no doc field change, no migration. **Implementation shipped on `fix/walkin-split-name`:** `admin-app/src/context/AdminContext.tsx` — `addWalkinBooking` parameter renamed `booking` → `input` and the type omits `guestName`; the wire payload's `guestDetails` now reads `firstName: trimmedFirst, lastName: trimmedLast` directly (no split); the two `||` fallbacks ("Guest", "Walkin") are gone. `admin-app/src/pages/BookingsPage.tsx` — modal state replaced `[guestName]` with `[walkinFirstName, walkinLastName]`; the single "Guest Full Name" input is replaced by a two-input flex row (stacks on mobile, side-by-side on sm+ per the Phase 11.7 contract); `handleWalkinSubmit` validates both fields, passes them to `addWalkinBooking`, and toasts the combined name on success. `admin-app/src/pages/CalendarPage.tsx` — the "Create Calendar Booking" modal gets the same two-field state + form + submit changes (it was the second caller of the same kludge). `admin-app/src/__tests__/walkin-split-name.test.ts` — 12 new tests pin the new contract. `admin-app/src/__tests__/phase-12-discount-controls.test.ts` — `discountType: booking.discountType` / `voucherCode: booking.voucherCode` updated to `input.discountType` / `input.voucherCode` (parameter rename). `plan/features/BOOKINGS-MANAGEMENT.md` — new "Walk-in Name Split" section documents the symmetry with the guest page, the kludge root cause, and the no-migration property. Branch: `fix/walkin-split-name`. |
| 128 | **Drop `guestName` from the email-alone 1-match single-booking response (approved 2026-07-25, the same day as #126).** The MBP-05 spec (#126) retired `guestName` from the picker row shape and claimed the name was still "gated by the `ref + email` second factor" on the single-booking card. That claim was wrong for the **1-match case in the email-alone path**: the user only typed an email, there's no `ref` to combine with it, and the page went straight to the single-booking card with the full name revealed. Decision: **the public `/my-booking` page never reflects a name back to anyone who arrived via email alone.** The `enrichAndRespond` helper now takes an `options: { omitGuestName?: boolean }` flag; the email-alone 1-match call site passes `{ omitGuestName: true }`. The strict paths (`ref+email`, `ref+token`, `ref alone`, `token alone`) still call the helper without the flag and continue to return `guestName` because each demonstrates possession of a non-email secret (the ref is in the confirmation email, the token is in the magic-link email). The `BookingData` interface on the client makes `guestName` optional; the single-booking card's "Lead Guest" section is gated on `activeBooking.guestName &&` and renders only when the name is present. The booking doc still stores `guestName` as before — every reader (drawer, table, PDF, email) still needs it; the public lookup just stops reflecting it back. The `bookings-lookup-list.test.ts` "1 match → single" test was renamed to assert `guestName` is absent; the "ref+email path returns kind: single" test was renamed and now asserts `guestName` is present (pinning the second-factor distinction). The `bookings-lookup.test.ts` "email alone returns the most recent booking when 1 match exists" test gained an `expect.not.toHaveProperty("guestName")` assertion. `plan/features/BOOKING-LOOKUP.md` privacy stance updated. Branch: `fix/mbp-no-name-on-card`. |
| 131 | **Drop `guestName` and `guestEmail` from the single-booking response on every path; surface `maskedEmail` instead (MBP-09, approved 2026-07-25, same-day follow-up on #126 + #128).** Decisions #126 (picker rows) and #128 (email-alone 1-match single card) tightened the privacy posture on the public `/my-booking` page, but #128 left a leak: the **strict paths** (ref+email, ref+token, ref alone, token alone) still reflected the full `guestName` back to the caller. A live staging screenshot showed the leak: a user who reached the single-booking card via the picker-strict path (the most common flow after the email-alone picker) saw "Daniel Sandimas" + "dksandimas.projects@gmail.com" in the "Lead Guest" section. Decision: **the public `/my-booking` page NEVER reflects the guest name OR the full email back to the caller — on any path.** The `enrichAndRespond` helper now (a) drops `guestName` from the response unconditionally (the per-call `omitGuestName` option from #128 is removed) and (b) drops `guestEmail` too, replacing it with `maskedEmail` (first char of local + `***` + full domain — same format as the picker). The email-alone 1-match call site no longer needs the option; the helper is uniform for every caller. The `BookingData` interface on the client replaces `guestName?: string` and `guestEmail: string` with `maskedEmail: string`. The "Lead Guest" section is replaced by a "Booked under" echo showing the masked email. The "Email sent to ..." success indicator on the resend flow becomes a generic "Confirmation email sent." — the card no longer echoes the full address. The cancel + resend flows now use the user's typed value (`emailInput` form state) instead of `activeBooking.guestEmail` — that value is already validated by the lookup that returned the booking, and the form is always mounted (per #130) so the state is always available. The booking doc still stores `guestName` and `guestEmail` for staff-gated readers (drawer, table, PDF, email); the public lookup just stops reflecting them back. **Tests:** `bookings-lookup-list.test.ts` "1 match → single" + "ref+email path returns kind: single" + "email alone returns the most recent booking when 1 match exists" + the `bookings-lookup.test.ts` "returns enriched booking + room data on a matching ref + email" test all updated to assert `guestName` and `guestEmail` are absent, `maskedEmail` is present. **Implementation shipped on `fix/lookup-no-name-anywhere`:** 1 server helper signature simplification + 1 field swap on the wire + 1 client interface change + 1 client UI section rewrite + 4 test updates. `plan/features/BOOKING-LOOKUP.md` privacy stance updated; `plan/project/ROADMAP.md` MBP-09 row added. |
| 129 | **Picker click no longer fails with "Bot verification token is missing" (MBP-07, approved 2026-07-25, same-day follow-up).** Staging screenshot showed a live UX bug: user entered an email with multiple bookings, the picker rendered, click a row → page reverted to the "Find your booking" form with the ref + email pre-filled and the server's 400 "Bot verification token is missing." error. Root cause: `performLookup`'s `finally` block unconditionally called `resetTurnstile()`, which consumed the token right after the email-alone lookup returned the list. The widget then needed 1-3 seconds to mint a new token, but the user clicked a row immediately. The second `performLookup` call fired with `turnstileToken: ""` (or the stale not-yet-rotated value) and the server rejected it. Decision: **the picker click reuses the same Turnstile token.** Tracked `showedPicker` in `performLookup`; the finally block now only calls `resetTurnstile()` when the picker was NOT shown. Cloudflare's `siteverify` is idempotent within the 300s validity window, so the second verification succeeds; the existing 10/min rate limit + 3-failure 1-hour backoff still gate enumeration. The bot-protection model is unchanged: each `resetTurnstile()` still requires a real browser interaction. The picker flow is a short-lived, same-session, email+ref-gated sequence, so reusing the token is acceptable. **Plus: URL-driven picker navigation.** `handlePickerSelect` now calls `navigate(\`/my-booking?ref=…&email=…\`, { replace: true })` instead of `performLookup` directly. The existing `useEffect` on `searchParams` handles the auto-lookup, gated on a fresh Turnstile token (which is alive because of the reset fix above). Bonus UX: the URL is bookmarkable, refreshable, shareable, and the Back button works. **Implementation shipped on `fix/mbp-picker-click-turnstile`:** 1 finally-block condition + 1 `navigate` call + `useNavigate` import. No server change, no schema change, no contract change. The wire shape is already covered by `bookings-lookup-list.test.ts` "email-alone with 2+ matches" + "ref+email path returns kind: single". `plan/features/BOOKING-LOOKUP.md` "Server contract" section notes the Turnstile reuse; `plan/project/ROADMAP.md` MBP-07 row added. |
| 126 | **Tighten the multi-booking picker: drop `guestName` from the wire entirely, add `maskedEmail` (approved 2026-07-25, the day after #123 shipped on `dev`).** The original #123 spec preserved a "single-name mode" that attached the full `guestName` to every row when every match shared the same trimmed+lowercased name. That still leaked the full name to anyone with access to the email — a spouse, ex-partner, shared family inbox — and the page's threat model treats email as the only auth secret on this public route. Decision: **retire the single-name vs multi-name mode distinction entirely** and return a uniform row shape regardless of who the bookings are for. The new picker payload is `{ id, bookingRef, maskedEmail, checkIn, checkOut, numNights, roomType, status }` — `maskedEmail` is first char of local + `***` + full domain (e.g. `j***@gmail.com`), the attacker already typed the email so the leak surface is zero, and the legit user gets a small UX confirmation that the search keyed on the right email. The full `guestName` is still revealed on the single-booking card after the user picks a row (which goes through the existing `ref + email` second factor, decision #123's deep-link pattern). The response shape is now uniform: there is no mode for the client to detect, which is also the cleanest path for the rate-limit-oracle concern (fewer fields ⇒ fewer ways to differentiate replies). **Implementation shipped on `fix/mbp-no-name`:** `guest-app/server/handlers/bookings.ts` — new module-level `maskEmail(email)` helper (defensive fallback `***` for malformed inputs); email-alone branch's entry-construction loop no longer reads `data.guestName` or computes `allShareName`; every entry now carries `maskedEmail`. The privacy-contract comment block was rewritten around the uniform row shape. `guest-app/src/pages/BookingLookupPage.tsx` — `PickerEntry` type drops `guestName?`, adds `maskedEmail: string`; row UI swaps the guestName line for the masked email line (same `text-xs text-gray-600 truncate` styling, so the row layout is identical from the user's perspective). `guest-app/tests/api/bookings-lookup-list.test.ts` — "single-name mode" test renamed to "uniform row shape with maskedEmail and NO guestName (decision #126)"; the multi-name test renamed to "STILL omits guestName (uniform row shape)" and now asserts the same `maskedEmail` is on every row; new "maskedEmail uses first char + *** + full domain" test pins `j***@gmail.com` so a future refactor can't accidentally widen the local-part leak; the 11+ matches test now asserts every row has `maskedEmail` and no `guestName`. `plan/features/BOOKING-LOOKUP.md` — the "Two privacy modes — picked automatically by the server" section retired; the row-shape spec and the privacy stance section rewritten around the uniform payload; the QA checklist and the row-data checklist both updated. Branch: `fix/mbp-no-name`. |
| 130 | **Keep the `/my-booking` search form always mounted (approved 2026-07-25, same-day follow-up on #129).** After the MBP-07 picker-click fix, users started exercising the full flow: enter email → picker → "Back to search" → re-submit. The first three steps worked (per #129), but the last step produced "The security check hasn't finished yet" because the Cloudflare widget was gone from the form. Root cause: the result area was a single ternary `{pickerResults ? <Picker/> : !activeBooking ? <Form/> : <Card/>}`. When the picker or card was active, the `<Form/>` branch was unmounted — including the Turnstile widget container div inside it. On "Back to search" the form was re-mounted with a brand-new div; the `useTurnstileToken` hook's effect deps `[enabled, containerRef]` were unchanged, so `tryRender` didn't re-run and the new div was empty. The token was empty (cleared by an earlier `expired-callback`/`error-callback`/`resetTurnstile()` cycle), so `handleSearch` bailed. Decision: **the form is always mounted**, with the `hidden` class (`display: none`) suppressing it when the picker or card is the active view. The form's container div is now a stable DOM node; the widget persists across picker/card transitions; the token stays valid. The picker and card are layered on top via two conditional renders (`{pickerResults && pickerResults.length > 0 && <Picker/>}` and `{activeBooking && <Card/>}`). `aria-hidden` is set on the form when suppressed so screen readers skip it. **Implementation shipped on `fix/picker-back-to-search-turnstile`:** one ternary → one always-mounted form + two conditional renders. No server change, no hook change, no schema change, no new test. The wire shape is unchanged. `plan/project/ROADMAP.md` MBP-08 row added. |
| 132 | **Hydrate `breakfastServed` from the booking snapshot mapper (BSP-01, approved 2026-07-25).** The dashboard `toggleBreakfastServed` writes the per-date/per-guest served map to `bookings/{id}.breakfastServed` via `updateBookingStatus(..., { breakfastServed })`, the Firestore rule permits the field, and the write succeeds. But the admin booking snapshot mapper in `AdminContext.tsx` only hydrated `breakfastSelections` and omitted `breakfastServed`, so the served map was silently re-read as `undefined` on the next snapshot — the dashboard re-rendered the row as unserved and any subsequent toggle started from `{}` (the spread of `undefined`), wiping previously-served keys. Confirmed defect: "Mark Served" on a row with two breakfast guests, refresh the dashboard, both rows show unserved. Decision: **hydrate the served map on the snapshot read** — `breakfastServed: data.breakfastServed || {}` in the mapper, next to `breakfastSelections`. No rules change, no schema change, no migration; the field was already on the TS interface (`shared/types/index.ts §Booking`) and the existing toggle already does `const next = { ...(booking.breakfastServed || {}) }` so the fix is fully client-side. The `bookings-lookup-list`-style "previous version" test surface for this defect was misleading: the existing source-presence test on `DashboardPage.tsx` only verified that the relevant names occurred, not that the mapper hydrated the field, so a future refactor that breaks the hydration would not have been caught. The new regression suite (`admin-app/src/__tests__/breakfast-served-persistence.test.ts`, 9 tests) is a mix of (a) source-text pins on the mapper shape and the dashboard read/write sites, and (b) pure-JS behavioral tests that simulate the full toggle → snapshot-refresh → toggle cycle including a cross-session visibility scenario and a legacy-booking `{}` default. Manual QA on the two-guest same-booking case is the remaining BSP-03 gate. `plan/docs/TYPES.md` adds the field annotation; `plan/docs/BACKEND.md` adds the row to the bookings table. Branch: `fix/breakfast-served-persistence`.
| 133 | **Gate the email-claim booking match on `email_verified === true` (HIGH-1 closed, approved 2026-07-25).** The Spark Rewards audit (2026-07-18, `plan/docs/AUDIT-SPARK-REWARDS-REPORT.md §Section 3 HIGH-1`) found that every member-scoped booking matcher keyed off the Firebase ID token's `email` claim with no `email_verified` check: `linkBookingsByEmail` (registration linkage), `handleListMemberStays` (the `/api/members/stays` projection that leaks `bookingRef` + `lookupToken` — together the public lookup/cancel credential), and `findBooking` (the member email branch used by `/api/email/early-checkin-request`). Firebase email/password signup lets a user claim an arbitrary unverified email, so an attacker who registers with a victim's email (any victim who booked anonymously and never created an account — the common hotel case) can link the victim's past bookings on registration, read them via `/api/members/stays`, and fire early check-in writes against them. Fix: (1) `authenticateUser` (in `guest-app/server/apiRouter.ts`) now surfaces `decodedToken.email_verified` on its return shape; (2) `handleListMemberStays` returns `403 { code: "EMAIL_NOT_VERIFIED" }` when the auth user's email isn't verified — Google sign-in tokens are always verified so this is effectively a gate on the email/password path; (3) `handleRegisterMember` still creates the `members/{uid}` doc but **skips `linkBookingsByEmail`** when the email isn't verified, and the response carries `{ data: { emailVerified: false, linkedBookings: 0 }, warning: "Verify your email to link your past bookings." }` so the client can render a "verify your email" prompt; (4) the router-level check in the `action === "early-checkin-request"` branch returns `403 { code: "EMAIL_NOT_VERIFIED" }` before `findBooking` runs, and `findBooking`'s `emailMatches` predicate is also hardened to require `user.email_verified === true` as a defensive second line (the `memberId` match remains safe — the attacker would need their own uid); (5) the client's `signUpWithEmail` now `void`-calls `sendEmailVerification(credential.user)` (non-blocking — a verify-email failure must not abort member registration) immediately after `createUserWithEmailAndPassword`, and a new reusable `<EmailVerifyBanner />` component surfaces the unverified state on `My Stays` + `My Rewards` (replaces the early check-in button while unverified) + `My Profile`, with a "Resend verification email" + "I already verified" pair of actions. The auth context gains `resendVerification()` (Firebase SDK call + `currentUser.reload()`) and `refreshAuthUser()`. The audit's "minimum viable fix" recommendation (gate all three match sites on `email_verified`) is implemented; the audit's broader recommendation (verify email on email/password signup) is also implemented via `sendEmailVerification` in the signup flow. No schema change, no rules change (the email claim is already on the ID token; we just enforce it), no migration. **Idempotency:** an email/password user who verifies after signup can re-call `/api/members/register` to trigger the `linkBookingsByEmail` pass — the registration path is idempotent (`memberNumber` is preserved). **Tests:** `guest-app/tests/api/members-register.test.ts` gains two new tests (`rejects stays list with EMAIL_NOT_VERIFIED when email is unverified` + `skips linkBookingsByEmail when the registering user's email is unverified`) and updates two existing tests to reflect the new `emailVerified` field in the response. Full admin + guest suite green (1526/1526, modulo the pre-existing emulator-dependent `firebase/tests/notifications.rules.test.ts` and the `@config`-workspace-dependent `admin-app/src/__tests__/api-base-url.test.ts` + `dashboard-overdue-checkouts.test.ts`). `plan/docs/SECURITY.md §Auth & Security` already had the corresponding "never do" rule (`plan/docs/GOTCHAS.md §Auth & Security`); this PR enforces it. `plan/features/SPARK-REWARDS.md §Known Issues` updated: HIGH-1 entry marked closed; a new "Auth & email verification" section documents the gate, the banner, the resend flow, and the idempotency property. Branch: `fix/spark-rewards-email-verified-gate`.
| 134 | **Manual points adjustment moves server-side (MED-1 closed, approved 2026-07-25).** Per the Spark Rewards audit 2026-07-18 MED-1, the manual points adjustment path was the *only* balance-mutation still performed with the client Firestore SDK (`AdminContext.updateMemberPoints` did a `runTransaction` that wrote `members/{uid}.rewardsPoints` and appended a `pointsHistory/{entryId}` row in the same client transaction). Because a client transaction must be allowed by rules, the `members/{uid}` update rule granted any `isStaff()` caller (front-desk **or** admin) the ability to write `rewardsPoints` directly with no field restriction — the "balance always equals sum(history)" invariant was enforced **only in app code**, not at the security-rule boundary. Fix: (1) new server endpoint `POST /api/members/manual-adjust` (`handleManualAdjustPoints` in `guest-app/server/handlers/members.ts`) — admin-only (front-desk 403), Zod-validated body `{ memberId, amount, reason }` (strict mode, non-zero integer, reason 1-500 chars), 10/min/IP rate limit, server-side `runTransaction` re-reads the member doc, refuses to drop the balance below zero, writes `rewardsPoints` + a `pointsHistory/{historyRef.id}` row with `type: "manual"` hardcoded (caller cannot inject "earn" / "redeem" rows) + the staff UID in the same commit. (2) Client `AdminContext.updateMemberPoints` now calls the endpoint via `fetch` with the staff ID token (10 new tests in `guest-app/tests/api/members-manual-adjust.test.ts` pin the contract: admin-only, debit + credit, below-zero guard, missing member, zero-amount, empty-reason, tokenless, method, hardcoded `type:"manual"`, transaction atomicity). (3) Firestore `members/{uid}` staff update allowlist is tightened to `["isActive", "isMember", "tier", "memberSince", "memberNumber", "notes", "handledBy", "updatedAt"]` — **`rewardsPoints` is no longer writable from any client**. The member self-update branch (`fullName`, `phone`, `photoUrl`) is unchanged. The `pointsHistory` subcollection still permits `isStaff()` `create` as a defense-in-depth fallback for any future backfill, but the only live writer in the codebase is the server. (4) Updated `audit-admin-critical-2026-07-07.test.ts §AA-01` to pin the new shape (server endpoint path, server `runTransaction`, hardcoded `type:"manual"`, members rule without `rewardsPoints`). **No CRITICAL/HIGH/MED remaining from the 2026-07-18 Spark Rewards audit after this PR + #133 (HIGH-1).** All 4 balance-mutation paths (earn, redeem, undo, manual-adjust) are now server-side and the integrity invariant is enforced at the rules boundary. No schema change, no client surface change (the existing `updateMemberPoints` signature + UI call site is unchanged), no migration. **Manual QA follow-up** — verify on a real test member: (a) admin can credit + debit points with a reason and the history row appears on the drawer, (b) front-desk gets 403 when attempting the same call, (c) trying to drive the balance below zero returns 400, (d) a malicious client call that tries to inject `type: "earn"` in the body still writes `type: "manual"` (the server hardcodes). 1536 tests pass (modulo the pre-existing emulator-dependent `firebase/tests/notifications.rules.test.ts` + the `@config`-workspace-dependent `admin-app/src/__tests__/api-base-url.test.ts` + `dashboard-overdue-checkouts.test.ts` failures, all confirmed pre-existing on dev). `plan/features/SPARK-REWARDS.md §Known Issues` updated: MED-1 entry closed. Branch: `fix/spark-rewards-manual-points-server-side`.
| 135 | **MED-3 spec-correction variant (closed 2026-07-25).** The Spark Rewards audit 2026-07-18 §MED-3 flagged that two `§Edge Cases & States` entries in `plan/features/SPARK-REWARDS.md` were marked `[x]` but neither was implemented in the codebase: (a) "Member books while logged in but uses different email — no auto-link; manual link by front desk from Member detail drawer" — the "no auto-link" half is correct (`linkBookingsByEmail` only matches on the token email) but the "manual link by front desk" half is unbuilt; there is no booking→member link action in the admin member drawer (`admin-app/src/pages/MembersPage.tsx` exposes only points adjustment + suspend/activate), and (b) "Google account email differs from booking email — after sign-in, prompt… " — no such guest self-service prompt exists anywhere in `guest-app/src`. The functional gap (no reconciliation flow) is real but the *misleading `[x]` markers* are the audit's primary concern. Decision: take the **spec-correction variant** (audit's recommended alternative to building either path), which is <15 min and ships no behavior change. (1) The two `§Edge Cases & States` entries now read `[ ]` (deferred) with a "Deferred (Spark Rewards audit 2026-07-18 MED-3)" prefix; the inline copy preserves the "no auto-link" half (still accurate) and explicitly notes the unbuilt half plus the two workarounds: (i) the guest can find the booking at `/my-booking` (ref + email), and (ii) admin can update the booking's `memberId` via the booking drawer (existing staff tool). (2) The §Known Issues MED-3 entry is marked `✅ Closed 2026-07-25 (spec-correction variant)`. The build variant (front-desk manual-link surface in `MembersPage` detail drawer — the smaller of the two surfaces) is still deferred; revisit when a second white-label client or an explicit hotel ask makes the link surface a priority. The functional gap is documented and not silently hidden behind `[x]` markers. No code change, no schema change, no rules change, no migration. No tests to update. 1536 tests still pass (no source files touched). Branch: `fix/spark-rewards-different-email-spec`. **Update 2026-07-29** — the build variant of MED-3 (front-desk manual link) now ships; see decision #140 for the implementation. The spec-correction-only closure of #135 is superseded for the front-desk half; the guest self-service prompt (the larger MED-3 half) remains the unbuilt piece.
| 136 | **Spark Rewards audit LOW-1..7 closed (approved 2026-07-25).** Closes all 6 LOW findings from the 2026-07-18 Spark Rewards audit (`plan/docs/AUDIT-SPARK-REWARDS-REPORT.md §Quick Wins`) plus LOW-3 (a §Section 1 LOW that was already in the same report). Each fix is small, isolated, and ships a small corresponding test in `shared/__tests__/spark-rewards-lows-batch.test.ts` (18 tests total). **LOW-1** — Privacy Policy + Terms of Service disclosure line under the Rewards landing page's one-click "Enroll" button (mirrors `ProfilePage` so the join-consent posture is consistent across both surfaces). The `/rewards` landing page now wraps the `<PrimaryButton>` in a flex column with the disclosure caption below. No new test fixtures; the spec already required this surface to match. **LOW-2** — explicit `setPersistence(auth, browserLocalPersistence)` call in `guest-app/src/firebase/config.ts`. The Firebase Web SDK defaults to local persistence on web today, but the contract was implicit; an SDK default change or future refactor could silently regress "stay signed in." The admin app retains `browserSessionPersistence` (different posture — staff should not stay signed in past tab close). The pin is one line + a `void`d `.catch` that warns on failure (the SDK falls back to its default, so a failure is a UX regression at worst, not a security issue). **LOW-3** — no fix needed; §Section 1 LOW-3 is `linkBookingsByEmail` batch not chunked to Firestore's 500-write limit, theoretical only (not realistic for a single-hotel guest). Re-classified as `⏸ Deferred` in the SPARK-REWARDS Known Issues section — a member whose email matches >500 bookings would be the only failure mode. **LOW-4** — `calculateEarnedPoints` deleted from `shared/utils/points.ts` (it had a per-₱100-block formula `Math.floor(totalPrice/100) * pointsPerHundred` that diverged from the live `calculateCheckoutPoints` proportional formula `Math.floor((totalPrice/100) * pointsPerHundred)`). The shared helper had no production call site — only its own test exercised it, so the divergence was a trap rather than an active bug. Two tests removed; the canonical formula stays in `bookings.ts → calculateCheckoutPoints`. The `My Rewards` "Earn N points per ₱100 spent" copy now notes "proportional — partial ₱100 still earn fractional points" so the user-facing estimate matches the actual crediting. **LOW-5** — `StaysPage` now renders an `<EarlyCheckInStatusLine>` for each booking that has an `earlyCheckIn` map (green/approved with confirmed time + staff note, red/declined with staff note, blue/requested awaiting front desk). The `StayRecord` interface gains an optional `earlyCheckIn` field (already returned by `/api/members/stays`). The spec's "My Stays + My Rewards show the request status on the relevant booking" rule is now satisfied. **LOW-6** — `SPARK-REWARDS.md §Spark Rewards — Admin Member Management` checklist no longer says "Export members list as CSV." The export is a "Members" worksheet inside the admin-only full-backup `.xlsx` (per `audit-admin-sev4-2026-07-07.test.ts §AA-32`); the spec line is rewritten to say "Export members list via the full data backup (XLSX) on the Reports page (admin-only …)" with a pointer to the AA-32 audit closure. **LOW-7** — `sendEmail` skip guard in `guest-app/server/handlers/email.ts` now matches BOTH `@example.invalid` (early "unverified email" test path) and `@invalid` (RA 10173 erasure path — `members.ts` anonymizes erased bookings with `guestEmail: "erased@invalid"`). A stray email trigger against an erased booking now skips cleanly instead of attempting delivery to a non-deliverable address and bouncing at Resend. **No CRITICAL/HIGH/MED/LOW remaining from the 2026-07-18 Spark Rewards audit after this PR + #133 (HIGH-1) + #134 (MED-1) + #135 (MED-3 spec variant).** 1552 tests pass (modulo the pre-existing emulator-dependent `firebase/tests/notifications.rules.test.ts` + the `@config`-workspace-dependent `admin-app/src/__tests__/api-base-url.test.ts` + `dashboard-overdue-checkouts.test.ts` failures, all confirmed pre-existing on dev). No schema change, no rules change, no client surface change beyond a copy + a button-wrap; no migration. Branch: `fix/spark-rewards-lows-batch`.
| 137 | **Editable Terms & Conditions (LCE-01, approved 2026-07-25).** The public `/terms` page is now admin-editable. The previous hardcoded 11-section body stays in place as the deploy-time fallback (so the page never goes blank during a cold-load window or before the first admin save). The admin app's **Settings → Legal Content** tab gains a Terms of Service body textarea (50 KB cap, plain text only — preserves paragraph + list structure via `whitespace-pre-line` on the public page) with a version badge in the editor header that mirrors the live stamp. **New server endpoint** — `POST /api/admin/update-terms` (`handleUpdateTerms` in `guest-app/server/handlers/legal.ts`). Admin-only (front-desk 403, tokenless 401, GET 405, 10/min/IP rate limit — mirrors the existing `set-active` route's posture). Zod-validated body `{ termsBody: string(1..50_000) }` (strict mode). Server reads the current `settings/websiteContent.termsVersion` inside a transaction, auto-bumps the patch level (`1.0.0 → 1.0.1`, preserves major + minor + any pre-release suffix), and writes `{ termsBody, termsVersion, termsLastUpdated, termsUpdatedBy, termsUpdatedAt }` to the same doc via `setDoc(..., { merge: true })` so the rest of the `websiteContent` keys (homepage hero, about story, contact, corporate, etc.) are preserved. Returns the new version + last-updated so the admin editor mirrors them client-side without a second Firestore round-trip. Falls back to `DEFAULT_TERMS_VERSION = "1.0.0"` (new shared constant) when the website has never saved terms. **Shared constants** — `DEFAULT_TERMS_VERSION = "1.0.0"` and `TERMS_BODY_MAX_LENGTH = 50_000` in `shared/constants/index.ts`. The cap is enforced both client-side (textarea `maxLength`) and server-side (Zod schema) so the admin sees a clear "exceeds the 50 KB limit" before submit. **Public `/terms` page** — `guest-app/src/pages/TermsPage.tsx` now fetches `settings/websiteContent` on mount. When `termsBody` is a non-empty string, it renders the custom body via `<pre class="whitespace-pre-wrap">` (no `dangerouslySetInnerHTML` — plain text only, no XSS surface). When the field is empty or the doc is missing, the page falls back to the original hardcoded 11-section body. The header shows the live version + last-updated when set, with a small "Admin-edited · v{X.Y.Z}" badge so guests can see the doc is current. **Booking consent version capture** — `guest-app/server/handlers/bookings.ts → handleCreateBooking` reads `settings/websiteContent.termsVersion` inside the same `runTransaction` that creates the booking and stamps the value on the new booking doc as `termsConsentVersion`. Two concurrent admin saves land in different transactions, so a booking created at time T is stamped with whichever version was live at T — the audit trail is reliable per-booking. Bookings created before LCE-01 don't carry the field at all; the per-booking copy on `/my-booking` renders the fallback gracefully. The `create-walkin` path does not stamp the field (walk-ins are staff-mediated; the staff signature on the registration form is the legal consent, not the `/terms` checkbox). **Tests** — 10 server tests in `guest-app/tests/api/admin-update-terms.test.ts` (admin-only, tokenless, method, payload validation, version auto-bump from 1.0.0 → 1.0.1, patch preservation from 1.0.4 → 1.0.5, pre-release suffix preservation, malformed-input fallback, missing-doc fallback, front-desk 403) + 20 source-text pins in `shared/__tests__/lce-01-batch.test.ts` (constants, server endpoint wiring, Zod schema, role gate, transaction shape, version-bump helper, booking consent capture, Settings editor fields, public page render path, fallback structure). The pre-existing W3.11 test in `batch-19-wave3-batch2.test.ts` is updated from "renders the literal `config.termsLastUpdated`" to "falls back to `config.termsLastUpdated` when no custom value is set" — the new contract is `lastUpdated = customLastUpdated || config.termsLastUpdated` (the fallback is preserved; the page just resolves through a variable now). 1582 tests pass (modulo the pre-existing emulator-dependent `firebase/tests/notifications.rules.test.ts` + the `@config`-workspace-dependent `admin-app/src/__tests__/api-base-url.test.ts` + `dashboard-overdue-checkouts.test.ts` failures, all confirmed pre-existing on dev). No schema change (one new optional field on `bookings/{id}`), no Firestore rules change, no client surface change beyond the new editor + a re-routed version display; no migration (legacy bookings without `termsConsentVersion` are fine). Branch: `fix/terms-editable-content`.

| 138 | **Spark Essentials guest catalog gets a search + category filter rail (GSD-01, approved 2026-07-25).** The in-room intercom store page (`/guest-app/src/pages/IntercomPage.tsx`) previously rendered all `isActive: true` items alphabetically with no way to narrow the catalog — a meaningful friction point once the catalog grows past ~10 items. Decision: **add a search field + a category chip rail below the store intro**, both client-side and memoized over the existing real-time `onSnapshot` (no extra Firestore reads per keystroke or chip tap). Search matches `name + description`, trimmed and case-insensitive, with the trim guard so a whitespace-only query is treated as "no filter" rather than a wildcard. Category chips start with **All** and then render only the categories that are actually represented in the live catalog (drinks / snacks / toiletries / rentals / other, in the canonical order), driven by a `representedStoreCategories` memo over the current snapshot. The two filters compose with AND semantics. A live result count (`{n} of {m} items match your filters`) sits above the grid, with a one-action **Clear filters** link visible only when at least one filter is non-default. Sort is **in-stock alphabetical first, out-of-stock alphabetical second** (a stock of `null` is treated as in-stock / unlimited). The chip rail is a horizontally scrolling flex container with `-mx-4 px-4 overflow-x-auto`, each chip is `min-h-[44px]`, and selected state is exposed both via `aria-pressed` and `aria-selected` for assistive tech. Filter state lives at the IntercomPage scope (not inside a Shop sub-component) so it survives Shop / Chat / cart / checkout view switches, and is held in two independent `useState` hooks (`storeSearch`, `storeCategoryFilter`) that never touch the cart. A distinct no-match state renders when the catalog has items but the filter pair produces zero — different copy from the existing "The shop is currently unavailable" empty state, with a primary **Clear filters** button as the obvious next step. The `StoreItem` interface gains a `category: StoreCategory` field, the snapshot mapper normalizes legacy / missing values to `"other"` (mirroring the admin-side `normalizeStoreCategory` in `AdminContext.tsx`), and the filter chrome is wrapped in a `storeItems.length > 0` guard so the unavailable empty state still stands on its own when the catalog is empty. New source-text regression suite `shared/__tests__/gsd-01-batch.test.ts` (30 tests) pins: the type system, the memoized filter + sort, the AND composition, the trim+lowercase normalization, the represented-categories memo, the chip rail ARIA + touch target, the result count + clear-all control, the no-match state, the empty-state unchanged, and a cart-preservation negative test that asserts `clearStoreFilters` does not mention `setCart`. No server change, no schema change, no new collection, no new index, no API bundle rebuild (client-only). Branch: `fix/store-discovery`. *(Per Spark Essentials roadmap GSD-01 entry in `plan/features/STORE-GUEST.md`.)*

| 139 | **Extend the "House rules" email card to booking-confirmed + check-in reminder (ECE-02, approved 2026-07-26).** ECE-01 (2026-07-24, decision #136 row) shipped the "House rules" card on the `payment-confirmed` email only — sourced from `settings.websiteContent.houseRules`, omitted when blank, HTML-escaped. The card's value is that the guest arrives already knowing the property's expectations, but that value is strongest at the *latest* "you're arriving soon" touchpoint, not the *earliest* one. Decision: **append the same card to `booking-confirmed` and `checkin-reminder` too**, so a guest receives the rules at three touchpoints (payment → confirmed → day-before reminder) and the staff-owned copy stays single-sourced from `settings.websiteContent.houseRules`. Implementation: (1) extract a shared `houseRulesCard(houseRules?: string | null): string` helper inside `guest-app/server/handlers/email.ts` — trims, omits when blank, renders via `escapeHtml`, no fallback copy; the three email functions (`paymentConfirmedEmail` / `bookingConfirmedEmail` / `checkinReminderEmail`) all append the result to the body; (2) `sendBookingTrigger` now gates the `websiteContent` doc read by a `HOUSE_RULES_ACTIONS = new Set<EmailAction>(["payment-confirmed", "booking-confirmed", "checkin-reminder"])` so adding a fourth action that needs the card is a one-line change — every other template still skips the round-trip; (3) `handleEmailPreview` accepts `houseRules` in the request body for all three templates (mirrors the ECE-01 payment-confirmed contract) so staff can preview the exact render from the admin panel. **Tests** — 19 new tests in `guest-app/tests/api/email-house-rules-ece02.test.ts` covering: live-send card include/omit/omit-whitespace/throws for `booking-confirmed`, live-send card include/omit for `checkin-reminder`, non-arrival triggers (`booking-cancelled` / `booking-submitted` / `discount-rejected`) skip the read entirely, preview renders for the two new templates (positive + negative + HTML-escape), and source-text guards on `houseRulesCard` (trim + escape + no hardcoded fallback) + `bookingConfirmedEmail` / `checkinReminderEmail` signatures + the `HOUSE_RULES_ACTIONS` Set + the preview routing. 1 existing ECE-01 source-text guard updated to pin the new `houseRulesCard(houseRules)` call shape instead of the inlined `houseRulesBlock` pattern (the trim/conditional logic moved to the helper). 1631/1631 green (modulo the 3 pre-existing emulator + workspace failures on dev). No schema change, no rules change, no new collection, no API bundle rebuild (server-side only). Branch: `fix/ece-02-house-rules-more-emails`. *(ECE-02 entry on `plan/project/ROADMAP.md`.)*
| 140 | **Front-desk manual link of an existing booking to a member (MED-3 build variant, approved 2026-07-29).** The Spark Rewards audit 2026-07-18 §MED-3 split into two paths: the front-desk manual-link surface (smaller) and the guest self-service prompt (larger). Decision #135 closed the doc-accuracy half on 2026-07-25 (spec-correction variant) and explicitly left the build path open: "build the front-desk manual-link surface from the MembersPage detail drawer (smaller surface than the guest self-service prompt)." Build it now. **New server endpoint** — `POST /api/members/link-booking` (`handleLinkBookingToMember` in `guest-app/server/handlers/members.ts`). Admin-only (front-desk 403, tokenless 401, GET 405, 10/min/IP rate limit — mirrors `manual-adjust` / `set-active`). Zod-validated strict body `{ memberUid: string(1..160), bookingId: string(1..160), reason: string(1..500) }`. The handler re-reads the member + the booking inside one `runTransaction` so a concurrent register or a concurrent erase cannot race the link. **Invariants** — (a) cancelled bookings are refused (400) — the cancelled stay would surface as a "you cancelled this" card in the member's My Stays list, which is confusing; the existing booking-lookup workaround + booking-drawer `memberId`-edit still let the staff re-attach the PII if needed; (b) test-run bookings are refused (400) — the test-isolation invariant the rest of the audit closes (ETR-07, audit S2.3); (c) bookings already linked to a *different* member are refused (409) — no silent overwrite, no unlink from this surface (the booking-drawer `memberId`-edit is the unlink work-around); (d) re-linking a booking to the *same* member is idempotent (no booking update, audit row still written, `alreadyLinked: true` in the response so the UI can show a softer "already linked" toast). On success the handler sets `memberId` + `linkedByStaff` + `linkedAt` + `linkedReason` on the booking doc, then writes an audit row under `bookings/audit/records/{bookingId}-link-{timestamp}` (same nested-collection shape the erasure audit uses — decision #49 / W1.4) with `{ action: "manual-link-member", fromMemberId, toMemberId, memberEmail, bookingEmail, reason, staffUid, staffRole, at }`. **AdminContext** — `linkBookingToMember(memberUid, bookingId, reason)` is added to `admin-app/src/context/AdminContext.tsx`, posts to `/api/members/link-booking` with the staff ID token, returns `{ success, error?, alreadyLinked?, bookingRef? }`. **MembersPage drawer** — a new "Link Existing Booking" form is rendered below the points adjustment form, gated on `selectedMember` (it's a member-scoped action). The helper copy surfaces `selectedMember.email` so the staff can see the account email next to the "differs from" wording — the different-email context is the whole reason this surface exists. The form fields are **Booking ID or Reference** + **Audited Reason for Link** (required, 500-char max). Toasts: `toast.success` on a fresh link, `toast.info` on a re-link (so the staff sees the idempotent re-link as a soft confirmation, not a fresh action). **Tests** — 14 server tests in `guest-app/tests/api/members-link-booking.test.ts` (method 405, tokenless 401, front-desk 403, missing-field 400, whitespace-only reason 400, >500-char reason 400, member-not-found 400, booking-not-found 400, cancelled 400, test-run 400, already-different-member 409, first link 200 with alreadyLinked:false + booking write + audit row, idempotent re-link 200 with alreadyLinked:true + no booking write + audit row, Admin SDK transaction posture) + 18 source-text tests in `admin-app/src/__tests__/med-3-link-booking.test.ts` (route wiring, handler contract, AdminContext function, MembersPage form). 930/930 admin + 544/544 guest tests green, typecheck clean. **MDs** — `plan/features/SPARK-REWARDS.md §Edge Cases` first MED-3 entry flipped to `[x]` with the full ship summary; the second entry (guest self-service prompt) stays `[ ]` deferred as the larger remaining MED-3 path. `plan/features/SPARK-REWARDS.md §Known Issues` MED-3 row updated to note the build variant ship and the supersession of the spec-correction-only closure. `plan/project/ROADMAP.md §Spark Rewards Feature Audit` MED-3 entry updated. Branch: `fix/spark-rewards-med-3-link-booking`.
| 141 | **Walk-in payment method sources from Settings; front desk is a fourth consumer governed by no surface flag (WPM-01..07 + WPM-03, approved 2026-07-31).** The walk-in modal's "Payment Term" dropdown was the only admin payment selector hardcoded (3 literal `<option>` tags — `pay-at-hotel` / `Cash on Hand` / `Onsite Card Reader`) while the bookings filter, store filter, Record Payment, and Verify & Record Payment all mapped `onsitePaymentMethodOptions` — the memo derived live from `settings/hotelConfig.paymentMethods[]`. Same hardcoded list also appeared in `CalendarPage`'s "Create Calendar Booking" modal. The two lists shared a keyspace but not a source, so the desk could be told one thing by the dropdown and a different thing by the persisted record. **Decision: source the walk-in list from the same Settings memo the other four selectors use.** Three sub-decisions. (1) **`pay-at-hotel` is prepended at the call site, not the memo** — `onsitePaymentMethodOptions` deliberately excludes `pay-at-hotel` via `NON_TENDER_ONSITE_PAYMENT_METHODS` (it's a booking-time *intent*, not a settlement tender, and including it would pollute Daily Close reconciliation). Walk-in creation genuinely needs it (per `SETTINGS.md §Payment Methods`, `pay-at-hotel` is protected from deletion specifically because walk-in creation depends on it), so the walk-in list is `pay-at-hotel` prepended to the memo, never the memo used raw. Same shape in `CalendarPage`'s modal. (2) **The front desk is a fourth consumer of `paymentMethods[]`, governed by no surface flag** — per #111 there are three surface toggles (`isEnabled` = guest booking flow, `showInStore`, `showInCorporate`) and none covers the front desk. `onsitePaymentMethodOptions` deliberately ignores `isEnabled` so the desk can record a tender the hotel does not offer online. A method switched off for guests still appears at the desk. **Explicitly decided NOT to add a `showAtFrontDesk` pill** — the desk is a staff surface where over-restricting costs more than over-offering, and a fourth flag means a Settings UI change, a schema field, a migration-free default, and four more code paths reading it. (3) **`card` moves from hardcoded to configured (CVQ-07 resolved: the hotel DOES take cards)** — after the fix the desk can no longer file a walk-in as "Onsite Card Reader" unless a `card` method exists under Settings → Payment Methods. Since the hotel takes cards, adding that method is part of shipping WPM, not an optional follow-up. `AdminContext` gains a third backfill category, `STAFF_ONSITE_TENDER_BACKFILL_DEFAULTS` (alongside `PROTECTED_PAYMENT_METHODS` for `pay-at-hotel`/`add-to-bill` and `STORE_PAYMENT_BACKFILL_DEFAULTS` for `cod`). `card` is backfilled with `{ method: "card", label: "Credit Card", isEnabled: true, showInStore: false, showInCorporate: false }` if missing. Unlike protected methods, the admin can delete it from Settings. Existing `card` bookings still render via `LEGACY_ONSITE_PAYMENT_METHOD_LABELS` ("Credit Card") without the backfill, so historical records are safe. **Implementation** — (a) `BookingsPage.tsx` walk-in modal: 3 literal `<option>` tags → `[{ method: "pay-at-hotel", label: "Pay at Hotel" }, ...onsitePaymentMethodOptions].map(...)`; field label "Payment Term" → "Payment Method" (WPM-05); 3 raw `selectedBooking.paymentMethod` renderings in the drawer routed through `getOnsitePaymentMethodLabel` (WPM-06). (b) `BookingDrawerWorkspace.tsx`: header takes `paymentMethodLabel: string` as a prop (resolved by the parent), uses it instead of the raw key. (c) `CalendarPage.tsx`: same walk-in pattern fix in the "Create Calendar Booking" modal; gains its own `onsitePaymentMethodOptions` memo + `NON_TENDER_ONSITE_PAYMENT_METHODS` constant (duplication is the cost of the quick fix — extract to a shared hook when a third caller appears). (d) `AdminContext.tsx`: new `STAFF_ONSITE_TENDER_BACKFILL_DEFAULTS` map + wired into the same idempotent `useEffect` as the other two backfill categories. **No server change** — `WalkinBookingSchema` already accepts `paymentMethod: z.string().trim().min(1).max(80)`. **Tests** — `admin-app/src/__tests__/walkin-payment-method-from-settings.test.ts` (13 source-text guards) pins: (1) walk-in selector maps the memo (guard against re-introducing literal `<option value="card">`); (2) `pay-at-hotel` is prepended exactly once; (3) memo still excludes non-tender keys; (4) `card` backfill exists and is wired into the `useEffect`; (5) field label is "Payment Method"; (6) drawer uses the label helper (3 sites in `BookingsPage` + the `paymentMethodLabel` prop on `BookingDrawerWorkspaceHeader`); (7) `CalendarPage` destructures `paymentMethods` and builds its memo with the same shape. `firebase/tests/payment-methods-array-write.emulator.test.ts` (the PMH-05 generalization of the PMH-03 `roomTypes[]` test) pins the array-write hazard on `paymentMethods[]` — N concurrent per-method writes lose all but the last, single batched `setDoc` persists every change. Together these prevent the "wrong list, no error" class of bug from shipping again on the same field the spec author flagged. **MDs** — `plan/features/BOOKINGS-MANAGEMENT.md` (new §Walk-in Payment Method from Settings section, walks through the fix + the WPM-03 decision + the WPM-04 backfill + the tests), `plan/features/SETTINGS.md §Payment Methods` (new "Front desk is a fourth consumer of the list, governed by no surface flag" bullet under the per-method surface pills), `plan/project/ROADMAP.md §Phase 12 WPM-01..07` (all seven items ✅ shipped with commit `77610ff`, merge `e7c7171`). 951/951 admin + 560/560 shared + 202/202 guest + 6/6 infra tests green; typecheck clean. Branch: `feat/wpm-payment-method-from-settings`. *(WPM entry on `plan/project/ROADMAP.md`.)*
| 142 | **Booking sources from Settings; NBS-08 (Reports silent-drop) fixed (NBS-01..10 minus NBS-04, approved 2026-07-31).** The walk-in modal hardcoded its source list, the `Booking` interface typed `source` as a 5-value union, the Bookings table filter hardcoded 5 `<option>` tags, and the Reports acquisition chart hardcoded its slice list — adding a new source (e.g. `agoda` per CVQ-08) required editing four files in lockstep, and a source that landed in the database without being added to Reports was **silently dropped from the chart**. That is the NBS-08 regression class: same shape as RTS-01 (wrong answer, no error, no test failure). Decision: move every booking-source concept onto `settings/hotelConfig.bookingSources[]` (seed: `DEFAULT_BOOKING_SOURCES` in `shared/constants`, the historical 5 + `agoda`). Three sub-decisions. (1) **`BookingSource` widens to `string`** (NBS-03) — same move already made for `PaymentMethod` and `RoomType` when those lists went dynamic; the inline duplicate in `AdminContext.tsx` (`source: "online" | "walk-in" | "phone" | "facebook" | "corporate"`) is removed; the type is no longer the source of truth, the configured list is. (2) **`selectableAtFrontDesk` flag IS warranted** (NBS-06) — per #111 the payment list rejected a `showAtFrontDesk` flag because the failure mode was merely over-permissive. Here it would be silent data corruption: choosing "Online" for a walk-in would corrupt acquisition reporting. The three system-assigned entries (`online` / `walk-in` / `corporate`) have `selectableAtFrontDesk: false` and never appear in the New Booking modal's source selector; the three front-desk-selectable ones (`phone` / `facebook` / `agoda`) have `true`. **Mirror `PROTECTED_PAYMENT_METHODS`** for the source field — `PROTECTED_BOOKING_SOURCES = ["online", "walk-in", "corporate"]`; `AdminContext.deleteBookingSource` refuses to delete a protected source and second-line-of-defenses by also blocking deletion if any booking references the source. **Keep the route name** — `POST /api/bookings/create-walkin` is unchanged (a live route rename buys nothing and risks the deploy-order skew class of failure that caused INC-01). (3) **NBS-08 (Reports): orphan sources are loud, not silent** — the acquisition chart memo derives its slice list from the configured list (filtered on `isEnabled`) and surfaces an `"Unconfigured: <raw-key>"` slice for any source that lands in the database without being in the configured list. The silent-drop class of bug becomes a labeled orphan slice — the admin sees it and either adds the source to Settings or fixes the data. Phone + Facebook are now separate slices per CVQ-09 (the previous `labelMap` collapsed them into one `"Social Media / Phone"` bucket which destroyed the breakdown). (4) **NBS-02 (server note): source-derived** — new `deriveSourceNote(source)` helper replaces the hardcoded `"Created on-site at Front Desk."` note that shipped on every walk-in booking (the bug was that a phone booking was claiming it was created at the desk). Walk-in keeps the historical line; phone / facebook / agoda get channel-accurate notes; unknown sources get `"Booked via <key>."`. The server validates the submitted source against `settings/hotelConfig.bookingSources[]` and falls back to `"walk-in"` for unknown values (warns to console, does NOT reject outright — a stale/typo source should not block a check-in). **Implementation** — (a) `shared/constants`: new `DEFAULT_BOOKING_SOURCES`, `PROTECTED_BOOKING_SOURCES`, `BookingSourceConfig` type. (b) `shared/types`: `BookingSource = string`. (c) `shared/schemas`: `WalkinBookingSchema` gains optional `source` with `"walk-in"` default. (d) `guest-app/server/handlers/bookings.ts`: handler validates against configured list, calls `deriveSourceNote`. (e) `admin-app/src/context/AdminContext.tsx`: new state + CRUD + backfill + delete protection; remove the inline union duplicate. (f) `admin-app/src/pages/BookingsPage.tsx`: rename "New Walk-in Booking" → "New Booking" + new Source dropdown mapping `selectableBookingSources` memo; Bookings table filter maps the configured list; active-filter chip renders the configured label. (g) `admin-app/src/pages/ReportsPage.tsx`: acquisition chart memo rewritten per (3) above. (h) `admin-app/src/__tests__/walkin-modal-scrollability.test.ts`: regex updated in lockstep per NBS-01. **Tests** — new `admin-app/src/__tests__/booking-sources.test.ts` (26 source-text guards) pins every contract; new `firebase/tests/booking-sources-array-write.emulator.test.ts` (PMH-05 generalization #3) pins the array-write hazard on `bookingSources[]` (same shape as the PMH-03 `roomTypes[]` and WPM-04 `paymentMethods[]` tests). 977/977 admin + 560/560 shared + 202/202 guest + 6/6 infra tests green; typecheck clean. **MDs** — `plan/features/BOOKINGS-MANAGEMENT.md` (walk-in → New Booking rename + source field note), `plan/features/REPORTS.md` (acquisition chart reads config), `plan/docs/TYPES.md` (`BookingSource` → string), `plan/docs/BACKEND.md` (`settings/hotelConfig.bookingSources[]`), `plan/docs/DECISIONS-FEATURES.md #142` (this entry), `plan/project/ROADMAP.md §Phase 12 NBS-01..10` (9/10 items ✅ shipped, NBS-04 deferred). **NBS-04 (Settings → Booking Sources tab UI) deferred** to a follow-up PR — the data model + CRUD + backfill + delete protection are all in place; the tab itself is a ~500-line clone of the Payment Methods tab. Branch: `feat/nbs-booking-sources`. *(NBS entry on `plan/project/ROADMAP.md`.)* The walk-in modal hardcoded its source list, the `Booking` interface typed `source` as a 5-value union, the Bookings table filter hardcoded 5 `<option>` tags, and the Reports acquisition chart hardcoded its slice list — adding a new source (e.g. `agoda` per CVQ-08) required editing four files in lockstep, and a source that landed in the database without being added to Reports was **silently dropped from the chart**. That is the NBS-08 regression class: same shape as RTS-01 (wrong answer, no error, no test failure). Decision: move every booking-source concept onto `settings/hotelConfig.bookingSources[]` (seed: `DEFAULT_BOOKING_SOURCES` in `shared/constants`, the historical 5 + `agoda`). Three sub-decisions. (1) **`BookingSource` widens to `string`** (NBS-03) — same move already made for `PaymentMethod` and `RoomType` when those lists went dynamic; the inline duplicate in `AdminContext.tsx` (`source: "online" | "walk-in" | "phone" | "facebook" | "corporate"`) is removed; the type is no longer the source of truth, the configured list is. (2) **`selectableAtFrontDesk` flag IS warranted** (NBS-06) — per #111 the payment list rejected a `showAtFrontDesk` flag because the failure mode was merely over-permissive. Here it would be silent data corruption: choosing "Online" for a walk-in would corrupt acquisition reporting. The three system-assigned entries (`online` / `walk-in` / `corporate`) have `selectableAtFrontDesk: false` and never appear in the New Booking modal's source selector; the three front-desk-selectable ones (`phone` / `facebook` / `agoda`) have `true`. **Mirror `PROTECTED_PAYMENT_METHODS`** for the source field — `PROTECTED_BOOKING_SOURCES = ["online", "walk-in", "corporate"]`; `AdminContext.deleteBookingSource` refuses to delete a protected source and second-line-of-defenses by also blocking deletion if any booking references the source. **Keep the route name** — `POST /api/bookings/create-walkin` is unchanged (a live route rename buys nothing and risks the deploy-order skew class of failure that caused INC-01). (3) **NBS-08 (Reports): orphan sources are loud, not silent** — the acquisition chart memo derives its slice list from the configured list (filtered on `isEnabled`) and surfaces an `"Unconfigured: <raw-key>"` slice for any source that lands in the database without being in the configured list. The silent-drop class of bug becomes a labeled orphan slice — the admin sees it and either adds the source to Settings or fixes the data. Phone + Facebook are now separate slices per CVQ-09 (the previous `labelMap` collapsed them into one `"Social Media / Phone"` bucket which destroyed the breakdown). (4) **NBS-02 (server note): source-derived** — new `deriveSourceNote(source)` helper replaces the hardcoded `"Created on-site at Front Desk."` note that shipped on every walk-in booking (the bug was that a phone booking was claiming it was created at the desk). Walk-in keeps the historical line; phone / facebook / agoda get channel-accurate notes; unknown sources get `"Booked via <key>."`. The server validates the submitted source against `settings/hotelConfig.bookingSources[]` and falls back to `"walk-in"` for unknown values (warns to console, does NOT reject outright — a stale/typo source should not block a check-in). **Implementation** — (a) `shared/constants`: new `DEFAULT_BOOKING_SOURCES`, `PROTECTED_BOOKING_SOURCES`, `BookingSourceConfig` type. (b) `shared/types`: `BookingSource = string`. (c) `shared/schemas`: `WalkinBookingSchema` gains optional `source` with `"walk-in"` default. (d) `guest-app/server/handlers/bookings.ts`: handler validates against configured list, calls `deriveSourceNote`. (e) `admin-app/src/context/AdminContext.tsx`: new state + CRUD + backfill + delete protection; remove the inline union duplicate. (f) `admin-app/src/pages/BookingsPage.tsx`: rename "New Walk-in Booking" → "New Booking" + new Source dropdown mapping `selectableBookingSources` memo; Bookings table filter maps the configured list; active-filter chip renders the configured label. (g) `admin-app/src/pages/ReportsPage.tsx`: acquisition chart memo rewritten per (3) above. (h) `admin-app/src/__tests__/walkin-modal-scrollability.test.ts`: regex updated in lockstep per NBS-01. **Tests** — new `admin-app/src/__tests__/booking-sources.test.ts` (26 source-text guards) pins every contract; new `firebase/tests/booking-sources-array-write.emulator.test.ts` (PMH-05 generalization #3) pins the array-write hazard on `bookingSources[]` (same shape as the PMH-03 `roomTypes[]` and WPM-04 `paymentMethods[]` tests). 977/977 admin + 560/560 shared + 202/202 guest + 6/6 infra tests green; typecheck clean. **MDs** — `plan/features/BOOKINGS-MANAGEMENT.md` (walk-in → New Booking rename + source field note), `plan/features/REPORTS.md` (acquisition chart reads config), `plan/docs/TYPES.md` (`BookingSource` → string), `plan/docs/BACKEND.md` (`settings/hotelConfig.bookingSources[]`), `plan/docs/DECISIONS-FEATURES.md #142` (this entry), `plan/project/ROADMAP.md §Phase 12 NBS-01..10` (9/10 items ✅ shipped, NBS-04 deferred). **NBS-04 (Settings → Booking Sources tab UI) deferred** to a follow-up PR — the data model + CRUD + backfill + delete protection are all in place; the tab itself is a ~500-line clone of the Payment Methods tab. Branch: `feat/nbs-booking-sources`. *(NBS entry on `plan/project/ROADMAP.md`.)*

| 143 | **Folio math extracted into `shared/utils/bookingFolio.ts` (PMH-02, approved 2026-07-31).** The booking folio math lived in three independent places: (a) `getBookingFolio` was a local closure inside `BookingsPage.tsx` (not exported; 8 call sites, all in that one file) — depended on the React state `selectedBookingPayments` / `selectedBookingCharges` / `storeOrders` to render the booking drawer's live updates after a payment or charge is recorded. (b) The server's `handleCreateBooking` / `handleConfirmWithBalance` transactions computed `folioTotal` and `computedBalance` inline. Different shape — just the totals, not the full folio object — but the same math. (c) `summarizeFolioSnapshot` in `admin-app/src/utils/finance.ts` aggregated the same math across many bookings. The client and server currently agreed by discipline, not by construction. **Decision: extract into `shared/` as a pure refactor with zero behavior change.** Two helpers in `shared/utils/bookingFolio.ts`: (1) `computeBookingFolio(input)` for the per-booking folio — filters store orders by the historical criteria (`bookingId` match + `paymentMethod === "add-to-bill"` + `status === "delivered"` + `isBilled === true`), sums store total + charges + payments (with the optimistic-update branch via `selectedBookingPayments` / `selectedBookingCharges`), returns the full folio object. (2) `computeServerFolioTotals(input)` for the server transactions — minimal input shape (just the four numbers), returns `{ folioTotal, computedBalance }` with the historical `Math.max(..., 0)` clamp on the balance and the defensive `Number(x) || 0` pattern for nullish inputs. Minimal input types (`FolioBooking`, `FolioStoreOrder`, `FolioCharge`, `FolioLivePayment`, `FolioPersistedPayment`) keep the shared utility independent of admin-app's `Booking` extension / `OnsitePayment` interface. **Why the spec's "do with EXB-02" pair was split into two PRs** — PMH-02 (folio) is the gating one for MRB-04 (the group folio); EXB-02 (add-on math) and the DSC math extraction are the next two pure-refactor PRs in the same pattern. One review, one revert point each. **Implementation** — (a) `shared/utils/bookingFolio.ts` (new, 6.3 KB) — the two helpers + the minimal input types. (b) `shared/index.ts` — `export * from "./utils/bookingFolio"`. (c) `admin-app/src/pages/BookingsPage.tsx` — the local `getBookingFolio` closure is now a thin wrapper over `computeBookingFolio` that feeds the React state in; the local `getBookingPaymentsTotal` / `getBookingStoreCharges` helpers are deleted (dead code, their logic is now in the wrapper or in the shared function). (d) `guest-app/server/handlers/bookings.ts` — the inline `folioTotal` / `computedBalance` math in the confirm-with-balance transaction now routes through `computeServerFolioTotals`. (e) Two pre-existing source-text tests updated in lockstep: `fin-14-incidental-charges.test.ts` (pinned the new `computeBookingFolio(` call site instead of the old `chargesTotal = charges.reduce(...)` shape) and `booking-receipt-pdf.test.ts` (end-marker updated since the deleted `getBookingPaymentsTotal` helper was used as a slice anchor). **Tests** — new `shared/__tests__/booking-folio.test.ts` (10 characterization tests) pins the new contract: basic shape, store-order filter criteria, persisted vs. live payments, the optimistic-update path, the full 8-call-site shape with realistic inputs (multiple store orders, a refund, breakfast add-on), the server-side `folioTotal` + `Math.max(..., 0)` clamp, the defensive `Number(x) || 0` pattern for nullish inputs. **Zero behavior change** — the pre-existing test suite (1755 tests across 4 layers: 212 shared + 560 admin + 977 admin + 6 infra) passes with no other changes. **MRB-04 is now unblocked** — the group folio can change money math in one function instead of three independent sites. No schema change, no rules change, no migration. **NOT in this PR (deferred)** — EXB-02 (extract breakfast add-on math; 4 sites: `CalendarPage.tsx:240`, `bookings.ts:4114`, `pricing.ts:30`, `finance.ts:172` — each a few-line inline `breakfastRate × numGuests × nights` expression) and the DSC math extraction (4 sites: `pricing.ts` (×2), `ReportsPage.tsx` (×2)) ship as the next two pure-refactor PRs. Branch: `refactor/pmh-02-folio-extract`. *(PMH-02 entry on `plan/project/ROADMAP.md`.)*

| 144 | **Breakfast add-on math extracted into `shared/utils/bookingAddOns.ts` (EXB-02, approved 2026-07-31).** The `breakfastRate × numGuests × numNights` expression (and its `hasBreakfast ? ... : 0` gate) was duplicated across **12 sites** in the admin app, the guest-app server, and `shared/utils/pricing.ts`. Each copy had a slightly different defensive pattern — `(x || 0)`, `nonNegativeFinite(x)`, `Number(x) || 0`, the `manualNightlyRate === null` guard — and the client and server agreed by discipline, not by construction. The original spec said 6 sites; the actual count (10–12 depending on grep scope) was higher, so all 12 were updated in one PR. The historical shape: a `hasBreakfast` ternary gate, a `breakfastRate` per-person-per-night rate snapshotted at booking time (so a later rate change never rewrites an existing bill), a `numGuests` count, and a `numNights` count. **Decision: extract into `shared/` as a pure refactor with zero behavior change.** One helper in `shared/utils/bookingAddOns.ts`: `calculateBreakfastAddOn(input)` returns `rate × guests × nights` when `hasBreakfast` is truthy and all three operands coerce to positive numbers, else `0`. The defensive coercion is `Number(x) || 0` per operand — at least as strong as every historical variant (the helper collapses `(x || 0)` and `nonNegativeFinite(x)` to the same shape: nullish / NaN / negative → 0, positive → value). The helper's `hasBreakfast` short-circuit replaces the historical outer guard. **The surrounding `hasBreakfast` ternary gates that omit the array entirely (rate-breakdown, finance) are preserved** — only the `amount: ...` expression inside the array literal is swapped. Where the array was always created (pricing, Reports), the helper's `hasBreakfast` short-circuit replaces the historical outer guard. **Sites updated (12 total):** `admin-app/src/utils/finance.ts:172` (1) — the breakfast gross in the folio summarization, where the surrounding `hasBreakfast` ternary decides whether to include the `breakfastSubtotal` line in the per-booking output; `admin-app/src/pages/CalendarPage.tsx:240` (1) — the "Create Calendar Booking" modal's totalPrice preview, where the surrounding `hasBreakfast` ternary gates the inline `breakfastRate × guestCount × selectedNights` expression; `admin-app/src/pages/ReportsPage.tsx` (6) — three `breakfastTotal` reconstructions in the Sales tab (occupancy chart, room-type chart, daily-breakdown) plus three `breakfastRevenue` aggregations in the Sales XLSX export + the Store detail table; `guest-app/server/handlers/bookings.ts:4132` (1) — the `handleCreateBooking` transaction's `breakfastAddOn` write to the `addOns[]` ledger line, where the `manualNightlyRate === null` guard is preserved (manual nightly rate bookings skip the breakfast add-on line because their total is locked in by staff at booking time); `guest-app/server/lib/rate-breakdown.ts` (2) — the `rebuildEarlyCheckoutRateBreakdown` function builds two parallel `addOns[]` arrays (one for the current stay, one for the original stay used as the receipt's "what you paid for" anchor) and both had the inline expression; `shared/utils/pricing.ts:30` (1) — the `calculateBookingTotal` helper used by the guest `/book` page, the admin New Booking modal, and the corporate booking flow. **Tests** — new `shared/__tests__/booking-addons.test.ts` (12 characterization tests) pins the contract: hasBreakfast gate covering `false` / `undefined` / `null`; defensive coercion covering `0` and `null` per operand (each operand independently — the function returns 0 if any one of them is 0 or nullish, even if the other two are positive); the happy path; and a matrix of realistic inputs (a 1-night 1-guest no-breakfast, a 3-night 2-guest full-breakfast, a 7-night 4-guest partial-rate). **One pre-existing source-text test updated in lockstep** — `admin-app/src/__tests__/fin-05-discounts.test.ts` now pins the new `breakfastTotal = calculateBreakfastAddOn(...)` shape (was pinning the old `hasBreakfast ?` ternary). Test intent unchanged: the gross-revenue reconstruction (`roomSubtotal + breakfastTotal`) still produces the same byte-equivalent total. **Why the spec's "do with PMH-02" pair was split into two PRs** — PMH-02 (folio, decision #143) is the gating one for MRB-04 (the group folio); EXB-02 (add-on math) is the gating one for CHD-10 (the children-in-breakfast toggle per CVQ-01, the change that touches all the add-on math sites). One review, one revert point each. **The DSC math extraction (4 sites: `pricing.ts` (×2), `ReportsPage.tsx` (×2)) ships as the next pure-refactor PR in the same pattern**, then CHD-10 extends the add-on stack with a second term (`rate × (adults + (childrenIncluded ? children : 0)) × nights` per decision #1's revised breakfast rule), then EXB-01..10 (model + capacity + UX + surfaces + bed inventory) is the next major block. **Zero behavior change** — the pre-existing test suite (1767 tests across 4 layers: 224 shared + 560 admin + 977 admin + 6 infra) passes with no other changes. No schema change, no rules change, no migration. Branch: `refactor/exb-02-addons-extract`. *(EXB-02 entry on `plan/project/ROADMAP.md`.)*

| 145 | **Discount math extracted into `shared/utils/bookingDiscounts.ts` (DSC extraction, approved 2026-07-31).** The discount percentage step `base × (pct/100)` and the clamped `Math.max(subtotal − deduction, 0)` subtraction were duplicated across **39 sites** (spec said 4; actual was 10× that — like EXB-02, the spec undercounted the surface area): `shared/utils/pricing.ts` (×2, the unrounded chain in `calculateBookingTotal`), `admin-app/src/pages/BookingsPage.tsx` (×5, the in-page `computeBookingTotalsFromForm` helper + the receipt-PDF deduction rows), `admin-app/src/pages/ReportsPage.tsx` (×9 across the Sales tab cards, the XLSX export row, and the per-booking deductions table), `guest-app/server/lib/rate-breakdown.ts` (×4, the discount chain in `composeRateBreakdown`), `guest-app/server/handlers/bookings.ts` (×15 across 5 functions: `handleCreateBooking` + `handleCreateCorporateInquiryBooking` + a reschedule-style handler + the reject-discount handler + another reschedule transaction), and `guest-app/src/pages/BookingPage.tsx` (×5: the voucher `useMemo` + the in-page `buildRateBreakdown` helper + the summary panel display). Each site had its own per-step defensive pattern — `Math.round` wrap vs. raw product, per-step `Math.max(..., 0)` clamp vs. only at the end, `discountPct > 0 ? ... : 0` gate vs. unconditional, the `b.discountRejected ? 0 : (b.discountPct || 0)` short-circuit on the reports surfaces, the `nonNegativeFinite` defensive coercion on `rate-breakdown`, the `manualNightlyRate === null` guard on `BookingsPage`. **Decision: extract the two per-step formulas into `shared/` as a pure refactor with zero behavior change.** Two helpers in `shared/utils/bookingDiscounts.ts`: (1) `calculatePercentDiscount(base, pct)` for the percentage step — returns `(Number(base) || 0) × ((Number(pct) || 0) / 100)`. Defensive `Number(x) || 0` on each operand (at least as strong as every historical variant: `(x || 0)`, `nonNegativeFinite(x)`, `Number(x) || 0`). Returns the raw product; caller decides whether to round, clamp, or short-circuit. (2) `calculateVoucherBase(subtotal, deduction)` for the clamped-subtract step — returns `Math.max((Number(subtotal) || 0) − (Number(deduction) || 0), 0)`. Used as both the voucher base (subtotal − seniorDiscount) and the post-voucher subtotal (afterSenior − voucherAmount) — the historical `Math.max(..., 0)` patterns are all the same shape. **The chain stays surface-specific** — every site orders or omits the steps differently, rounds or doesn't, and clamps or doesn't. What gets centralized is the math itself. **Why this is a separate PR from DSC-01..06 (the actual feature work)** — DSC-01..06 introduces admin-toggleable scope (which charge components each discount class applies to: room · breakfast · extra bed, plus DSC-06's VAT breakdown). The scope of "what the discount applies to" is independent of "how the discount is computed". Doing the math extraction first means DSC-01..06 lands on top of one function each instead of editing 4 implementations in lockstep. **Why the spec said 4 sites and the actual was 39** — the spec mentioned only `pricing.ts`, `bookings.ts` (create + corporate inquiry), and `ReportsPage.tsx` (Gross-to-Net + XLSX). The grep pass found 10× that — every site that touches the senior/member percentage step or the voucher-base subtraction was duplicating the math, including `BookingsPage.tsx` (both the in-page helper and the receipt-PDF discount rows), `rate-breakdown.ts` (which is called by every `buildRateBreakdown` / `rebuildRateBreakdown` site), and the two additional server handlers in `bookings.ts` (the line-1901 and line-4157 transactions, plus the reject-discount handler at line 2005). **Two pre-existing source-text tests updated in lockstep** — `fin-05-discounts.test.ts` now pins the new `calculatePercentDiscount(...)` shape (was pinning the old `subtotal × (discountPct/100)` form, test intent unchanged: the senior + member discount chain still produces the same byte-equivalent values), and `audit-admin-sev4-2026-07-07.test.ts` AA-30 now pins `calculatePercentDiscount(storedDiscountBase, b.discountPct)` (was pinning the old `storedDiscountBase × (b.discountPct/100)` form, but the `storedDiscountBase` source — the canonical pre-discount total snapshotted on the booking — is preserved as the assertion guard; the .not.toMatch for `subtotal × (b.discountPct / 100)` is still valid because the file no longer contains that pattern). **CHD-10 is now unblocked** — the children-in-breakfast per CVQ-01 change composes a second add-on term with the existing discount stack, and the math now lives in one function each (EXB-02's `calculateBreakfastAddOn` + DSC's `calculatePercentDiscount` + DSC's `calculateVoucherBase`). **Tests** — new `shared/__tests__/booking-discounts.test.ts` (20 characterization tests) pins the new contracts: basic shape (`base × (pct/100)` returns the raw product for positive inputs), byte-equivalence matrix (every input in `[1000, 999, 500, 200, 50, 0] × [20, 10, 12.5, 50, 0, 100]` produces the same value the inline expression did), defensive coercion per operand (nullish / NaN / 0 on each side independently), the realistic chain scenarios matching the server (`handleCreateBooking` voucher base exactly), the reports (`afterVoucher` clamp exactly), and the receipt-PDF (BookingsPage's two-step `discountBase − senior − voucher` member base exactly). **Zero behavior change** — the pre-existing test suite (1787 tests across 4 layers: 244 shared + 560 admin + 977 admin + 6 infra) passes with no other changes; the +20 are the new booking-discounts characterization tests. No schema change, no rules change, no migration. Branch: `refactor/dsc-discounts-extract`. *(DSC extraction entry on `plan/project/ROADMAP.md`.)*

| 146 | **Children-in-breakfast toggle per booking + admin default (CHD-10, approved 2026-07-31).** Per CVQ-01: "Breakfast for children is an include/exclude option, not a fixed rate." A per-booking `breakfastIncludesChildren` flag, snapshotted onto the booking doc at create time, plus an **admin default** (`settings/breakfastConfig.breakfastIncludesChildrenDefault`) so the front desk is not deciding it per booking. The room rate is unchanged — children are always free of the **room** charge; the toggle only affects the **breakfast** line. **Decision: hotel-wide default (not per room type).** The policy is about the guest, not the room, and the small-change rationale (narrowing is a one-line change; unwinding under-charged bills is not) maps cleanly to a single `breakfastConfig` field. Per room type would split the admin surface for no real semantic gain. **Helper change** — `calculateBreakfastAddOn` now accepts optional `numAdults` + `numChildren` + `breakfastIncludesChildren`. When `numAdults > 0`, the math becomes `rate × (numAdults + (flag ? numChildren : 0)) × nights`. When only `numGuests` is provided (the historical shape — every call site today), the helper falls back to `rate × numGuests × nights` — **byte-equivalent** to the pre-CHD-10 inline patterns. When the flag is nullish, it defaults to `true` (the historical "children pay the full rate" default) so existing bookings without the field render the same total. **The pure-refactor trilogy** (PMH-02 folio + EXB-02 add-on + DSC extraction discount) makes this PR possible — the new math composes with the discount stack cleanly, and the add-on stack now has a second term shape (`flag ? second_term : 0`) that the EXB-01..10 extra-bed work can reuse verbatim. **Why the spec was "blocked on PMH-02 / EXB-02"** — the original CHD-10 was going to add a second term (`rate × (adults + children) × nights`) to six hand-maintained copies of the breakfast formula; that was the original "full rate" answer from decision (1). The CVQ-01 revision to "include/exclude" was the change that made it touch every implementation. Doing the math extractions first means the change is one helper signature, one schema field, and three server reads. **Server behavior** — `handleCreateBooking` reads the per-booking override from the body, snapshots the admin default (with `true` as the safe fallback when no setting exists), writes the result to the booking doc, and passes it to the helper. `handleCreateCorporateInquiryBooking` does the same (no per-booking override on the corporate surface — staff-created, default applies). The reschedule transaction at line 4157 reads the existing `booking.breakfastIncludesChildren` and threads it through (legacy bookings read as `true` via the helper's defensive coercion, so a reschedule never changes the math). **Client behavior** — guest `/book` Step 1 adds a "Children (0-11)" stepper alongside the existing guests stepper. `numAdults` is derived as `guests - numChildren` (validated to stay ≥ 1). The summary panel adds an "Include N children in breakfast" toggle (visible only when `numChildren > 0`) that shows the live price impact: "+₱X for children" when on, "Free for children" when off. The booking-page `breakfastTotal` and the `BookingReviewAside` summary both now route through `calculateBreakfastAddOn` (was an inline `activeBreakfastRate × guests × nights`). **Settings behavior** — admin Settings → Breakfast adds an "Include children in the breakfast charge by default" toggle alongside the existing rate input; saves to `settings/breakfastConfig.breakfastIncludesChildrenDefault`. The admin can flip the policy here as business needs change; existing bookings are unaffected (the snapshot is per-booking, not per-policy). **Tests** — 10 new characterization tests in `shared/__tests__/booking-addons.test.ts` (was 12 EXB-02 tests; +10 CHD-10 tests = 22 total). Covers: numAdults not provided → falls back to numGuests; numAdults + flag false → counts only adults; numAdults + flag true → counts adults + children; flag undefined → defaults to true; numChildren nullish → 0; numGuests ignored when numAdults is provided; the "children stay free" framing holds when the toggle is off. **NOT in this PR (deferred to follow-up items)** — CHD-02 (`maxChildren` per `RoomTypeEntry`) is independent and lands when the room-type form work resumes; CHD-04 (capacity validation splits into adults vs. children) depends on CHD-02; CHD-05/06 (occupancy picker on admin walk-in + corporate `/book` surfaces) is a UI clone of the guest `/book` picker; CHD-07 (display the split in receipt PDF / confirmation email / booking drawer / guest lookup card) — the booking stores the new fields, but the existing displays still show "5 guests" until the surface work lands; CHD-08 (Reports) — Reports shows totals, not per-guest splits, no change needed; CHD-09 (full test suite + MD sync across the CHD block) is the closing item. **EXB-01..10 (extra bed) is now unblocked** — the add-on stack is ready for a second term (`extraBedCount × extraBedRate × nights`), and the discount stack composes cleanly per the DSC extraction. **Zero behavior change for legacy bookings** — the pre-existing test suite (1797 tests across 4 layers: 254 shared + 560 admin + 977 admin + 6 infra) passes with no other changes; the +10 are the new CHD-10 helper tests. No schema change for legacy data (the new fields are all optional + nullish-handled). No rules change, no migration. Branch: `feature/chd-10-breakfast-includes-children`. *(CHD-10 entry on `plan/project/ROADMAP.md`.)*

| 147 | **Extra-bed add-on per booking + per-room-type allowance (EXB-01, approved 2026-08-01).** A per-booking `extraBedCount` + snapshotted `extraBedRate` for rollaway-bed add-ons. Mirrors the existing `breakfastRate` pattern: the room type owns `maxExtraBeds` (allowance) and `extraBedRate` (per-night rate); the booking snapshots the rate at create time so a later rate change never rewrites an existing bill. The math is `extraBedCount × extraBedRate × numNights` via the shared `calculateExtraBedAddOn` helper. **No separate `allowsExtraBed` boolean** — "allowed" is `maxExtraBeds > 0`, which keeps one source of truth instead of two that can disagree. **Why "no separate boolean"** — the spec's reasoning: a separate `allowsExtraBed` flag and a separate `maxExtraBeds > 0` check would create two sources of truth that can drift (e.g. `allowsExtraBed: true, maxExtraBeds: 0` would let the UI render the selector but reject every count, which is silently broken — the same class of bug RTS-01 / NBS-08 / MRB-11 are designed to prevent). **Why hotel-wide default, not per room type** — the spec is "per room type" by owner decision #2 (separate adult and child caps per room type — a Single can allow 0 children while Family allows more). The same logic applies to extra beds: a Single doesn't have floor space; a Family does. `maxExtraBeds: 0` is the safe default for any type that doesn't allow them. **Why `extraBedRate` is per-room-type, not hotel-wide** — extra beds are physically delivered to the room (the bed has to be in the room before check-in, per EXB-08 housekeeping concerns). Some room types might have a higher delivery cost (e.g. suites with more space), some a lower one. Per-room-type pricing gives the hotel the right granularity. **Helper** — new `calculateExtraBedAddOn({ extraBedCount, extraBedRate, numNights })` in `shared/utils/bookingAddOns.ts`. Sibling to `calculateBreakfastAddOn`: same defensive `Number(x) || 0` coercion per operand; nullish / 0 inputs short-circuit to 0. No `hasBreakfast`-style gate (a count of 0 is the "off" state). The math is the historical inline pattern `extraBedCount * extraBedRate * numNights` — byte-equivalent. **CHD-10 paved the way** — the add-on stack now composes cleanly with a second term. The discount stack (DSC extraction) reads both via the existing `subtotal = roomTotal + breakfastTotal + extraBedTotal` reconstruction. The original "full rate" answer (decision #1, superseded by CVQ-01) would have been 6 hand-maintained copies of the breakfast formula; the refactor trilogy (PMH-02 + EXB-02 + DSC extraction) made this a clean helper signature + one schema field + three server reads. **Server behavior** — public create schema + `WalkinBookingSchema` accept optional `extraBedCount` (bounded 0..20). `handleCreateBooking` + `handleCreateWalkin` (which also covers the corporate inquiry conversion — same staff surface, no per-booking override on the UI) validate the count against the room type's `maxExtraBeds` (throws on excess), snapshot the rate, and add the new term to the subtotal. The reschedule transaction at line 4148 reads the existing `booking.extraBedCount` / `booking.extraBedRate` (legacy bookings read as 0 via the helper's defensive coercion, so a reschedule never changes the math). The booking doc write includes the new fields. **Client behavior** — guest `/book` Step 1 gains an "Extra beds" stepper that renders only when the selected room type has `maxExtraBeds > 0` (the spec's "no separate `allowsExtraBed` boolean" rule). The stepper caps at `maxExtraBeds`; switching room types resets the count to 0. The summary panel + `BookingReviewAside` thread the count + rate through so the add-on line and the live price impact are visible. The create payload includes `extraBedCount`. **Settings behavior** — `addRoomType` + `updateRoomType` accept the new fields; `addRoomType` clamps `maxExtraBeds` to ≥ 0 and `extraBedRate` to ≥ 0. The Room Types Add form + Edit modal gain "Max extra beds" (0 = not offered) + "Extra bed rate" (₱/bed/night) inputs. The success toast surfaces the extra-bed terms when `maxExtraBeds > 0`. **Tests** — 11 new characterization tests in `shared/__tests__/booking-addons.test.ts` (happy path, byte-equivalence matrix against the inline pattern, defensive coercion per operand, the realistic 1-bed 3-night + 2-bed 2-night scenarios). **EXB-04 (breakfast follows automatically) holds** — the extra occupant is counted in `numAdults` / `numChildren` (per CHD-10) and therefore in `numGuests`, so the breakfast math picks them up with no extra work. Confirmed by the new tests. **NOT in this PR (deferred to follow-up EXB items)** — EXB-03 (capacity overflow rule `max(0, adults − maxCapacity) + max(0, children − maxChildren) ≤ extraBedCount`, depends on CHD-02 `maxChildren`); EXB-05 (Rates Management matrix column for `extraBedRate`); EXB-07 (admin walk-in + corporate `/book` occupancy pickers — the walk-in path already accepts `extraBedCount` via the schema; the admin UI clone is a follow-up); EXB-08 (surface downstream: receipt PDF / confirmation email / booking drawer / guest lookup card); EXB-10 (bed inventory: hotel-wide rollaway count enforced inside the availability transaction — CVQ-04 already specifies the inventory model). **Zero behavior change for legacy bookings** — the pre-existing test suite (1808 tests across 4 layers: 265 shared + 560 admin + 977 admin + 6 infra) passes with no other changes; the +11 are the new EXB-01 helper tests. No schema change for legacy data (the new fields are all optional + nullish-handled). No rules change, no migration. Branch: `feature/exb-01-extra-bed-model`. *(EXB-01 entry on `plan/project/ROADMAP.md`.)*

| 148 | **Per-class discount scope with per-booking snapshot (DSC-01..05, approved 2026-08-01).** Per CVQ-06: the senior / voucher / member discount chain now respects an admin-configurable per-class **scope** — each class can be narrowed to one or more of {room, breakfast, extra bed}. The "broad" default (all components true) is byte-equivalent to the pre-DSC-01 behavior. A later policy change never rewrites an existing bill because the scope is snapshotted onto each booking at create time (the same per-booking pattern that CHD-10 uses for `breakfastIncludesChildren` and EXB-01 uses for `extraBedRate`). **Why per-class scope, not a single global flag** — the three discount classes are commercially different. Voucher + Spark Rewards are the hotel's own programs (scope them freely, no exposure). Senior/PWD is statutory (RA 9994 / RA 10754) — a careless toggle can configure the hotel into non-compliance. Same mechanism, different guardrails. The decision is to keep one mechanism (the per-class 3×3 matrix) and layer guardrails on the **senior row**: (a) the entire Discounts tab is admin-only in the Settings UI; (b) the senior row is admin-only even on code paths that reach the editor. The voucher + member rows are editable by admin only too (the whole tab is admin-only) but carry no statutory constraint. **Why 3×3 (per component), not 3×1 (one global toggle per class)** — the 3×3 is the most expressive shape that still fits in one editor (3 components × 3 classes = 9 cells), and each cell has a clear semantic (does this class apply to the room subtotal? the breakfast subtotal? the extra-bed subtotal?). The "broad default" is the safe all-true state; narrowing is opt-in. **Why a per-booking snapshot, not a live read on every discount application** — the reschedule transaction (around line 4148 of `bookings.ts`) and the discount-rejection handler both re-apply the chain when the booking moves or the senior ID fails verification. If they read the live `settings/hotelConfig.discountScope` instead of the snapshotted `booking.discountScopeSnapshot`, a later admin scope change would silently rewrite the math for an already-priced booking. The per-booking snapshot pins the scope at create time and is read back on every re-application. **Helper** — new `calculateDiscountChain({ roomTotal, breakfastTotal, extraBedTotal, seniorPct, voucherAmount, memberPct, scope, round })` in `shared/utils/bookingDiscounts.ts`. Decomposes the subtotal by component, applies each class's scope to its own base, runs the senior → voucher → member chain with per-step `Math.round` (`round: true` for the server, `round: false` for the client preview). For the broad default scope the output is byte-equivalent to the pre-DSC-01 chain `subtotal → seniorPwdDiscount → afterSeniorPwd → afterVoucher → memberDiscount → total`. Sibling to `calculateBreakfastAddOn` and `calculateExtraBedAddOn`: same defensive `Number(x) || 0` coercion per operand; nullish / NaN / 0 inputs short-circuit to 0. **`normalizeDiscountScope(scope)`** fills in the broad default for any nullish / partial scope (legacy settings without the field, or a half-loaded snapshot). **`BROAD_DISCOUNT_SCOPE`** is the safe default — every component true for every class. **Server** — `handleCreateBooking` and `handleCreateWalkin` snapshot `settings/hotelConfig.discountScope` onto the booking via `booking.discountScopeSnapshot`; the chain math routes through `calculateDiscountChain` with the snapshotted scope and `round: true`. The walkin honors the manual `totalPriceOverride` collapse (treats the override as the room term, breakfast + extra bed = 0) so a staff-overridden price is byte-equivalent to the previous behavior under the broad scope. The reschedule transaction reads `booking.discountScopeSnapshot` (legacy → broad) and feeds it to the chain. The discount-rejection handler re-applies the post-senior chain (`voucher + member + points`) with `seniorPct: 0` so the rejection respects the snapshotted scope on the voucher + member steps. **`calculateBookingTotal` in `shared/utils/pricing.ts`** now routes through the chain with optional `extraBedTotal` + `discountScope` inputs — byte-equivalent to the pre-DSC-01 client preview for the broad default. **Admin** — new "Discounts" tab in `SettingsPage.tsx` (Percent icon) renders the 3×3 matrix. The whole tab is admin-only (front-desk sees the amber "Admin-only section" placeholder per the existing W3.2 pattern). Each class row has a "Check all" / "Uncheck all" button; an amber advisory banner under the matrix names the statutory scope. `AdminContext.tsx` hydrates `hotelConfig.discountScope` from the snapshot, always via `normalizeDiscountScope` so legacy settings (or partial objects) hydrate to the broad default. The snapshot listener normalizes on every update. **Pinned by 19 new characterization tests** in `shared/__tests__/booking-discounts.test.ts` (broad-scope byte-equivalence, narrow-scope matrix for each cell, defensive coercion, `normalizeDiscountScope` partial-input handling, `BROAD_DISCOUNT_SCOPE` shape). **Implementation shipped on `feature/dsc-01-discount-scope` (v0.197.0, merge `e90a3dc`):** `shared/utils/bookingDiscounts.ts` — new types + helpers; `shared/utils/pricing.ts` — chain routing; `shared/types/index.ts` — `Booking.discountScopeSnapshot`; `guest-app/server/handlers/bookings.ts` — create + walkin + reschedule + reject-discount; `admin-app/src/context/AdminContext.tsx` — `hotelConfig.discountScope` state; `admin-app/src/pages/SettingsPage.tsx` — Discounts tab + matrix editor; `shared/__tests__/booking-discounts.test.ts` — 19 new tests. **Deferred follow-ups (tracked in the ROADMAP):** DSC-06 (VAT breakdown on top of the scope-aware chain), DSC-07 (live-preview / receipt-PDF surfaces showing the scoped deductions), DSC-08 (admin booking drawer surfaces the snapshot), and the follow-on scope-tighten for the voucher's base when the senior step itself is scoped (the chain's voucher cap is computed against the chain's scoped voucher base, so the chain itself is correct; the only narrow-scope edge case left is the walkin path which still uses the unscoped senior step for the voucher's pre-cap when `totalPriceOverride` is set — a known limitation, fix in a follow-up if needed). |

| 149 | **VAT breakdown (12% VAT, VATable Sales, VAT-Exempt Sales, VAT Amount) per decision #115 + DSC-06 (approved 2026-08-01).** Decision #115 (logged 2026-07-24) stated the system "calculates and exports the tax breakdown (12% VAT, VATable Sales, VAT-Exempt Sales for OSCA-mandated Senior/PWD bookings, and VAT Amount) client-side in the reports and XLSX exports" but shipped only the *label* "Senior Citizen & PWD Deductions (20% Exemption)" on the Gross-to-Net bridge. FIN-06 was closed in the archive as "decision logged in `DECISIONS-FEATURES.md`" — accurate, but the calculation never followed. **Two consequences** at the time: (a) the hotel's accountant was expecting monthly figures the system did not produce; (b) VAT exemption and the 20% discount are linked under RA 9994, so DSC's scope choice should apply consistently to the VAT-exempt base — and there was no VAT-exempt base to be consistent with. **Decision: build the minimal version, pre-launch.** 12% VAT is arithmetic on figures already computed, and VAT-exempt classification is just "did this booking carry a senior/PWD discount" — so the breakdown is three or four extra columns on the existing XLSX export plus the matching Reports lines, not a tax engine. The alternative was the hotel's accountant deriving it by hand every month forever. **Coordinate with DSC-01..05:** the discount scope chosen there determines the VAT-exempt base, so build this *after* DSC-01/02 so the two agree rather than drifting. **Model** (the simplest defensible shape that matches Philippine BIR conventions for hotel receipts under RA 9994): `vatExclusiveSales = totalPrice / (1 + vatRate)` (the bill, divided by 1.12 to extract the embedded VAT — for non-senior the full bill is VATable; for senior the senior discount is reported separately as a VAT-exempt sale and the VAT is computed on the net bill); `vatExemptSales = seniorDiscount` (RA 9994; 0 for non-senior); `vatAmount = vatExclusiveSales × vatRate` (12% of the VATable base). **Why this model and not the alternative** — the BIR alternative would be "report the original bill's VATable, then subtract the senior discount from VATable, then 12% the remainder." This requires the gross bill to be reconstructed (it isn't stored per-booking — the booking stores the *net* totalPrice), so the model would have to derive the gross as `totalPrice + seniorDiscount + voucherDiscount + memberDiscount + pointsValue` (approximately). The chosen model sidesteps that entirely by computing the VAT on the net bill (which *is* stored) and reporting the senior discount as a separate VAT-exempt sale — the BIR's standard interpretation of RA 9994. The reconciliation still works: `vatExclusiveSales + vatAmount = totalPrice` for any input (pinned by the test "reconciles end-to-end"). **Helper** — new `calculateVatBreakdown({ totalPrice, seniorDiscountAmount, vatRate })` in `shared/utils/bookingVat.ts`. Sibling to `computeBookingFolio`, `calculateBreakfastAddOn`, `calculateExtraBedAddOn`, and the discount helpers: same defensive `Number(x) || 0` coercion per operand, `Math.max(x, 0)` clamp on negatives, nullish / NaN / 0 inputs short-circuit. Default `vatRate: 0.12` (configurable for testing + non-PH deployments; the 0% zero-rated case is pinned by the test "supports a 0% vatRate (zero-rated or exempt industry)"). The helper is scope-agnostic — it just takes the senior discount amount as input. The DSC-01..05 chain computes the senior discount correctly under any scope; a narrow senior scope flows through correctly because the exempt portion is exactly the senior's scoped share, no more. **Coordination with DSC-01..05 is automatic** — the per-booking `discountScopeSnapshot` field (decision #148) means a narrow senior scope produces a smaller senior discount, and the VAT helper reports exactly that amount as VAT-Exempt. The same chain that already respects the scope for the senior deduction now also respects the scope for the VAT exemption. **No server change** — the VAT computation is fully client-side (the surface for the accountant's monthly export) per the spec's "arithmetic on figures already computed" framing. The senior discount flows in from the existing chain math, which already runs in every server path (create / walkin / reschedule / reject-discount). **Reports surfaces** — `ReportsPage.tsx` gains a `vatSummary` useMemo computed alongside `discountsSummary` (same `rangeBookings` input, same chain). Threaded as a prop to the `SalesTab` component (alongside `discountsSummary`). 4 new on-screen cards: VAT Rate (12%), VATable Sales (VAT-exclusive), VAT-Exempt Sales (RA 9994), VAT Amount (12%). A reconciliation line below the cards verifies `VATable + VAT = Net Bookings Revenue`. The XLSX export (`handleExportSalesXLSX`) gains 4 new rows on the Summary sheet + 3 new per-booking columns on the Bookings sheet (VATable Sales, VAT-Exempt Sales, VAT Amount) so the accountant can reconcile monthly figures back to individual receipts. The per-booking VAT columns flow through the same `calculateVatBreakdown` helper, so the per-row math matches the on-screen + summary numbers exactly. **Tests** — 17 new characterization tests in `shared/__tests__/booking-vat.test.ts` (happy paths: non-senior, senior, senior + voucher, senior + voucher + member; defensive coercion: nullish / NaN / negative inputs per operand; alternative `vatRate`: 0% zero-rated, 10% test; the BIR reconciliation rule: `vatExclusiveSales + vatAmount = totalPrice` for `[100, 500, 1000, 1234.56, 9999.99]`). Total test count: 1844 across 4 layers (shared 301, guest-app api 560, admin-app 977, infra 6). **Implementation shipped on `feature/dsc-06-vat-breakdown` (v0.198.0, merge `8f8f62a`):** `shared/utils/bookingVat.ts` (new helper + 2 interfaces); `shared/__tests__/booking-vat.test.ts` (17 new tests); `shared/index.ts` (re-export); `admin-app/src/pages/ReportsPage.tsx` (the `vatSummary` useMemo + SalesTab prop + 4 new on-screen cards + 3 new per-booking XLSX columns + 4 new summary rows). No server change. **NOT in this PR (deferred follow-ups)** — the `discountScopeSnapshot` field is read by every server path; the VAT breakdown is read by the reports surface only. A future "VAT breakdown on the booking receipt" (DSC-07-adjacent) would thread the same helper into the receipt PDF + the booking drawer; a future "monthly BIR form export" (DSC-08-adjacent) would add a dedicated XLSX sheet summing the per-booking VAT columns by month. Both are surface work, not math work — the helper is the canonical answer. **Zero behavior change for legacy bookings** — the pre-existing test suite (1827 tests across 4 layers) passes with no other changes; the +17 are the new VAT helper tests. No schema change, no rules change, no migration. Branch: `feature/dsc-06-vat-breakdown`. *(DSC-06 entry on `plan/project/ROADMAP.md`.)* |

| 150 | **VAT breakdown surfaces — receipt PDF, admin booking drawer, guest /book live preview (DSC-07, approved 2026-08-01).** Per the DSC-06 spec note: the VAT breakdown (12% VAT, VATable Sales, VAT-Exempt Sales, VAT Amount) that the XLSX export + Reports Sales tab now compute (decision #149) belongs on every surface the hotel hands to a guest or uses internally. The DSC-06 row flagged the receipt PDF + admin booking drawer + guest /book live preview as the three surfaces to wire next; this PR ships all three. **Helper** — new `getBookingVatBreakdown(booking)` in `shared/utils/bookingVat.ts`. Takes the booking's stored fields (`totalPrice` + `originalTotalPrice` + `discountType` + `discountPct` + `discountRejected`) and returns the full VAT breakdown via the existing `calculateVatBreakdown` helper from #149. Uses the existing `calculatePercentDiscount` helper from the discount chain for the senior step (broad-scope approximation; documented narrow-scope edge case in the helper header). **Why these three surfaces, in this order** — the receipt PDF is the surface the hotel hands to every guest (it should show the same VAT breakdown the monthly XLSX export does, the same way a BIR-issued receipt would); the admin booking drawer is the staff view of the same booking (it should match the receipt for consistency); the guest /book live preview is the price-impact panel the guest sees before submitting (it should preview the same VAT figures the receipt will show). All three compose with the DSC-01..05 chain automatically — a narrow senior scope produces a smaller senior discount, and the receipt path reads the exact value from the chain-written `rateBreakdown.deductions` (so the receipt VAT is exact for both broad and narrow scope); the drawer + live preview use the broad-scope approximation (a future follow-up can tighten by reading the snapshotted scope + the per-component split, the same way the reports surface will). **Receipt PDF** (`admin-app/src/pages/BookingsPage.tsx:printBookingReceiptPDF`) — new "VAT Breakdown (12% Philippine standard)" section after the Booking Total banner, before the Folio Charges. 3 lines via the existing `drawAmountRow` helper (VATable Sales, VAT-Exempt Sales, VAT Amount). Same VAT computation as the XLSX export; the senior discount flows in from the booking's stored fields via the helper. **Admin booking drawer** (`AdminPriceBreakdown` component) — 3 muted lines (11px text, dashed border above) below the "Total Bill Amount" line. Component signature now takes `originalTotalPrice` + `discountType` + `discountPct` + `discountRejected` (all optional); the call site at the drawer's collapsible rate breakdown threads all 4 from the selected booking. **Guest /book live preview** (`<PriceBreakdown>` + `<BookingReviewAside>` in `guest-app/`) — 3 muted VAT lines below the "Total" line. The receipt path (`<PriceBreakdown>`) reads the senior discount from `rateBreakdown.deductions` (the chain already wrote it there at server time), so the VAT math is exact for both broad and narrow scope on the receipt path. The live preview path (`<BookingReviewAside>`) uses the simple formula `discountPct × subtotal` for the senior deduction; byte-equivalent to the chain for broad scope. **Known narrow-scope edge case** — the drawer + live preview use the broad-scope formula for the senior deduction, which over-estimates the VAT-exempt portion for narrow-scope bookings. The over-estimate is at most the scope-removed portion (e.g. the breakfast + extra-bed 20% if the senior scope is "room only"). This is a *conservative over-claim* (the senior is under-charged; the hotel over-reports the VAT-exempt sale and under-reports the VATable base), which is the safer direction for a BIR audit (BIR audits the under-claim, not the over-claim). The receipt path is exact (reads the chain-written value). A follow-up can tighten the drawer + live preview to read the snapshotted scope + the per-component split (the same way the reports surface will). **No server change** — the helper is fully client-side, the same way DSC-06 was; the booking already stores every figure the helper reads (`originalTotalPrice` + `discountType` + `discountPct` + `discountRejected` are all on the Booking type from the start). **Tests** — 13 net new characterization tests in `shared/__tests__/booking-vat.test.ts` for the new helper (happy paths: non-senior, senior, PWD-as-senior, rejected-senior, alternative vatRate; defensive coercion: nullish / NaN / 0 inputs per field; byte-equivalence with the broad-scope chain for senior-only and senior + voucher scenarios). Total test count: 1857 across 4 layers (shared 314, guest-app api 560, admin-app 977, infra 6). **Implementation shipped on `feature/dsc-07-vat-surfaces` (v0.199.0, merge `7700b17`):** `shared/utils/bookingVat.ts` (new helper + `BookingForVat` interface); `shared/__tests__/booking-vat.test.ts` (13 new tests); `admin-app/src/pages/BookingsPage.tsx` (receipt PDF + booking drawer); `guest-app/src/components/PriceBreakdown.tsx` (receipt path); `guest-app/src/pages/BookingPage.tsx` (live preview path). No server change. **NOT in this PR (deferred follow-ups)** — the narrow-scope edge case (drawer + live preview over-estimating the VAT-exempt portion for narrow senior scopes); the "monthly BIR form export" (DSC-08-adjacent, a dedicated XLSX sheet summing the per-booking VAT columns by month); the booking-confirmation email body (DSC-07-adjacent, threads the same helper into the email PDF). All three are surface work, not math work — the helper is the canonical answer. **Zero behavior change for legacy bookings** — the pre-existing test suite (1844 tests across 4 layers) passes with no other changes; the +13 are the new helper tests. No schema change, no rules change, no migration. Branch: `feature/dsc-07-vat-surfaces`. *(DSC-07 entry on `plan/project/ROADMAP.md`.)* |

| 151 | **Weekend Rate Visibility on the Step 1 room cards (WRV-01..03, approved 2026-08-01).** The Step 1 room-type cards' "From {base rate} per night" headline was the only signal of the night's price before the guest reached the totals, and it silently misled on any non-regular rate. The rate panel below the options (the "This stay uses mixed nightly rates" breakdown) was gated on `roomLines.length > 1` — so a Saturday→Sunday weekend-only stay, the most common weekend booking, produced a single rate line and the panel didn't render at all. The card then read "From ₱2,400 per night" while the total charged ₱2,700. The "From" hedge technically covered it, but it read as a pricing surprise, and it was the worst-case version precisely because no second line made the weekend rate self-evident. The same blind spot applied to a stay fully inside a seasonal/holiday window. **Decision: drop the line-count gate and show the panel whenever any line is non-regular.** The single source renders as one labelled line (e.g. "Weekend nights: 1 × ₱2,700" with the line subtotal on the right) — no "mixed nightly rates" claim, which would be false. Mixed stays (regular + weekend, weekend + seasonal, etc.) keep the existing "This stay uses mixed nightly rates" heading + per-source line list unchanged. **Decision: option price labels match the selected stay.** The Room Only and Room + Breakfast options kept showing the base weekday rate per night even when every selected night used a higher weekend/seasonal rate. For a one-source stay, the option shows that source's actual nightly amount (e.g. ₱2,700 for a weekend-only stay). For a multi-source stay, the option price is prefixed with "From" and the breakdown below the options reconciles the actual mix + exact total. Fully regular stays fall through with no "From" prefix and the base rate (the existing "no From" path stays correct for them). **Why the option labels use the existing `typeRoomBreakdown` and the existing breakfast calculation, not a second pricing formula** — the option price is derived from the same `roomLines[0].nightlyRate` (single source) or the base rate (multi source) and added to the existing `liveBreakfastRate × effectiveBreakfastOccupancy` breakfast term. No new math, no new utility, no second source of truth. The option total is unchanged (`roomOnlyTotal` / `breakfastTotal` from `calculateBookingTotal`, which the chain already feeds). **Why the existing "From" headline above the option cards stays unconditional** — the headline is the only piece of "rate signal" the guest sees before scrolling; hedging it as "From" is correct for every case (regular, single-source non-regular, multi-source). The fix is on the option labels, which currently show a specific nightly amount and therefore make a specific promise. **Why no helper extraction, no schema change, no server change** — the change is purely Step 1 UI. The breakdown data is already produced server-side via `calculateSeasonalAwareRoomBreakdown` and is already flowing into the panel; the option labels just need to read from the same `roomLines[0].nightlyRate` (single source) or the base rate (multi source). The booking persists the same fields as before. **Tests** — new `guest-app/src/__tests__/weekend-rate-visibility.test.ts` (7 source-text guards) pins: the gate is `hasNonRegularRate = roomLines.some(line => line.source !== "regular")` (NOT `roomLines.length > 1`); the single-source panel renders one line item WITHOUT the "mixed nightly rates" heading; the multi-source panel keeps the existing heading + line list; the entire panel is gated on `hasNonRegularRate`; both option price labels use the `optionRatePrefix` + `optionNightlyRate` derived from `roomLines[0].nightlyRate` (single source) or the base rate (multi source); fully regular stays get no "From" prefix. **Implementation** — `guest-app/src/pages/BookingPage.tsx`: replaced the `hasMixedRates = roomLines.length > 1` gate with `isSingleSource` + `hasNonRegularRate` + `isMultiSource`; the panel renders the single line directly for one-source stays, the existing heading + list for multi-source; both option price labels now read from `optionRatePrefix` + `optionNightlyRate` (single source = `roomLines[0].nightlyRate`, multi source = base rate with "From" prefix). **Zero behavior change for fully regular stays** — the pre-existing test suite (1857 tests across 4 layers) passes with no other changes; the +7 are the new source-text guards. No schema change, no rules change, no migration. **NOT in this PR (deferred follow-ups)** — the "From" headline above the option cards is unchanged (still unconditional); the Step 3 review's full breakdown is unchanged (the `PriceBreakdown` already shows all rate lines). The WRV fix is bounded to the Step 1 option cards because that is the only surface where the headline silently misled against a specific option price. Branch: `feature/wrv-weekend-rate-visibility`. *(WRV-01..03 entry on `plan/project/ROADMAP.md`.)* |

| 144 | **Children in booking — adults + children split with per-room-type child cap (CHD-01..02 + CHD-04, approved 2026-08-01).** Per owner request 2026-07-31: split occupancy into **adults (12+)** and **children (0–11, free of the room rate)**, with a configurable per-room-type child limit. CHD-10 (the breakfast toggle) shipped the breakfast side of the change; this PR ships the occupancy side. **Decision: `numGuests` keeps its current meaning — total occupancy.** `numAdults` + `numChildren` are added *alongside* `numGuests`, not replacing it. Every existing read site, the rate breakdown, and the capacity displays keep working. Legacy bookings without the new fields read as `numAdults = numGuests`, `numChildren = 0` (the historical "all guests are adults" shape, byte-equivalent to pre-CHD-01). The new fields are optional + nullable in the Booking type. **Decision: split is server-validated, client-supplied but not client-trusted.** The server derives the split from the request body, validates `numAdults + numChildren === guests` (the spec's "no trusting either value from the client" rule), and rejects any client-supplied total that disagrees. The client picks the split via the existing CHD-10 stepper (`numAdults` is derived as `guests - numChildren` in the client), and the server re-derives + re-validates before the booking write. **Decision: `maxCapacity` is now the ADULT cap.** The semantic shift is safe precisely because every existing booking has `numChildren = 0`. A Single realistically allows 0 children even though the client's stated default is 2; the default seed encodes the per-type reality (Single 0, Std Double 1, Std Twin 1, Executive 1, Family 2) so the helper never overrides the admin's choice when present. The new `maxChildren` field is optional + nullable in the type; legacy settings without it hydrate via the new `normalizeMaxChildren(raw, maxCapacity)` helper in `shared/utils/roomTypes.ts` (falls back to the per-capacity default). The single normalization point — `applyRoomTypeDefaults(raw)` — is what every read site (rooms.ts availability + room-blocks + the create / walkin / reschedule transactions) routes through. **Why `numChildren > maxChildren` is a hard reject (not a soft cap)** — the spec calls for "separate adult and child caps per room type — a Single can allow 0 children while Family allows more". A soft cap (allow but warn) would be silently broken in the same class as RTS-01 / NBS-08 / MRB-11 (the array-write-integrity / unknown-source / double-count bugs the spec keeps flagging). The hard reject is the only authority. **Why the breakfast helper is unaffected by this PR** — `calculateBreakfastAddOn` (per CHD-10) already accepts `numAdults` + `numChildren` + `breakfastIncludesChildren`. The handleCreateBooking call now passes the split when present, so the breakfast math tracks the persisted booking fields, not just `numGuests`. The CHD-10 helper paved the way; CHD-01 just calls it with the new shape. **Why "count only, no per-child ages"** — the owner decision 2026-07-31 #4. The guest picks "how many children aged 0–11", the age rule is stated in the UI, and no dates of birth are collected (which also keeps identifiable data about minors out of scope for RA 10173). Per-child ages would require a per-child field on the booking (one per child) + a per-child display in the booking drawer + a per-child field in the receipt PDF. The "how many" shape is what the hotel needs to know (count vs. ages is irrelevant to housekeeping + the billing math). **Why the room-block handler is affected by the same PEX-02 + CHD-04 logic** — a room block on dates that include an expired `pending` hold does not block the block (the hold is not occupying per the PEX-02 rule). A room block on dates that include a confirmed booking does block. The room-blocks handler now routes through `isBookingOccupyingRoom` + the per-doc filter; the new CHD-04 capacity validation does not apply to the block path (a block has no occupancy, it's a staff action). **0 schema + 0 rules + 0 server-only change** — the `maxChildren` field is optional + nullable in the type; the new `numAdults` + `numChildren` booking fields are optional + nullable too. The `addRoomType` + `updateRoomType` admin context API accepts the new field (the Settings UI for the child cap is a focused follow-up PR — the data path + normalization are in place so the UI is a clone of the existing `maxExtraBeds` / `extraBedRate` editors). **Tests** — 7 unit tests on the `normalizeMaxChildren` + `applyRoomTypeDefaults` helpers in `shared/__tests__/room-types.test.ts` (valid values; fractional flooring; per-capacity default for 1/2/3+ adult caps; the safe fallback when capacity is missing; the admin's choice is respected when `maxChildren` is explicit; null/undefined/non-object inputs return the safe default). 11 source-text guards in `guest-app/tests/api/chd-children-occupancy.test.ts` (Booking type carries the optional fields; the public create schema accepts the optional split; the new walk-in booking is written with the snapshotted split on the doc; `calculateBreakfastAddOn` receives the split when present; `DEFAULT_ROOM_TYPES` seed encodes the per-type reality; `normalizeMaxChildren` falls back to a per-capacity default; `handleCreateBooking` validates `numAdults + numChildren === guests`; `handleCreateBooking` rejects `numAdults > maxCapacity`; `handleCreateBooking` rejects `numChildren > maxChildren`; `handleCreateWalkin` applies the same two-checks). **Missing** — the Java-emulator behavioral tests for the CHD-04 race (a confirmed booking arrives while an admin narrows `maxChildren` for the room type) require Java and are out of scope for this sandbox (PMH-03's documented constraint). The source-text guards above pin the contract those tests will later exercise. **Implementation** — `shared/types/index.ts` (Booking type gains optional `numAdults` + `numChildren`) + `shared/constants/index.ts` (`RoomTypeEntry` type + `DEFAULT_ROOM_TYPES` seed gain `maxChildren`) + `shared/utils/roomTypes.ts` (new — `normalizeMaxChildren` + `applyRoomTypeDefaults`) + `shared/schemas/booking.ts` (the optional split on the walk-in schema) + `shared/index.ts` (re-export the helper) + `admin-app/src/context/AdminContext.tsx` (the `addRoomType` + `updateRoomType` API accepts `maxChildren`) + `guest-app/server/handlers/bookings.ts` (the public create schema + `handleCreateBooking` + `handleCreateWalkin` accept + validate + persist the split; `calculateBreakfastAddOn` receives the split when present) + `shared/__tests__/room-types.test.ts` (new — 7 unit tests) + `guest-app/tests/api/chd-children-occupancy.test.ts` (new — 11 source-text guards). Branch: `feature/chd-children-occupancy`. *(CHD-01..02 + CHD-04 entries on `plan/project/ROADMAP.md`; CHD-03..09 deferred follow-ups per the spec's MD-sync list.)* |

| 152 | **Room-type delete confirm + empty room-types re-hydrate (RTS-05 + RTS-06, approved 2026-08-01).** Two real-but-not-active defects from the original RTS-01..04 batch ship together. **RTS-05** — the room-type Delete button used a 3-second auto-disarm "Click to confirm" pattern. There was no countdown or progress affordance, so a user who clicked Delete, read the label change, hesitated past 3s, and clicked again simply RE-armed it. Repeat indefinitely and nothing ever deletes. **Decision: replace the auto-disarm with a proper `Modal + ConfirmForm`** (matching the RoomsPage delete flow) using the `ConfirmForm` primitive Phase 11.7 already ships. `variant="danger"` + `reasonRequired: true` — destructive action requires a typed audit-log reason before the confirm button enables. A blocking "cannot delete while N rooms still use this type" branch shows a different `variant="primary"` ConfirmForm (matching the RoomsPage blocked path) so the destructive action never appears for an in-use type. `isDeletingRoomType` state tracks the in-flight request and disables the cancel during the round-trip. The form's onConfirm wires to `handleDeleteRoomType(pendingDeleteRoomType.value)`; onCancel + the Modal's onClose both clear the state. **RTS-06** — the `roomTypes` sync effect in `AdminContext.tsx` was guarded by `hotelConfig.roomTypes.length > 0`. A hotel that deleted every room type had `[]` persisted correctly, but on the next page load the effect skipped, leaving the local state on its `DEFAULT_ROOM_TYPES` initializer — every deleted type silently reappeared. **Decision: add a `roomTypesLoaded: boolean` flag** (initial: `false`, flipped to `true` inside the same effect that calls `setRoomTypes`). Drop the `length > 0` guard — any array value (including `[]`) now syncs, and the flag tracks "the snapshot has been observed at least once". The initial paint still shows the `DEFAULT_ROOM_TYPES` initializer so the UI is not blank before the first snapshot arrives. **Why a separate flag and not a check on `hotelConfig` itself** — the local `hotelConfig` state is initialized with `roomTypes: [...DEFAULT_ROOM_TYPES]`, so checking the array's contents (e.g. `length > 0`) cannot distinguish "still on the initial value" from "loaded and legitimately empty". A separate boolean tracks the transition cleanly. **Why a flag and not `null` as the initial value of `roomTypes`** — the `roomTypes` state is consumed by ~20+ read sites across `BookingsPage`, `CalendarPage`, `RatesPage`, the dashboard, and the booking flow. Switching the type to `RoomTypeEntry[] | null` would force every read site to handle the null branch. The flag is local to `AdminContext` and changes the read sites' type by zero. **Why a flag and not "track the last-seen value"** — the snapshot path is idempotent (the same `hotelConfig.roomTypes` value triggers the same effect), so a memoized "have we seen this exact array before" check would not work. A boolean is simpler and the spec's recommendation. **Why the blocked path uses a different `ConfirmForm` variant** — the destructive form is for an action the user is about to take; the blocked form is informational (the action cannot proceed). Mixing them into one form with conditional fields would make the audit-log reason field appear for a confirmation that the user cannot actually complete, which is a small UX tax. Two forms keeps the destructive form lean and the blocked form honest. **No schema, no rules, no server change** — both fixes are local to the admin app (`SettingsPage.tsx` for RTS-05, `AdminContext.tsx` for RTS-06). The booking persists the same fields, the rules accept the same writes, the server returns the same shape. **Tests** — new `admin-app/src/__tests__/rts-05-06-confirm-empty-rehydrate.test.ts` (7 source-text guards): the 3-second timer is gone from the room-type delete useEffect, the Delete buttons just call `setPendingDeleteRoomType(type)`, the Modal uses the type's label in the title, the confirm form uses `variant="danger"` + `reasonRequired`, the import path matches the RoomsPage pattern, the blocking branch has its own `testId`, the `roomTypesLoaded` flag is declared with `useState(false)`, and the `length > 0` guard is removed. The pre-existing 977 admin tests pass with no other changes. **Implementation** — `admin-app/src/pages/SettingsPage.tsx` + `admin-app/src/context/AdminContext.tsx` + `admin-app/src/__tests__/rts-05-06-confirm-empty-rehydrate.test.ts`. Branch: `fix/rts-05-06-confirm-empty-rehydrate`. *(RTS-05 + RTS-06 entries on `plan/project/ROADMAP.md`.)* |

| 147 | **Pending booking expiry (PEX, approved 2026-08-01).** Per CVQ-12 (2026-07-31): the hotel accepted a no-cap policy on rooms per online booking — "all if available". Under the existing shape, an abandoned `pending` booking held its room indefinitely (no expiry + no auto-cancel), and a single submitter could hold every available room by submitting N requests per minute. The expiry is the *only* bound on a whole-hotel hold, so the design is load-bearing. **Why a snapshotted `holdExpiresAt` and not a live-read of the admin's window** — the spec calls for a "longer window for 1–2 rooms and a shorter one for large groups", which the admin can tune post-launch. A live read at every occupancy check would re-evaluate the deadline on every read of every booking (the create transaction's per-candidate overlap check, the public availability endpoint, the room-blocks check, the reschedule transaction). A live read would also mean a future Settings change retroactively shortens a guest's promise. **Decision: snapshot the deadline onto each booking at create time from `settings/hotelConfig.paymentHoldWindowHours`**, write `holdExpiresAt: Timestamp` to the booking doc, and have every read site key on the snapshotted value. A later Settings change never rewrites an existing guest's promise. **Why 24h default, not per-room-count** — per-room-count is the spec's preference ("longer for 1–2 rooms, shorter for large groups"), but the spec also says "record the chosen values and reasoning in decision #147". 24h is a conservative default that the hotel can tune via the Settings UI in a follow-up PR. The data path + the normalization helper (`normalizePaymentHoldWindowHours` clamps to 1..72h) are in place so the UI is a straightforward clone of the existing DSC Discounts tab. **Why the `isBookingOccupyingRoom` rule is the only authority, and why it has both a status gate and a deadline gate** — three different surfaces ask "does this booking block the room?" (the create / walkin / reschedule transactions, the public availability endpoint, the room-blocks handler). A `status in [...]` Firestore query is cheap (index-friendly) but a coarse filter — it cannot express "this `pending` booking's deadline has passed". A `createdAt + window-hours` heuristic at every read site is brittle (a status change resets the window, the heuristic drifts between call sites). **Decision: one shared rule, one source of truth.** The `status in [...]` Firestore query stays as the coarse pre-selector (the index is already there), and the per-doc `isBookingOccupyingRoom` filter is the authoritative gate. An expired `pending` hold does not occupy; a `payment-uploaded` booking always occupies (staff-review, never auto-expired per PEX-04); a `pending` booking without `holdExpiresAt` occupies (legacy default — the field was added by this change, so the absence means the booking was created under the pre-PEX rules and should not be silently freed). **Why atomic retirement in the create transaction, not just in the cron** — the in-transaction retirement is the *primary* path: when the next guest tries to take a room, the create transaction finds the expired hold, atomically cancels it, and creates the new booking in the same Firestore write. A separate cron would race the create transaction (the cron could fire between the candidate-loop read and the booking's `transaction.set`, retaking the room). The cron is the *secondary* path for holds nobody claimed. The two paths compose: every retirement in the create / walkin / reschedule transaction is atomic with the new booking; the cron catches leftovers. **Why the retirement writes `cancellationReason: "payment-hold-expired"` (a specific string), not a generic "cancelled"** — Reports + guest lookup filter on this string (the cancelled-with-expiry bucket is its own metric for "how many expired holds did we retire today"). The string is the `EXPIRED_HOLD_CANCELLATION_REASON` constant in `shared/utils/bookingOccupancy.ts`, exported and pinned by both the in-transaction retirement and the cron. **Why reject-payment is now transactional and stamps a fresh `holdExpiresAt` from `paymentRejectedAt`** — the previous read+update was non-transactional. A parallel admin shortening the window could land between the read and the update, producing a stale deadline. The transaction guarantees the window we read is the window we snapshot. The fresh deadline is computed from the same `paymentHoldWindowHours` the create handler used — the guest gets the same window they had on the first proof upload, not a shorter one derived from a `createdAt` heuristic. The retained `paymentProofPath` / legacy `paymentProofUrl` are audit evidence only and do NOT exempt the booking — the deadline is the only authority. **Why no advance-warning email** — Hobby cron precision is daily, and the spec's per-room-count windows may be shorter than 24h. We do not promise a window the deployment cannot guarantee; the immediate disclosure (Step 4 + booking-submitted email) is the only path. A proof re-upload returns the booking to `payment-uploaded` (staff-review, no auto-expiry) — that state transition is the "expiry is paused" message in the email. **Why the cron lives in `guest-app/vercel.json`, NOT the monorepo-root** — the deployed guest/API Vercel project is the project-local config owner (per `DEPLOY.md`); the monorepo-root `vercel.json` is the Vercel-wide project for the admin app. Registering the cron in the wrong file would either fail to fire (the project-local config doesn't know about it) or fire against the admin project (the wrong route). Pinned by the source-text test "must not be registered in monorepo-root vercel.json". **Why `MRB-06` is unblocked** — the multi-room Step 1 surfaces a quantity selector per available room type. Under the no-cap policy, one unpaid request could otherwise hold the entire hotel. PEX closes the vector: the in-transaction retirement at MRB-06 (which generalizes the create transaction to N rooms) is the same pattern as handleCreateBooking's per-candidate loop, just N-candidate. The atomic retirement guarantee transfers to MRB unchanged. **0 schema rules + 0 server-only change** — the `holdExpiresAt` field is optional + nullable (legacy bookings without it read as "occupies indefinitely" per the null-deadline rule). The new `/api/holds/expire` route is gated by `CRON_SECRET`, same as the existing janitor / storage-sweep / notifications-prune crons. The `paymentHoldWindowHours` setting field is admin-editable via the existing `updateSettings("hotelConfig", ...)` path; the hydrate normalizes legacy values to the 24h default. **Tests** — 16 unit tests on the shared rule + the helpers in `shared/__tests__/booking-occupancy.test.ts` (status-gate matrix, deadline-gate matrix, defensive coercion, the canonical 5-value `BOOKING_OCCUPYING_STATUSES` constant, the documented `EXPIRED_HOLD_CANCELLATION_REASON` string, the `DEFAULT_PAYMENT_HOLD_WINDOW_HOURS` = 24 constant, the 1..72h clamp). 20 source-text guards in `guest-app/tests/api/pex-hold-expiry.test.ts` (PEX-01..07 contract: the create handler writes `holdExpiresAt`; the shared rule is imported + applied at every call site; the in-transaction retirement writes `cancellationReason: EXPIRED_HOLD_CANCELLATION_REASON`; the per-retirement `booking-cancelled` email fires from the post-transaction path; `handleRejectPayment` is transactional and stamps a fresh deadline; the cron is CRON_SECRET-gated, idempotent, registered in `guest-app/vercel.json` ONLY; the legacy-booking rule via the Firestore coarse filter not matching missing `holdExpiresAt`; the Booking type carries the new optional field). **Missing** — the Java-emulator behavioral tests (PEX-07's "expired hold + replacement booking + late payment cannot produce two occupying bookings" race) require Java and are out of scope for this sandbox (PMH-03's documented constraint). The source-text guards above pin the contract those tests will exercise. **Implementation** — `shared/utils/bookingOccupancy.ts` (new, 4.7 KB) + `shared/types/index.ts` + `shared/index.ts` + `admin-app/src/context/AdminContext.tsx` + `admin-app/src/__tests__/launch-readiness-must-fix.test.ts` + `guest-app/server/handlers/bookings.ts` (the create + walkin + reschedule + reject-payment handlers + `getOccupancyConflictReason` + the new `isExpiredPendingHold` helper) + `guest-app/server/handlers/rooms.ts` + `guest-app/server/handlers/room-blocks.ts` + `guest-app/server/handlers/hold-expiry.ts` (new) + `guest-app/server/apiRouter.ts` + `guest-app/vercel.json` + `shared/__tests__/booking-occupancy.test.ts` (new) + `guest-app/tests/api/pex-hold-expiry.test.ts` (new). Branch: `feature/pex-pending-booking-expiry`. *(PEX-01..06 + PEX-04 + PEX-05 entries on `plan/project/ROADMAP.md`; PEX-07 marked partial with the source-text guard scope documented; PEX-08 ships the MD sync itself.)* |
| 153 | **Capacity overflow rule — extra beds grant additional occupant slots usable by an adult OR a child (EXB-03, approved 2026-08-01).** Per owner decision 2026-07-31 #1 + decision #145. The CHD-04 capacity validation (decision #144) was written as two independent hard rejects — `numAdults > typeMaxCapacity` and `numChildren > typeMaxChildren` — which is the right shape for rooms that don't take extra beds, but is **wrong for rooms with extra beds**. Writing it as two independent raised caps double-counts the beds: a Family with `maxCapacity: 4, maxChildren: 2, maxExtraBeds: 1` would naively allow `max(0, adults − 4) + max(0, children − 2) ≤ 2` (the two raised caps), when the room physically has only 1 extra bed. **Decision: a single overflow rule** — `max(0, numAdults − maxCapacity) + max(0, numChildren − maxChildren) ≤ extraBedCount`, plus `extraBedCount ≤ maxExtraBeds` (the per-type cap, unchanged). Extra beds grant additional occupant slots usable by an adult **or** a child — each extra bed can serve 1 extra person, and the rule does not distinguish which bed serves which occupant. When `extraBedCount === 0`, the rule reduces to the two hard caps (CHD-04's original shape, byte-equivalent); when `extraBedCount > 0`, the rule allows overflow up to the extra bed count. The two hard rejects are subsumed — keeping them as a separate first gate would create a second source of truth that drifts (the same class of bug RTS-01 / NBS-08 / MRB-11 are designed to prevent). **Why one helper, not three call sites with inline math** — the create / walkin / reschedule transactions all need the same calculation, and the inline `Math.max(0, ...)` pattern is the kind of math that drifts between call sites. **Decision: `requiredExtraBedsFor({ numAdults, numChildren, maxCapacity, maxChildren })` in `shared/utils/roomTypes.ts`** returns `{ overflowAdults, overflowChildren, requiredExtraBeds }`. Defensive `Number(x) || 0` on every operand (nullish / NaN / negative → 0), then `Math.floor` to handle fractional inputs, then the two `max(0, ...)` subtractions. The function is the only authoritative capacity check on the three server call sites. **Why the per-type `extraBedCount ≤ maxExtraBeds` cap stays as a separate first gate** — the cap is a hard limit on the extra-bed count itself (the room type's documentation), not an overflow check. The cap fires before the overflow check; both must reject, in that order. Removing the cap would let a guest request 10 extra beds on a Single (which the type says allows 0). **Why the reschedule gets the same check** — the CHD PR (#144) deferred the reschedule split validation per the CHD roadmap note "the reschedule transaction (line 4683) gets the same split validation in a follow-up; the existing PMH-02 extraction routes the reschedule math through the shared helpers so the CHD-04 follow-up is a focused diff." EXB-03 closes both gaps at once. The reschedule transaction at `guest-app/server/handlers/bookings.ts:4803` reads the existing snapshotted `booking.extraBedCount` from the doc and the new room type's `maxCapacity` + `maxChildren` + `maxExtraBeds` from the transaction's `hotelConfig` read. Legacy pre-CHD bookings without `numAdults` / `numChildren` derive `numAdults = numGuests, numChildren = 0` (the historical "all guests are adults" shape). **The previous PF-03 combined-cap check at the reschedule (`numGuests > maxCapacity`) is removed** — the overflow helper subsumes it and adds the child-overflow axis. PF-03 could not express the overflow case: a booking with extra beds fits a larger occupancy than the type's `maxCapacity`. **Error messages** — the create + walkin reject with "Not enough extra beds: X overflow adult(s) + Y overflow child(ren) = Z extra bed(s) needed, but only N extra bed(s) selected. The room type allows up to M extra bed(s)." so the desk / guest can see exactly which axis is over. The reschedule reject uses a parallel "Booking does not fit the target room type: ... snapshotted." phrasing so the desk knows the booking's existing extra-bed count was the limiting factor, not the new request. **EXB-04 (breakfast follow-through) is now a code-only verification** — `numGuests` already includes the extra-bed occupants (CHD-01 + EXB-01), and the breakfast math reads `numGuests` per CHD-10; the existing tests in `shared/__tests__/booking-addons.test.ts` cover the case. **Helper test cases pinned by `shared/__tests__/room-types.test.ts`** (6 net new cases): 2 adults in a Single (1 cap, 0 children) → 1 overflow adult; 2 adults in a Family (4 cap, 2 children) → 0 overflow; 6 adults in a Family → 2 overflow adults; 2 adults + 2 children in a Std Double (2 cap, 1 child) → 1 overflow child; 2 adults + 1 child in a Single with 2 extra beds → 2 overflow total (the rule does not distinguish which extra bed serves the adult vs. the child); empty input → 0 overflow (defensive default for nullish / missing fields). **Source-text regression suite** — new `guest-app/tests/api/exb-03-overflow-rule.test.ts` (11 guards) pins: the helper exists in `shared/utils/roomTypes.ts`; the helper is re-exported from the shared barrel; the three call sites each invoke the helper with the right local variables; the per-type `extraBedCount ≤ maxExtraBeds` cap is preserved as the first gate in create + walkin; the legacy PF-03 combined-cap check is gone from reschedule; the two CHD-04 hard rejects are gone from create + walkin; the helper is imported from `@spark-inn/shared`. The 3 CHD-04 source-text assertions in `chd-children-occupancy.test.ts` are updated to pin the new EXB-03-subsumed pattern (the intent is unchanged but the source-text regex now matches the `requiredExtraBedsFor` call + the `if (overflow.requiredExtraBeds > extraBedCount)` reject instead of the two removed `if`s). **No client changes** — the booking-page Step 1 occupancy picker is unchanged; the new helper is purely server-side and surfaces a clearer server error when the requested occupancy cannot fit. **Implementation** — `shared/utils/roomTypes.ts` (new helper + the extensive JSDoc explaining the rule + the edge cases the test pins); `shared/__tests__/room-types.test.ts` (+6 EXB-03 cases); `guest-app/server/handlers/bookings.ts` (import `requiredExtraBedsFor` from `@spark-inn/shared`; `handleCreateBooking` and `handleCreateWalkin` remove the two CHD-04 hard rejects and call the helper after the per-type `extraBedCount ≤ maxExtraBeds` cap; `handleRescheduleBooking` replaces the legacy PF-03 combined-cap check with the helper, deriving `numAdults` from the booking doc with the legacy fallback); `guest-app/api/[...route].js` (rebuilt by the pre-commit `check:api-bundle` hook); `guest-app/tests/api/chd-children-occupancy.test.ts` (the 3 CHD-04 assertions updated); `guest-app/tests/api/exb-03-overflow-rule.test.ts` (new — 11 source-text guards). **Tests** — 609/609 guest-app (was 598; +11 for EXB-03 source-text guards), 343/343 shared (was 337; +6 for the EXB-03 unit tests in room-types.test.ts), 984/984 admin-app (unchanged — pure server + test work). Branch: `feature/exb-03-overflow-rule`. *(EXB-03 entry on `plan/project/ROADMAP.md`.)*

| 154 | **Rates Management gains the `extraBedRate` column (EXB-05, approved 2026-08-01).** Per the EXB-01 spec, `RoomTypeEntry.extraBedRate` is the per-bed-per-night rate for the rollaway add-on, snapshotted onto each booking at create time so a later rate change never rewrites history. The data path + the Settings → Room Types Add/Edit editor both shipped inside EXB-01 (per the EXB-01 ship summary at `plan/project/ROADMAP.md §EXB-01` — "the Room Types Add form + Edit modal gain 'Max extra beds' (0 = not offered) + 'Extra bed rate' (₱/bed/night) inputs"). The stale `EXB-06` roadmap row is folded into the EXB-01 record (decision #153's lead-paragraph cleanup, and the line 328 stale-row removal in this PR). What was missing is the **Rates Management matrix column** — the per-type matrix beside Standard / Weekend / Corporate that the desk uses to update the per-room rates in bulk. Without it, the `extraBedRate` could only be edited one room type at a time through the Add/Edit modal; a hotel with 5 room types had to click 5 modals + save 5 times to roll out a new rate. **Decision: add a 4th column to the matrix** (`extraBedRate` per-room-type, per-bed-per-night). Same shape as the existing 3 rate columns — the per-type editable rate lives in a form buffer, the snapshot-sync effect re-seeds it on every Firestore echo, the dirty-set guard prevents a late snapshot from clobbering an in-flight edit, and the save handler routes through the single-batched `saveRoomTypes(next)` write (RTS-02's invariant). **Why a 4th column, not a separate "extra-bed rates" tab** — the desk's mental model is one rate matrix per room type. Splitting extra-bed rates into a separate tab doubles the click surface for a per-type edit (open Room Types tab → click Edit → switch to Extra Bed Rates tab → find the type → edit → save vs. open Rates → edit the type's extra-bed cell in the same row as the base rate). The 4-column matrix is the same shape as every other per-type-rate config the desk already maintains. **Why the matrix cell is `min: 0`, not `required`** — the per-type cap `maxExtraBeds` is a separate field (0 = not allowed, on the type editor); the matrix cell is the per-bed rate, which a 0-extra-bed type leaves at 0. A `required` cell with `min: 0` would force the desk to type `0` for any type that doesn't offer extra beds, which is wasted input. **Why a defensive `Number(t.extraBedRate) || 0` on the snapshot seed** — the field is optional on the type (legacy settings without the field, the same pattern as the #111 surface flags + `applyRoomTypeDefaults`). The defensive coercion handles nullish / NaN / negative → 0, matching the existing `Number(x) || 0` permissive pattern. **Why the single-batched write is critical** — the 4th column is a natural regression vector for RTS-01: a refactor that computes the 4-column form buffer but writes it via `Promise.all(roomTypes.map(t => updateRoomType(t.value, { ...t, extraBedRate: next.extraBed })))` would re-introduce the last-write-wins bug (RTS-01's race on the shared `roomTypes[]` array field). The 9 source-text tests in this PR pin the single-batched `saveRoomTypes(next)` write as part of the new column's contract — the test reads the `handleSaveRates` body and asserts `Promise.all(` is absent + `roomTypes.map(t => updateRoomType` is absent + `await saveRoomTypes(nextRoomTypes)` is present + the `extraBedRate: next.extraBed` line is present. **Why no Settings UI changes beyond the matrix cell** — the per-type `maxExtraBeds` + `extraBedRate` editors are already in the Add/Edit modal (per the EXB-01 ship). The matrix is the bulk-editing path; the modal is the spot-edit + new-type-creation path. Both read/write the same `RoomTypeEntry` fields, so the snapshot sync keeps them in lockstep. **Why no client (guest) changes** — the booking create / walkin / reschedule transactions already accept `extraBedCount` (bounded 0..20) and snapshot `extraBedRate` from the type doc (per EXB-01). The EXB-03 overflow helper (per decision #153) is the authority on whether a requested occupancy fits. The matrix just makes the rate editable; the booking math doesn't change. **Why the Rates page's "Pricing priority" advisory is unchanged** — the advisory explains the room-rate selection order (Corporate → Seasonal → Regular). The extra-bed rate is a separate add-on term, not a room-rate priority tier. Adding it to the advisory would muddy the priority explanation. The descriptive paragraph above the matrix gains "extra-bed rates" so the page purpose reflects the new column. **Why the EXB-06 roadmap row is removed (not flipped to ✅)** — the surface actually shipped inside EXB-01. Leaving a duplicate `EXB-06` row would mislead future readers into thinking the Settings UI is still open work. The removal is the source of truth (the EXB-01 ship summary at line 323 IS the EXB-06 record); the road map's "last updated" line above cites the removal for context. **Tests** — 9 new source-text guards in `admin-app/src/__tests__/exb-05-rates-extra-bed-column.test.ts` (the field is on `RoomTypeEntry`, the form buffer's `{ base, weekend, corporate, extraBed }` shape, the snapshot-sync seed + dirty-guard for `extraBed`, the `updateRateField` 4-field signature + spread, the save handler mapping + single-batched write, the mobile card 4th field, the desktop table 4th `<th>` + 4th `<td>`, the 3 existing rate fields are unchanged). The existing `phase-11.7-mobile-pt3.test.ts` test that pinned the 3-input shape is updated to the 4-input shape + asserts all 4 field names are bound. **Implementation shipped on `feature/exb-05-rates-extra-bed-column` (v0.204.0, merge `5d8656d`):** `admin-app/src/pages/RatesPage.tsx` (form buffer type, snapshot-sync effect, `updateRateField` signature, save handler mapping, mobile card 4th input, desktop table 4th `<th>` + 4th `<td>`, descriptive paragraph above the matrix); `admin-app/src/__tests__/phase-11.7-mobile-pt3.test.ts` (3-input → 4-input pin); `admin-app/src/__tests__/exb-05-rates-extra-bed-column.test.ts` (new — 9 source-text guards). **No server change** — the server already accepts the field via the `RoomTypeEntry` type + the `addRoomType` / `updateRoomType` write paths; EXB-05 only adds the admin Settings surface. **Zero behavior change for existing settings** — the matrix's 3 existing rate columns render exactly as before; the new column is purely additive. Legacy room types without `extraBedRate` hydrate to 0 in the form buffer (the same permissive pattern the rest of the room-type defaults use). The booking math + the EXB-03 overflow check (per decision #153) are unchanged. Branch: `feature/exb-05-rates-extra-bed-column`. *(EXB-05 entry on `plan/project/ROADMAP.md`; the EXB-06 stale-row removal is also in this PR's roadmap diff.)*

| 155 | **Booking flow occupancy pickers — adult/child split + extra bed steppers + 'blocked by cap' UX (EXB-07, approved 2026-08-01).** Per owner decision 2026-07-31 + decision #155. The "offer the bed at the right moment" UX — a guest (or staff) blocked by the occupancy cap should be offered the extra bed as the path through, not shown a dead end — was the main reason decision (1) raised the cap (per the EXB-01 spec rationale). The guest `/book` page already had it (per EXB-01's ship summary: "guest `/book` Step 1 gains an 'Extra beds' stepper that renders only when the selected room type has `maxExtraBeds > 0`"). What was missing was the same UX on the admin walk-in modal + the corporate `/book` page — the two surfaces where the staff creates a booking without a guest-picked split. The result was the exact bug EXB-07's lead paragraph calls out: the desk could only enter a single `numGuests` total, with no way to split into adults + children, no way to add an extra bed, and no client-side preview of whether the requested occupancy fits the type's caps (the server's EXB-03 overflow check would reject the request at the transaction). **Decision: extend the guest `/book` 3-stepper pattern to the admin walk-in + corporate `/book` surfaces.** All 3 surfaces now share the same shape: (1) **3 steppers** — adults (>= 1, required), children (>= 0), extra beds (>= 0, capped at the type's `maxExtraBeds`, hidden entirely when the type allows 0); (2) **derived `numGuests` = `numAdults + numChildren`** (per CHD-04, the server's authoritative derivation); (3) **contextual overflow hint** — when the EXB-03 overflow exceeds the selected extra bed count, the picker shows "This room type allows up to X adults + Y children (or X+M/Y+M with M extra bed(s)). Add N extra bed(s) to fit your group, or pick a different room." (or "and does not allow extra beds. Pick a different room type" when `maxExtraBeds === 0`). The hint uses the same `requiredExtraBedsFor` helper the server uses so the client-side preview is byte-equivalent to the server's `handleCreateWalkin` / `handleCreateBooking` check — a drift between the client preview and the server check would produce a confusing UX (the desk sees "OK" locally and then a 400 from the server). **Why a unified shape across all 3 surfaces** — three different rendering patterns for the same occupancy concept (the 3-stepper on guest `/book`, the 1-stepper on the walk-in, the 1-stepper on corporate) would be a maintenance burden and a UX inconsistency. The desk creates bookings via the walk-in + corporate surfaces; the guest creates via `/book`. A guest who later walks in (or vice versa) sees the same 3-stepper in both flows, which builds the muscle memory for the new feature. **Why the legacy `setGuests(...)` in the corporate /book Step 2 form input is removed** — the `guests` total is now derived from the Step 1 adult + child sum, and the Step 2 confirmation input is a "type the total" override that updates `numAdults` (preserving `numChildren`). A legacy URL caller with `?guests=N` still works — the initial state hydrates `numAdults = N, numChildren = 0` (the historical "all guests are adults" shape, preserved for back-compat with existing marketing links). **Why the local `Booking` interface in `AdminContext.tsx` gains 3 optional fields** — the interface was a stale local duplicate that didn't track the CHD-01 + EXB-01 + EXB-07 fields. Pinning the 3 optional fields (`numAdults?` + `numChildren?` + `extraBedCount?`) keeps the typecheck in sync with the wire shape. A future cleanup PR can replace the local interface with the shared one (the long-term direction per the existing inline note at the AdminContext comment block). **Why the walk-in submit-success reset block clears all 3 fields** — the next walk-in starts from the 1-adult / 0-children / 0-extra-bed state. A stale `walkinExtraBedCount > 0` from the previous walk-in would charge the next guest for a bed they didn't book. **Why no server change** — the server's `WalkinBookingSchema` (per CHD-01) and the public create schema (per EXB-01) already accept the 3 fields; the validation runs the EXB-03 overflow check (per decision #153) and writes the new fields to the booking doc. EXB-07 is purely a client-side UX surface. The 3 steppers in the guest `/book` page (per EXB-01) are unchanged — they already match this shape. **Tests** — 14 new source-text guards in `admin-app/src/__tests__/exb-07-occupancy-pickers.test.ts` (3 stepper state vars + derived `numGuests` in both surfaces, payload sends the 3 fields in both surfaces, 3 aria-labeled steppers in both surfaces, the 'blocked by cap' hint in both surfaces, the `requiredExtraBedsFor` helper usage in both surfaces, the local `Booking` interface carries the 3 fields, the walk-in resets the 3 fields to defaults, the corporate `/book` Step 2 form redirects to `setAdults` instead of the legacy `setGuests`, the helper is exported from the shared barrel). **Implementation shipped on `feature/exb-07-occupancy-pickers` (v0.205.0, merge `3d08e6e`):** `admin-app/src/pages/BookingsPage.tsx` (walk-in form: 3 steppers, derived `numGuests`, overflow hint, payload fields, reset block); `admin-app/src/context/AdminContext.tsx` (local `Booking` interface gains `numAdults?` + `numChildren?` + `extraBedCount?`); `guest-app/src/pages/CorporateBookingPage.tsx` (Step 1: 3 steppers, derived `guests`, overflow hint, payload fields, Step 2 form input redirects to `setAdults`); `admin-app/src/__tests__/exb-07-occupancy-pickers.test.ts` (new — 14 source-text guards). **Zero behavior change for legacy callers** — the public create body still accepts the legacy `numGuests` alone; the walk-in schema still accepts the legacy fields; the guest `/book` page is unchanged; the corporate /book page still accepts the legacy `?guests=N` URL param. The new fields are purely additive. Branch: `feature/exb-07-occupancy-pickers`. *(EXB-07 entry on `plan/project/ROADMAP.md`.)*

