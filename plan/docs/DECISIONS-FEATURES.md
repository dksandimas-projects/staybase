# Feature & Product Decisions
> Requires: CLAUDE.md

Resolved product, feature scope, business rules, compliance, and UX decisions. Load this when building a specific feature to understand what was decided and why.

For architecture and stack decisions see `plan/docs/DECISIONS-ARCH.md`.

---

| # | Decision |
|---|---|
| 1 | Room photos: architectural renders for now — managed via Room Management and Settings |
| 2 | Payment flow Phase 1: manual screenshot upload — no payment gateway |
| 3 | Payment gateway Phase 2: PayMongo (deferred) |
| 4 | Admin accounts: DK creates initial; owner gets admin; front desk accounts by admin only |
| 5 | About page: Mission, vision, hotel story only — no team/owner section |
| 6 | Add to Calendar included on booking confirmation page |
| 7 | BIR receipts: NOT generated — system generates booking confirmation receipts only |
| 8 | Corporate rates: NOT shown on marketing page — inquiry-based or via `/corporate/book` |
| 9 | Corporate booking: `/corporate/book` reuses 4-step flow with flat rate (public) or access code (negotiated) |
| 10 | Guest intercom: QR-based, browser chat, no app, no login — quick requests configurable in Settings |
| 11 | Booking statuses: Pending → Payment Uploaded → Payment Confirmed → Confirmed → Checked In → Checked Out → Cancelled |
| 12 | Discounts: Senior Citizen 20% + PWD 20% — OSCA-mandated; guest uploads OSCA Card / PWD ID photo at booking Step 3; staff verifies or rejects in booking drawer; rejection restores full `totalPrice`, triggers discount-rejected email instructing guest to pay full amount at check-in |
| 13 | Promo vouchers: guest enters code at Step 3 of booking flow — admins manage campaigns on the Rates page; front desk sees applied voucher outcomes in booking operations |
| 13b | **Discount stacking order** (when multiple discounts apply on the same booking): **1.** Senior/PWD (20% subtotal) → **2.** Voucher (flat or percent, applied to post–Senior/PWD subtotal) → **3.** Spark Rewards member discount (percent, applied to post–voucher subtotal). Member discount only applies when the guest is signed in AND `settings/rewardsConfig.memberDiscountEnabled === true`. Implemented in `shared/utils/pricing.ts` `calculateBookingTotal()` — applied in the listed order, not cumulatively on the original subtotal. UI shows each discount as a separate line in the price summary. |
| 14 | Walk-in bookings: front desk creates manually from dashboard — source field captures origin |
| 15 | Housekeeping status: tracked per room as clean / dirty / in-progress — toggleable from dashboard |
| 16 | Intercom notification sound: Web Audio API only — no extra library — hosted in Firebase Storage |
| 17 | Intercom notification sound plays on every incoming guest message — not just the first per conversation |
| 18 | Intercom security model: physical QR gate + room ID from URL + name prompt — no login required |
| 19 | Reports: occupancy + revenue + bookings by source — Recharts — exportable PDF/CSV/XLSX |
| 63 | Data backup: single "Download Full Backup" button (admin-only) generates one multi-sheet XLSX covering all 8 operational collections — Bookings, Payments, Members, Store Orders, Store Catalog, Breakfast Selections, Vouchers, Corporate Inquiries; all queries run in parallel client-side via SheetJS |
| 73 | Additional onsite payments tracked in `bookings/{id}/payments` subcollection — append-only audit trail; outstanding balance computed client-side; covers discount rejections, points reversals, and Pay at Hotel confirmations; no editing or deletion allowed |
| 64 | DOT/RA 11862 compliance: guest registry fields (nationality, ID type + number, DOB, gender, address) collected at physical check-in via the Guest Registration Form PDF — not required at online booking step |
| 65 | DOT/RA 11862 compliance: guest registry records must be retained minimum 6 months — erasure requests cannot apply to this data within that window |
| 66 | RA 11862 (Anti-Trafficking): unaccompanied minor warning shown as informational banner in booking detail drawer — front desk must verify guardian presence at check-in; system does not block the booking |
| 67 | Guest ID photo captured by front desk at check-in via upload in booking detail drawer — stored in Firebase Storage (staff-only), `guestIdPhotoUrl` on booking document, embedded in Guest Registration Form PDF; if not yet uploaded, PDF shows blank placeholder box |
| 68 | Reports page organized into two tabs: Performance (occupancy, bookings by source) and Sales (all revenue streams consolidated) |
| 69 | Sales Report consolidates all three revenue streams — room bookings, breakfast add-ons, store orders — into one view with summary cards, charts, detail tables, printable PDF, and multi-sheet XLSX export |
| 70 | Sales Report PDF uses html2canvas to capture Recharts SVG charts + jsPDF autoTable for data tables — generated client-side, no API route needed |
| 71 | Sales XLSX export is multi-sheet: Summary, Bookings, Breakfast, Store Orders — one file covers all revenue data for the selected period |
| 72 | Breakfast kitchen prep report and store low-stock alerts remain as separate operational tools — their revenue figures appear in the Sales tab, not duplicated |
| 20 | Website content editable from Settings (homepage, about, corporate sections) — no full CMS |
| 21 | `isCorporate` and `corporateCode` set server-side — never trusted from client |
| 22 | Corporate codes and vouchers validated server-side via API route |
| 23 | Booking reference format: `{config.bookingRefPrefix}-YYYYMMDD-NNN` — generated server-side |
| 24 | Domain: `sparkinnbohol.com` — DK purchases as part of project |
| 25 | Data Privacy Act of 2012 (RA 10173) compliance required — hotel processes guest PII |
| 26 | Data retention: indefinitely — guests may request erasure via email to hotel |
| 27 | Privacy Policy and Terms of Service: dedicated `/privacy` and `/terms` pages on guest site — linked from footer, booking form, and emails where applicable |
| 28 | Consent checkbox required at booking Step 2 — links to `/privacy` and `/terms`, blocks submission if unchecked |
| 29 | Hotel owner/admin serves as Data Protection Officer (DPO) |
| 30 | Data breach notification: NPC within 72 hours if breach affects guest PII |
| 31 | Legal content (privacy policy body, cancellation policy, house rules) editable at runtime from Settings |
| 32 | Spark Essentials store accessible only via room QR scan — not from public website |
| 33 | Store orders linked to room ID and active booking (null if no active booking found) |
| 34 | Store payment methods: CoD, Add to Bill, GCash — independent of booking payment methods |
| 35 | "Add to Bill" = note for front desk to collect at checkout — does not auto-update booking totalPrice |
| 36 | Store stock: null = unlimited, 0 = out of stock, n = tracked quantity |
| 37 | Stock decremented on order confirmed (not placed) — restored if cancelled before confirmed |
| 38 | Store order status flow: Placed → Confirmed → Out for Delivery → Delivered; Cancelled is terminal at any point |
| 39 | Store item deleted with existing orders: soft-delete only (isActive: false) — never hard delete |
| 40 | Store order ref format: SO-YYYYMMDD-NNN — generated server-side |
| 41 | Spark Rewards Phase 1: auth, member profile, booking history, configurable points earning, configurable member discount, SR-XXXXX member card |
| 42 | Spark Rewards Phase 2: points redemption, tier system, tier-based perks — TBD |
| 43 | Guest auth: Google Sign-In + email/password — separate from admin auth, same Firebase project |
| 44 | Guest registration available post-booking (Step 4 prompt) and standalone at `/rewards` |
| 45 | Past anonymous bookings linked to member account by email match on registration |
| 46 | Points redemption is Phase 1 but admin-only — staff applies it manually from the booking detail drawer; guests cannot redeem online; redemption rate is configurable in Settings |
| 47 | Early check-in request: always available to members — not configurable on/off; sends tagged intercom message or email to front desk |
| 48 | Manual points adjustment by staff requires a reason — always logged to points history |
| 49 | Member account deletion triggers data erasure per RA 10173 right to erasure |
| 50 | `guests/` collection = staff only; `members/` collection = guest loyalty members |
| 51 | Breakfast add-on: per person per night — rate × numGuests × numNights |
| 52 | Breakfast shown at Step 1 as "Room Only" vs "Room + Breakfast" combined rate — not as a separate add-on at Step 3 |
| 53 | Breakfast rate locked at booking time — stored as `breakfastRate` on booking document |
| 54 | Silog selection: per guest per day — each guest can choose a different silog each morning |
| 55 | Silog menu fully configurable from Settings — hotel can add/remove/disable items |
| 56 | Silog selections entered by front desk in booking detail drawer at check-in — not by guest online |
| 57 | Silog choices appear on the guest registration form PDF — guests fill in at check-in |
| 58 | Daily kitchen prep report: counts of each silog needed for a given morning — printable |
| 59 | Breakfast globally enable/disable from Settings — hides option from booking flow when off |
| 60 | Homepage Services section (Tour Packages, Car Rentals) — display only, CTA links to Contact Us — no booking or pricing |
| 61 | Homepage Spark Rewards section — shows Join CTA for non-members, Welcome back for logged-in members |
| 62 | Services and Spark Rewards homepage sections editable from Settings → Website Content — sections hidden when disabled |
| 74 | Email acknowledgment: the booking submitted email acts as an acknowledgment/receipt submission, warning the guest that their booking and payment are under manual review and a final confirmation email will follow after verification |
| 75 | Breakfast pricing model: add-on only — booking flow shows "Room Only" vs "Room + Breakfast" toggle at Step 1, with `breakfastRate` charged per-person-per-night on top of the room rate. No `includedInRoomRate` field on `breakfastConfig`. The current implementation already handles the use case. If a future hotel client needs "breakfast always included" as a per-room differentiator, add it as a scoped feature then. *(Per audit W1.7, 2026-06-15)* |
| 76 | Contact form on `/contact` is in scope for Phase 1 — wire the existing form to a real `/api/contact` endpoint. Match the corporate inquiry pattern: Zod schema (strict), honeypot (`_hp`), Turnstile verification, rate-limit (5/min per IP), new `contactInquiries/{id}` Firestore collection, new `contactInquiryEmail` template sent to `settings/hotelConfig.supportEmail`. The current `setTimeout`-based fake success in `ContactPage.tsx` is removed. Full spec in `plan/features/CONTACT-INQUIRIES.md`. **Implementation deferred to Phase 1 (with the other 5 SEV-1s).** *(Per audit W1.8, 2026-06-15)* |
| 77 | `payment-confirmed` is a real state — set automatically by `handleAddPayment` when the running payment total ≥ `totalPrice` (in the same transaction that records the payment). Admin's "Confirm Payment & Booking" button then flips `payment-confirmed → confirmed`. Closes the gap where the state machine listed 7 statuses but the runtime only transitioned through 5. *(Per audit W1.1, 2026-06-15)* |
| 78 | Room block date range semantics — store as structured fields: `blockedFrom: Timestamp`, `blockedTo: Timestamp`, plus the existing `blockReason: string`. The booking creation transaction iterates active block ranges and rejects overlaps. The current `addRoomBlock` strings the date range into `blockReason` (lossy, dead data) — that implementation is replaced. Migration: backfill `blockedFrom`/`blockedTo` from existing string-encoded `blockReason` values. *(Per audit W1.2, 2026-06-15)* |
| 79 | `isCorporate` is server-authoritative — never trusted from the client. Client sends `corporateCode` only. Server looks up `corporateCodes/{code}` inside the booking transaction, validates `isActive` + not-expired + `usageCount < usageCap`, applies `ratePerRoomType` if valid, and sets `isCorporate: true` + `companyName` server-side. Invalid/missing code falls back to standard/corporate rate. Increment `usageCount` in the same transaction (currently never written). *(Per audit W1.3, 2026-06-15)* |
| 80 | Store stock is decremented on `confirmed`, not on `placed` (matches `DECISIONS-FEATURES.md #37` and resolves the contradiction with `STORE-MANAGEMENT.md`). Changes: (a) `handleCreateStoreOrder` no longer decrements stock; (b) new `handleConfirmStoreOrder` API decrements inside a transaction; (c) admin "Confirm" action routes through the new endpoint; (d) `handleCancelStoreOrder` only restores stock that was decremented at confirmation. Backfill: existing `placed` orders in production are migrated. *(Per audit W1.5, 2026-06-15)* |
| 81 | Vouchers live in Rates page, not Settings page. Rates is admin-only; Settings is admin-only. Vouchers need front-desk access for walk-in redemptions — front desk can apply voucher codes during walk-in creation (validated server-side), but campaign management (create, disable, view codes) is admin-only. The `SETTINGS.md` section count is updated to drop the Vouchers entry; the Rates page already owns the CRUD. *(Per audit W1.6, 2026-06-15; corrected per audit FD-03, 2026-07-17)* |
| 82 | Booking Confirmation Receipt PDF is in scope for Phase 1 — add `printBookingReceiptPDF(booking)` next to `printRegistrationPDF` in `admin-app/src/pages/BookingsPage.tsx`. PDF includes: booking ref, guest info, room, dates, nights, guests, rate, applied discounts (senior/PWD, voucher, member), payment ledger with Total Collected + Outstanding Balance, hotel branding header. Reused as email attachment in the `booking-confirmed` email template. Closes the 1.7 SEV-1 finding. *(Per audit W1.9, 2026-06-15)* |
| 83 | Check-in reminder cron adds an idempotency marker — `reminderSentAt: Timestamp` written to the booking in the same transaction that sends the email. Cron query filters `where("reminderSentAt", "==", null)` so retries do not double-send. Closes the at-least-once delivery risk. *(Per audit W1.10, 2026-06-15)* |
| 84 | Firestore `bookings.checkIn` and `bookings.checkOut` are always stored as `Timestamp.fromDate(jsDate)`, never as raw `Date` or ISO string. Read paths use `data.checkIn.toDate()`. Remove the dead `adminDb.doc(...).firestore.valueType` ternary at `bookings.ts:281` and the `parseDateString` helper. The `TYPES.md §Booking` `checkIn`/`checkOut: Timestamp` declaration becomes accurate. *(Per audit W1.11, 2026-06-15)* |
| 85 | `AdminContext.members` is sourced from a real `onSnapshot(collection(db, "members"), …)` listener — not a `useState` mock with one hardcoded fake entry. The Members page becomes functional: list, detail drawer, manual points adjustment (debit/credit), pointsHistory append, suspend/activate via real API. Closes 1.2 SEV-1 #5, 1.3 SEV-2 #2d, 1.5 SEV-1 (Members). *(Per audit W1.12, 2026-06-15)* |
| 86 | No developer's personal name in any default. The seeded `settings/hotelConfig` payment-method defaults `accountName` field (e.g. `"BDO: 001234567890 (Daniel Sandimas)"`) is removed. Replaced with empty string or `config.legalName + " — " + config.supportEmail`. This is a hard-rule violation per `GOTCHAS.md §Never log PII` and `WHITE-LABEL.md`. **Implemented in `86bb50b` (Phase 11.6 Batch 1)** *(Per audit W1.13, 2026-06-15)* |
| 87 | Honeypot inputs are always inside the `<form>` element, hidden via CSS (absolute position + opacity 0). The current `CorporateStaysPage.tsx:567-578` honeypot lives outside the form (works by accident because of React-controlled state) — moved inside to match the booking-flow pattern. **Implemented in `56bf8d4` (regression test; implementation already correct)** *(Per audit W1.14, 2026-06-15)* |
| 88 | Housekeeping cycle order: `clean → dirty → in-progress → clean` (per `DASHBOARD-OVERVIEW.md` spec). Current `AdminContext.tsx:452-459` cycles `clean → in-progress → dirty → clean` — wrong middle state. Fix the cycle and surface all 3 states (`clean | dirty | in-progress`) in the dashboard housekeeping button. **Implemented in `7266c34` (Phase 11.6 Batch 1)** *(Per audit W1.15, 2026-06-15)* |
| 89 | Overlapping bookings by the same email are **allowed** — repeat guests can book multiple rooms (or back-to-back stays) in overlapping date ranges. The room conflict check prevents the same room from being double-booked; cross-room overlap is fine. Spec was silent; chosen default is the more permissive option. *(Per audit W2.1, 2026-06-15)* |
| 90 | Member discount server-side enforcement: client sends nothing member-related in the body. If the request includes an `Authorization: Bearer <firebaseIdToken>` header, the server verifies it, looks up the member by `authUser.uid`, and applies the 3rd stacking discount step per `DECISIONS-FEATURES.md #13b`. If no header, `memberId` is `null` and no member discount. Any client-supplied `memberDiscount` / `memberDiscountPct` field is rejected by the Zod schema (strict mode). *(Per audit W2.2, 2026-06-15)* |
| 91 | Past-booking linkage beyond email is **deferred to Phase 2**. `linkBookingsByEmail` runs at registration only. A future "claim past stays" UI lets the member manually add past bookings not caught by email match. *(Per audit W2.3, 2026-06-15)* |
| 92 | Early check-in request when the member has multiple upcoming bookings: pick the **first confirmed or checked-in booking by `checkIn` ascending**. If only one upcoming booking exists, skip the picker. If none, show an error "no upcoming booking — book a stay first". The flow is implemented as a small picker modal (1-3 booking cards) when count > 1. **Implemented in `2d41ed7` (Phase 11.6 Batch 2)** *(Per audit W2.4, 2026-06-15)* |
| 93 | No member-registration welcome email for Phase 1. The "Welcome Gift" marketing copy on `RewardsLandingPage.tsx:281-292` is removed (or reworded to "Welcome to the program" without a tangible promise). A transactional welcome email can be added in a future phase. **Implemented in `6796fc6` (Phase 11.6 Batch 2)** *(Per audit W2.5, 2026-06-15)* |
| 94 | Multiple concurrent calls in admin inbox: **second wins**. Accepting a new call writes `status: "ended", endedAt: serverTimestamp()` to the old call doc (if it was active) before accepting the new one. The previous guest's UI sees the call end naturally via the snapshot listener. **Implemented in `4f6aa92` (Phase 11.6 Batch 1)** *(Per audit W2.6, 2026-06-15)* |
| 95 | Intercom thread auto-archive on checkout: `handleCheckoutBooking` sets `intercoms/{roomNumber}.resolved: true` in the same transaction that flips the booking status. Staff can reopen from the admin Inbox by clicking the resolved thread and toggling `resolved: false`. **Implemented in `e787594` (Phase 11.6 Batch 1)** *(Per audit W2.7, 2026-06-15)* |
| 96 | Cancellation messages are rendered as a distinct visual state: greyed-out card with a "Cancelled" header in both the guest's chat history and the admin Inbox. The `isCancelledOrder: true` flag is set on the message doc by the cancellation handler. **Implemented in `bc8e01f` (Phase 11.6 Batch 1)** *(Per audit W2.8, 2026-06-15)* |
| 97 | Notification sound mute: persisted in `localStorage` per-staff (key: `notif_sound_muted`). A `Bell` / `BellOff` icon button in the inbox header toggles. `playNotificationSound` early-returns when muted. Per-staff Firestore setting is overkill for Phase 1. *(Per audit W2.9, 2026-06-15)* |
| 98 | `calls/{roomId}` retention: **delete after 30s grace** from when both sides have observed `status: "ended"`. A `setTimeout` in `stopGuestCallResources` (guest) and `cleanupAdminCall` (admin) issues `deleteDoc(callRef)` after the grace period. The `calls` collection is bounded; no doc accumulates forever. **Implemented in `67beb4c` (Phase 11.6 Batch 1)** *(Per audit W2.10, 2026-06-15)* |
| 99 | LOU (Letter of Undertaking) for corporate chargeback bookings: **no file upload in Phase 1**. The current `CorporateBookingPage.tsx:1230-1255` upload widget is replaced with a UI note "our accounts team will email you within 24 hours to request your LOU". The booking drawer gets an `louReceived: boolean` toggle (default `false`) so staff can track receipt manually. Spec was silent on the LOU flow; deferring is safe. **Implemented in `f3054b1` (Phase 11.6 Batch 2)** *(Per audit W2.11, 2026-06-15)* |
| 100 | Corporate bookings never accept promo vouchers. The `CorporateBookingPage.tsx:200` `voucherDiscount: 0` hardcode is correct. Negotiated rates (per W2.13) are already a discount; stacking voucher on top would be double-dipping. *(Per audit W2.12, 2026-06-15)* |
| 101 | Negotiated corporate rate model: **flat rate per room type** via `corporateCodes/{code}.ratePerRoomType: Record<roomType, rate>`. The UI label is "Negotiated rate applied" — the previous `(X% additional discount applied)` wording is removed. If `ratePerRoomType[roomType]` is undefined, fall back to `room.corporateRate` and log a warning. **Implemented in `c8282ed` (Phase 11.6 Batch 2)** *(Per audit W2.13, 2026-06-15)* |
| 102 | Converted-inquiry bookings get a `linkedInquiryId?: string` field on the booking document (add to `shared/types/index.ts §Booking`). The original inquiry gets a back-reference `convertedBookingId: string`. Both fields are set in the same transaction when staff clicks "Convert to Booking". **Implemented in `551760c` (Phase 11.6 Batch 2)** *(Per audit W2.14, 2026-06-15)* |
| 103 | Booking source for a converted inquiry is **`"corporate"`** (not `"walk-in"`). The channel is what matters for revenue reporting. `BookingSource` enum already has `walk-in | online | corporate | facebook` per `TYPES.md` — `"corporate"` is the right pick. *(Per audit W2.15, 2026-06-15)* |
| 104 | **Email template extensions (Wave 4 #4)** — 7 new transactional email templates are in scope, raising the total from 8 to 15. New templates: `voucher-issued` (guest, when admin creates a voucher with a guest email), `store-order-placed` / `-confirmed` / `-out-for-delivery` / `-delivered` / `-cancelled` (guest, fire on each store order status transition), `staff-new-booking` (admin, fire on new online booking), `staff-new-payment` (admin, fire on payment proof upload). Closes the "guest misses order update after closing the intercom tab" gap and the "staff miss new booking when logged out" gap. Full spec in `plan/features/EMAIL-AUDIT-EXTENSIONS.md`. **Implementation deferred to Phase 1.5.** *(Per audit W4.4, 2026-06-15)* |
| 105 | **Wave 3 — UI/UX spec closures (12 questions, all approved 2026-06-15)** — answers 12 minor spec gaps so the polish work in Phase 11.6 has a clear target. Items: booking payment methods live in Rates (#105a), Spark Rewards tab is admin-only with explicit role guard (#105b), Room Types migrates from localStorage to `settings/hotelConfig.roomTypes` (#105c), reports backup button role guard is page-level (#105d), Reports Performance tab swaps "Avg Length of Stay" for "Avg Occupancy" + "Busiest Room Type" per `REPORTS.md` spec (#105e), AboutPage "Brand Promise" banner kept + `STATIC-PAGES.md` updated (#105f), CorporateStaysPage "Integration Process" + "Retreat CTA" sections kept + `STATIC-PAGES.md` updated (#105g), PrivacyPage + TermsPage include global `<Navbar />` for consistency (#105h), PrivacyPage section 3 heading renamed to "How Long We Keep It" per spec wording (#105i), new `config.rewardsName: "Spark Rewards"` for white-label (#105j), new `config.termsLastUpdated` field (#105k), 404 page renders a tiny `<p>v{VERSION}</p>` (resolves the spec contradiction) (#105l). *(Per audit W3.1–W3.12, 2026-06-15)* |
| 106 | **Wave 4 — Infrastructure closures (8 remaining questions, all approved 2026-06-15)** — answers the spec/build questions for the cross-cutting work. Items: corporate rate lookup fails soft to `room.corporateRate` with a console warning if `ratePerRoomType[roomType]` is undefined (#106a), `index.html` static OG meta is templated at Vite build time via a small `transformIndexHtml` plugin reading `hotel.config.ts` (#106b), `WHITE-LABEL.md` schema is updated to match the actual `HotelConfig` fields used in `hotel.config.ts` (#106c), Storage rule for `store-orders/{roomNumber}/payment-proof/` uses `roomNumber` (matching the existing client upload path) (#106e), CORS `Allow-Credentials: true` is removed; explicit allowlist from `config.domain` + `config.adminDomain` + localhost dev origins (#106f), `prompt("Enter cancellation reason:")` is replaced with a small input field in the existing cancellation drawer (#106g), Privacy + Terms pages keep the `Republic of the Philippines` hardcoded copy in legally-relevant sections but expose `config.applicableLaw` for the rest (#106h), `Spark Inn Hotel Corp` hardcoded `accountName` fallback becomes `config.legalName` (#106i). *(Per audit W4.1–W4.9, 2026-06-15)* |
| 107 | **Admin app gets a responsive mobile layout (Phase 11.7).** The admin app was built desktop-first for an assumed minimum of 768px tablet width — below 768px the fixed 240px sidebar consumes the entire viewport, the header overflows, tables force horizontal scrolling, and drawers get clipped. Phase 11.7 adds a three-mode responsive sidebar (mobile slide-in / tablet icon-only / desktop full), a compact sticky mobile header, full-screen bottom-sheet drawers with sticky action footers, a `DataTable` mobile card view via a new `renderMobileCard` prop, and per-page rules for the 11 existing pages. Replaces all `alert()` / `confirm()` / `prompt()` calls with inline forms + toast (extends #106g to the whole admin app). No PWA, no offline, no install prompt (per `DECISIONS-ARCH.md #47`). Estimate: ~6 dev days. Full spec: `plan/features/ADMIN-MOBILE.md`. *Identified 2026-06-17 during mobile UX assessment.* **Implemented 2026-06-18 on `dev` at v0.90.0** — branch `feature/phase-11.7-admin-mobile`, 9 commits, 9 new test files (94 tests, 342/342 total green), build + typecheck clean. P0 foundations + Drawer/Modal/Toast + DataTable mobile card view + Bookings filter/cleanup + Intercom single-pane + Settings tab bar + focus trap are all shipped. P3 manual QA matrix (18 screens × 6 breakpoints) + real-device testing deferred to post-staging. |
| 108 | **Booking payment methods are a fully dynamic, admin-managed list** (Settings → Payment Methods, per `plan/features/SETTINGS.md §2`). Each method owns its `method` key, `label`, `accountName`, `accountNumber`, `qrUrl`, and `isEnabled` flag. QR images are uploaded to Firebase Storage at `assets/payment-methods/{method}/{filename}` (public read, staff write — new `match /assets/payment-methods/{method}/{fileName}` rule). "Pay at Hotel" is just another method (`method: "pay-at-hotel"`, `isEnabled: true/false`); the previous standalone `payAtHotelEnabled` global toggle was removed in favour of the per-method `isEnabled` flag. The schema is open (`method: string`) so admins can add custom methods, but the UI surfaces two policies: (a) a persistent callout lists the supported methods (`SUPPORTED_PAYMENT_METHODS` in `shared/constants`); (b) `UNSUPPORTED_PAYMENT_METHODS` (currently just `pesonet`) triggers a two-step save confirm. The schema is not hard-blocked, so future business changes don't require a code deploy — the friction is policy, not enforcement. The `Booking.paymentMethod` type was already a string union (`"pay-at-hotel" | "gcash" | "paypal" | string`), so the booking record itself is unchanged — only the admin's edit surface + the guest booking page render from the dynamic list. Migration: pre-feature docs that carry `bookingPaymentMethods[]` (with the old `accountInfo` field) are auto-reshaped on the first `AdminContext` snapshot into the new `paymentMethods[]` shape; the legacy key is left in place on the doc since `setDoc(..., { merge: true })` cannot remove fields. *(Per feature request, 2026-07-01, implemented in `feature/payment-methods`.)* |
| 109 | **Protected payment methods** — entries whose keys are in `PROTECTED_PAYMENT_METHODS` in `shared/constants` cannot be deleted from the admin Payment Methods tab. Current protected keys are `["pay-at-hotel", "add-to-bill"]`. Defense in depth: the row in `SettingsPage.tsx` hides Delete and shows a blue "Required" pill, and `AdminContext.deletePaymentMethod` blocks direct calls. A one-shot backfill appends missing protected methods to existing `paymentMethods[]`. `pay-at-hotel` is protected because walk-in creation, corporate inquiry conversion, and booking step-3 fallback default to it. `add-to-bill` is protected because booking checkout folios and reports derive billed store charges from `storeOrders.paymentMethod === "add-to-bill"`. |
| 110 | **In-room store payment methods are managed only from Settings → Payment Methods** — `settings/hotelConfig.paymentMethods[]` is now the single source for booking, store, and corporate payment visibility. The prior `settings/storeConfig.useBookingPaymentMethods` toggle and `settings/storeConfig.paymentMethods[]` fields are legacy and ignored by checkout. `cod` and `add-to-bill` are backfilled into `paymentMethods[]` as store-only methods (`isEnabled: false`, `showInStore: true`, `showInCorporate: false`). `getEffectiveStorePaymentMethods(hotelConfig.paymentMethods)` filters by `showInStore !== false`, excludes `pay-at-hotel`, and is called by both guest checkout and `/api/store/create-order` inside the Firestore transaction. Any non-`cod`/non-`add-to-bill` method requires a payment proof screenshot. Rationale: hotels should not configure GCash/Maya/PayPal details on two tabs, and Add to Bill belongs in the same list as a protected folio method. *(Supersedes the earlier opt-in toggle model from #110; finalized 2026-07-03.)* |
| 111 | **Each payment method owns three independent surface visibility switches** — `isEnabled` (regular booking), plus optional `showInStore` and `showInCorporate` booleans. The Settings → Payment Methods row renders three inline pill toggles per method (Booking, Store, Corp). Missing `showInStore` / `showInCorporate` values are read permissively for custom legacy methods; built-ins are normalized to sensible defaults (`pay-at-hotel` store-hidden, `cod` / `add-to-bill` store-visible and booking/corporate-hidden). `CorporateBookingPage.tsx` adds `showInCorporate !== false` to its existing `gcash`/`maya`/`bank` allowlist. The Zod schema marks both new fields optional. Rationale: one global on/off switch is too coarse for hotels that want, e.g., PayPal on the booking site but not in the in-room store. *(Per feature request, 2026-07-03, implemented in `feature/per-method-surface-toggles`.)* |
| 112 | **Google Sign-In is authentication only; Spark Rewards enrollment requires explicit consent.** The `/signin` Google button never creates or enrolls `members/{uid}` by itself. The `/signup` Google path first requires the Privacy Policy / Terms checkbox, then calls `registerCurrentMember()` as the explicit enrollment step. Signed-in non-members can join from Rewards/Profile surfaces through the same helper, and registration failures are surfaced inline instead of being swallowed. This closes the RA 10173 consent asymmetry from GA-07 while preserving Google as a sign-in provider. *(Per `AUDIT-GUEST-APP-2026-07-07.md` GA-07, implemented in `fix/audit-ga-sev2`.)* |
| 113 | **Homepage featured room cards do not expose live per-room operational status.** The homepage cards are type-driven marketing cards sourced from `settings/websiteContent.homepage.featuredTypeValues` and `settings/hotelConfig.roomTypes[]`; they must not label an occupied physical room as "Blocked" or leak live operational state. Date-aware availability remains in the booking flow and `/api/rooms/availability`. *(Per `AUDIT-GUEST-APP-2026-07-07.md` GA-13, implemented in `fix/audit-ga-sev3`.)* |
| 114 | **Self-service cross-provider auth linking is deferred to Phase 2.** Phase 1 handles Firebase `auth/account-exists-with-different-credential` with provider-conflict copy that tells guests to use their existing sign-in method first. The full `linkWithPopup(googleProvider)` prompt and booking-email mismatch prompt remain Phase 2 work because they need additional UX/state around pending credentials and account ownership. *(Per `AUDIT-GUEST-APP-2026-07-07.md` GA-11, implemented in `fix/audit-ga-sev3`.)* |
| 115 | **BIR/VAT scope decision** — The system does not generate or print official BIR-registered receipts or sales invoices. Official receipt (OR) issuance remains a manual or external process handled by the hotel front desk. However, to assist the hotel's accountant in monthly filings, the system calculates and exports the tax breakdown (12% VAT, VATable Sales, VAT-Exempt Sales for OSCA-mandated Senior/PWD bookings, and VAT Amount) client-side in the reports and XLSX exports. *(Per finance audit FIN-06, resolved 2026-07-11)* |
| 116 | **Direct-paid store orders join the shared collections ledger on delivery.** The staff-only delivery transition atomically marks the order delivered and appends one deterministic tender under that order. COD is recorded as Cash; configured direct methods retain their method key. Add to Bill creates no store tender because it remains an unsettled booking-folio charge. Store tenders stay outside `bookings/{id}/payments` so they reconcile store revenue and Daily Close without incorrectly reducing the room balance. Store revenue is recognized on `deliveredAt` (legacy fallback: `createdAt`). *(Per Finance Lifecycle Audit FL-05, resolved 2026-07-13.)* |
| 117 | **Early departure retains the contracted booking total.** Checkout still shortens `checkOut` and `numNights` to release inventory accurately, but it does not issue an automatic refund or reduce `totalPrice`. The locked breakdown is rebuilt around the consumed nights/add-ons, preserves the booking's deductions, and adds an explicit “Early departure — original total retained” adjustment so receipts remain mathematically transparent. Any discretionary refund uses the existing admin refund ledger. *(Per Finance Lifecycle Audit FL-10, approved and resolved 2026-07-13.)* |
| 118 | **Spark Rewards points require a fully settled folio and cover net booking spend only.** Eligible spend is the locked `booking.totalPrice` (room and breakfast after booking-level deductions); incidental charges and store orders do not earn points. Checkout snapshots the charge-inclusive folio and stamps any unpaid balance. A settled member folio earns immediately; an unpaid folio locks a pending award that the final `/api/bookings/add-payment` transaction consumes exactly once. Checkout remains allowed with a balance as a front-desk judgment call. *(Per Finance Lifecycle Audit FL-11, approved and resolved 2026-07-13.)* |
| 119 | **Production environment split carry-over:** The production environment database starts as a clean slate for bookings and transactions (no bookings or related collections data are carried over from staging). Only the active staff accounts are recreated and pre-provisioned on the production project. *(Approved and resolved 2026-07-14.)* |
| 120 | **Notification Center (Phase 12) ships as a persisted `notifications` Firestore collection, not a session-only ring buffer.** The admin app already **rings** for 5 event types (booking / payment / message / arrival / departure) via `AdminContext.playSynthNotification`, but the ring is ephemeral — no record of what happened if staff look away. Owner chose Option B: a `notifications/{notificationId}` collection written **server-side via the Admin SDK** from the existing API routes (booking create / add-payment / confirm / check-in / check-out / store order placed). One doc per event; never a guest write. Per-staff read trail via a `readBy: Record<uid, timestamp>` map; the Firestore rule restricts client updates to the `readBy` field only. Guest chat messages are **not** persisted (no server route for guest sends; rejected B2) — they remain live-derived from the existing `intercoms` listener (B1, decided 2026-07-15). Build must include: bell + panel reachable from every admin page, 30-day retention cron (`/api/notifications/prune`, `0 3 * * *`), per-staff read-state, deep links, and respect for the existing sound mute setting (Decision #97 — visual-only mode). Out of scope: browser push (needs FCM — off-stack), per-event-type mute granularity. Full spec: `plan/features/NOTIFICATION-CENTER.md`. *Resolved 2026-07-15.* |
| 121 | **Purpose of stay is required at physical check-in and defaults to `Leisure`.** Add the field to the admin booking drawer's guest-registration form and the server-authoritative check-in readiness gate. The control opens with `Leisure` selected while allowing staff to choose another supported purpose before saving. Store the selected value with the guest registration and include it in the registration PDF. This is an owner-required operational field, not a DOT minimum-particular requirement. *(Approved 2026-07-23.)* |
| 122 | **"Confirm with balance" lets staff transition a `payment-uploaded` booking to `confirmed` when the partial payment is intentional and the rest will be collected at check-in (CWB-01..05, approved 2026-07-23).** Reuses the existing `hotelConfig.unpaidCheckoutApprovalThreshold` (default 5,000) as the same "four-eyes" gate the unpaid-checkout flow uses — `front-desk` may confirm with a balance up to the threshold, above it requires an authenticated `admin` claim. The reason is free text, 500-char max (mirrors `unpaidCheckoutReason`). A new `booking-confirmed-with-balance` email is fired with the balance and reason so the guest arrives knowing what to settle. Server stamps four fields atomically inside the transaction: `confirmedWithBalance`, `confirmedWithBalanceReason`, `confirmedWithBalanceAt`, `confirmedWithBalanceBy` — all nullable on existing bookings (no migration). A balance-owed indicator renders in the booking drawer through `checked-out` while `confirmedWithBalance > 0` and any unpaid balance remains; it auto-hides at ₱0. The form renders a **persistent threshold info banner** (not a hidden tooltip) so the staff is *guided/reminded* by the limit before they submit, and the submit button is disabled when the current balance is above the threshold AND the operator is `front-desk`. Scope is MVP (~250 server + ~300 client lines) — no auto-collection, no partial-settlement flows. Full spec: `plan/features/BOOKINGS-MANAGEMENT.md §Confirm with Balance (CWB)`. |
| 123 | **Multi-booking picker for the public `/my-booking` page when the email-alone path returns more than one booking (MBP-01..04, shipped 2026-07-24).** The previous email-alone path returned only the most-recent booking (RA 10173 privacy choice). Loosen to a privacy-preserving list: when the email matches >1 booking, the endpoint returns up to 10 most-recent under a new `kind: "list"` response shape. Two auto-detected modes: **single-name mode** (all matches share the same trimmed lowercased `guestName` — the same person) renders the full row including name; **multi-name mode** (mixed names — likely a shared email) omits the name. The picker never discloses which mode fired. Click → existing strict `ref + email` lookup (no new auth, no PII widening). Strict `ref + (email | token)` paths unchanged and now explicitly return `kind: "single"` for backward-compat. Cap at 10 via `limit(11)` (11th = "more exist" sentinel, no second query). Sort: `checkIn` desc with `createdAt` desc tiebreaker. No new collection, no schema change, no composite index at 14 rooms (FLR-03 trigger fires for the scale-out path). **Implementation:** `guest-app/server/handlers/bookings.ts` — email-alone branch changed from `limit(50) + pick-top-by-createdAt` to `limit(11) + sort-by-checkIn-desc-then-createdAt-desc`; 1 match keeps the existing single-booking enriched flow; 2+ matches return `{ kind: "list", bookings: entries, moreExist }` where each entry is `{ id, bookingRef, checkIn, checkOut, numNights, roomType, status, guestName? }` (`guestName` only in single-name mode). The single-booking `enrichAndRespond` wrapper now sets `kind: "single"`. `guest-app/src/pages/BookingLookupPage.tsx` — new `pickerResults` state + `PickerEntry` type; result area cycles search form → picker (when `kind: "list"`) → single-booking card (after a row click); "10 most recent — contact the front desk for older stays" footer when `moreExist` is true. `guest-app/tests/api/bookings-lookup-list.test.ts` — 8 new tests pin the contract: 1-match → single, 2-match same name → single-name list, 2-match mixed names → multi-name list, mixed-case same name resolves to single-name, 11+ matches → `moreExist: true` with 10 entries, `checkIn`/`createdAt` sort order, `ref+email` returns single (picker unreachable from strict paths), 0 matches → 404. Updated `bookings-lookup.test.ts` (the "email alone with several bookings" test was checking the old auto-pick behavior; now scoped to 1 match). Spec: `plan/features/BOOKING-LOOKUP.md §Multi-Booking Picker`. |
| 124 | **Unify the payment reference number onto the payment ledger (approved 2026-07-24).** The previous two-field design — a guest-entered `Booking.paymentReferenceNumber` at booking time plus a staff-entered `OnsitePayment.transactionReference` on each ledger entry — was confusing (the staff rarely trusts the guest's claim anyway, since they always look up the actual GCash ref / bank trace in the wallet app at verify time) and surfaced contradictory copy in the dashboard + reports. Drop the top-level `Booking.paymentReferenceNumber` field entirely; the canonical reference is now exclusively the `transactionReference` on the relevant `bookings/{id}/payments/{paymentId}` entry. Guests no longer type a ref at booking time on either `/book` or `/corporate/book`; staff populate it on **Verify & Record Payment** / **Record Payment** (server-enforced via the `requireReferenceNumber` flag on the payment method). The admin booking drawer surfaces a read-only "Reference" cell sourced from the latest ledger entry via a small `getLatestPaymentReference(booking)` helper (defined in `BookingsPage.tsx` and `BookingDrawerWorkspace.tsx`). The Firestore rules' staff update allowlist for `bookings/{id}` no longer carries `paymentReferenceNumber`; the `bookings/{id}/payments/{paymentId}` ledger is already append-only at the rules level, so the canonical reference can never drift. Old bookings that still carry the field render unchanged; no migration. Backwards-compatible — neither a new collection nor a new index is needed. Full spec: `plan/features/BOOKINGS-MANAGEMENT.md §Payment Reference Semantics`. |
| 125 | **Use `heic-to@1.5.2` (LGPL-3.0) for HEIC client-side conversion in the admin guest ID upload (HSD-01 + HSD-02..05, decided 2026-07-24).** Library eval compared three candidates against Vite 6 + Node 20 + React 19: `heic2any@0.0.4` is **unmaintained** (`alexcorvi/heic2any#63`), `heic-convert@2.1.0` is maintained but wraps a semi-stale `heic-decode`, and `heic-to@1.5.2` (LGPL-3.0, last publish ~1 month ago) is the actively maintained successor that tracks libheif 1.20.2. All three ship a ~500-720 KB gzipped libheif WASM blob — the original "<200 KB" budget in the HSD spec was wishful, no realistic HEIC library gets close. **720 KB gzipped is the floor**, paid only when a HEIC file is detected via dynamic `import()`. LGPL-3.0 is acceptable for a CDN-loaded browser library (the combined-work requirement doesn't apply via dynamic import; no source modifications are made to the lib). **Implementation (HSD-02..05) shipped 2026-07-24 on `feat/heic-support`:** `handleGuestIdUpload` branches on `HEIC_INPUT_MIME_TYPES = { image/heic, image/heif }` BEFORE the allowlist + compress path; the branch dynamic-imports `heic-to`, converts to JPEG at quality 0.92 via `heicTo({ blob, type: "image/jpeg", quality: 0.92 })`, wraps the result in `new File([converted], "id.jpg", { type: "image/jpeg" })`, and feeds it into the standard compress + upload flow. The conversion uses Web Workers internally (per the library README) so the UI thread is not blocked. JPEG/PNG/WebP uploads never pay the lazy-chunk cost. **Bundle delta measured 2026-07-24:** initial bundle +0.59 KB gzipped (just the import statement); lazy chunk 734.59 KB gzipped (`dist/assets/heic-to-*.js`), loaded only on HEIC detection and cached after first use. The picker `accept` attribute is `image/jpeg,image/png,image/webp,image/heic,image/heif` so iOS Safari surfaces HEIC photos in the "Recents" list. Conversion errors (corrupt HEIC, oversized, WASM init failure) fall through to a user-facing toast with a "re-capture or convert manually" next step; the existing 5s decode-timeout in the registration PDF generator remains as defense-in-depth. Branch: `feat/heic-support`. Spec: `plan/features/BOOKINGS-MANAGEMENT.md §Guest ID upload`. Regression: `admin-app/src/__tests__/heic-support.test.ts` (9 tests pin the dynamic-import + allowlist + accept + copy + package-wiring contract); updated `admin-app/src/__tests__/pdf-generation-repair.test.ts` to assert the new HEIC-precedes-allowlist order. |
| 127 | **Walk-in modal mirrors the guest `/book` page: collect first + last name as separate fields, drop the `split(" ")` kludge (WSN-01..02, approved 2026-07-25).** The walk-in modal (and the `/calendar` "Create Calendar Booking" modal, which shares the same `addWalkinBooking` entry point) used to collect a single `guestName` string ("Guest Full Name", placeholder "Maria Santos"). On submit, the client did `firstName = booking.guestName.split(" ")[0] || "Guest"` and `lastName = booking.guestName.split(" ").slice(1).join(" ") || "Walkin"`. That kludge silently produced `lastName: "Walkin"` for any single-name guest (e.g. "Cher"), mangled compound names ("Maria Teresa Santos" → firstName="Maria", lastName="Teresa Santos"), and reversed "Last, First" inputs. The `WalkinGuestDetailsSchema` server schema was already correct (`firstName` + `lastName` required) — the modal was the only outlier. Decision: **mirror the guest `/book` page** — two input fields (first + last, both `required`, `autoComplete="given-name"`/`"family-name"`, mobile-stacking flex layout that keeps the Phase 11.7 single-column-on-mobile rule intact), `addWalkinBooking` accepts `{ firstName, lastName }` directly via `Omit<Booking, …, "guestName">` (the input no longer carries a `guestName` field), and the context refuses empty inputs before the network. The booking doc still stores `guestName = \`${firstName} ${lastName}\`` server-side (same logic the guest `/book` flow has always used). No schema change, no doc field change, no migration. **Implementation shipped on `fix/walkin-split-name`:** `admin-app/src/context/AdminContext.tsx` — `addWalkinBooking` parameter renamed `booking` → `input` and the type omits `guestName`; the wire payload's `guestDetails` now reads `firstName: trimmedFirst, lastName: trimmedLast` directly (no split); the two `||` fallbacks ("Guest", "Walkin") are gone. `admin-app/src/pages/BookingsPage.tsx` — modal state replaced `[guestName]` with `[walkinFirstName, walkinLastName]`; the single "Guest Full Name" input is replaced by a two-input flex row (stacks on mobile, side-by-side on sm+ per the Phase 11.7 contract); `handleWalkinSubmit` validates both fields, passes them to `addWalkinBooking`, and toasts the combined name on success. `admin-app/src/pages/CalendarPage.tsx` — the "Create Calendar Booking" modal gets the same two-field state + form + submit changes (it was the second caller of the same kludge). `admin-app/src/__tests__/walkin-split-name.test.ts` — 12 new tests pin the new contract. `admin-app/src/__tests__/phase-12-discount-controls.test.ts` — `discountType: booking.discountType` / `voucherCode: booking.voucherCode` updated to `input.discountType` / `input.voucherCode` (parameter rename). `plan/features/BOOKINGS-MANAGEMENT.md` — new "Walk-in Name Split" section documents the symmetry with the guest page, the kludge root cause, and the no-migration property. Branch: `fix/walkin-split-name`. |
| 128 | **Drop `guestName` from the email-alone 1-match single-booking response (approved 2026-07-25, the same day as #126).** The MBP-05 spec (#126) retired `guestName` from the picker row shape and claimed the name was still "gated by the `ref + email` second factor" on the single-booking card. That claim was wrong for the **1-match case in the email-alone path**: the user only typed an email, there's no `ref` to combine with it, and the page went straight to the single-booking card with the full name revealed. Decision: **the public `/my-booking` page never reflects a name back to anyone who arrived via email alone.** The `enrichAndRespond` helper now takes an `options: { omitGuestName?: boolean }` flag; the email-alone 1-match call site passes `{ omitGuestName: true }`. The strict paths (`ref+email`, `ref+token`, `ref alone`, `token alone`) still call the helper without the flag and continue to return `guestName` because each demonstrates possession of a non-email secret (the ref is in the confirmation email, the token is in the magic-link email). The `BookingData` interface on the client makes `guestName` optional; the single-booking card's "Lead Guest" section is gated on `activeBooking.guestName &&` and renders only when the name is present. The booking doc still stores `guestName` as before — every reader (drawer, table, PDF, email) still needs it; the public lookup just stops reflecting it back. The `bookings-lookup-list.test.ts` "1 match → single" test was renamed to assert `guestName` is absent; the "ref+email path returns kind: single" test was renamed and now asserts `guestName` is present (pinning the second-factor distinction). The `bookings-lookup.test.ts` "email alone returns the most recent booking when 1 match exists" test gained an `expect.not.toHaveProperty("guestName")` assertion. `plan/features/BOOKING-LOOKUP.md` privacy stance updated. Branch: `fix/mbp-no-name-on-card`. |
| 131 | **Drop `guestName` and `guestEmail` from the single-booking response on every path; surface `maskedEmail` instead (MBP-09, approved 2026-07-25, same-day follow-up on #126 + #128).** Decisions #126 (picker rows) and #128 (email-alone 1-match single card) tightened the privacy posture on the public `/my-booking` page, but #128 left a leak: the **strict paths** (ref+email, ref+token, ref alone, token alone) still reflected the full `guestName` back to the caller. A live staging screenshot showed the leak: a user who reached the single-booking card via the picker-strict path (the most common flow after the email-alone picker) saw "Daniel Sandimas" + "dksandimas.projects@gmail.com" in the "Lead Guest" section. Decision: **the public `/my-booking` page NEVER reflects the guest name OR the full email back to the caller — on any path.** The `enrichAndRespond` helper now (a) drops `guestName` from the response unconditionally (the per-call `omitGuestName` option from #128 is removed) and (b) drops `guestEmail` too, replacing it with `maskedEmail` (first char of local + `***` + full domain — same format as the picker). The email-alone 1-match call site no longer needs the option; the helper is uniform for every caller. The `BookingData` interface on the client replaces `guestName?: string` and `guestEmail: string` with `maskedEmail: string`. The "Lead Guest" section is replaced by a "Booked under" echo showing the masked email. The "Email sent to ..." success indicator on the resend flow becomes a generic "Confirmation email sent." — the card no longer echoes the full address. The cancel + resend flows now use the user's typed value (`emailInput` form state) instead of `activeBooking.guestEmail` — that value is already validated by the lookup that returned the booking, and the form is always mounted (per #130) so the state is always available. The booking doc still stores `guestName` and `guestEmail` for staff-gated readers (drawer, table, PDF, email); the public lookup just stops reflecting them back. **Tests:** `bookings-lookup-list.test.ts` "1 match → single" + "ref+email path returns kind: single" + "email alone returns the most recent booking when 1 match exists" + the `bookings-lookup.test.ts` "returns enriched booking + room data on a matching ref + email" test all updated to assert `guestName` and `guestEmail` are absent, `maskedEmail` is present. **Implementation shipped on `fix/lookup-no-name-anywhere`:** 1 server helper signature simplification + 1 field swap on the wire + 1 client interface change + 1 client UI section rewrite + 4 test updates. `plan/features/BOOKING-LOOKUP.md` privacy stance updated; `plan/project/ROADMAP.md` MBP-09 row added. |
| 129 | **Picker click no longer fails with "Bot verification token is missing" (MBP-07, approved 2026-07-25, same-day follow-up).** Staging screenshot showed a live UX bug: user entered an email with multiple bookings, the picker rendered, click a row → page reverted to the "Find your booking" form with the ref + email pre-filled and the server's 400 "Bot verification token is missing." error. Root cause: `performLookup`'s `finally` block unconditionally called `resetTurnstile()`, which consumed the token right after the email-alone lookup returned the list. The widget then needed 1-3 seconds to mint a new token, but the user clicked a row immediately. The second `performLookup` call fired with `turnstileToken: ""` (or the stale not-yet-rotated value) and the server rejected it. Decision: **the picker click reuses the same Turnstile token.** Tracked `showedPicker` in `performLookup`; the finally block now only calls `resetTurnstile()` when the picker was NOT shown. Cloudflare's `siteverify` is idempotent within the 300s validity window, so the second verification succeeds; the existing 10/min rate limit + 3-failure 1-hour backoff still gate enumeration. The bot-protection model is unchanged: each `resetTurnstile()` still requires a real browser interaction. The picker flow is a short-lived, same-session, email+ref-gated sequence, so reusing the token is acceptable. **Plus: URL-driven picker navigation.** `handlePickerSelect` now calls `navigate(\`/my-booking?ref=…&email=…\`, { replace: true })` instead of `performLookup` directly. The existing `useEffect` on `searchParams` handles the auto-lookup, gated on a fresh Turnstile token (which is alive because of the reset fix above). Bonus UX: the URL is bookmarkable, refreshable, shareable, and the Back button works. **Implementation shipped on `fix/mbp-picker-click-turnstile`:** 1 finally-block condition + 1 `navigate` call + `useNavigate` import. No server change, no schema change, no contract change. The wire shape is already covered by `bookings-lookup-list.test.ts` "email-alone with 2+ matches" + "ref+email path returns kind: single". `plan/features/BOOKING-LOOKUP.md` "Server contract" section notes the Turnstile reuse; `plan/project/ROADMAP.md` MBP-07 row added. |
| 126 | **Tighten the multi-booking picker: drop `guestName` from the wire entirely, add `maskedEmail` (approved 2026-07-25, the day after #123 shipped on `dev`).** The original #123 spec preserved a "single-name mode" that attached the full `guestName` to every row when every match shared the same trimmed+lowercased name. That still leaked the full name to anyone with access to the email — a spouse, ex-partner, shared family inbox — and the page's threat model treats email as the only auth secret on this public route. Decision: **retire the single-name vs multi-name mode distinction entirely** and return a uniform row shape regardless of who the bookings are for. The new picker payload is `{ id, bookingRef, maskedEmail, checkIn, checkOut, numNights, roomType, status }` — `maskedEmail` is first char of local + `***` + full domain (e.g. `j***@gmail.com`), the attacker already typed the email so the leak surface is zero, and the legit user gets a small UX confirmation that the search keyed on the right email. The full `guestName` is still revealed on the single-booking card after the user picks a row (which goes through the existing `ref + email` second factor, decision #123's deep-link pattern). The response shape is now uniform: there is no mode for the client to detect, which is also the cleanest path for the rate-limit-oracle concern (fewer fields ⇒ fewer ways to differentiate replies). **Implementation shipped on `fix/mbp-no-name`:** `guest-app/server/handlers/bookings.ts` — new module-level `maskEmail(email)` helper (defensive fallback `***` for malformed inputs); email-alone branch's entry-construction loop no longer reads `data.guestName` or computes `allShareName`; every entry now carries `maskedEmail`. The privacy-contract comment block was rewritten around the uniform row shape. `guest-app/src/pages/BookingLookupPage.tsx` — `PickerEntry` type drops `guestName?`, adds `maskedEmail: string`; row UI swaps the guestName line for the masked email line (same `text-xs text-gray-600 truncate` styling, so the row layout is identical from the user's perspective). `guest-app/tests/api/bookings-lookup-list.test.ts` — "single-name mode" test renamed to "uniform row shape with maskedEmail and NO guestName (decision #126)"; the multi-name test renamed to "STILL omits guestName (uniform row shape)" and now asserts the same `maskedEmail` is on every row; new "maskedEmail uses first char + *** + full domain" test pins `j***@gmail.com` so a future refactor can't accidentally widen the local-part leak; the 11+ matches test now asserts every row has `maskedEmail` and no `guestName`. `plan/features/BOOKING-LOOKUP.md` — the "Two privacy modes — picked automatically by the server" section retired; the row-shape spec and the privacy stance section rewritten around the uniform payload; the QA checklist and the row-data checklist both updated. Branch: `fix/mbp-no-name`. |
| 130 | **Keep the `/my-booking` search form always mounted (approved 2026-07-25, same-day follow-up on #129).** After the MBP-07 picker-click fix, users started exercising the full flow: enter email → picker → "Back to search" → re-submit. The first three steps worked (per #129), but the last step produced "The security check hasn't finished yet" because the Cloudflare widget was gone from the form. Root cause: the result area was a single ternary `{pickerResults ? <Picker/> : !activeBooking ? <Form/> : <Card/>}`. When the picker or card was active, the `<Form/>` branch was unmounted — including the Turnstile widget container div inside it. On "Back to search" the form was re-mounted with a brand-new div; the `useTurnstileToken` hook's effect deps `[enabled, containerRef]` were unchanged, so `tryRender` didn't re-run and the new div was empty. The token was empty (cleared by an earlier `expired-callback`/`error-callback`/`resetTurnstile()` cycle), so `handleSearch` bailed. Decision: **the form is always mounted**, with the `hidden` class (`display: none`) suppressing it when the picker or card is the active view. The form's container div is now a stable DOM node; the widget persists across picker/card transitions; the token stays valid. The picker and card are layered on top via two conditional renders (`{pickerResults && pickerResults.length > 0 && <Picker/>}` and `{activeBooking && <Card/>}`). `aria-hidden` is set on the form when suppressed so screen readers skip it. **Implementation shipped on `fix/picker-back-to-search-turnstile`:** one ternary → one always-mounted form + two conditional renders. No server change, no hook change, no schema change, no new test. The wire shape is unchanged. `plan/project/ROADMAP.md` MBP-08 row added. |
| 132 | **Hydrate `breakfastServed` from the booking snapshot mapper (BSP-01, approved 2026-07-25).** The dashboard `toggleBreakfastServed` writes the per-date/per-guest served map to `bookings/{id}.breakfastServed` via `updateBookingStatus(..., { breakfastServed })`, the Firestore rule permits the field, and the write succeeds. But the admin booking snapshot mapper in `AdminContext.tsx` only hydrated `breakfastSelections` and omitted `breakfastServed`, so the served map was silently re-read as `undefined` on the next snapshot — the dashboard re-rendered the row as unserved and any subsequent toggle started from `{}` (the spread of `undefined`), wiping previously-served keys. Confirmed defect: "Mark Served" on a row with two breakfast guests, refresh the dashboard, both rows show unserved. Decision: **hydrate the served map on the snapshot read** — `breakfastServed: data.breakfastServed || {}` in the mapper, next to `breakfastSelections`. No rules change, no schema change, no migration; the field was already on the TS interface (`shared/types/index.ts §Booking`) and the existing toggle already does `const next = { ...(booking.breakfastServed || {}) }` so the fix is fully client-side. The `bookings-lookup-list`-style "previous version" test surface for this defect was misleading: the existing source-presence test on `DashboardPage.tsx` only verified that the relevant names occurred, not that the mapper hydrated the field, so a future refactor that breaks the hydration would not have been caught. The new regression suite (`admin-app/src/__tests__/breakfast-served-persistence.test.ts`, 9 tests) is a mix of (a) source-text pins on the mapper shape and the dashboard read/write sites, and (b) pure-JS behavioral tests that simulate the full toggle → snapshot-refresh → toggle cycle including a cross-session visibility scenario and a legacy-booking `{}` default. Manual QA on the two-guest same-booking case is the remaining BSP-03 gate. `plan/docs/TYPES.md` adds the field annotation; `plan/docs/BACKEND.md` adds the row to the bookings table. Branch: `fix/breakfast-served-persistence`.
| 133 | **Gate the email-claim booking match on `email_verified === true` (HIGH-1 closed, approved 2026-07-25).** The Spark Rewards audit (2026-07-18, `plan/docs/AUDIT-SPARK-REWARDS-REPORT.md §Section 3 HIGH-1`) found that every member-scoped booking matcher keyed off the Firebase ID token's `email` claim with no `email_verified` check: `linkBookingsByEmail` (registration linkage), `handleListMemberStays` (the `/api/members/stays` projection that leaks `bookingRef` + `lookupToken` — together the public lookup/cancel credential), and `findBooking` (the member email branch used by `/api/email/early-checkin-request`). Firebase email/password signup lets a user claim an arbitrary unverified email, so an attacker who registers with a victim's email (any victim who booked anonymously and never created an account — the common hotel case) can link the victim's past bookings on registration, read them via `/api/members/stays`, and fire early check-in writes against them. Fix: (1) `authenticateUser` (in `guest-app/server/apiRouter.ts`) now surfaces `decodedToken.email_verified` on its return shape; (2) `handleListMemberStays` returns `403 { code: "EMAIL_NOT_VERIFIED" }` when the auth user's email isn't verified — Google sign-in tokens are always verified so this is effectively a gate on the email/password path; (3) `handleRegisterMember` still creates the `members/{uid}` doc but **skips `linkBookingsByEmail`** when the email isn't verified, and the response carries `{ data: { emailVerified: false, linkedBookings: 0 }, warning: "Verify your email to link your past bookings." }` so the client can render a "verify your email" prompt; (4) the router-level check in the `action === "early-checkin-request"` branch returns `403 { code: "EMAIL_NOT_VERIFIED" }` before `findBooking` runs, and `findBooking`'s `emailMatches` predicate is also hardened to require `user.email_verified === true` as a defensive second line (the `memberId` match remains safe — the attacker would need their own uid); (5) the client's `signUpWithEmail` now `void`-calls `sendEmailVerification(credential.user)` (non-blocking — a verify-email failure must not abort member registration) immediately after `createUserWithEmailAndPassword`, and a new reusable `<EmailVerifyBanner />` component surfaces the unverified state on `My Stays` + `My Rewards` (replaces the early check-in button while unverified) + `My Profile`, with a "Resend verification email" + "I already verified" pair of actions. The auth context gains `resendVerification()` (Firebase SDK call + `currentUser.reload()`) and `refreshAuthUser()`. The audit's "minimum viable fix" recommendation (gate all three match sites on `email_verified`) is implemented; the audit's broader recommendation (verify email on email/password signup) is also implemented via `sendEmailVerification` in the signup flow. No schema change, no rules change (the email claim is already on the ID token; we just enforce it), no migration. **Idempotency:** an email/password user who verifies after signup can re-call `/api/members/register` to trigger the `linkBookingsByEmail` pass — the registration path is idempotent (`memberNumber` is preserved). **Tests:** `guest-app/tests/api/members-register.test.ts` gains two new tests (`rejects stays list with EMAIL_NOT_VERIFIED when email is unverified` + `skips linkBookingsByEmail when the registering user's email is unverified`) and updates two existing tests to reflect the new `emailVerified` field in the response. Full admin + guest suite green (1526/1526, modulo the pre-existing emulator-dependent `firebase/tests/notifications.rules.test.ts` and the `@config`-workspace-dependent `admin-app/src/__tests__/api-base-url.test.ts` + `dashboard-overdue-checkouts.test.ts`). `plan/docs/SECURITY.md §Auth & Security` already had the corresponding "never do" rule (`plan/docs/GOTCHAS.md §Auth & Security`); this PR enforces it. `plan/features/SPARK-REWARDS.md §Known Issues` updated: HIGH-1 entry marked closed; a new "Auth & email verification" section documents the gate, the banner, the resend flow, and the idempotency property. Branch: `fix/spark-rewards-email-verified-gate`.
| 134 | **Manual points adjustment moves server-side (MED-1 closed, approved 2026-07-25).** Per the Spark Rewards audit 2026-07-18 MED-1, the manual points adjustment path was the *only* balance-mutation still performed with the client Firestore SDK (`AdminContext.updateMemberPoints` did a `runTransaction` that wrote `members/{uid}.rewardsPoints` and appended a `pointsHistory/{entryId}` row in the same client transaction). Because a client transaction must be allowed by rules, the `members/{uid}` update rule granted any `isStaff()` caller (front-desk **or** admin) the ability to write `rewardsPoints` directly with no field restriction — the "balance always equals sum(history)" invariant was enforced **only in app code**, not at the security-rule boundary. Fix: (1) new server endpoint `POST /api/members/manual-adjust` (`handleManualAdjustPoints` in `guest-app/server/handlers/members.ts`) — admin-only (front-desk 403), Zod-validated body `{ memberId, amount, reason }` (strict mode, non-zero integer, reason 1-500 chars), 10/min/IP rate limit, server-side `runTransaction` re-reads the member doc, refuses to drop the balance below zero, writes `rewardsPoints` + a `pointsHistory/{historyRef.id}` row with `type: "manual"` hardcoded (caller cannot inject "earn" / "redeem" rows) + the staff UID in the same commit. (2) Client `AdminContext.updateMemberPoints` now calls the endpoint via `fetch` with the staff ID token (10 new tests in `guest-app/tests/api/members-manual-adjust.test.ts` pin the contract: admin-only, debit + credit, below-zero guard, missing member, zero-amount, empty-reason, tokenless, method, hardcoded `type:"manual"`, transaction atomicity). (3) Firestore `members/{uid}` staff update allowlist is tightened to `["isActive", "isMember", "tier", "memberSince", "memberNumber", "notes", "handledBy", "updatedAt"]` — **`rewardsPoints` is no longer writable from any client**. The member self-update branch (`fullName`, `phone`, `photoUrl`) is unchanged. The `pointsHistory` subcollection still permits `isStaff()` `create` as a defense-in-depth fallback for any future backfill, but the only live writer in the codebase is the server. (4) Updated `audit-admin-critical-2026-07-07.test.ts §AA-01` to pin the new shape (server endpoint path, server `runTransaction`, hardcoded `type:"manual"`, members rule without `rewardsPoints`). **No CRITICAL/HIGH/MED remaining from the 2026-07-18 Spark Rewards audit after this PR + #133 (HIGH-1).** All 4 balance-mutation paths (earn, redeem, undo, manual-adjust) are now server-side and the integrity invariant is enforced at the rules boundary. No schema change, no client surface change (the existing `updateMemberPoints` signature + UI call site is unchanged), no migration. **Manual QA follow-up** — verify on a real test member: (a) admin can credit + debit points with a reason and the history row appears on the drawer, (b) front-desk gets 403 when attempting the same call, (c) trying to drive the balance below zero returns 400, (d) a malicious client call that tries to inject `type: "earn"` in the body still writes `type: "manual"` (the server hardcodes). 1536 tests pass (modulo the pre-existing emulator-dependent `firebase/tests/notifications.rules.test.ts` + the `@config`-workspace-dependent `admin-app/src/__tests__/api-base-url.test.ts` + `dashboard-overdue-checkouts.test.ts` failures, all confirmed pre-existing on dev). `plan/features/SPARK-REWARDS.md §Known Issues` updated: MED-1 entry closed. Branch: `fix/spark-rewards-manual-points-server-side`.
| 135 | **MED-3 spec-correction variant (closed 2026-07-25).** The Spark Rewards audit 2026-07-18 §MED-3 flagged that two `§Edge Cases & States` entries in `plan/features/SPARK-REWARDS.md` were marked `[x]` but neither was implemented in the codebase: (a) "Member books while logged in but uses different email — no auto-link; manual link by front desk from Member detail drawer" — the "no auto-link" half is correct (`linkBookingsByEmail` only matches on the token email) but the "manual link by front desk" half is unbuilt; there is no booking→member link action in the admin member drawer (`admin-app/src/pages/MembersPage.tsx` exposes only points adjustment + suspend/activate), and (b) "Google account email differs from booking email — after sign-in, prompt… " — no such guest self-service prompt exists anywhere in `guest-app/src`. The functional gap (no reconciliation flow) is real but the *misleading `[x]` markers* are the audit's primary concern. Decision: take the **spec-correction variant** (audit's recommended alternative to building either path), which is <15 min and ships no behavior change. (1) The two `§Edge Cases & States` entries now read `[ ]` (deferred) with a "Deferred (Spark Rewards audit 2026-07-18 MED-3)" prefix; the inline copy preserves the "no auto-link" half (still accurate) and explicitly notes the unbuilt half plus the two workarounds: (i) the guest can find the booking at `/my-booking` (ref + email), and (ii) admin can update the booking's `memberId` via the booking drawer (existing staff tool). (2) The §Known Issues MED-3 entry is marked `✅ Closed 2026-07-25 (spec-correction variant)`. The build variant (front-desk manual-link surface in `MembersPage` detail drawer — the smaller of the two surfaces) is still deferred; revisit when a second white-label client or an explicit hotel ask makes the link surface a priority. The functional gap is documented and not silently hidden behind `[x]` markers. No code change, no schema change, no rules change, no migration. No tests to update. 1536 tests still pass (no source files touched). Branch: `fix/spark-rewards-different-email-spec`. **Update 2026-07-29** — the build variant of MED-3 (front-desk manual link) now ships; see decision #140 for the implementation. The spec-correction-only closure of #135 is superseded for the front-desk half; the guest self-service prompt (the larger MED-3 half) remains the unbuilt piece.
| 136 | **Spark Rewards audit LOW-1..7 closed (approved 2026-07-25).** Closes all 6 LOW findings from the 2026-07-18 Spark Rewards audit (`plan/docs/AUDIT-SPARK-REWARDS-REPORT.md §Quick Wins`) plus LOW-3 (a §Section 1 LOW that was already in the same report). Each fix is small, isolated, and ships a small corresponding test in `shared/__tests__/spark-rewards-lows-batch.test.ts` (18 tests total). **LOW-1** — Privacy Policy + Terms of Service disclosure line under the Rewards landing page's one-click "Enroll" button (mirrors `ProfilePage` so the join-consent posture is consistent across both surfaces). The `/rewards` landing page now wraps the `<PrimaryButton>` in a flex column with the disclosure caption below. No new test fixtures; the spec already required this surface to match. **LOW-2** — explicit `setPersistence(auth, browserLocalPersistence)` call in `guest-app/src/firebase/config.ts`. The Firebase Web SDK defaults to local persistence on web today, but the contract was implicit; an SDK default change or future refactor could silently regress "stay signed in." The admin app retains `browserSessionPersistence` (different posture — staff should not stay signed in past tab close). The pin is one line + a `void`d `.catch` that warns on failure (the SDK falls back to its default, so a failure is a UX regression at worst, not a security issue). **LOW-3** — no fix needed; §Section 1 LOW-3 is `linkBookingsByEmail` batch not chunked to Firestore's 500-write limit, theoretical only (not realistic for a single-hotel guest). Re-classified as `⏸ Deferred` in the SPARK-REWARDS Known Issues section — a member whose email matches >500 bookings would be the only failure mode. **LOW-4** — `calculateEarnedPoints` deleted from `shared/utils/points.ts` (it had a per-₱100-block formula `Math.floor(totalPrice/100) * pointsPerHundred` that diverged from the live `calculateCheckoutPoints` proportional formula `Math.floor((totalPrice/100) * pointsPerHundred)`). The shared helper had no production call site — only its own test exercised it, so the divergence was a trap rather than an active bug. Two tests removed; the canonical formula stays in `bookings.ts → calculateCheckoutPoints`. The `My Rewards` "Earn N points per ₱100 spent" copy now notes "proportional — partial ₱100 still earn fractional points" so the user-facing estimate matches the actual crediting. **LOW-5** — `StaysPage` now renders an `<EarlyCheckInStatusLine>` for each booking that has an `earlyCheckIn` map (green/approved with confirmed time + staff note, red/declined with staff note, blue/requested awaiting front desk). The `StayRecord` interface gains an optional `earlyCheckIn` field (already returned by `/api/members/stays`). The spec's "My Stays + My Rewards show the request status on the relevant booking" rule is now satisfied. **LOW-6** — `SPARK-REWARDS.md §Spark Rewards — Admin Member Management` checklist no longer says "Export members list as CSV." The export is a "Members" worksheet inside the admin-only full-backup `.xlsx` (per `audit-admin-sev4-2026-07-07.test.ts §AA-32`); the spec line is rewritten to say "Export members list via the full data backup (XLSX) on the Reports page (admin-only …)" with a pointer to the AA-32 audit closure. **LOW-7** — `sendEmail` skip guard in `guest-app/server/handlers/email.ts` now matches BOTH `@example.invalid` (early "unverified email" test path) and `@invalid` (RA 10173 erasure path — `members.ts` anonymizes erased bookings with `guestEmail: "erased@invalid"`). A stray email trigger against an erased booking now skips cleanly instead of attempting delivery to a non-deliverable address and bouncing at Resend. **No CRITICAL/HIGH/MED/LOW remaining from the 2026-07-18 Spark Rewards audit after this PR + #133 (HIGH-1) + #134 (MED-1) + #135 (MED-3 spec variant).** 1552 tests pass (modulo the pre-existing emulator-dependent `firebase/tests/notifications.rules.test.ts` + the `@config`-workspace-dependent `admin-app/src/__tests__/api-base-url.test.ts` + `dashboard-overdue-checkouts.test.ts` failures, all confirmed pre-existing on dev). No schema change, no rules change, no client surface change beyond a copy + a button-wrap; no migration. Branch: `fix/spark-rewards-lows-batch`.
| 137 | **Editable Terms & Conditions (LCE-01, approved 2026-07-25).** The public `/terms` page is now admin-editable. The previous hardcoded 11-section body stays in place as the deploy-time fallback (so the page never goes blank during a cold-load window or before the first admin save). The admin app's **Settings → Legal Content** tab gains a Terms of Service body textarea (50 KB cap, plain text only — preserves paragraph + list structure via `whitespace-pre-line` on the public page) with a version badge in the editor header that mirrors the live stamp. **New server endpoint** — `POST /api/admin/update-terms` (`handleUpdateTerms` in `guest-app/server/handlers/legal.ts`). Admin-only (front-desk 403, tokenless 401, GET 405, 10/min/IP rate limit — mirrors the existing `set-active` route's posture). Zod-validated body `{ termsBody: string(1..50_000) }` (strict mode). Server reads the current `settings/websiteContent.termsVersion` inside a transaction, auto-bumps the patch level (`1.0.0 → 1.0.1`, preserves major + minor + any pre-release suffix), and writes `{ termsBody, termsVersion, termsLastUpdated, termsUpdatedBy, termsUpdatedAt }` to the same doc via `setDoc(..., { merge: true })` so the rest of the `websiteContent` keys (homepage hero, about story, contact, corporate, etc.) are preserved. Returns the new version + last-updated so the admin editor mirrors them client-side without a second Firestore round-trip. Falls back to `DEFAULT_TERMS_VERSION = "1.0.0"` (new shared constant) when the website has never saved terms. **Shared constants** — `DEFAULT_TERMS_VERSION = "1.0.0"` and `TERMS_BODY_MAX_LENGTH = 50_000` in `shared/constants/index.ts`. The cap is enforced both client-side (textarea `maxLength`) and server-side (Zod schema) so the admin sees a clear "exceeds the 50 KB limit" before submit. **Public `/terms` page** — `guest-app/src/pages/TermsPage.tsx` now fetches `settings/websiteContent` on mount. When `termsBody` is a non-empty string, it renders the custom body via `<pre class="whitespace-pre-wrap">` (no `dangerouslySetInnerHTML` — plain text only, no XSS surface). When the field is empty or the doc is missing, the page falls back to the original hardcoded 11-section body. The header shows the live version + last-updated when set, with a small "Admin-edited · v{X.Y.Z}" badge so guests can see the doc is current. **Booking consent version capture** — `guest-app/server/handlers/bookings.ts → handleCreateBooking` reads `settings/websiteContent.termsVersion` inside the same `runTransaction` that creates the booking and stamps the value on the new booking doc as `termsConsentVersion`. Two concurrent admin saves land in different transactions, so a booking created at time T is stamped with whichever version was live at T — the audit trail is reliable per-booking. Bookings created before LCE-01 don't carry the field at all; the per-booking copy on `/my-booking` renders the fallback gracefully. The `create-walkin` path does not stamp the field (walk-ins are staff-mediated; the staff signature on the registration form is the legal consent, not the `/terms` checkbox). **Tests** — 10 server tests in `guest-app/tests/api/admin-update-terms.test.ts` (admin-only, tokenless, method, payload validation, version auto-bump from 1.0.0 → 1.0.1, patch preservation from 1.0.4 → 1.0.5, pre-release suffix preservation, malformed-input fallback, missing-doc fallback, front-desk 403) + 20 source-text pins in `shared/__tests__/lce-01-batch.test.ts` (constants, server endpoint wiring, Zod schema, role gate, transaction shape, version-bump helper, booking consent capture, Settings editor fields, public page render path, fallback structure). The pre-existing W3.11 test in `batch-19-wave3-batch2.test.ts` is updated from "renders the literal `config.termsLastUpdated`" to "falls back to `config.termsLastUpdated` when no custom value is set" — the new contract is `lastUpdated = customLastUpdated || config.termsLastUpdated` (the fallback is preserved; the page just resolves through a variable now). 1582 tests pass (modulo the pre-existing emulator-dependent `firebase/tests/notifications.rules.test.ts` + the `@config`-workspace-dependent `admin-app/src/__tests__/api-base-url.test.ts` + `dashboard-overdue-checkouts.test.ts` failures, all confirmed pre-existing on dev). No schema change (one new optional field on `bookings/{id}`), no Firestore rules change, no client surface change beyond the new editor + a re-routed version display; no migration (legacy bookings without `termsConsentVersion` are fine). Branch: `fix/terms-editable-content`.

| 138 | **Spark Essentials guest catalog gets a search + category filter rail (GSD-01, approved 2026-07-25).** The in-room intercom store page (`/guest-app/src/pages/IntercomPage.tsx`) previously rendered all `isActive: true` items alphabetically with no way to narrow the catalog — a meaningful friction point once the catalog grows past ~10 items. Decision: **add a search field + a category chip rail below the store intro**, both client-side and memoized over the existing real-time `onSnapshot` (no extra Firestore reads per keystroke or chip tap). Search matches `name + description`, trimmed and case-insensitive, with the trim guard so a whitespace-only query is treated as "no filter" rather than a wildcard. Category chips start with **All** and then render only the categories that are actually represented in the live catalog (drinks / snacks / toiletries / rentals / other, in the canonical order), driven by a `representedStoreCategories` memo over the current snapshot. The two filters compose with AND semantics. A live result count (`{n} of {m} items match your filters`) sits above the grid, with a one-action **Clear filters** link visible only when at least one filter is non-default. Sort is **in-stock alphabetical first, out-of-stock alphabetical second** (a stock of `null` is treated as in-stock / unlimited). The chip rail is a horizontally scrolling flex container with `-mx-4 px-4 overflow-x-auto`, each chip is `min-h-[44px]`, and selected state is exposed both via `aria-pressed` and `aria-selected` for assistive tech. Filter state lives at the IntercomPage scope (not inside a Shop sub-component) so it survives Shop / Chat / cart / checkout view switches, and is held in two independent `useState` hooks (`storeSearch`, `storeCategoryFilter`) that never touch the cart. A distinct no-match state renders when the catalog has items but the filter pair produces zero — different copy from the existing "The shop is currently unavailable" empty state, with a primary **Clear filters** button as the obvious next step. The `StoreItem` interface gains a `category: StoreCategory` field, the snapshot mapper normalizes legacy / missing values to `"other"` (mirroring the admin-side `normalizeStoreCategory` in `AdminContext.tsx`), and the filter chrome is wrapped in a `storeItems.length > 0` guard so the unavailable empty state still stands on its own when the catalog is empty. New source-text regression suite `shared/__tests__/gsd-01-batch.test.ts` (30 tests) pins: the type system, the memoized filter + sort, the AND composition, the trim+lowercase normalization, the represented-categories memo, the chip rail ARIA + touch target, the result count + clear-all control, the no-match state, the empty-state unchanged, and a cart-preservation negative test that asserts `clearStoreFilters` does not mention `setCart`. No server change, no schema change, no new collection, no new index, no API bundle rebuild (client-only). Branch: `fix/store-discovery`. *(Per Spark Essentials roadmap GSD-01 entry in `plan/features/STORE-GUEST.md`.)*

| 139 | **Extend the "House rules" email card to booking-confirmed + check-in reminder (ECE-02, approved 2026-07-26).** ECE-01 (2026-07-24, decision #136 row) shipped the "House rules" card on the `payment-confirmed` email only — sourced from `settings.websiteContent.houseRules`, omitted when blank, HTML-escaped. The card's value is that the guest arrives already knowing the property's expectations, but that value is strongest at the *latest* "you're arriving soon" touchpoint, not the *earliest* one. Decision: **append the same card to `booking-confirmed` and `checkin-reminder` too**, so a guest receives the rules at three touchpoints (payment → confirmed → day-before reminder) and the staff-owned copy stays single-sourced from `settings.websiteContent.houseRules`. Implementation: (1) extract a shared `houseRulesCard(houseRules?: string | null): string` helper inside `guest-app/server/handlers/email.ts` — trims, omits when blank, renders via `escapeHtml`, no fallback copy; the three email functions (`paymentConfirmedEmail` / `bookingConfirmedEmail` / `checkinReminderEmail`) all append the result to the body; (2) `sendBookingTrigger` now gates the `websiteContent` doc read by a `HOUSE_RULES_ACTIONS = new Set<EmailAction>(["payment-confirmed", "booking-confirmed", "checkin-reminder"])` so adding a fourth action that needs the card is a one-line change — every other template still skips the round-trip; (3) `handleEmailPreview` accepts `houseRules` in the request body for all three templates (mirrors the ECE-01 payment-confirmed contract) so staff can preview the exact render from the admin panel. **Tests** — 19 new tests in `guest-app/tests/api/email-house-rules-ece02.test.ts` covering: live-send card include/omit/omit-whitespace/throws for `booking-confirmed`, live-send card include/omit for `checkin-reminder`, non-arrival triggers (`booking-cancelled` / `booking-submitted` / `discount-rejected`) skip the read entirely, preview renders for the two new templates (positive + negative + HTML-escape), and source-text guards on `houseRulesCard` (trim + escape + no hardcoded fallback) + `bookingConfirmedEmail` / `checkinReminderEmail` signatures + the `HOUSE_RULES_ACTIONS` Set + the preview routing. 1 existing ECE-01 source-text guard updated to pin the new `houseRulesCard(houseRules)` call shape instead of the inlined `houseRulesBlock` pattern (the trim/conditional logic moved to the helper). 1631/1631 green (modulo the 3 pre-existing emulator + workspace failures on dev). No schema change, no rules change, no new collection, no API bundle rebuild (server-side only). Branch: `fix/ece-02-house-rules-more-emails`. *(ECE-02 entry on `plan/project/ROADMAP.md`.)*
| 140 | **Front-desk manual link of an existing booking to a member (MED-3 build variant, approved 2026-07-29).** The Spark Rewards audit 2026-07-18 §MED-3 split into two paths: the front-desk manual-link surface (smaller) and the guest self-service prompt (larger). Decision #135 closed the doc-accuracy half on 2026-07-25 (spec-correction variant) and explicitly left the build path open: "build the front-desk manual-link surface from the MembersPage detail drawer (smaller surface than the guest self-service prompt)." Build it now. **New server endpoint** — `POST /api/members/link-booking` (`handleLinkBookingToMember` in `guest-app/server/handlers/members.ts`). Admin-only (front-desk 403, tokenless 401, GET 405, 10/min/IP rate limit — mirrors `manual-adjust` / `set-active`). Zod-validated strict body `{ memberUid: string(1..160), bookingId: string(1..160), reason: string(1..500) }`. The handler re-reads the member + the booking inside one `runTransaction` so a concurrent register or a concurrent erase cannot race the link. **Invariants** — (a) cancelled bookings are refused (400) — the cancelled stay would surface as a "you cancelled this" card in the member's My Stays list, which is confusing; the existing booking-lookup workaround + booking-drawer `memberId`-edit still let the staff re-attach the PII if needed; (b) test-run bookings are refused (400) — the test-isolation invariant the rest of the audit closes (ETR-07, audit S2.3); (c) bookings already linked to a *different* member are refused (409) — no silent overwrite, no unlink from this surface (the booking-drawer `memberId`-edit is the unlink work-around); (d) re-linking a booking to the *same* member is idempotent (no booking update, audit row still written, `alreadyLinked: true` in the response so the UI can show a softer "already linked" toast). On success the handler sets `memberId` + `linkedByStaff` + `linkedAt` + `linkedReason` on the booking doc, then writes an audit row under `bookings/audit/records/{bookingId}-link-{timestamp}` (same nested-collection shape the erasure audit uses — decision #49 / W1.4) with `{ action: "manual-link-member", fromMemberId, toMemberId, memberEmail, bookingEmail, reason, staffUid, staffRole, at }`. **AdminContext** — `linkBookingToMember(memberUid, bookingId, reason)` is added to `admin-app/src/context/AdminContext.tsx`, posts to `/api/members/link-booking` with the staff ID token, returns `{ success, error?, alreadyLinked?, bookingRef? }`. **MembersPage drawer** — a new "Link Existing Booking" form is rendered below the points adjustment form, gated on `selectedMember` (it's a member-scoped action). The helper copy surfaces `selectedMember.email` so the staff can see the account email next to the "differs from" wording — the different-email context is the whole reason this surface exists. The form fields are **Booking ID or Reference** + **Audited Reason for Link** (required, 500-char max). Toasts: `toast.success` on a fresh link, `toast.info` on a re-link (so the staff sees the idempotent re-link as a soft confirmation, not a fresh action). **Tests** — 14 server tests in `guest-app/tests/api/members-link-booking.test.ts` (method 405, tokenless 401, front-desk 403, missing-field 400, whitespace-only reason 400, >500-char reason 400, member-not-found 400, booking-not-found 400, cancelled 400, test-run 400, already-different-member 409, first link 200 with alreadyLinked:false + booking write + audit row, idempotent re-link 200 with alreadyLinked:true + no booking write + audit row, Admin SDK transaction posture) + 18 source-text tests in `admin-app/src/__tests__/med-3-link-booking.test.ts` (route wiring, handler contract, AdminContext function, MembersPage form). 930/930 admin + 544/544 guest tests green, typecheck clean. **MDs** — `plan/features/SPARK-REWARDS.md §Edge Cases` first MED-3 entry flipped to `[x]` with the full ship summary; the second entry (guest self-service prompt) stays `[ ]` deferred as the larger remaining MED-3 path. `plan/features/SPARK-REWARDS.md §Known Issues` MED-3 row updated to note the build variant ship and the supersession of the spec-correction-only closure. `plan/project/ROADMAP.md §Spark Rewards Feature Audit` MED-3 entry updated. Branch: `fix/spark-rewards-med-3-link-booking`.
