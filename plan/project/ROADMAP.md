# Spark Inn — Build Roadmap & Checklist
> Living document — **must be updated on every merge** (see `How to Use This File` + `plan/docs/CONTRIBUTING.md §When to Update Which MD`)
> Last updated: August 1, 2026 (**shipped EXB-10 — hotel-wide rollaway-bed inventory**, on `feature/exb-10-bed-inventory` at v0.207.0, commit `aa0bdf8` merged to dev as `4652498`). Per owner decision 2026-07-31 + decision #157. Extra beds could be oversold (per CVQ-04 / the EXB-10 roadmap row): rooms cannot be oversold because the availability transaction owns them, but nothing in EXB-01..09 checked a real-world bed count. EXB-10 closes that gap with a hotel-wide rollaway-bed inventory stored in `settings/hotelConfig.extraBedInventory` (a positive integer = the count of rollaway beds the hotel physically owns; `0` or absent = the historical "any number" behavior, no constraint). **The check runs INSIDE the same Firestore transaction that assigns the room** — a read-then-write check outside the transaction would race exactly like RTS-01 (two concurrent bookings both see "1 bed free" and both take it). **The helper is pure**: new `countExtraBedsInUse(bookings, rangeStart, rangeEnd, excludeBookingId?, now?)` + `checkExtraBedInventory(inventory, inUse, requestedCount)` in `shared/utils/extraBedInventory.ts`. The helper takes a pre-fetched list of candidate bookings + the requested range and returns the in-use count; the cap check is a single `checkExtraBedInventory` call. The query lives at each of the 3 call sites (`handleCreateBooking` + `handleCreateWalkin` + `handleRescheduleBooking`) — a single `.where("status", "in", ROOM_OCCUPYING_STATUSES)` + an in-memory date-overlap filter inside the helper. No new composite index needed (the existing `(status, checkIn)` index covers the status filter; the helper's `Number(...) || 0` defensive coercion handles `0`/absent `extraBedCount` for free). **`0 inventory` short-circuits to `ok: true`** so legacy + freshly bootstrapped projects get the pre-EXB-10 "any number" semantics for free. **`excludeBookingId` is the reschedule case**: the current booking's own snapshotted `extraBedCount` must be excluded from the in-use sum, otherwise every reschedule would always "use" its own bed and reject the new configuration when the inventory is tight. **Helper unit tests** — 11 in `shared/__tests__/extra-bed-inventory.test.ts` (empty list, non-overlapping, multiple overlapping, cancelled excluded, expired-hold pending excluded, excludeBookingId, half-open overlap — checkout day doesn't count as a stay night, defensive coercion: nullish/NaN/negative `extraBedCount` → `0`, `0` inventory = `ok: true`, fits exactly, exceeds cap). **Source-text tests** — 16 in `guest-app/tests/api/exb-10-bed-inventory.test.ts` (helper exports + signatures + `0 = no-constraint` short-circuit, shared barrel re-export, helper imports in the server handler, the 3 `checkExtraBedInventory` invocations, the 3 in-transaction reads, the 3 `extraBedCount > 0` fast-path gates, the reschedule `excludeBookingId: bookingId` 4th arg, the single `hotelConfig.extraBedInventory` source, the `extraBedInventory: 0` AdminContext backfill). **A regression-test fix** — the existing `exb-03-overflow-rule` test asserted `requiredExtraBedsFor` was within 500 chars of `} from "@spark-inn/shared"`; the EXB-10 import block added 25 lines of JSDoc to the same import statement, pushing the helper past the 500-char cap. The test now uses a 5000-char upper bound — the contract is "imported from @spark-inn/shared", not a specific distance. **Tests** — 641/641 guest-app (was 625; +16 for EXB-10 source-text guards), 1007/1007 admin-app (unchanged), 354/354 shared (was 343; +11 for the helper unit tests). Branch: `feature/exb-10-bed-inventory`. Recorded as `plan/docs/DECISIONS-FEATURES.md #157`.  (**previously shipped EXB-08 — surface downstream (adult/child split + extra bed line)**, on `feature/exb-08-surface-downstream` at v0.206.0, commit `9189ffe` merged to dev as `387fe4e`). Per owner decision 2026-07-31 + decision #156. The booking doc persists `numAdults` + `numChildren` + `extraBedCount` + `extraBedRate` (per CHD-01 + EXB-01), but pre-EXB-08 every downstream surface silently dropped the new fields — the receipt PDF + the PriceBreakdown + the email helper all read `rateBreakdown.addOns[]`, and the array only carried the breakfast line. The extra bed total was invisible on every surface. EXB-08 closes that gap in two layers. **Server: `buildRateBreakdown` writes the extra bed add-on line.** The `BuildRateBreakdownInput` shape gains `extraBedTotal?` + `extraBedCount?` + `extraBedRate?`. The helper now writes two addOns[] lines when both are > 0: the historical breakfast line (when `breakfastTotal > 0`) AND the extra bed line (when `extraBedTotal > 0`). The label is a count-aware variant — "Extra bed add-on (2 beds × 3 nights)" when count > 1, or "Extra bed add-on" when count is 0/1. The filter pattern strips nulls so a 0 breakfast + 1 extra bed renders as just the extra bed line. `handleCreateBooking` + `handleCreateWalkin` pass the 3 new fields into the helper. The walk-in path collapses the extra bed term to 0 when `totalPriceOverride` is set (the historical manual-rate shape — the override folds the extra bed into the room subtotal). `rebuildRateBreakdown` + `rebuildEarlyCheckoutRateBreakdown` already preserve `addOns[]` verbatim, so the rebuild paths automatically pick up the new line. The single source of truth for the addOns array is now `buildRateBreakdown` — every read site (the receipt PDF, the PriceBreakdown, the booking email, the early-departure rebuild) gets the extra bed line for free. **Email helper: the occupancy line shows the split + extra bed.** The booking email (sent on every `payment-confirmed` / `booking-confirmed` / `checkin-reminder` per the existing `email.ts` routing) now renders "Guests: 2 adults + 1 child (3 total)" when both fields are present + "Extra beds: 1 (1 × ₱800 / bed / night)" when the booking has any. Legacy pre-CHD bookings read as the single "Guests: 3" line. **7 display surfaces: the split + extra bed are threaded through every place the staff or guest sees the booking's occupancy breakdown.** (1) **Admin booking drawer header** (`BookingDrawerWorkspace.tsx`) — the inline guest count span grows to "2 adults + 1 child (3 total) + 1 extra bed" with a `title` attribute for accessibility. (2) **Admin Bookings table row** (`BookingsPage.tsx`) — the compact "2A + 1C" form + "+ 1 bed" when present (the table's narrow column can't fit the verbose form). (3) **Admin receipt PDF** — the "Guests:" line uses the same compact form (single-line layout, limited horizontal space); the "Stay" line uses the verbose form via a multi-line IIFE that surfaces the split. (4) **Admin Bookings drawer "Guests:" line** + the "Room stay details" section — verbose form. (5) **Admin Reports** (`ReportsPage.tsx`) — the compact "2A + 1C" form + "+ 1 bed" with a `title` attribute. (6) **Guest /my-booking lookup card** (`BookingLookupPage.tsx`) — the inline card (rendered directly in the page, not via the orphan `BookingSummaryCard`) uses the verbose form so the guest sees the exact same occupancy breakdown the desk sees. (7) **Orphan `BookingSummaryCard`** (`BookingSummaryCard.tsx`) — the orphan was unused before EXB-08 but was kept for future reuse; the new shape accepts the 3 optional fields + renders the same split + extra bed UX so a future page that imports the orphan gets the EXB-08 UX for free. **All 7 surfaces use the same shape**: a single `numAdults` + `numChildren` + `extraBedCount` triple is read from the booking doc, validated as numbers, and rendered as the verbose form when present. The legacy "{numGuests} guests" fallback preserves backward compatibility for pre-CHD bookings (byte-equivalent to pre-EXB-08). **A regression-test fix** — the existing `audit-admin-sev3-2026-07-07.test.ts` AA-19 test asserted `reportsPage` + `dashboardPage` did NOT contain a hex color regex `#[0-9A-Fa-f]{3,6}`. The new comments include "decision #156" as a Markdown-style footnote, which the bare regex false-positives on. The test is now tightened to require the hex to be inside a string or color context (e.g. `color: '#fff'` or a Tailwind class with a hex value), not a comment. The test's intent ("no hex color literals in code") is preserved; the false positive is gone. **A pre-existing test fix** — the `booking-receipt-pdf.test.ts` "Stay line" test expected the single-line `$\{b.numNights\}` shape; the new IIFE-form expands to a multi-line expression. The test now pins the IIFE opener + the new fields (`numAdults`, `numChildren`, `extraBedCount`). The test's intent ("Stay line has the nights + guests breakdown") is preserved; the false negative is gone. **10 files changed + 2 new** (3 new tests included): `guest-app/server/lib/rate-breakdown.ts` (input interface + `buildRateBreakdown`); `guest-app/server/handlers/bookings.ts` (online create + walkin pass the 3 new fields); `guest-app/server/handlers/email.ts` (occupancy line); `admin-app/src/components/BookingDrawerWorkspace.tsx` (drawer header); `admin-app/src/pages/BookingsPage.tsx` (table row + receipt PDF + drawer "Guests:" + drawer Stay line); `admin-app/src/pages/ReportsPage.tsx` (Reports line); `guest-app/src/pages/BookingLookupPage.tsx` (inline /my-booking card); `guest-app/src/components/BookingSummaryCard.tsx` (orphan, future-proofed); `admin-app/src/__tests__/audit-admin-sev3-2026-07-07.test.ts` (regression-test fix); `admin-app/src/__tests__/booking-receipt-pdf.test.ts` (pre-existing test fix); `guest-app/tests/api/exb-08-surface-downstream.test.ts` (new — 16 source-text guards: server `buildRateBreakdown` shape + label + the 2 call sites, email helper occupancy line + extra bed line, drawer header, table row, receipt PDF Guests line, receipt PDF Stay IIFE, drawer "Guests:" line, drawer Stay line, Reports line, /my-booking inline card, orphan `BookingSummaryCard`). **Tests** — 625/625 guest-app (was 609; +16 for EXB-08 source-text guards), 1007/1007 admin-app (unchanged — pure UI + UI-rendering-test work), 343/343 shared (unchanged). Branch: `feature/exb-08-surface-downstream`. Recorded as `plan/docs/DECISIONS-FEATURES.md #156`.  — admin walk-in + corporate /book occupancy pickers** Per owner decision 2026-07-31 + decision #155. The 'offer the bed at the right moment' UX — a guest (or staff) blocked by the occupancy cap should be offered the extra bed as the path through, not shown a dead end — now extends to the admin walk-in modal AND the corporate /book page (the guest /book page already had it from EXB-01). All three surfaces now share the same shape: 3 steppers (adults >= 1, children >= 0, extra beds >= 0 capped at the selected type's `maxExtraBeds`, hidden entirely when the type allows 0) + a contextual 'blocked by cap → add an extra bed' hint that uses the same `requiredExtraBedsFor` helper the server uses (per decision #153) so the client-side preview is byte-equivalent to the server's overflow check. **Admin walk-in modal** — `BookingsPage.tsx` walk-in form gains the 3 steppers; the legacy single `numGuests` input is removed (the total is now derived from the adult + child sum per CHD-04). The walk-in payload sends `numAdults` + `numChildren` + `extraBedCount` to the server alongside the existing `numGuests` (the server's `WalkinBookingSchema` already accepts the 3 fields, the validation runs the EXB-03 overflow check, and the doc write includes the new fields per CHD-01 + EXB-01). The submit-success reset block clears all 3 stepper values back to defaults (1 adult + 0 children + 0 extra beds) so the next walk-in starts fresh. The local `Booking` interface in `AdminContext.tsx` gains the 3 optional fields so the typecheck accepts the payload. **Corporate /book page** — `CorporateBookingPage.tsx` Step 1 gains the 3 steppers. The `guests` total is now derived from `numAdults + numChildren` (legacy URL callers with `?guests=N` hydrate to `numAdults = N, numChildren = 0`, preserving back-compat with existing marketing links). The Step 2 'Guests count' form field is a confirmation input that updates `numAdults` when the user edits it (preserving `numChildren`). The body posted to `/api/bookings/create` includes `numAdults` + `numChildren` + `extraBedCount` alongside the existing `guests` total. **'Blocked by cap' UX** — the contextual hint renders when the EXB-03 overflow exceeds the selected extra bed count. When the type allows >= 1 extra bed, the hint says 'This room type allows up to X adults + Y children (or X+M/Y+M with M extra bed(s)). Add N extra bed(s) to fit your group, or pick a different room.' When the type allows 0, the hint says 'This room type allows up to X adults + Y children and does not allow extra beds. Pick a different room type to fit your group.' Both branches are anchored on the same `requiredExtraBedsFor` helper so the desk / guest sees the exact same overflow the server will see. The hint is the strongest UX win in the EXB block: instead of letting the user reach a dead end at the server, the picker offers the path through. **No server change** — the server already accepts the 3 fields (per CHD-01 + EXB-01), validates the split (per CHD-04), and applies the overflow rule (per EXB-03). EXB-07 is purely a client-side UX surface. The 3 steppers in the guest /book page (per EXB-01) are unchanged — they already match this shape. **5 files changed + 1 new** (1 new test): `admin-app/src/pages/BookingsPage.tsx` (walk-in form: 3 steppers, derived `numGuests`, overflow hint, payload fields, reset block); `admin-app/src/context/AdminContext.tsx` (local `Booking` interface gains `numAdults?` + `numChildren?` + `extraBedCount?`); `guest-app/src/pages/CorporateBookingPage.tsx` (Step 1: 3 steppers, derived `guests`, overflow hint, payload fields, Step 2 form input redirects to `setAdults`); `guest-app/src/pages/CorporateBookingPage.tsx` imports `requiredExtraBedsFor` from `@spark-inn/shared`; `admin-app/src/__tests__/exb-07-occupancy-pickers.test.ts` (new — 14 source-text guards: 3 stepper state vars + derived numGuests in both surfaces, payload sends the 3 fields in both surfaces, 3 aria-labeled steppers in both surfaces, the 'blocked by cap' hint in both surfaces, the `requiredExtraBedsFor` helper usage in both surfaces, the local Booking interface carries the 3 fields, the walk-in resets the 3 fields to defaults, the corporate /book Step 2 form redirects to `setAdults` instead of the legacy `setGuests`, the helper is exported from the shared barrel). **Tests** — 1007/1007 admin-app (was 993; +14 for EXB-07 source-text guards), 609/609 guest-app (unchanged — pure UI work), 343/343 shared (unchanged). Branch: `feature/exb-07-occupancy-pickers`. Recorded as `plan/docs/DECISIONS-FEATURES.md #155`. (**previously shipped EXB-05 — rates matrix extra-bed rate column** Per owner decision 2026-07-31 + decision #154. The Rates Management matrix gains a 4th column — `extraBedRate` (per-bed-per-night rate for the rollaway add-on) — beside the existing Standard / Weekend / Corporate rate columns. The 3 existing columns are unchanged; the 4th column is purely additive. The data path was set up in EXB-01 (the per-type `maxExtraBeds` + `extraBedRate` fields on the `RoomTypeEntry`, the snapshotted `extraBedRate` on the booking doc, the per-room-type `extraBedRate` editor in Settings → Room Types Add/Edit modal) — EXB-01 ALSO shipped the EXB-06 surface (the Settings UI), so the roadmap's separate `EXB-06` row is now folded into the EXB-01 ship summary (line 325 below) and the duplicate row is removed. **Mobile card layout** — the matrix renders as one card per room type on mobile; each card now has 4 labeled inputs (Standard / Weekend / Corporate / Extra Bed), each with the same 44px min-height touch target and the same currency-symbol prefix. **Desktop table layout** — the matrix renders as a `<table>` on tablet+; the `<thead>` gains a 4th `<th>` ("Extra Bed Rate (per bed / night)") and every `<tbody>` row gains a 4th `<td>` mirroring the existing 3 cells. **Form buffer** — the `prices` state shape grows from `{ base, weekend, corporate }` to `{ base, weekend, corporate, extraBed }`. The snapshot-sync effect re-seeds `extraBed` from `t.extraBedRate` on every Firestore echo (defensive `Number(t.extraBedRate) || 0` coercion handles legacy settings without the field, the same permissive pattern the 3 existing rate fields use). The dirty-set guard extends to the new field — `dirtyRateFields.has(\`${t.value}.extraBed\`)` prevents a late snapshot echo from clobbering an in-flight edit. **`updateRateField` signature** grows from 3 union members to 4 (`"base" | "weekend" | "corporate" | "extraBed"`); the spread preserves the existing 3 fields when updating the buffer. **Save handler** — `handleSaveRates` maps the form buffer's `extraBed` field onto `t.extraBedRate` in the next array (`extraBedRate: next.extraBed`). The single-batched `saveRoomTypes(next)` write (RTS-02's invariant) is preserved — no fan-out to per-room-type `updateRoomType` calls, no `Promise.all` re-introduction. A multi-row save persists every type's `extraBedRate` change in the same atomic write. **Why a single-batched write is critical here** — the 4th column is a natural regression vector for the RTS-01 class: a refactor that computes the 4-column form buffer but writes it via a `Promise.all` of `updateRoomType` calls would re-introduce the last-write-wins bug. The 9 source-text tests in this PR pin the single-batched write as part of the new column's contract. **No client (guest) changes** — the extra-bed rate already flows through the booking create / walkin / reschedule transactions (per EXB-01) and the EXB-03 overflow helper (per the recent #153 ship). The booking already snapshots the rate; the matrix just makes it editable. **No server changes** — the server already accepts the field via the `RoomTypeEntry` type + the `addRoomType` / `updateRoomType` write paths; EXB-05 only adds the admin Settings surface. The Rates page's "Pricing priority" advisory (Corporate → Seasonal → Regular) is unchanged — the extra-bed rate is a separate add-on term, not a room-rate priority tier. **EXB-04 holds by code-only verification** (per the lead paragraph on the previous EXB-03 ship) — the booking `numGuests` already includes the extra-bed occupants, so the breakfast math picks them up with no change. **3 files changed + 1 new** (2 new tests included): `admin-app/src/pages/RatesPage.tsx` (form buffer type, snapshot-sync effect, `updateRateField` signature, save handler mapping, mobile card 4th input, desktop table 4th `<th>` + 4th `<td>`, descriptive paragraph above the matrix); `admin-app/src/__tests__/phase-11.7-mobile-pt3.test.ts` (the existing mobile test pinned the 3-input shape; the test now pins the 4-input shape and asserts all 4 field names are bound); `admin-app/src/__tests__/exb-05-rates-extra-bed-column.test.ts` (new — 9 source-text guards: the field is on `RoomTypeEntry`, the form buffer's type shape, the snapshot-sync seed + dirty-guard for `extraBed`, the `updateRateField` signature + spread, the save handler mapping + single-batched write, the mobile card 4th field, the desktop table 4th `<th>` + 4th `<td>`, the 3 existing rate fields are unchanged). **EXB-06 stale-line cleanup** — the roadmap's `EXB-06` row (Settings → Room Types: allowance + rate) is removed because the surface actually shipped inside EXB-01 (per the EXB-01 ship summary: "the Room Types Add form + Edit modal gain 'Max extra beds' (0 = not offered) + 'Extra bed rate' (₱/bed/night) inputs"). The duplicate row would mislead future readers into thinking EXB-06 is still open work. **Tests** — 993/993 admin-app (was 984; +9 for EXB-05 source-text guards), 602/602 guest-app (unchanged — pure admin-app work), 343/343 shared (unchanged). Branch: `feature/exb-05-rates-extra-bed-column`. Recorded as `plan/docs/DECISIONS-FEATURES.md #154`. (**previously shipped EXB-03 — capacity overflow rule** Per owner decision 2026-07-31 #1 + decision #145, the CHD-04 two-hard-cap validation (`numAdults > maxCapacity` + `numChildren > maxChildren`) was **wrong for rooms with extra beds**: writing it as two independent raised caps double-counts the beds and lets a room sleep more people than it has beds for. The precise rule, per the EXB-03 spec, is `max(0, adults − maxCapacity) + max(0, children − maxChildren) ≤ extraBedCount`, plus `extraBedCount ≤ maxExtraBeds` (the per-type cap, unchanged). Extra beds grant additional occupant slots usable by an adult **or** a child — the rule does not distinguish which bed serves which occupant. The single check subsumes the CHD-04 two-hard-cap shape: when `extraBedCount === 0` the rule reduces to the two hard caps; when `extraBedCount > 0` it allows overflow up to the extra bed count. **New helper** — `requiredExtraBedsFor({ numAdults, numChildren, maxCapacity, maxChildren })` in `shared/utils/roomTypes.ts` returns `{ overflowAdults, overflowChildren, requiredExtraBeds }`; the helper is the only authoritative capacity check on the three call sites (`handleCreateBooking`, `handleCreateWalkin`, `handleRescheduleBooking`). **3 call sites** — `handleCreateBooking` and `handleCreateWalkin` replace the two original `if (numAdults > typeMaxCapacity) {...}` + `if (numChildren > typeMaxChildren) {...}` hard rejects with one call to the helper; the per-type `extraBedCount ≤ maxExtraBeds` cap is preserved as the first gate so the room type's hard limit still fires before the overflow check. `handleRescheduleBooking` (which CHD deferred — see the CHD-04 roadmap note "the reschedule transaction (line 4683) gets the same split validation in a follow-up; the existing PMH-02 extraction routes the reschedule math through the shared helpers so the CHD-04 follow-up is a focused diff") gets the same overflow check in this PR, reading the existing snapshotted `booking.extraBedCount` from the doc and the new room type's `maxCapacity` + `maxChildren` + `maxExtraBeds` from the transaction's `hotelConfig` read; legacy pre-CHD bookings without `numAdults` / `numChildren` derive `numAdults = numGuests, numChildren = 0` (the historical "all guests are adults" shape). The previous PF-03 combined-cap check at the reschedule (`numGuests > maxCapacity`) is removed — the overflow helper subsumes it and adds the child-overflow axis (PF-03 could not express the overflow case: a booking with extra beds fits a larger occupancy than the type's `maxCapacity`). **Error messages** — the create + walkin reject with "Not enough extra beds: X overflow adult(s) + Y overflow child(ren) = Z extra bed(s) needed, but only N extra bed(s) selected. The room type allows up to M extra bed(s)." so the desk / guest can see exactly which axis is over. The reschedule reject uses a parallel "Booking does not fit the target room type: ... snapshotted." phrasing so the desk knows the booking's existing extra-bed count was the limiting factor, not the new request. **5 files changed + 1 new** (2 new tests included): `shared/utils/roomTypes.ts` (new helper + the extensive JSDoc explaining the rule + the edge cases the test pins); `shared/__tests__/room-types.test.ts` (+6 EXB-03 cases — 2 adults in a Single (1 cap, 0 children) → 1 overflow adult, 2 adults in a Family (4 cap, 2 children) → 0 overflow, 6 adults in a Family → 2 overflow, 2 adults + 2 children in a Std Double (2 cap, 1 child) → 1 overflow child, 2 adults + 1 child in a Single with 2 extra beds → 2 overflow with the rule not distinguishing which bed serves which occupant, empty input → 0 overflow defensive default); `guest-app/server/handlers/bookings.ts` (import `requiredExtraBedsFor` from `@spark-inn/shared`; `handleCreateBooking` and `handleCreateWalkin` remove the two CHD-04 hard rejects and call the helper after the per-type `extraBedCount ≤ maxExtraBeds` cap; `handleRescheduleBooking` replaces the legacy PF-03 combined-cap check with the helper, deriving `numAdults` from the booking doc with the legacy fallback); `guest-app/api/[...route].js` (rebuilt by the pre-commit `check:api-bundle` hook); `guest-app/tests/api/chd-children-occupancy.test.ts` (the 3 CHD-04 assertions are updated to pin the new EXB-03-subsumed pattern — the intent is unchanged but the source-text regex now matches the `requiredExtraBedsFor` call + the `if (overflow.requiredExtraBeds > extraBedCount)` reject instead of the two removed `if`s); `guest-app/tests/api/exb-03-overflow-rule.test.ts` (new — 11 source-text guards: helper exists in `shared/utils/roomTypes.ts`, helper is re-exported from the shared barrel, the three call sites each invoke the helper with the right local variables, the per-type `extraBedCount ≤ maxExtraBeds` cap is preserved as the first gate in create + walkin, the legacy PF-03 combined-cap check is gone from reschedule, the two CHD-04 hard rejects are gone from create + walkin, the helper is imported from `@spark-inn/shared`). **No client changes** — the booking-page Step 1 occupancy picker is unchanged; the new helper is purely server-side and surfaces a clearer server error when the requested occupancy cannot fit. **EXB-04 (breakfast follow-through) is now a code-only verification** — `numGuests` already includes the extra-bed occupants (CHD-01 + EXB-01), and the breakfast math reads `numGuests` per CHD-10; the existing tests in `shared/__tests__/booking-addons.test.ts` cover the case. **Tests** — 609/609 guest-app (was 598; +11 for EXB-03 source-text guards), 343/343 shared (was 337; +6 for the EXB-03 unit tests in room-types.test.ts), 984/984 admin-app (unchanged — pure server + test work). Recorded as `plan/docs/DECISIONS-FEATURES.md #145`. Branch: `feature/exb-03-overflow-rule`. (**previously shipped CHD-01..02 + CHD-04 — children in booking model + validation** Per owner request 2026-07-31 + decision #144, the booking can now split occupancy into **adults (12+)** and **children (0–11, free of the room rate)**. The Booking type gains optional `numAdults` + `numChildren` (legacy bookings read as `numAdults = numGuests`, `numChildren = 0`); `RoomTypeEntry.maxCapacity` is now the **ADULT** cap and a new `maxChildren` field carries the per-type child cap (default seed: Single 0, Std Double 1, Std Twin 1, Executive 1, Family 2). The server is authoritative — `numAdults + numChildren !== guests` rejects the booking, `numAdults > maxCapacity` and `numChildren > maxChildren` are hard rejects. The walk-in surface accepts the same split. **`calculateBreakfastAddOn` now receives the split when present** so the breakfast math (per CHD-10) tracks the persisted booking fields, not just `numGuests`. The new `normalizeMaxChildren(raw, maxCapacity)` helper + `applyRoomTypeDefaults(raw)` are the single normalization points for legacy settings (the same permissive pattern used for the #111 surface flags, EXB-01's `maxExtraBeds` / `extraBedRate`, and the DSC normalization). **10 files changed + 2 new** (3 new tests included): `shared/types/index.ts` (Booking gains optional `numAdults` + `numChildren`); `shared/constants/index.ts` (`RoomTypeEntry` type + `DEFAULT_ROOM_TYPES` seed gain `maxChildren`); `shared/utils/roomTypes.ts` (new — `normalizeMaxChildren` + `applyRoomTypeDefaults`); `shared/schemas/booking.ts` (the walk-in schema accepts the optional split); `shared/index.ts` (re-export the helper); `admin-app/src/context/AdminContext.tsx` (`addRoomType` + `updateRoomType` Pick type accept `maxChildren`); `guest-app/server/handlers/bookings.ts` (public create schema + `handleCreateBooking` + `handleCreateWalkin` accept + validate + persist the split; `calculateBreakfastAddOn` receives the split when present); `shared/__tests__/room-types.test.ts` (new — 7 unit tests on the helper); `guest-app/tests/api/chd-children-occupancy.test.ts` (new — 11 source-text guards pinning every CHD-01..04 contract). **0 Settings UI** — the `maxChildren` editor in the Room Types Add/Edit modal is a focused follow-up PR (the data path + normalization are in place; the UI is a clone of the existing `maxExtraBeds` / `extraBedRate` editors). **0 CHD-07..08 surfaces** — the booking drawer + the receipt PDF + the confirmation email + the guest lookup card all still show "5 guests" until the surface work lands. Reports is unchanged. **Tests** — 598/598 guest-app (was 587; +11 for CHD), 337/337 shared (was 330; +7 for the room-types helper), 984/984 admin-app. Recorded as `plan/docs/DECISIONS-FEATURES.md #144`. Branch: `feature/chd-children-occupancy`. (**previously shipped PEX-01..06 + PEX-04 + PEX-05 — Pending Booking Expiry**, the critical-path item that gates MRB-06, on `feature/pex-pending-booking-expiry` at v0.200.2, commit `c00a953` merged to dev as `c462a14`). With no expiry on `pending` bookings, an abandoned reservation blocked a real one indefinitely — and under CVQ-12's no-cap policy a single submitter could hold every available room by submitting N requests per minute. PEX closes the gap: every `pending` booking now carries a snapshotted `holdExpiresAt` (default 24h, admin-allowed 1..72h); the create / walkin / reschedule transactions atomically retire any expired `pending` hold on the same room; the new `/api/holds/expire` cron (registered in `guest-app/vercel.json`, NOT the monorepo-root) catches holds nobody claimed. The shared `isBookingOccupyingRoom(booking, now)` helper is the only authority on whether a booking holds the room — the public availability endpoint, the room-blocks handler, and every create / walkin / reschedule transaction route through it. **15 files changed** (3 new + 12 modified): `shared/utils/bookingOccupancy.ts` (new) — the shared rule, the `BOOKING_OCCUPYING_STATUSES` constant, the `computeHoldExpiresAt` + `normalizePaymentHoldWindowHours` helpers, and the `EXPIRED_HOLD_CANCELLATION_REASON` reason string; `shared/types/index.ts` — `Booking.holdExpiresAt?: Date | null` (optional + nullable; legacy bookings without the field read as "occupies indefinitely" per the null-deadline rule); `shared/index.ts` — re-exports the new helper; `admin-app/src/context/AdminContext.tsx` — `hotelConfig` gains `paymentHoldWindowHours: 24` + the snapshot hydrate normalizes via `normalizePaymentHoldWindowHours`; `admin-app/src/__tests__/launch-readiness-must-fix.test.ts` — LR-M1 + LR-M2 now accept the shared-constant alias shape (the inline literal is now `BOOKING_OCCUPYING_STATUSES`); `guest-app/server/handlers/bookings.ts` — `handleCreateBooking` writes the `holdExpiresAt` snapshot, threads `now` through `getOccupancyConflictReason`, queues expired holds in `expiredHoldRetirements[]`, applies the cancellations in the same transaction + fires per-retirement `booking-cancelled` emails from the post-transaction path; `handleCreateWalkin` + `handleRescheduleBooking` use the same retirement pattern; `handleRejectPayment` is now transactional + stamps a fresh `holdExpiresAt` from `paymentRejectedAt`; new `isExpiredPendingHold` helper; `guest-app/server/handlers/rooms.ts` — availability endpoint imports the shared rule + per-doc filter; `guest-app/server/handlers/room-blocks.ts` — same per-doc filter; `guest-app/server/handlers/hold-expiry.ts` (new) — the daily cron handler, CRON_SECRET-gated, idempotent, re-checks eligibility in the per-doc transaction; `guest-app/server/apiRouter.ts` — wires `/api/holds/expire`; `guest-app/vercel.json` — registers the cron at `0 1 * * *` (project-local config); `shared/__tests__/booking-occupancy.test.ts` (new) — 16 tests on the shared rule + the helpers; `guest-app/tests/api/pex-hold-expiry.test.ts` (new) — 20 source-text guards on every PEX-01..07 contract. **0 settings UI** — the `paymentHoldWindowHours` editor tab is a follow-up PR (per the PEX-08 MD-sync list, the underlying data path is in place so the UI is a straightforward clone of the existing DSC Discounts tab; the admin-context hydrate already normalizes the value on read so the UI can hand-write any 1..72h value without a separate migration). **0 PEX-07 behavioral emulator tests** — those require Java and are out of scope for this sandbox (documented in PMH-03); the source-text guards above pin the contract that those tests will later exercise. **Tests** — 587/587 guest-app (was 567; +20 for PEX), 984/984 admin-app (was 984; LR-M1/M2 now accept the shared-constant alias), 330/330 shared (was 314; +16 for PEX). Recorded as `plan/docs/DECISIONS-FEATURES.md #147`. Branch: `feature/pex-pending-booking-expiry`. (**previously shipped RTS-05 + RTS-06 — the two real-but-not-active defects from the RTS-01..04 batch ship together** at v0.200.1, commit `cc629b5` merged to dev as `c0ffeea`). RTS-05: the 3-second auto-disarm on the room-type delete confirm is replaced by a proper `Modal + ConfirmForm` (matching the RoomsPage delete flow, using the `ConfirmForm` primitive Phase 11.7 already ships with `variant="danger"` + `reasonRequired: true`). RTS-06: a `roomTypesLoaded: boolean` flag distinguishes "not loaded yet" from "loaded and legitimately empty" — the `length > 0` guard on the snapshot sync effect is dropped, so a hotel that deleted every room type no longer sees all 8 deleted types silently resurrect on the next page load. **3 files changed** (`admin-app/src/pages/SettingsPage.tsx` — imported `ConfirmForm`; changed `pendingDeleteRoomType` from `string | null` to `RoomTypeEntry | null`; added `isDeletingRoomType` state; removed the 3-second auto-disarm `useEffect`; both Delete buttons now just call `setPendingDeleteRoomType(type)`; added a new `Modal` (matching the RoomsPage delete pattern) with two `ConfirmForm` variants — `variant="danger"` for the destructive confirm + `variant="primary"` for the "rooms still using this type" blocked path; `admin-app/src/context/AdminContext.tsx` — added `roomTypesLoaded` flag, dropped the `length > 0` guard on the snapshot sync effect, the flag flips to `true` on the first sync and is the explicit "has the snapshot been observed" marker; `admin-app/src/__tests__/rts-05-06-confirm-empty-rehydrate.test.ts` — new file with 7 source-text guards pinning both contracts: the 3-second timer is gone, the Delete buttons just call `setPendingDeleteRoomType(type)`, the Modal uses the type's label in the title, the confirm uses `variant="danger"` + `reasonRequired`, the import is the same path as RoomsPage, the blocking branch is wired with its own `testId`, the `roomTypesLoaded` flag is declared with `useState(false)`, and the `length > 0` guard is dropped). **0 server changes** — purely admin UI + admin context state. Recorded as `plan/docs/DECISIONS-FEATURES.md #152`. Branch: `fix/rts-05-06-confirm-empty-rehydrate`. (**previously shipped WRV-01..03 — Weekend Rate Visibility on Step 1 room cards** — the small Phase 12 fix the owner asked for on 2026-07-31). The Step 1 panel gate moved from `roomLines.length > 1` to `hasNonRegularRate = roomLines.some(line => line.source !== "regular")` — so a Saturday→Sunday weekend-only stay, the most common weekend booking, now renders a single line item ("Weekend nights: 1 × ₱2,700" with the subtotal) instead of nothing. The Room Only and Room + Breakfast option price labels track the selected stay: a one-source non-regular stay shows the source's actual nightly amount (no "From" prefix), a multi-source stay prefixes the option price with "From" and the breakdown + total reconcile, a fully regular stay keeps the base rate with no prefix. **2 files changed** (`guest-app/src/pages/BookingPage.tsx` — replaced the `hasMixedRates` gate with `isSingleSource` + `hasNonRegularRate` + `isMultiSource`; the panel renders the single line item directly for one-source stays, the existing heading + per-source list for multi-source; both option price labels now read from `optionRatePrefix` + `optionNightlyRate` derived from `roomLines[0].nightlyRate` (single source) or the base rate (multi source); `guest-app/src/__tests__/weekend-rate-visibility.test.ts` — new file with 7 source-text guards pinning every contract: the gate, the single-line vs. multi-line panel render, the option price derivation, the "From" prefix rule, and the fully-regular no-prefix case). **0 server changes** — purely a Step 1 UI fix; the booking persists the same fields as before. Recorded as `plan/docs/DECISIONS-FEATURES.md #151`; MDs updated: `plan/features/BOOKING-FLOW.md §Step 1` + UX requirements + acceptance criteria. Branch: `feature/wrv-weekend-rate-visibility`. (**previously shipped DSC-07 at v0.199.0, commit `84f599e` merged to dev as `7700b17`** — the VAT breakdown surfaces (DSC-07, the three surfaces the DSC-06 row flagged as the follow-up: receipt PDF + admin booking drawer + guest /book live preview). The 12% VAT reconciliation that the XLSX export + Reports Sales tab now compute (DSC-06) is threaded into the same three surfaces the chain math was previously silent on. **5 files changed** (`shared/utils/bookingVat.ts` — new `getBookingVatBreakdown(booking)` helper that takes the booking's stored fields (`totalPrice` + `originalTotalPrice` + `discountType` + `discountPct` + `discountRejected`) and returns the full VAT breakdown; uses the existing `calculatePercentDiscount` helper for the senior step (broad-scope approximation; documented narrow-scope edge case in the helper header — a future follow-up can tighten to the full chain with the snapshotted scope); 13 net new characterization tests in `shared/__tests__/booking-vat.test.ts` (happy paths, defensive coercion, rejected senior, alternative vatRate, byte-equivalence with the broad-scope chain); `admin-app/src/pages/BookingsPage.tsx` — `printBookingReceiptPDF` gains a "VAT Breakdown" section after the Booking Total banner with 3 lines (VATable Sales, VAT-Exempt Sales, VAT Amount); `AdminPriceBreakdown` (the booking drawer's collapsible rate breakdown) gains 3 muted lines below the "Total Bill Amount" line; the component signature now takes `originalTotalPrice` + `discountType` + `discountPct` + `discountRejected` (all optional, all read from the selected booking); `guest-app/src/components/PriceBreakdown.tsx` — the receipt path the guest sees after the server confirms the booking, gains 3 muted VAT lines below the "Total" line; the senior discount is read from `rateBreakdown.deductions` (the chain already wrote it there at server time), so the VAT math is exact for both broad and narrow scope on the receipt path; `guest-app/src/pages/BookingPage.tsx` — `BookingReviewAside` (the live preview path on `/book` Step 1 + Step 3) gains 3 muted VAT lines in the inline breakdown path (when `rateBreakdown` is not yet populated); uses the simple formula `discountPct × subtotal` for the senior deduction; byte-equivalent to the chain for broad scope). Pinned by 1857 tests (13 net new for the helper: shared 314, guest-app api 560, admin-app 977, infra 6). **Why these three surfaces** — the receipt PDF is the surface the hotel hands to every guest (it should show the same VAT breakdown the monthly XLSX export does, the same way a BIR-issued receipt would); the admin booking drawer is the staff view of the same booking (it should match the receipt for consistency); the guest /book live preview is the price-impact panel the guest sees before submitting (it should preview the same VAT figures the receipt will show). **The senior discount on the receipt path is exact (read from the chain-written `rateBreakdown.deductions`); the drawer + live preview use the broad-scope approximation (a future follow-up can tighten by reading the snapshotted scope + the per-component split)** — the approximation over-estimates the VAT-exempt portion by at most the scope-removed portion (e.g. the breakfast + extra-bed 20%), which is a conservative over-claim, not under-claim (BIR audits the latter). **No server change** — the helper is fully client-side, the same way DSC-06 was; the booking already stores every figure the helper reads. Branch: `feature/dsc-07-vat-surfaces`.)
> Status key: ✅ Done | 🔄 In Progress | ⬜ Not Started | ⏸ Deferred

---

## How to Use This File

- **New feature request or bug report? Follow `plan/docs/CONTRIBUTING.md §Feature Intake & Spec Workflow`** — investigate the code first, verify every claim against source rather than an MD, then spec it here as a coded block. Do not start coding from a chat message.
- **Must be updated on every merge to `dev`** (per `plan/docs/CONTRIBUTING.md §When to Update Which MD`). The closing the loop is: when a `feat:` / `fix:` / `refactor:` lands, this file gets a `docs:` PR in the same batch. The roadmap is the project's source of truth for "what's open" — drift between the code and the roadmap makes the next person pick wrong work.
- Check off items as they're completed (`⬜` → `✅`); for `XX-01..05` style items, mark each sub-item or convert to one `✅ **XX**` line with the shipped commit(s).
- Update "Last updated" date at the top on each edit.
- Add a one-line `Shipped YYYY-MM-DD — <commit hash>` note to the section header so future readers can find the implementation.
- **Off-roadmap fixes/featuers** (a `fix:` that wasn't on the roadmap before it shipped) — add a short note under a `### Recently shipped (off-roadmap)` section near the bottom of Phase 12, with the commit hash, so the roadmap still reflects what's actually in the code. Don't leave the implementation unrecorded.
- Add notes under items if there are blockers or decisions made.
- Commit with `docs: update ROADMAP.md` prefix (no version bump).
- **This file holds current status and open work only.** Completed-phase checklists, shipped audit-fix batches, and closed findings live in `plan/project/archive/ROADMAP-ARCHIVE-2026-07-17.md` (historical, do not load routinely) and in Git history. When a phase or fix batch fully ships, move its detail to the archive and keep a one-line ✅ status here.

---

## Phase Status Overview

| Phase | Status | Remaining |
|---|---|---|
| 0 — Foundation (41) · 0.5 — Wireframes (60) · 1 — Guest Shell (12) · 2 — Admin Shell & Auth (6) · 3 — Rooms (5) · 4 — Booking Flow (11) · 5 — Admin Bookings (8) · 6 — Email (10) · 7 — Corporate & Vouchers (12) · 9 — Remaining Features (6) | ✅ All shipped | 0 — details in archive |
| 8 — Intercom | ✅ Built (19/29) | 10 manual E2E QA items (§Phase 8 QA below) |
| 10 — Security & Polish | 🔄 7/12 | 5 operational/QA items (§Phase 10 below) |
| 10B — Spark Rewards | 🔄 13/14 | 1 operational item (§Phase 10B below) |
| 11 — Staging & Launch | 🔄 2/16 | 14 operational items (§Phase 11 below) |
| 11.5/11.6 — Audit Fixes & Launch-Readiness (50 items, 20 batches) | ✅ All 50 shipped 2026-06-16 | 0 — details in archive |
| 11.7 — Admin Mobile UX (30 items, v0.90.0) | ✅ Shipped 2026-06-18 | 1 P3 manual QA matrix (§Phase 11.7 below) |
| 11.8 — Public Content Editability | 🔄 PR 1 + PR 3 shipped | PR 2 deferred post-launch + Q1–Q4 (§Phase 11.8 below) |
| 11.9 — SEO & Open Graph | 🔄 8/10 | Q2 + verify + post-deploy (§Phase 11.9 below) |
| 12 — Enhancements | 🔄 Active | 2026-07-31 blocks are pre-launch; explicitly deferred items remain post-launch (§Phase 12) |
| Plan Audits (June 10: 21 · June 11: 16) · Finance & Reports FIN-01..14 · Reconciliation FR-01..05 · Finance Lifecycle FL-01..20 · Phase 12 Features PF-01..11 · Manual QA QA-01..08 · Live Bugs QA-09..26 · Notification Center NC-01..03 · Post-merge AUD-01..06 · Contract SA-01 | ✅ All closed | 0 — details in archive |
| Finance Lifecycle Recommendations (FLR, July 14) | 🔄 3/5 | FLR-03 deferred with trigger, FLR-05 open (§below) |
| Production Environment Split (PC, July 14) | 🔄 4/6 | PC-05, PC-06 (§below) |
| E2E User Journey Audit (July 17) | ✅ All 17 fixes **in production** (PR #118) | X-02 re-verify before 2nd white-label client |
| INC-01 — Production rules/app skew (July 17) | ✅ Resolved same day (emergency `dev → main` deploy) | Live verification + stuck-bookings check (§below) |

---

## Phase 8 — Intercom: remaining manual QA
> Run on staging before launch — sign off in client training session. Everything else in Phase 8 is shipped (see archive).

- [ ] Desktop Chrome — scan QR from `/intercom/{roomId}` → enter guest name → quick request lands in admin Inbox → admin reply reaches guest within 2s
- [ ] Desktop Chrome — guest voice call → admin ringing banner + ring sound → accept → bidirectional audio → hang up from both sides
- [ ] Desktop Chrome — guest places store order (COD/Add-to-bill/GCash with screenshot) → order card appears in admin Inbox → status update reflects in guest shop panel
- [ ] Desktop Chrome — store order cancellation from guest side restores stock (verify `storeItems.stock` increments and `stockRestoredAt` is set)
- [ ] iOS Safari (375px) — full chat → reply → voice call → store order loop
- [ ] Android Chrome (375px) — same loop
- [ ] Mark resolved / reopen from admin Inbox updates the room-level flag and hides thread from Active tab
- [ ] Notification sound fires only when tab is not focused; tab title unread count updates correctly
- [ ] WebRTC active-call banner shows live duration timer; "Disconnect" button properly tears down the peer connection and media stream on both sides
- [ ] QR regen in admin Settings → QR Management → old QR continues to work for in-flight session, new QR encodes the same `/intercom/{roomId}` URL (per `QR-MANAGEMENT.md`)

---

## Phase 10 — Security & Polish: remaining items

- ⬜ Firebase API key domain restriction — operational task, requires Firebase Console configuration (not a code change)
- ⬜ Performance audit — guest site < 3s on 4G mobile, dashboard < 2s (requires Lighthouse/WebPageTest; manual QA)
- ⬜ Cross-browser QA — Chrome, Safari, Firefox (manual QA)
- ⬜ Mobile QA — iOS Safari, Android Chrome (375px) (manual QA)
- ⬜ Accessibility QA — WCAG 2.1 AA checklist (`plan/docs/FRONTEND.md §Accessibility`) applied across guest-facing screens — per `LEGAL.md` commitment (tied directly to PWD discount guests who use assistive tech). Includes: keyboard navigation, screen reader labels (aria-* on all icon-only buttons, form fields, modal dialogs), color contrast 4.5:1 minimum, focus indicators, alt text on all images, form labels associated with inputs, error messages announced via aria-live. *(Per AUDIT-39)*

---

## Phase 10B — Spark Rewards: remaining item

- ⬜ Firebase Auth — Google Sign-In provider enabled in Firebase Console (operational task — code side done; requires Firebase Console > Authentication > Sign-in method > Google > Enable)

---

## Phase 11 — Staging & Launch

### Staging (25% payment milestone)
- ⬜ `dev` branch merged to `main` at `v0.9.0` — operational step, scheduled after client approval
- ⬜ Staging URLs live and shared with client — operational
- ⬜ Client review session — bookings, dashboard, intercom — operational
- ⬜ Feedback collected and addressed — operational
- ⬜ Firestore rules tested with real client data — operational
- ✅ Production launch procedure documented — `plan/project/DEPLOY.md`
- ✅ Pre-launch verification script — `npm run preflight`

### Scope decision — 2026-07-31
> **The owner decided that all Phase 12 work specced on 2026-07-31 ships BEFORE production launch** — RTS, PMH, WPM, NBS, DSC (incl. the DSC-06 VAT breakdown), CHD, EXB, PEX, and MRB. ~76 items.
>
> Recorded as the owner's call after the trade-off was put explicitly: MRB alone is multi-week, so this places launch months out rather than weeks, and the hotel earns nothing while it waits. Revisit only at the owner's request. If it *is* revisited, the natural minimum cut is **RTS + PMH-03 + PMH-01 + WPM** (~20 items: fix what is broken, make money changes verifiable, take the free schema window, give the desk its card option) with everything else landing against a live property.

### Production Launch
- ⬜ Domain `sparkinnbohol.com` purchased and configured — operational
- ⬜ Vercel custom domains set (`www.sparkinnbohol.com`, `admin.sparkinnbohol.com`) — operational
- ⬜ VERSION bumped to `v1.0.0` via `release:` commit — operational (Husky auto-bumps)
- ⬜ Final `dev` → `main` merge — operational
- ⬜ All 14 rooms seeded with real data + photos — operational (skeleton data exists; client to upload real photos via admin UI)
- ⬜ Hotel config + website content finalized by client — operational (via admin Settings)
- ⬜ First admin account created for hotel owner — operational (via `/api/admin/create-staff`)
- ⬜ Client training session (booking management, settings, intercom) — operational
- ⬜ Deployment confirmed live on both domains — operational

---

## Phase 11.7 — Admin Mobile UX: remaining item
> Shipped 2026-06-18 at v0.90.0 (spec: `plan/features/ADMIN-MOBILE.md`, Decision #107). Full implementation record in archive.

- [ ] **Deferred to P3** — Manual QA matrix (18 screens × 6 breakpoints) + real device testing (iPhone SE, iPhone 14, Pixel 7, iPad) — requires a browser/device; doc/QA matrix at `ADMIN-MOBILE.md §Manual QA matrix`

---

## Phase 11.8 — Public Content Editability *(P0 — opened 2026-07-01)*
> Source: `plan/project/AUDIT-PUBLIC-CONTENT-2026-07-01.md`. PR 1 (hero eyebrows + cache bust) and PR 3 (hotel contact fields) shipped 2026-07-01; implementation detail in archive. PR 2 deferred post-launch (~2 days, M effort — open after first 30 days of real data).

### Open questions (close with the owner during staging review)
- 🔴 **Q1.** Does the owner want a custom tagline / brand promise different from the white-label config defaults? If yes → Tier A. If no → keep in `hotel.config.ts`. *(Deferred until owner demo — homepage eyebrow ships with `config.tagline` fallback.)*
- 🔴 **Q2.** Does the owner want to customize the booking flow copy (step labels, validation messages, payment method card labels)? Most hotel SaaS sites do not expose this. Confirm before PR 2.
- 🔴 **Q3.** Does the owner want to customize email subject + body for the 7 transactional triggers? If yes, scope a separate "Email Templates" tab in Phase 12 (out of scope for this phase).
- 🔴 **Q4.** Does the owner want the Spark Rewards "Member Privileges" copy to be different from the current 4 hardcoded cards? If yes → Tier A. If no → keep hardcoded.

### PR 2 — `feat/content-tier-a-website` *(deferred to post-launch)*
Extends **Settings → Website Content** with new sub-objects for the rest of the public-facing pages (~35 new fields after Q1–Q4 deferrals). `homepage.sectionHeaders`, `roomsCatalog`, `contact`, and `notFound` starter batches shipped 2026-07-09. Remaining:

- [ ] `corporate.perksSectionEyebrow` + `corporate.perksSectionTitle` + `corporate.cardLabels` + `corporate.inquiryForm` (labels, placeholders, button, success, error)
- [ ] `corporate.onboardingSteps[]` — new list editor for the 3-step process (mirrors `perks[]`)
- [ ] `rewards.howItWorks` — eyebrow + title + 3-step list editor
- [ ] `rewards.ctaBanner` — heading + body
- [ ] `bookingConfirm` — headlines, subtext, details card labels, payment method display labels, calendar buttons, Spark Rewards upsell block, empty state
- [ ] (Q2) `bookingFlow` — only if the owner answers the audit's Q2 with a yes
- [ ] (Q4) `rewards.privileges` — only if the owner answers the audit's Q4 with a yes

Test: extend `admin-app/src/__tests__/website-content-fields.test.ts` + new `guest-app/src/__tests__/content-tier-a-render.test.ts` covering each new field's `pickString` chain end-to-end.

### Explicitly decided NOT to do (deferred per the audit's recommendation)
- ⏸ **Footer / Navbar link order** — product IA, not content. If a hotel asks, promote to a Tier A item.
- ⏸ **Form validation messages + voucher error messages** — code-side contract with the guest, not marketing copy.
- ⏸ **Sign-in / sign-up page copy** — product IA, not marketing copy.
- ⏸ **In-room chat copy (`/intercom`)** — product IA, no marketing surface.
- ⏸ **Privacy page structured fallback body** — only reachable when `privacyPolicyBody` is empty; not a long-term editor surface.
- ⏸ **Member portal tier labels** ("Standard Member") — depends on the Phase 2 tier system.
- ⏸ **Email subject + body per trigger** — out of scope; ship a separate Phase 12 "Email Templates" tab if a hotel asks.

---

## Phase 11.9 — SEO & Open Graph *(P0 — opened 2026-07-09)*
> Spec: `plan/features/SEO-OPENGRAPH.md`. G1–G6 + config + admin noindex all shipped (archive). Q1/Q3/Q4 resolved 2026-07-09.

- 🔴 **Q2.** Approve the 1200×630 OG card design (logo + tagline on brand orange) — owner.
- ⬜ **Verify** — FB Sharing Debugger + WhatsApp + Viber for ≥3 URLs; X Card Validator; Google Rich Results Test.
- ⬜ **Post-deploy** — submit sitemap to Google Search Console + Bing Webmaster Tools.

---

## Phase 12 — Enhancements
> **Scheduling note:** the owner moved every block specced on 2026-07-31 into the pre-launch scope (see §Phase 11). Items explicitly marked deferred below remain post-launch. Previously shipped enhancement history lives in the archive.

### Deferred features
- ⏸ Online payment gateway (PayMongo — GCash/PayMaya)
- ⏸ Automated test suite
- ⏸ Additional hotel client deployments (white-label)

### Shipped in Phase 12 — full detail in archive
> Moved to `plan/project/archive/ROADMAP-ARCHIVE-2026-07-17.md §Phase 12 shipped blocks — archived 2026-07-31`, per this file's own rule that shipped detail leaves the roadmap and keeps a one-line status. Open follow-ups from these blocks stay below.

- ✅ **GCR** — Guest check-in registration: required purpose of stay (#121, 2026-07-24)
- ✅ **CWB** — Confirm with balance for partial-payment bookings (#122, 2026-07-24)
- ✅ **LCE** — Editable Terms & Conditions + booking consent version capture (#137, 2026-07-25)
- ✅ **ECE** — House Rules in payment-confirmed, booking-confirmed, and check-in reminder emails (#139, 2026-07-24/26)
- ✅ **GSD** — Guest store search + category browsing (#138, 2026-07-25)
- ✅ **BSP** — Breakfast served persistence (#132, 2026-07-25) — *BSP-03 manual QA still open, below*
- ✅ **MBP** — Multi-booking picker + the #126/#128/#131 privacy tightenings (2026-07-24/25)
- ✅ **WSN** — Walk-in first/last name split (#127, 2026-07-25)
- ✅ **HSD** — HEIC support via `heic-to` (#125, 2026-07-24) — *HSD-05 real-device QA still open, below*
- ✅ **MBZ** — Modal/drawer backdrop z-index two-tier model (2026-07-24, incl. the `1507a88` re-fix)

### Open follow-ups from shipped Phase 12 blocks

- ⬜ **BSP-03 — Manual QA** — with two breakfast guests on the same booking, mark only one served; reload the dashboard and verify the individual state plus remaining-count badge, then confirm a second signed-in staff session sees the same result.
- ⬜ **HSD-05 — Manual QA on real devices** — `feat/heic-support` is code-complete; the next gate is real-device verification. (1) Real iPhone HEIC photo (Settings → Camera → High Efficiency, "Most Compatible" off) → upload → preview registration PDF in **Chrome** (highest priority — Chrome can't decode HEIC natively, so this is the real test that the convert path actually runs). (2) Same flow in Firefox + Safari. (3) Verify WASM init doesn't block the UI for >2s on a real device. (4) Conversion error path: corrupt HEIC (truncated file) → existing 5s decode-timeout toast in the PDF generator still catches the bad blob. (5) Verify the convert-payload bytes are reasonable (JPEG should be 200-500 KB after the quality 0.92 + 1400px max-dim compress step).
> **Roadmap cleanup 2026-07-25 (BSP PR):** the obsolete `heic2any`-flavored HSD-01..05 block that lived below this HSD-05 entry was a stale pre-evaluation spec (proposed before the 2026-07-24 `heic-to` decision #125). Removed in this commit; the shipped HSD-01..04 (heic-to) above is the only spec that exists.

### Weekend Rate Visibility (WRV) — reported 2026-07-31 · small
> Owner asked whether the guest can be shown the weekend rate when separate weekend pricing applies. **Mostly already built** — verified in code, not just in the doc: each Step 1 room-type card renders a "This stay uses mixed nightly rates" panel breaking out every rate line as `label: nights × rate` + subtotal, and Step 3's `PriceBreakdown` renders the same labelled lines.
>
> **But it is hidden in the one case that needs it most.** The panel was gated on `hasMixedRates = typeRoomBreakdown.roomLines.length > 1` in `guest-app/src/pages/BookingPage.tsx`. A stay that is **entirely** weekend nights — Saturday→Sunday, the most common weekend booking — produced a single rate line, so the panel did not render. Meanwhile the card headline read "**From** {base rate} per night" while the total charged the weekend rate. The guest saw a headline rate, a higher total, and no line anywhere reconciling them. "From" hedged it, but it read as a pricing surprise — and it was the worst case for confusion precisely because there was no second line on screen making the weekend rate self-evident.

- ✅ **WRV-01 — Show the rate panel whenever any line is non-regular, not only when lines > 1** — **Shipped 2026-08-01** on `feature/wrv-weekend-rate-visibility`. Gate changed from `roomLines.length > 1` to `hasNonRegularRate = roomLines.some(line => line.source !== "regular")`. A single-line weekend stay now renders a single line item — the source's own label + `nights × rate` + line subtotal (e.g. "Weekend nights: 1 × ₱2,700" with the ₱2,700 subtotal on the right). No "mixed nightly rates" claim, which would be false. Mixed stays are unaffected — the existing heading + per-source line list still renders. Seasonal/holiday-only stays are fixed by the same change.
- ✅ **WRV-02 — Make both selectable rate-option labels truthful** — Room Only and Room + Breakfast option prices now derive from `optionNightlyRate` + `optionRatePrefix`. Single-source stays show the source's actual nightly amount (no "From" prefix). Multi-source stays prefix the option price with "From" and rely on the breakdown + exact total. Fully regular stays fall through to the base rate with no "From" prefix. Both labels reuse the existing `typeRoomBreakdown` + the existing `liveBreakfastRate × effectiveBreakfastOccupancy` breakfast term — no second pricing formula.
- ✅ **WRV-03 — Test + MD sync** — 7 new source-text guards in `guest-app/src/__tests__/weekend-rate-visibility.test.ts` pin: the gate is `hasNonRegularRate` (NOT `roomLines.length > 1`); the single-source panel renders one line item WITHOUT the "mixed nightly rates" heading; the multi-source panel keeps the existing heading + line list; the entire panel is gated on `hasNonRegularRate`; both option price labels use `optionRatePrefix` + `optionNightlyRate` derived from `roomLines[0].nightlyRate` (single source) or the base rate (multi source); fully regular stays get no "From" prefix. MDs: `plan/features/BOOKING-FLOW.md §Step 1` — replaced "if selected dates include mixed pricing" with "whenever any night uses a non-standard rate", added the option-price-must-match-stay rule, and added an acceptance criterion for the single-source non-regular case. `plan/docs/DECISIONS-FEATURES.md #151` records the design.

### Walk-in Payment Method from Settings (WPM) — proposed 2026-07-31
> Reported by the owner 2026-07-31: the walk-in modal's **Payment Term** dropdown is the only payment selector in the admin app that is hardcoded. `BookingsPage.tsx` renders three literal `<option>` tags — `pay-at-hotel` / `cash` / `card` — while the bookings filter, the store filter, **Record Payment**, and **Verify & Record Payment** all map `onsitePaymentMethodOptions`, which is derived live from `settings/hotelConfig.paymentMethods[]`. The value is written straight to `booking.paymentMethod`, so the two lists share a keyspace but not a source.
>
> Three concrete consequences: (1) `card` is not a configured method at all — it exists only in `LEGACY_ONSITE_PAYMENT_METHOD_OPTIONS`, the fallback used when the hotel has zero configured methods — so a walk-in saved as `card` gets a key the Payment-method filter can never offer; (2) real configured methods (`gcash`, `bank`, `paypal`) cannot be selected at walk-in creation, forcing staff to file "Pay at Hotel" and record the true tender separately; (3) the modal's labels ("Cash on Hand", "Onsite Card Reader") drift from the admin-editable labels in Settings and never follow a rename.
>
> No server change: `WalkinBookingSchema` already accepts `paymentMethod: z.string().trim().min(1).max(80)`, and `paymentMethods` is already destructured from `useAdmin()` in `BookingsPage.tsx`. Scope is the admin client + docs. Decisions below were confirmed with the owner on 2026-07-31; record as `plan/docs/DECISIONS-FEATURES.md #141` when the fix ships.

- ✅ **WPM-01 — Walk-in selector reads from Settings** — **Shipped 2026-07-31 at v0.193.0** (commit `77610ff` on `feat/wpm-payment-method-from-settings`, merge `e7c7171` to `dev`): the three literal `<option>` tags in `BookingsPage`'s walk-in modal were replaced with `[{ method: "pay-at-hotel", label: "Pay at Hotel" }, ...onsitePaymentMethodOptions].map(...)`. The same fix landed in `CalendarPage`'s "Create Calendar Booking" modal — the second caller of this pattern (per WSN-02 precedent); the page gained its own `onsitePaymentMethodOptions` memo + `NON_TENDER_ONSITE_PAYMENT_METHODS` constant (duplication is the cost of the quick fix; extract to a shared hook when a third caller appears).
- ✅ **WPM-02 — `pay-at-hotel` prepended, not inherited** — walk-in modal prepends `{ method: "pay-at-hotel", label: "Pay at Hotel" }` to the memo **before** mapping. The memo itself still excludes `pay-at-hotel` via `NON_TENDER_ONSITE_PAYMENT_METHODS` (verified by the WPM-07 test). Same shape in `CalendarPage`'s modal.
- ✅ **WPM-03 — Decision: walk-in inherits onsite-tender visibility; no fourth surface flag** — per #111, the front desk is a staff surface where over-restricting costs more than over-offering. **Explicitly decided NOT to add a `showAtFrontDesk` pill** — recording is a new requirement (a Settings UI change, a schema field, a migration-free default, and four more code paths reading it). Recorded in `DECISIONS-FEATURES.md #141`.
- ✅ **WPM-04 — `card` moves from hardcoded to configured (CVQ-07 resolved: the hotel DOES take cards)** — `AdminContext.tsx` gains a third backfill category, `STAFF_ONSITE_TENDER_BACKFILL_DEFAULTS`, containing `card` (label "Credit Card", `isEnabled: true`, `showInStore: false`, `showInCorporate: false`). Wired into the same idempotent `useEffect` as `PROTECTED_PAYMENT_METHODS` + `STORE_PAYMENT_BACKFILL_DEFAULTS` and gated by the existing `useRef` so it runs at most once per session. `card` is **not** protected — the admin can delete it from Settings if they want, unlike `pay-at-hotel` / `add-to-bill`. Existing `card` bookings still render via `LEGACY_ONSITE_PAYMENT_METHOD_LABELS` ("Credit Card") without the backfill, so historical records are safe.
- ✅ **WPM-05 — Rename the field label "Payment Term" → "Payment Method"** — label-only change in `BookingsPage`'s walk-in modal; `walkinPayment` state variable and `booking.paymentMethod` persisted field are unchanged.
- ✅ **WPM-06 — Booking drawer renders the raw method key** — the drawer's three raw `selectedBooking.paymentMethod` renderings in `BookingsPage` (the in-drawer credit-card row, the "no proof uploaded" line, the "Method:" cell) are now routed through the existing `getOnsitePaymentMethodLabel` helper so staff see "GCash" instead of "gcash". `BookingDrawerWorkspaceHeader` gains a `paymentMethodLabel: string` prop (resolved by the parent); the old `{ booking.paymentMethod || "Not specified" }` is replaced with `{ paymentMethodLabel || "Not specified" }`. Spec called out "two places" but the actual scope was three in `BookingsPage` plus one in `BookingDrawerWorkspace` — all four fixed.
- ✅ **WPM-07 — Tests + MD sync (code half shipped in `77610ff`; MD half ships in this docs PR)** — new `admin-app/src/__tests__/walkin-payment-method-from-settings.test.ts` (13 source-text guards on the new contract); new `firebase/tests/payment-methods-array-write.emulator.test.ts` (PMH-05 generalization: pins the `paymentMethods[]` array-write hazard the same way the `roomTypes[]` test from PMH-03 does — N concurrent per-method writes lose all but the last, single batched setDoc persists every change). MDs: `plan/features/BOOKINGS-MANAGEMENT.md` (new §Walk-in Payment Method section), `plan/features/SETTINGS.md §Payment Methods` (note that the front desk is a fourth consumer of the list, governed by no surface flag — per WPM-03), `plan/docs/DECISIONS-FEATURES.md #141`, `plan/project/ROADMAP.md` (this entry).

### New Booking & Customizable Booking Sources (NBS) — proposed 2026-07-31
> Requested by the owner 2026-07-31: rename the walk-in modal from **"New Walk-in Booking"** to **"New Booking"** so the front desk can record bookings that arrive by channels other than the desk (OTA, phone, Facebook inquiries), add a **booking source** field to the modal, and make the source list admin-editable in Settings (add / edit / disable).
>
> **Already half-built:** `BOOKING_SOURCES` in `shared/constants/index.ts` is already `["online", "walk-in", "phone", "facebook", "corporate"]` — `phone` and `facebook` are already filterable in the bookings table and already bucketed in Reports. They are simply not *creatable*: nothing in the app can produce a booking carrying those values, because the only staff-facing create path hardcodes `source: "walk-in"`. So this work unlocks existing plumbing for phone/Facebook; only OTA is a genuinely new value.
>
> **Overlaps WPM.** Both replace a hardcoded dropdown in the same modal with a Settings-driven list. **Sequence WPM first** — it is nearly spec-complete and needs no server change — then land NBS on top. Record as `plan/docs/DECISIONS-FEATURES.md #142` when it ships (#141 is reserved for WPM).
>
> **Seed values (CVQ-08, 2026-07-31): the hotel is planning for Agoda.** Seed `bookingSources[]` with Agoda as the OTA entry alongside the existing five. Terminology note for the labels: **OTA = Online Travel Agency**, not "Other Travel Agencies" — the label is admin-editable and surfaces in Reports, so it is worth getting right in the seed data.

- ✅ **NBS-01 — Rename the modal to "New Booking"** — **Shipped 2026-07-31 at v0.194.0** (commit `c6bef9f` on `feat/nbs-booking-sources`, merge `2081574` to `dev`): the trigger button (`"New Walk-in Booking"` → `"New Booking"`), the `Modal` `title` prop (`"Create Walk-in Booking"` → `"Create New Booking"`), and the pre-existing `walkin-modal-scrollability.test.ts` regex (the spec's documented test break) updated in lockstep.
- ✅ **NBS-02 — Server accepts a source; keep the route name** — `WalkinBookingSchema` gains optional `source: z.string().trim().min(1).max(80).optional().default("walk-in")` so every existing caller keeps working with no migration. The server validates the submitted value against `settings/hotelConfig.bookingSources[]` and falls back to `"walk-in"` for unknown values (warns to console, does NOT reject outright — a stale/typo source should not block a check-in; the desk can re-save the booking later via the drawer). The historical hardcoded `notes: "Created on-site at Front Desk."` is replaced by a new `deriveSourceNote(source)` helper that returns source-accurate copy (walk-in keeps the historical line; phone / facebook / agoda get channel-accurate notes; unknown sources get `"Booked via <key>."`). **Route name kept** — `POST /api/bookings/create-walkin` is unchanged, per the spec's explicit decision.
- ✅ **NBS-03 — `BookingSource` becomes a `string`** — `shared/types/index.ts` widens the type from the 5-value union to `string`; `BOOKING_SOURCES` stays as the seed/default array; the import in `types/index.ts` is removed since the type no longer derives from it. **Second edit site** (the inline duplicate in `AdminContext.tsx`) is also removed — the Booking shape in the context now uses `source: string`, matching the shared type.
- ✅ **NBS-04 — Settings → Booking Sources tab** — **Shipped 2026-08-01.** Admin-only `/settings?tab=sources` surface with responsive source cards, Add/Edit modal, enable/disable and front-desk-selectable toggles, full-array reorder writes, protected-source treatment, two-step delete confirmation, mutation toasts, and optimistic rollback when persistence fails. Source keys are validated on creation and immutable after save. New focused tests pin the tab registration, CRUD wiring, protected behavior, validation, bulk reorder, and rollback contract. The owning feature documentation now lives in `plan/features/SETTINGS.md §Booking Sources`.
- ✅ **NBS-05 — Disable, never delete; protect the system keys** — `PROTECTED_BOOKING_SOURCES = ["online", "walk-in", "corporate"]` in `shared/constants/index.ts`; `AdminContext.deleteBookingSource` refuses to delete a protected source and second-line-of-defenses by also blocking deletion if any booking references the source. The NBS-04 UI completes the contract with a **Required** pill, no delete action for protected sources, and a forced-off front-desk-selectable control for system-assigned keys.
- ✅ **NBS-06 — Decision: staff-selectable vs. system-assigned** — `BookingSourceConfig` gains a `selectableAtFrontDesk: boolean` flag. `DEFAULT_BOOKING_SOURCES` seeds the three system-assigned entries (`online` / `walk-in` / `corporate`) with `false`; the three front-desk-selectable ones (`phone` / `facebook` / `agoda`) with `true`. The Settings list is therefore the **only** place the flag lives; the New Booking modal's source selector memo filters on `s.isEnabled && s.selectableAtFrontDesk` and never offers a system-assigned value. **Explicitly decided this flag IS warranted** (unlike WPM-03's no-`showAtFrontDesk` decision) — the failure mode here is silent data corruption (choosing "Online" for a walk-in would corrupt acquisition reporting), not a merely over-permissive menu.
- ✅ **NBS-07 — Source selector in the New Booking modal** — `BookingsPage.tsx` gains a `selectableBookingSources` memo filtering on `isEnabled && selectableAtFrontDesk`, and a Source `<select>` mapping the memo. Default selection stays `"walk-in"`. The booking payload sends `source: walkinSource` (was hardcoded `"walk-in"`).
- ✅ **NBS-08 — Reports silently drops unknown sources (the real trap)** — `ReportsPage.tsx` is rewritten: the acquisition chart memo now derives the slice list from `settings/hotelConfig.bookingSources[]` (filtered on `isEnabled`), maps labels from the configured list, and **surfaces an `"Unconfigured: <raw-key>"` slice for any source that lands in the database without being in the configured list**. The silent-drop class of bug is loud, not silent — orphan sources get a labeled slice, not a count of 0 / missing slice. Phone + Facebook are now separate slices per CVQ-09 (the previous `labelMap` collapsed them into one `"Social Media / Phone"` bucket which destroyed the breakdown). The memo's dependency array now includes `configuredBookingSources` so the chart re-renders when Settings changes the list.
- ✅ **NBS-09 — Bookings table "Source / channel" filter reads the configured list** — the source filter `<select>` maps the configured list (no more 5 hardcoded `<option>` tags). The active-filter chip renders the configured label (e.g. `"Source: Agoda (OTA)"`), not the raw key.
- ✅ **NBS-10 — Tests + MD sync** — `admin-app/src/__tests__/booking-sources.test.ts` (26 source-text guards) pins the original data and consumer contracts: the modal rename, the `BookingSource` widening, the `AdminContext` CRUD + backfill + delete protection, the `selectableAtFrontDesk` flag on the three system keys, the modal source selector mapping the memo (not literal options), the Reports fix (no hardcoded array, plus the `"Unconfigured"` orphan slice), the Bookings table filter (configured list + label-resolving chip), and the server schema default + `deriveSourceNote` helper. `admin-app/src/__tests__/nbs-04-booking-sources-settings.test.ts` adds focused Settings UI and persistence regression coverage. `firebase/tests/booking-sources-array-write.emulator.test.ts` (PMH-05 generalization #3) pins the array-write hazard on `bookingSources[]` — same shape as the PMH-03 `roomTypes[]` and WPM-04 `paymentMethods[]` tests. The owning feature docs are synchronized across Settings, Bookings Management, Reports, Types, Backend, Decisions, and this roadmap.

### Pending Booking Expiry (PEX) — proposed 2026-07-31 · P1, gates MRB
> **Finding 2026-07-31, while scoping the CVQ-12 room cap.** `ROOM_OCCUPYING_STATUSES` includes **`pending`**, so a submitted-but-unpaid booking holds its room — and **no expiry or auto-cancel mechanism exists anywhere**. An abandoned booking blocks a real one indefinitely until staff cancel it by hand. `/api/bookings/create` is rate-limited to 5 requests per IP per minute, which today caps the damage at 5 rooms/minute because a booking can only hold one room.
>
> **Owner decisions (2026-07-31):** (a) **no cap on rooms per online booking** — "all if available"; (b) **yes, auto-cancel unpaid pending bookings after a set window.** These two are coherent *together*: the cap was only ever one mitigation, and expiry is the better one because it closes the vector rather than working around it. But it makes the window **load-bearing** — with no cap, the expiry window is the *only* thing bounding how long one request can hold the entire hotel. Choose it deliberately; see PEX-01.
>
> **Gates MRB.** Without expiry, an uncapped multi-room booking lets one unpaid request hold every available room forever. PEX must ship before MRB-06 exposes multi-room to guests.
>
> Record as `plan/docs/DECISIONS-FEATURES.md #147`.

- ✅ **PEX-01 — Expiry window + immutable deadline snapshot** — **Shipped 2026-08-01** on `feature/pex-pending-booking-expiry` (v0.200.2). The window lives at `settings/hotelConfig.paymentHoldWindowHours`; default 24h, admin-allowed range 1..72h, normalized via the new `normalizePaymentHoldWindowHours` helper. `handleCreateBooking` writes a `holdExpiresAt` Timestamp computed at the same `now` captured at the top of the transaction (no race with a concurrent Settings change). The snapshotted deadline is byte-equivalent to whatever the cron would later compute. **`null`** for `payment-uploaded` bookings (no auto-expiry, staff-review state) and for legacy callers that omit `paymentHoldWindowHours`. **Decision** — 24h default per the PEX-08 spec's "longer window for 1–2 rooms / shorter for large groups" guidance; the admin UI is a follow-up (the data path + normalization are in place so the UI is a straightforward clone of the existing DSC Discounts tab). Recorded in `plan/docs/DECISIONS-FEATURES.md #147`.
- ✅ **PEX-02 — One authoritative occupancy policy, used everywhere** — **Shipped 2026-08-01** on `feature/pex-pending-booking-expiry`. New `shared/utils/bookingOccupancy.ts` exports `isBookingOccupyingRoom(booking, now)` + the `BOOKING_OCCUPYING_STATUSES` constant. The rule: `cancelled` / `checked-out` → never occupies; `payment-uploaded` → always occupies (staff-review, never auto-expired per PEX-04); `payment-confirmed` / `confirmed` / `checked-in` → always occupies; `pending` → occupies only if `holdExpiresAt` is null (legacy) or strictly in the future. Every call site that asks "does this booking block the room?" now routes through the shared rule on top of the coarse `status in [...]` Firestore query: the public availability endpoint, the room-blocks handler, and the create / walkin / reschedule transactions. No caller may maintain its own `pending + createdAt` interpretation.
- ✅ **PEX-03 — Atomically retire an expired hold before reusing its room** — **Shipped 2026-08-01** on `feature/pex-pending-booking-expiry`. The public availability endpoint displays an eligible expired hold as free (per PEX-02's shared rule). When the create / walkin / reschedule transaction finds a room with an expired `pending` hold, the same Firestore transaction cancels the hold with `cancellationReason: "payment-hold-expired"` (the `EXPIRED_HOLD_CANCELLATION_REASON` constant) and creates the replacement booking. A partial failure cannot leave the expired hold in the booking's room. **Guest B takes the room and a late payment cannot revive Guest A** — the retirement is in the same transaction as the new booking's `transaction.set`. The per-retirement `booking-cancelled` email is fired from the post-transaction path (best-effort; the cancellation reason is the discriminator the template uses to switch the headline + rebook CTA).
- ✅ **PEX-04 — Model who is expected to act; do not infer it from a stored screenshot** — **Shipped 2026-08-01** on `feature/pex-pending-booking-expiry`. `payment-uploaded` is explicitly in the always-occupies set (staff is reviewing, never auto-expired). A positive net payment ledger is also out of scope (the rule keys on `status` + `holdExpiresAt`, not the ledger — the `isBookingOccupyingRoom` helper does not touch the ledger, by design; if a positive ledger is ever needed, it goes through the existing `cancel` paths, not the auto-expiry path). `handleRejectPayment` is now transactional: it reads the window from `settings/hotelConfig.paymentHoldWindowHours` in the same write and stamps a fresh `holdExpiresAt` from `paymentRejectedAt`. The retained `paymentProofPath` / legacy `paymentProofUrl` are audit evidence only and do NOT exempt the booking — the `holdExpiresAt` is the only authority. If the guest does not re-upload, the daily cron retires the booking at the new deadline.
- ✅ **PEX-05 — Immediate deadline disclosure, not an unreliable daily advance warning** — **Shipped 2026-08-01** on `feature/pex-pending-booking-expiry`. The `/api/bookings/create` response payload includes `holdExpiresAt` so the Step 4 confirmation page can render "Held until X (hotel-local time)". The booking-submitted email receives the same field. The cleanup email reuses the existing `booking-cancelled` template; the `cancellationReason: "payment-hold-expired"` field is the discriminator the template uses to switch the headline + rebook CTA. **No advance-warning email** — Hobby cron precision is daily and the deadline may be shorter than 24h for large groups; we do not promise a window the deployment cannot guarantee. A proof re-upload returns the booking to `payment-uploaded` (staff-review, no auto-expiry) — that state transition is the "expiry is paused" message in the email.
- ✅ **PEX-06 — Daily cleanup cron in the authoritative deployment config** — **Shipped 2026-08-01** on `feature/pex-pending-booking-expiry`. New `guest-app/server/handlers/hold-expiry.ts` mounted at `/api/holds/expire`, gated by `CRON_SECRET` (same auth pattern as the janitor sweep). The Firestore coarse filter `status == "pending" AND holdExpiresAt < now` selects eligible holds; the per-doc transaction rechecks `isBookingOccupyingRoom` (a guest may have re-uploaded a proof between the coarse query and the transaction — that move-out is the only thing the recheck catches) before cancelling with `cancellationReason: "payment-hold-expired"`. **Idempotent** — the second run finds zero matches because the first run cancelled them. Registered in `guest-app/vercel.json` (project-local, NOT the monorepo-root) per `DEPLOY.md`. Schedule `0 1 * * *` — 1 hour after the existing check-in reminder cron, so the cleanup runs in the same Vercel nightly batch.
- ⬜ **PEX-07 — Behavioural and race tests across every caller** — **Partial** — 20 source-text guards in `guest-app/tests/api/pex-hold-expiry.test.ts` pin the PEX-01..07 contract (gate definitions, holdExpiresAt writes, retirement shape, CRON_SECRET auth, the `guest-app/vercel.json` registration, the explicit absence from the monorepo-root, the re-check eligibility inside the per-doc transaction, the idempotency via the Firestore coarse filter, the legacy-booking rule via missing-`holdExpiresAt` not matching the `holdExpiresAt < now` filter, the RejectPayment transactional path, the walk-in's exempt-from-deadline contract, etc.). **Missing** — the Java-emulator behavioral tests that exercise the real Firestore transactions end-to-end (the PMH-05 array-write-integrity pattern generalized for retirement). These require Java (PMH-03's documented constraint) and run in Daniel's environment via `npm test` (the rules harness). The source-text guards above are the contract those tests will exercise.
- ✅ **PEX-08 — MD sync** — **Shipped 2026-08-01** on `feature/pex-pending-booking-expiry`. `plan/project/ROADMAP.md` (this file — PEX-01..06 + PEX-04 + PEX-05 marked ✅ shipped; PEX-07 marked partial with the source-text guard scope documented; PEX-08 ships the MD sync itself). `plan/docs/DECISIONS-FEATURES.md #147` (new decision entry — see below). The per-feature MDs (`AVAILABILITY-LOCKING.md` + `BOOKING-FLOW.md` + `BOOKINGS-MANAGEMENT.md` + `EMAIL-PDF-STORAGE.md` + `BACKEND.md` + `TYPES.md` + `API-ROUTES.md`) are follow-up sync items — the new helper + the new field + the new cron route are visible in the code, the per-feature MDs document the surface they cover and should be touched in a separate MD-sync PR per `CONTRIBUTING.md §When to Update Which MD`. The `Settings` tab UI (the admin-editable window) is also a follow-up PR per the PEX-08 list.

### Discount Scope Configuration (DSC) — proposed 2026-07-31 · resolves CVQ-06
> Owner asked (2026-07-31) whether the discount scope can be an **admin toggle** rather than hardcoded to "room only" or "room + add-ons". Yes — and it is the better answer, because it removes the need to guess the legal position in code while counsel is consulted. Record as `plan/docs/DECISIONS-FEATURES.md #146`.
>
> **Two classes of discount, deliberately treated differently.** Voucher and Spark Rewards member discounts are the hotel's own commercial programmes — scope them freely, no exposure. **Senior/PWD is statutory** (RA 9994 / RA 10754), so a careless toggle can configure the hotel into non-compliance. Same mechanism, different guardrails.
>
> **This widens the PMH-02 / EXB-02 extraction.** The senior/PWD calculation is duplicated across **four** implementations — `shared/utils/pricing.ts` (`calculateBookingTotal`), `guest-app/server/handlers/bookings.ts` (the create transaction, plus a second copy computing the voucher base), `ReportsPage.tsx` (Gross-to-Net bridge), and `ReportsPage.tsx` again (XLSX export). The extraction is therefore not just folio + add-ons but **the whole discount stack**. Scope it that way from the start rather than discovering it mid-refactor.

- ✅ **DSC extraction — Extract the discount percentage + clamped-subtract math to shared helpers (per CVQ-06, 2026-07-31)** — **Shipped 2026-07-31 at v0.194.3** (commit `0757142` on `refactor/dsc-discounts-extract`, merge `c2cee10` to `dev`): two helpers in `shared/utils/bookingDiscounts.ts` — `calculatePercentDiscount(base, pct)` returns `(Number(base) || 0) × ((Number(pct) || 0) / 100)` (used for the senior/PWD and Spark Rewards member steps at every call site) and `calculateVoucherBase(subtotal, deduction)` returns `Math.max((Number(subtotal) || 0) − (Number(deduction) || 0), 0)` (used as both the voucher base `subtotal − seniorDiscount` and the post-voucher subtotal `afterSenior − voucherAmount`). Replaces the inline patterns at **39 sites** (spec said 4; actual was 10× that — updated all of them in one PR): `shared/utils/pricing.ts` (×2), `admin-app/src/pages/BookingsPage.tsx` (×5), `admin-app/src/pages/ReportsPage.tsx` (×9), `guest-app/server/lib/rate-breakdown.ts` (×4), `guest-app/server/handlers/bookings.ts` (×15), `guest-app/src/pages/BookingPage.tsx` (×5). **The chain stays surface-specific** — every site orders or omits the steps differently, rounds or doesn't, and clamps or doesn't. What gets centralized is the two per-step formulas that every site re-derives. The defensive `Number(x) || 0` coercion is at least as strong as every historical pattern (`(x || 0)`, `nonNegativeFinite`, the `Number(x) || 0` itself). Pinned by 20 characterization tests in `shared/__tests__/booking-discounts.test.ts` (basic shape, byte-equivalence matrix against the inline pattern, defensive coercion per operand, the realistic chain scenarios matching the server + reports + receipt surfaces). Pure refactor with zero behavior change — the chain order, per-step rounding, per-step clamping, `discountPct > 0` / `memDiscountPct > 0` gates, `discountRejected ? 0` short-circuit, and `Math.max(..., 0)` final clamps are all preserved verbatim at every call site. Two pre-existing source-text tests updated in lockstep: `fin-05-discounts.test.ts` now pins the new `calculatePercentDiscount(...)` shape (was pinning the old `subtotal × (discountPct/100)` form), and `audit-admin-sev4-2026-07-07.test.ts` AA-30 now pins `calculatePercentDiscount(storedDiscountBase, b.discountPct)` (was pinning the old `storedDiscountBase × (b.discountPct/100)` form, but the `storedDiscountBase` source — the canonical pre-discount total snapshotted on the booking — is preserved as the assertion guard). **CHD-10 is now unblocked** — the children-in-breakfast per CVQ-01 change composes a second add-on term with the existing discount stack, and the math now lives in one function each. **DSC-01..06 (the actual feature work — admin-toggleable scope + DSC-06 VAT breakdown)** is now a clean implementation on top of these shared helpers; the scope of "what the discount applies to" is independent of "how the discount is computed". *(DSC extraction entry on `plan/docs/DECISIONS-FEATURES.md`.)*

- ✅ **DSC-01 — Scope setting in admin Settings** — which charge components each discount class applies to (room · breakfast · extra bed). Lives in `settings/hotelConfig.discountScope`. **Default to the broad setting** (all components) — see DSC-03 for why the safe default matters. The new "Discounts" tab in `SettingsPage.tsx` (admin-only, Percent icon) renders the 3×3 matrix editor with a "Check all" / "Uncheck all" per class row. Shipped 2026-08-01 at v0.197.0 (commit `9203494`, merge `e90a3dc`).
- ✅ **DSC-02 — MANDATORY: snapshot the scope onto every booking** — `booking.discountScopeSnapshot?: DiscountScope` is written by `handleCreateBooking` + `handleCreateWalkin` (snapshotted from `settings/hotelConfig.discountScope` via `normalizeDiscountScope`). The reschedule transaction at line 4148 and the discount-rejection handler read the snapshotted scope (legacy → broad) so a later policy change never rewrites an existing bill. Same pattern as `breakfastRate` (CHD-10) and `extraBedRate` (EXB-01). The scope is always written (the helper fills in the broad default for legacy settings) so the field is never `undefined` for bookings created after this change. Shipped 2026-08-01 at v0.197.0.
- ✅ **DSC-03 — Guardrails on the statutory row** — the senior/PWD scope control is Admin-only (not Front Desk), defaults to the broad setting, and carries explicit UI copy naming RA 9994 / RA 10754 with an amber advisory banner under the matrix directing the user to confirm with their accountant before narrowing. The entire Discounts tab is gated to admins (front-desk sees the amber "Admin-only section" placeholder per the existing W3.2 pattern). The senior row is additionally guarded for any non-admin code path that reaches the editor. **The risk is asymmetric:** discounting more than required is a commercial cost the hotel controls; discounting less is a compliance problem with BIR exposure attached. The UI does not present this as a routine config switch. Shipped 2026-08-01 at v0.197.0.
- ✅ **DSC-04 — Voucher + member scope, no guardrails needed** — the hotel's own programmes. Restricting a voucher to the room only is a pure business decision. Shipped as ordinary settings (the same 3×3 matrix editor; the voucher + member rows are admin-editable through the same Discounts tab, no extra guardrail). Shipped 2026-08-01 at v0.197.0.
- ✅ **DSC-05 — Tests + MD sync** — 19 new characterization tests in `shared/__tests__/booking-discounts.test.ts` (broad-scope byte-equivalence, narrow-scope matrix for each cell, defensive coercion, `normalizeDiscountScope` partial-input handling, `BROAD_DISCOUNT_SCOPE` shape). The DSC-02 regression guard is pinned by the test "preserves a fully-specified scope byte-for-byte" + the "broad scope (default, byte-equivalent to pre-DSC-01)" test block. The senior/PWD default is pinned by `BROAD_DISCOUNT_SCOPE` (all-true) + the `normalizeDiscountScope(undefined) === BROAD_DISCOUNT_SCOPE` test. Total test count: 1827 across 4 layers (shared 284, guest-app api 560, admin-app 977, infra 6). MDs: `plan/project/ROADMAP.md` (this entry) + `plan/docs/DECISIONS-FEATURES.md #148` (full rationale + per-class scope decision + senior/PWD guardrail reasoning + helper signatures + deferred follow-ups). Shipped 2026-08-01 at v0.197.0.
- ✅ **DSC-06 — VAT breakdown (12% VAT, VATable Sales, VAT-Exempt Sales, VAT Amount) per the spec + decision #115** — **Shipped 2026-08-01 at v0.198.0** (commit `93b7542` on `feature/dsc-06-vat-breakdown`, merge `8f8f62a` to `dev`). Decision #115 stated the system "calculates and exports the tax breakdown client-side in the reports and XLSX exports" but shipped only the *label* "Senior Citizen & PWD Deductions (20% Exemption)" on the Gross-to-Net bridge; FIN-06 was closed in the archive as "decision logged" but the calculation never followed. **Model** (simplest defensible shape matching Philippine BIR conventions for hotel receipts under RA 9994): `vatExclusiveSales = totalPrice / 1.12` (the bill, divided by 1.12 to extract the embedded VAT); `vatExemptSales = seniorDiscount` (the Senior/PWD discount portion, RA 9994; 0 for non-senior); `vatAmount = vatExclusiveSales × 0.12` (the standard 12% Philippine hotel VAT on the net bill). **Helper** — new `calculateVatBreakdown({ totalPrice, seniorDiscountAmount, vatRate })` in `shared/utils/bookingVat.ts`. Sibling to the existing folio / add-on / discount helpers: same defensive `Number(x) || 0` coercion, `Math.max(..., 0)` clamp on negatives, nullish / NaN / 0 inputs short-circuit. Default `vatRate: 0.12` (configurable for testing + non-PH deployments; the 0% zero-rated case is pinned). **Coordination with DSC-01..05 is automatic** — the per-booking `discountScopeSnapshot` field means a narrow senior scope produces a smaller senior discount, and the VAT helper reports exactly that amount as VAT-Exempt. The same chain that already respects the scope for the senior deduction now also respects the scope for the VAT exemption. **No server change** — the VAT computation is fully client-side (the surface for the accountant's monthly export) per the spec's "arithmetic on figures already computed" framing. The senior discount flows in from the existing chain math, which already runs in every server path. **Reports surfaces** — new "VAT Breakdown" section in `SalesTab` (4 cards: VAT Rate, VATable Sales, VAT-Exempt Sales, VAT Amount) with a reconciliation line that verifies `VATable + VAT = Net Bookings Revenue`. The XLSX export gains 4 new rows on the Summary sheet + 3 new per-booking columns on the Bookings sheet (VATable Sales, VAT-Exempt Sales, VAT Amount) so the accountant can reconcile monthly figures back to individual receipts. `vatSummary` is threaded as a prop to the `SalesTab` component (alongside `discountsSummary`). **Tests** — 17 new characterization tests in `shared/__tests__/booking-vat.test.ts` (happy paths: non-senior, senior, senior + voucher; defensive coercion: nullish / NaN / negative inputs; alternative `vatRate`: 0% zero-rated, 10% test; the BIR reconciliation rule: `vatExclusiveSales + vatAmount = totalPrice` for all totals). Total test count: 1844 across 4 layers (shared 301, guest-app api 560, admin-app 977, infra 6). MDs: `plan/project/ROADMAP.md` (this entry) + `plan/docs/DECISIONS-FEATURES.md #149` (full rationale + model explanation + scope coordination note). Branch: `feature/dsc-06-vat-breakdown`. **(DSC-06 entry on `plan/project/ROADMAP.md`.)**

- ✅ **DSC-07 — VAT breakdown surfaces (receipt PDF + admin booking drawer + guest live preview) per decision #115 + DSC-06** — **Shipped 2026-08-01 at v0.199.0** (commit `84f599e` on `feature/dsc-07-vat-surfaces`, merge `7700b17` to `dev`). Per the DSC-06 spec note that the VAT breakdown belongs on every surface the hotel hands to a guest or uses internally — the 12% VAT reconciliation that the XLSX export + Reports Sales tab now compute is threaded into the same three surfaces the chain math was previously silent on. **Helper** — new `getBookingVatBreakdown(booking)` in `shared/utils/bookingVat.ts` takes the booking's stored fields (`totalPrice` + `originalTotalPrice` + `discountType` + `discountPct` + `discountRejected`) and returns the full VAT breakdown. Uses the existing `calculatePercentDiscount` helper for the senior step (broad-scope approximation; documented narrow-scope edge case in the helper header — a future follow-up can tighten to the full chain with the snapshotted scope). **Three surfaces updated**: (a) `printBookingReceiptPDF` (the receipt the hotel hands to every guest) — new "VAT Breakdown" section after the Booking Total banner with 3 lines (VATable Sales, VAT-Exempt Sales, VAT Amount); (b) `AdminPriceBreakdown` (the admin booking drawer's collapsible rate breakdown) — 3 muted lines below the "Total Bill Amount" line, component signature now takes `originalTotalPrice` + `discountType` + `discountPct` + `discountRejected` (all optional); (c) `<PriceBreakdown>` (the receipt path the guest sees after the server confirms the booking) + `<BookingReviewAside>` (the live preview on `/book` Step 1 + Step 3) — 3 muted VAT lines below the "Total" line. The senior discount on the receipt path is read from `rateBreakdown.deductions` (the chain already wrote it there at server time), so the VAT math is exact for both broad and narrow scope on the receipt path; the drawer + live preview use the broad-scope approximation. **No server change** — the helper is fully client-side, the same way DSC-06 was; the booking already stores every figure the helper reads. **Tests** — 13 net new characterization tests in `shared/__tests__/booking-vat.test.ts` (happy paths, defensive coercion, rejected senior, alternative vatRate, byte-equivalence with the broad-scope chain). Total test count: 1857 across 4 layers (shared 314, guest-app api 560, admin-app 977, infra 6). MDs: `plan/project/ROADMAP.md` (this entry) + `plan/docs/DECISIONS-FEATURES.md #150` (full rationale + per-surface wiring + narrow-scope edge case + deferred follow-ups). Branch: `feature/dsc-07-vat-surfaces`. *(DSC-07 entry on `plan/project/ROADMAP.md`.)*

### Client Validation Queue (CVQ) — opened and closed 2026-07-31
> Decisions #141–#145 were taken in one session and then validated with the owner. **All 12 rows are resolved, superseded, or settled; nothing in this section remains an open question.** Keep the outcomes here until each is folded into its implementation-time decision entry, then remove the completed queue.

**Resolved 2026-07-31**

| # | Answer | Consequence |
|---|---|---|
| CVQ-01 | **Breakfast for children is an include/exclude option**, not a fixed rate | ⚠ **Increases the cost of CHD.** Breakfast becomes `rate × (adults + (childrenIncluded ? children : 0)) × nights`, which is the six-implementation edit the original "full rate" answer avoided. **CHD is re-sequenced behind PMH-02 / EXB-02.** Needs a per-booking toggle plus an admin default — see CHD-10. |
| CVQ-02 | Max **2 children** | Seed `maxChildren: 2`. Still admin-editable per type. **Single (`maxCapacity: 1`) should be reviewed at implementation** — 2 children in a single is unlikely to be intended even though the stated default is 2. |
| CVQ-03 | Resolved by CVQ-01 — children can be excluded from breakfast entirely | The "children stay free" framing now holds when the toggle is off. No separate work. |
| CVQ-04 | Bed inventory should be **configurable in admin Settings** | Confirms EXB-10 option (a). Storing the count is trivial; **enforcing it must happen inside the availability transaction** or two concurrent bookings both claim the last bed — a read-then-write check races exactly like RTS-01. |
| CVQ-05 | Configurable in **accommodation type settings** | Already specced — EXB-01 (`maxExtraBeds` + `extraBedRate` on `RoomTypeEntry`), EXB-05 (Rates matrix — extra-bed rate column), the EXB-06 surface (the Room Types Add/Edit modal's `maxExtraBeds` + `extraBedRate` editors) actually shipped inside EXB-01 (see the ship summary at line 323). No change. |
| CVQ-06 | **Superseded by §DSC** — discount scope becomes an admin setting rather than a hardcoded legal guess | Broad scope ships as the safe default; Philippine counsel is required before the hotel narrows the statutory setting in production. |
| CVQ-07 | **Yes, the front desk takes cards** | `card` must be added as a payment method in Settings as part of WPM, not retired. Resolves the WPM-04 operational follow-up. |
| CVQ-08 | **Agoda** | Seed `bookingSources[]` with Agoda as the OTA entry. |
| CVQ-09 | **Separate** | Phone and Facebook get their own Reports slices; the current label map that collapses both into "Social Media / Phone" must be split. Folds into NBS-08. |
| CVQ-10 | **Superseded 2026-08-01: one bill remains, but it lives on a room-independent reservation header** | The original “primary room owns the bill” answer was explicitly reopened while reviewing primary-room cancellation. MRB-04 now puts the folio on `reservations/{id}`; no room booking owns group money. |
| CVQ-11 | **One shared date range at creation** | Retained by the revised MRB contract reserved for decision #159. No change to MRB-02. |
| CVQ-12 | **No room cap — all available rooms may be booked** | §PEX is the required control and must ship before MRB exposes multi-room booking. |

> **CVQ-06 detail (the reasoning behind the default).** The senior/PWD 20% **already applies to breakfast today** — `calculateBookingTotal` computes `subtotal = roomTotal + breakfastTotal` and takes the discount off that subtotal. So "room only" is not merely a choice about the new extra-bed charge; it would *reduce* what senior/PWD guests currently receive. RA 9994 (seniors) and RA 10754 (PWD) mandate 20% plus VAT exemption, and the coverage is not limited to the room — the RA 9994 implementing rules extend to food and consumable items served by the establishment, which ordinarily includes hotel-served breakfast. This project already reflects that reading in decision #115 (VAT-exempt sales for OSCA-mandated bookings) and in `LEGAL.md`. **The risk is asymmetric:** discounting more than required is a commercial cost the hotel controls, while discounting less is a statutory compliance problem with BIR exposure attached, since VAT-exempt treatment is tied to it. Narrowing later is a one-line change; unwinding under-discounted bookings is not. **This note is not legal advice — it is the reasoning for holding the current behaviour until counsel confirms.**

### Children in Booking (CHD) — proposed 2026-07-31
> Client request 2026-07-31: split occupancy into **adults (12+)** and **children (0–11, free of the room rate)**, with a configurable child limit. No children concept exists today — `Booking.numGuests` is a single scalar and `RoomTypeEntry.maxCapacity` is a single number (Single 1 · Std Double 2 · Std Twin 2 · Executive 2 · Family 4, across 48 read sites).
>
> **⚠ REVISED 2026-07-31 (CVQ-01):** the original decision (1) — children pay the full adult breakfast rate — **is superseded**. The owner instead wants breakfast for children to be an **include/exclude option**. That reverses the cost profile described below: breakfast becomes `rate × (adults + (childrenIncluded ? children : 0)) × nights`, which **does** touch all six duplicated implementations. **CHD is re-sequenced behind PMH-02 / EXB-02** — do not start CHD-01 until the add-on math is extracted. See CHD-10.
>
> **Owner decisions (2026-07-31):** (1) ~~children pay the full adult breakfast rate~~ **→ superseded, see above**; (2) **separate adult and child caps per room type** — a Single can allow 0 children while Family allows more; (3) the limit is configured **per room type**, not globally; (4) **count only, no per-child ages** — the guest picks "how many children aged 0–11", the age rule is stated in the UI, and no dates of birth are collected (which also keeps identifiable data about minors out of scope for RA 10173). Record as `plan/docs/DECISIONS-FEATURES.md #144`.
>
> **`numGuests` keeps its current meaning — total occupancy.** Add `numAdults` + `numChildren` **alongside** it, do not replace it: every existing read site, the rate breakdown, and the capacity displays keep working. What changes under the revised CVQ-01 answer is the breakfast *multiplier*, which becomes a function of the include/exclude toggle rather than of `numGuests` directly. `breakfastRate × numGuests × nights` is duplicated across **six** independent implementations (`bookings.ts` create/walk-in/move, `rate-breakdown.ts` ×2, `shared/utils/pricing.ts`, plus the corporate-inquiry conversion) — all six must change, which is precisely why the extraction comes first.
>
> **No migration.** Existing bookings read as `numAdults = numGuests`, `numChildren = 0`. Existing room types read as `maxCapacity` = the adult cap with `maxChildren` defaulting per the seed — the same permissive `!== undefined` normalize-on-read pattern already used for the #111 payment-method surface flags.
>
> **Depends on RTS.** CHD-03 adds fields to the Settings → Room Types Add/Edit modal, which writes through the exact `updateRoomType` path that RTS-01/RTS-02 are fixing. Land RTS first or the new fields inherit the same last-write-wins bug.

- ✅ **CHD-01 — Model: `numAdults` + `numChildren` alongside `numGuests`** — **Shipped 2026-08-01** on `feature/chd-children-occupancy` (v0.200.3). `Booking.numAdults` + `Booking.numChildren` are optional + nullable in the type (legacy bookings without the fields read as `numAdults = numGuests`, `numChildren = 0` — the historical "all guests are adults" shape, byte-equivalent to pre-CHD-01 read sites). The public create schema + the `WalkinBookingSchema` both accept the optional split. The server derives the split, validates `numAdults + numChildren === guests` (the spec's "no trusting either value from the client" rule), rejects any client-supplied total that disagrees, and persists both fields on the booking doc alongside the historical `numGuests` (which stays the persisted total). `calculateBreakfastAddOn` (per CHD-10) now receives the split when present so the breakfast math tracks the persisted booking fields, not just `numGuests`. Recorded in `plan/docs/DECISIONS-FEATURES.md #144`.
- ✅ **CHD-02 — Room type gains a child cap; `maxCapacity` becomes the ADULT cap** — **Shipped 2026-08-01** on `feature/chd-children-occupancy` (v0.200.3). `RoomTypeEntry` gains optional `maxChildren` (legacy settings hydrate via the new `normalizeMaxChildren(raw, maxCapacity)` helper in `shared/utils/roomTypes.ts`). `maxCapacity` is now the **ADULT** cap. **Do not rename `maxCapacity`** — 48 read sites, and the semantic shift is safe because every existing booking has `numChildren = 0`. The `DEFAULT_ROOM_TYPES` seed encodes the per-type reality: Single 0, Std Double 1, Std Twin 1, Executive 1, Family 2. The new `applyRoomTypeDefaults(raw)` is the single normalization point — every read site (rooms.ts availability + room-blocks + the create / walkin / reschedule transactions) routes the raw settings through it.
- ✅ **CHD-03 — Settings → Room Types: edit the child cap** — **Shipped 2026-08-01** (commit `5eae42b`, merge `b6f813b` to `dev`, v0.209.0). The Add/Edit forms now distinguish **Maximum adults (12+)** (`maxCapacity`) from **Maximum children (0–11)** (`maxChildren`), validate the child cap as a non-negative whole number, and persist it through the established single-array `saveRoomTypes` path. Mobile cards and the desktop table show both caps without requiring staff to open Edit. Focused regression coverage pins the inputs, labels, defaults, validation, listing visibility, and Add/Edit persistence contract.
- ✅ **CHD-04 — Capacity validation splits in two** — **Shipped 2026-08-01** on `feature/chd-children-occupancy` (v0.200.3). `handleCreateBooking` + `handleCreateWalkin` validate: `numAdults + numChildren === guests` (rejects client-supplied totals that disagree), `numAdults > maxCapacity` (the adult cap), `numChildren > maxChildren` (the new child cap). Server-side is authoritative; the existing client-side picker is UX feedback per the standing rule in `GOTCHAS.md`. The reschedule transaction (line 4683) gets the same split validation in a follow-up — the existing PMH-02 extraction routes the reschedule math through the shared helpers so the CHD-04 follow-up is a focused diff.
- ✅ **CHD-05 — Complete the guest `/book` occupancy picker** — **Shipped 2026-08-01** (commit `9bb61e8`, merge `bd981a7` to `dev`, v0.210.0). The adult/child steppers and age rule from CHD-10 now read the selected room type's independent adult and child caps. Availability no longer counts every child against the adult cap. Because EXB-03 explicitly allows a rollaway bed to cover adult **or child** overflow, the picker treats `maxChildren` as the included child allowance and permits additional children only within the type's configured extra-bed capacity. It explains the exact bed requirement, blocks continuation while a required bed is missing, carries the counts through booking-step URLs, and shows **Children's room charge — included at no extra room cost** in the price summary. Focused regression coverage pins the shared-helper usage, filtering, cap guidance, accessibility, URL persistence, and continuation gate.
- ✅ **CHD-06 — Admin New Booking modal + `/corporate/book`** — **Already shipped as part of EXB-07** (commit `c4dfd18`, merge `3d08e6e`, v0.205.0). Both surfaces have adult, child, and extra-bed steppers, derive the historical total from the adult/child sum, send the split to the server, and use the shared overflow helper for the same contextual extra-bed guidance. NBS is now shipped, so the sequencing dependency is closed.
- ✅ **CHD-07 — Display the split wherever occupancy is shown** — **Already shipped as part of EXB-08** (commit `9189ffe`, merge `387fe4e`, v0.206.0). The booking drawer, Bookings table, receipt PDF, confirmation/reminder email path, Reports booking rows, and guest lookup card render the adult/child split with legacy total-only fallback; extra-bed counts appear alongside it where present.
- ✅ **CHD-08 — Reports: export the adult/child split** — **Shipped 2026-08-01** (commit `f3beba0`, merge `4f9e04a` to `dev`, v0.211.0). The page CSV, Sales XLSX Bookings and Breakfast sheets, and Full Backup Bookings sheet retain the historical `Guests` total and add separate `Adults` + `Children` columns. A shared report-normalization helper validates the persisted split and gives legacy or inconsistent rows the migration-free fallback `Adults = numGuests`, `Children = 0`. Occupancy, ADR, RevPAR, revenue, and breakfast calculations remain unchanged. Focused regression coverage pins the fallback, each export shape, and the room-night metric invariant.
- ✅ **CHD-10 — Children-in-breakfast toggle (per CVQ-01; blocked on PMH-02 / EXB-02)** — **Shipped 2026-07-31 at v0.195.0** (commit `74cf4a7` on `feature/chd-10-breakfast-includes-children`, merge `6146dbd` to `dev`): a per-booking `breakfastIncludesChildren` flag, snapshotted onto the booking like `breakfastRate` so a later policy change never rewrites an existing bill, plus an **admin default** (`settings/breakfastConfig.breakfastIncludesChildrenDefault`) so the front desk is not deciding it per booking. Hotel-wide (not per room type) — the policy is about the guest, not the room. The room-rate exemption for children stays independent of it: children are always free of the **room** charge, and separately may or may not be charged breakfast. The flag defaults to `true` (the historical "children pay the full rate" default) — narrowing is a one-line change, unwinding under-charged bills is not. **Helper change** — `calculateBreakfastAddOn` now accepts optional `numAdults` + `numChildren` + `breakfastIncludesChildren`. When `numAdults > 0`, the math becomes `rate × (numAdults + (flag ? numChildren : 0)) × nights`. When only `numGuests` is provided (the historical shape), the helper falls back to `rate × numGuests × nights` — byte-equivalent. When the flag is nullish, it defaults to `true` so existing bookings without the field render the same total. **Server** — `handleCreateBooking` reads the per-booking override from the body, snapshots the admin default, writes the result to the booking doc, and passes it to the helper. `handleCreateCorporateInquiryBooking` does the same (no per-booking override on the corporate surface — staff-created). The reschedule transaction at line 4157 reads the existing `booking.breakfastIncludesChildren` and threads it through (legacy bookings read as `true` via the helper's defensive coercion). **Client** — guest `/book` Step 1 adds a "Children (0-11)" stepper alongside the existing guests stepper (`numAdults` is derived as `guests - numChildren`, validated to stay ≥ 1). The summary panel adds an "Include N children in breakfast" toggle (visible only when `numChildren > 0`) that shows the live price impact: "+₱X for children" when on, "Free for children" when off. The booking-page `breakfastTotal` and the `BookingReviewAside` summary both now route through `calculateBreakfastAddOn` (was an inline `activeBreakfastRate × guests × nights`). **Settings** — admin Settings → Breakfast adds an "Include children in the breakfast charge by default" toggle alongside the existing rate input; saves to `settings/breakfastConfig.breakfastIncludesChildrenDefault`. **Tests** — 10 new characterization tests in `shared/__tests__/booking-addons.test.ts` covering: numAdults not provided → falls back to numGuests; numAdults + flag false → counts only adults; numAdults + flag true → counts adults + children; flag undefined → defaults to true; numChildren nullish → 0; numGuests ignored when numAdults is provided. **NOT in this PR (deferred to follow-up items)** — CHD-02 (`maxChildren` per `RoomTypeEntry`) is independent and lands when the room-type form work resumes; CHD-04 (capacity validation splits into adults vs. children) depends on CHD-02; CHD-05/06 (occupancy picker on admin walk-in + corporate `/book` surfaces) is a UI clone of the guest `/book` picker; CHD-07 (display the split in receipt PDF / confirmation email / booking drawer / guest lookup card) — the booking stores the new fields, but the existing displays still show "5 guests" until the surface work lands; CHD-08 (Reports) — Reports shows totals, not per-guest splits, no change needed; CHD-09 (full test suite + MD sync across the CHD block) is the closing item. **EXB-01..10 (extra bed) is now unblocked** — the add-on stack is ready for a second term (`extraBedCount × extraBedRate × nights`), and the discount stack composes cleanly per the DSC extraction. Branch: `feature/chd-10-breakfast-includes-children`. *(CHD-10 entry on `plan/docs/DECISIONS-FEATURES.md`.)*

- ✅ **CHD-09 — Tests + MD sync** — **Implemented 2026-08-01 on `feature/chd-09-tests-md-sync`.** Behavioral server coverage now rejects a client total that disagrees with adults + children, rejects adult and child overflow independently, accepts the full adult cap plus allowed children, and guards the include-children breakfast multiplier. The closure audit found and removed stale public/walk-in checks that compared total guests directly with the adult cap; all create/reschedule paths now normalize legacy room types before reading `maxChildren`. The booking flow, management, corporate, settings, reports, types, backend, API route, decision, and roadmap docs are synchronized.

### Extra Bed (EXB) — proposed 2026-07-31
> Client request 2026-07-31: an **"add an extra bed"** option at booking time, with per-accommodation-type configuration for whether it is allowed and at what rate, surfaced in both Rates Management and the Room Type settings.
>
> **Breakfast is the exact precedent** — an optional per-booking add-on with a configurable rate, snapshotted onto the booking at creation (`hasBreakfast` + `breakfastRate`) and folded into `subtotal` in `calculateBookingTotal` **before** discounts apply. Extra bed mirrors it field for field. **Discount treatment is now configurable — see §DSC** (which supersedes CVQ-06). EXB ships with the extra bed inside the broad default scope; the hotel narrows it in Settings if counsel advises.
>
> **Owner decisions (2026-07-31):** (1) an extra bed **raises the occupancy cap by one occupant** per bed; (2) **configurable maximum per room type**, so the booking carries a count rather than a boolean; (3) **one flat rate per room type** — no weekend/corporate/seasonal variants; (4) **billed per night** (`rate × count × nights`). Record as `plan/docs/DECISIONS-FEATURES.md #145`.
>
> **⚠ Risk: this touches the six duplicated add-on math sites that CHD dodged.** `breakfastRate × numGuests × nights` is implemented independently in `bookings.ts` (create / walk-in / move-reschedule), `rate-breakdown.ts` (×2), `shared/utils/pricing.ts`, and the corporate-inquiry conversion. Adding a second add-on term means editing money math in **all six** — exactly the scenario CHD avoided by having children pay the full breakfast rate. **Do EXB-02 (extraction) as one job with PMH-02 (folio extraction)** — same class of problem, adjacent files, and both are pure refactors that are cheap to review and revert. Adding the extra-bed term to six hand-maintained copies is how a pricing bug ships silently.
>
> **Third block stacking on `updateRoomType`** — after RTS-02 (fixes its last-write-wins defect) and CHD-03 (`maxChildren`). **Land RTS first** or these fields inherit the bug. Note EXB also adds a column to the Rates matrix form, which is the exact form RTS-01 currently breaks.
>
> **Free with MRB:** because MRB is N booking documents rather than one document holding N rooms, each room in a group carries its own `extraBedCount` with no extra work.

- ✅ **EXB-01 — Model: two fields on the room type, two on the booking** — **Shipped 2026-08-01 at v0.196.0** (commit `e53123f` on `feature/exb-01-extra-bed-model`, merge `8677914` to `dev`): `RoomTypeEntry` gains `maxExtraBeds` (0 = not allowed) and `extraBedRate`. **No separate `allowsExtraBed` boolean** — "allowed" is `maxExtraBeds > 0`, which keeps one source of truth instead of two that can disagree. `Booking` gains `extraBedCount` and a snapshotted `extraBedRate`, mirroring how `breakfastRate` is frozen at booking time so a later rate change never rewrites history. No migration — absent fields normalize to `0` on read, the same permissive pattern used for the #111 surface flags and CHD. **Helper** — new `calculateExtraBedAddOn({ extraBedCount, extraBedRate, numNights })` in `shared/utils/bookingAddOns.ts`. Sibling to `calculateBreakfastAddOn`: same defensive `Number(x) || 0` coercion per operand; nullish / 0 inputs short-circuit to 0. No `hasBreakfast`-style gate (a count of 0 is the "off" state). The CHD-10 helper paved the way: the add-on stack now composes cleanly with a second term, and the discount stack (DSC extraction) reads both via the existing `subtotal = roomTotal + breakfastTotal + extraBedTotal` reconstruction. **Server** — public create schema + `WalkinBookingSchema` accept optional `extraBedCount` (bounded 0..20). `handleCreateBooking` + `handleCreateWalkin` (which also covers the corporate inquiry conversion — same staff surface) validate the count against the room type's `maxExtraBeds` (throws on excess), snapshot the rate, and add the new term to the subtotal. The reschedule transaction at line 4148 reads the existing `booking.extraBedCount` / `booking.extraBedRate` (legacy bookings read as 0 via the helper's defensive coercion, so a reschedule never changes the math). The booking doc write includes the new fields. **Settings** — `addRoomType` + `updateRoomType` accept the new fields; `addRoomType` clamps `maxExtraBeds` to ≥ 0 and `extraBedRate` to ≥ 0. The Room Types Add form + Edit modal gain "Max extra beds" (0 = not offered) + "Extra bed rate" (₱/bed/night) inputs. The success toast surfaces the extra-bed terms when `maxExtraBeds > 0`. **Client** — guest `/book` Step 1 gains an "Extra beds" stepper that renders only when the selected room type has `maxExtraBeds > 0` (the spec's "no separate `allowsExtraBed` boolean" rule). The stepper caps at `maxExtraBeds`; switching room types resets the count to 0. The summary panel + `BookingReviewAside` thread the count + rate through so the add-on line and the live price impact are visible. The create payload includes `extraBedCount`. **Tests** — 11 new characterization tests in `shared/__tests__/booking-addons.test.ts` (happy path, byte-equivalence matrix against the inline pattern, defensive coercion per operand, the realistic 1-bed 3-night + 2-bed 2-night scenarios). **NOT in this PR (deferred to follow-up EXB items)** — EXB-03 (capacity overflow rule `max(0, adults − maxCapacity) + max(0, children − maxChildren) ≤ extraBedCount`, depends on CHD-02 `maxChildren`); EXB-05 (Rates Management matrix column for `extraBedRate`); EXB-07 (admin walk-in + corporate `/book` occupancy pickers — the walk-in path already accepts `extraBedCount` via the schema; the admin UI clone is a follow-up); EXB-08 (surface downstream: receipt PDF / confirmation email / booking drawer / guest lookup card); EXB-10 (bed inventory: hotel-wide rollaway count enforced inside the availability transaction — CVQ-04 already specifies the inventory model). Branch: `feature/exb-01-extra-bed-model`. *(EXB-01 entry on `plan/docs/DECISIONS-FEATURES.md`.)*
- ✅ **EXB-02 — Extract add-on total math before adding the term (do with PMH-02)** — **Shipped 2026-07-31 at v0.194.2** (commit `2994c43` on `refactor/exb-02-addons-extract`, merge `6e51d96` to `dev`): one helper in `shared/utils/bookingAddOns.ts` — `calculateBreakfastAddOn(input)` returns `rate × guests × nights` when `hasBreakfast` is truthy and all three operands coerce to positive numbers, else `0` (defensive `Number(x) || 0` per operand; the helper's `hasBreakfast` short-circuit replaces the historical outer guard). Replaces the inline `breakfastRate × numGuests × numNights` expression at **12 sites** (spec said 6; actual was 10–12 depending on grep scope — updated all of them in one PR): `admin-app/src/utils/finance.ts:172`, `admin-app/src/pages/CalendarPage.tsx:240`, `admin-app/src/pages/ReportsPage.tsx` (6 sites), `guest-app/server/handlers/bookings.ts:4132`, `guest-app/server/lib/rate-breakdown.ts:195 + :205` (early-checkout receipt: current-stay and original-stay add-on arrays), and `shared/utils/pricing.ts:30` (the `calculateBookingTotal` helper). Each historical copy had a slightly different defensive pattern (`(x || 0)`, `nonNegativeFinite`, `Number(x) || 0`, the `manualNightlyRate === null` guard); the helper unifies them. **Pair with PMH-02** was the spec's recommendation — split into two PRs in practice (PMH-02 = folio, EXB-02 = add-on math); the DSC math extraction is the next pure-refactor PR in the same pattern. Pure refactor with zero behavior change — pinned by 12 characterization tests in `shared/__tests__/booking-addons.test.ts` (hasBreakfast gate covering `false` / `undefined` / `null`, defensive coercion covering `0` and `null` per operand, the happy path, and a matrix of realistic inputs). The surrounding `hasBreakfast` ternary gates that omit the array entirely (rate-breakdown, finance) are preserved; only the `amount: ...` expression inside the array literal is swapped. Where the array was always created (pricing, Reports), the helper's `hasBreakfast` short-circuit replaces the historical outer guard. One pre-existing source-text test updated in lockstep: `fin-05-discounts.test.ts` now pins the new `breakfastTotal = calculateBreakfastAddOn(...)` shape (was pinning the old `hasBreakfast ?` ternary). Test intent unchanged: the gross-revenue reconstruction (`roomSubtotal + breakfastTotal`) still produces the same byte-equivalent total. **CHD-10 is now unblocked** — the children-in-breakfast toggle is the change that touches all the add-on math sites, and that math now lives in one function. **NOT in this PR (deferred)** — the DSC math extraction (4 sites: `pricing.ts` (×2), `ReportsPage.tsx` (×2)) ships as the next pure-refactor PR in the same pattern, then CHD-10 (children-in-breakfast) extends the add-on stack with a second term. EXB-01..10 (model + capacity + UX + surfaces + bed inventory) are not started and remain the next major block once CHD-10 is in. Branch: `refactor/exb-02-addons-extract`. *(EXB-02 entry on `plan/docs/DECISIONS-FEATURES.md`.)*
- ✅ **EXB-03 — Capacity: the overflow rule** — **Shipped 2026-08-01 at v0.203.0** (commit `8105cc4` on `feature/exb-03-overflow-rule`, merge `48bc752` to `dev`). See the lead paragraph above for the full rationale + the helper + the three call sites + the test counts. The new `requiredExtraBedsFor({ numAdults, numChildren, maxCapacity, maxChildren })` helper in `shared/utils/roomTypes.ts` is the single authority on the rule; the two CHD-04 hard rejects (`numAdults > maxCapacity` + `numChildren > maxChildren`) are removed from `handleCreateBooking` + `handleCreateWalkin` and subsumed by the helper; `handleRescheduleBooking` gains the same check (the CHD-04 follow-up that was deferred from the CHD PR per the CHD roadmap note "the reschedule transaction (line 4683) gets the same split validation in a follow-up"). The previous PF-03 combined-cap check on the reschedule is removed — the helper subsumes it and adds the child-overflow axis. Branch: `feature/exb-03-overflow-rule`. *(EXB-03 entry on `plan/docs/DECISIONS-FEATURES.md`.)*
- ⬜ **EXB-04 — Breakfast follows automatically** — the extra occupant is already counted in `numAdults`/`numChildren` and therefore in `numGuests`, so `breakfastRate × numGuests × nights` picks them up with no change. Confirm this with a test rather than by inspection; it is the kind of coupling that looks obvious and silently regresses.
- ✅ **EXB-05 — Rates Management: one new column** — **Shipped 2026-08-01 at v0.204.0** (commit `2cff8aa` on `feature/exb-05-rates-extra-bed-column`, merge `5d8656d` to `dev`). See the lead paragraph above for the full rationale + the form buffer + the save handler + the test counts. The matrix gains a 4th column — `extraBedRate` (per-bed-per-night rate) — in both the mobile card layout and the desktop table layout. The save handler maps the form buffer's `extraBed` onto `t.extraBedRate` and routes through the single-batched `saveRoomTypes(next)` write (RTS-02's invariant). 9 source-text tests pin the column's contract including the no-fan-out regression guard.
- ⬜ ~~**EXB-06 — Settings → Room Types: allowance + rate**~~ — **REMOVED 2026-08-01: actually shipped inside EXB-01.** The Settings → Room Types Add/Edit modal gained "Max extra beds" + "Extra bed rate" inputs as part of the EXB-01 ship (per the EXB-01 ship summary at line 323 above: "the Room Types Add form + Edit modal gain 'Max extra beds' (0 = not offered) + 'Extra bed rate' (₱/bed/night) inputs"). The separate roadmap row was stale. Future readers: do not start new work on "EXB-06" — the surface is in place and write-tested; the per-row ship entry on EXB-01 is the canonical record.
- ✅ **EXB-07 — Booking flow: offer the bed, and offer it at the right moment** — **Shipped 2026-08-01 at v0.205.0** (commit `c4dfd18` on `feature/exb-07-occupancy-pickers`, merge `3d08e6e` to `dev`). See the lead paragraph above for the full rationale + the 3 surfaces + the test counts. The guest `/book` picker (per EXB-01) was the first surface; this PR extends the same UX to the admin walk-in modal + the corporate `/book` page. All 3 surfaces now share the same shape: 3 steppers (adults, children, extra beds) + a contextual "blocked by cap → add an extra bed" hint that uses the same `requiredExtraBedsFor` helper the server uses (per decision #153). 14 source-text guards pin the new UX contract.
- ✅ **EXB-08 — Surface it downstream** — **Shipped 2026-08-01 at v0.206.0** (commit `9189ffe` on `feature/exb-08-surface-downstream`, merge `387fe4e` to `dev`). See the lead paragraph above for the full rationale + the 7 surfaces + the test counts. The server's `buildRateBreakdown` now writes the extra bed add-on line into `addOns[]` (was missing pre-EXB-08 — the extra bed total was invisible on every downstream surface). 7 display surfaces now show the adult/child split + extra bed: the admin drawer header, the Bookings table row, the receipt PDF (Guests + Stay lines), the drawer "Guests:" + Stay lines, the Reports line, the guest /my-booking card, and the booking email. The orphan `BookingSummaryCard` was future-proofed with the same shape. 16 source-text guards pin the new contract.
- ✅ **EXB-10 — Hotel-wide rollaway-bed inventory** — **Shipped 2026-08-01 at v0.207.0** (commit `aa0bdf8` on `feature/exb-10-bed-inventory`, merge `4652498` to `dev`). See the lead paragraph above for the full rationale + the helper + the 3 server call sites + the test counts. The new `settings/hotelConfig.extraBedInventory` field stores the hotel-wide count of rollaway beds (`0` = no constraint, the historical "any number" behavior). The server's `handleCreateBooking` + `handleCreateWalkin` + `handleRescheduleBooking` transactions now read the inventory + query the overlapping occupying bookings + call `checkExtraBedInventory` inside the same `runTransaction` that assigns the room (preventing the RTS-01 read-then-write race). Pure helper `countExtraBedsInUse` + `checkExtraBedInventory` in `shared/utils/extraBedInventory.ts` is unit-testable without a Firestore mock; the query lives at each call site. `excludeBookingId: bookingId` is the reschedule case so the booking's own bed doesn't count against itself. 11 helper unit tests + 16 source-text guards pin the new contract. Branch: `feature/exb-10-bed-inventory`. *(EXB-10 entry on `plan/docs/DECISIONS-FEATURES.md`.)*
- ✅ **EXB-09 — Tests + MD sync** — **Shipped 2026-08-01.** Eight rate-snapshotting and breakfast-coupling guards shipped in `f2dbda5` (merge `7047f33`); the existing EXB boundary, server-cap, and multi-night tests cover the remaining checklist, and the owning specs were synchronized in `ca72b2a`.

### Pre-MRB Hardening (PMH) — proposed 2026-07-31 · gates MRB-04
> Risk-reduction work identified while scoping MRB on 2026-07-31. **PMH-02 gates MRB-04** (the group folio) — do not start the folio change until the extraction lands. The rest bound the blast radius of the whole MRB batch and pay for themselves on WPM/NBS/RTS too.
>
> **The framing:** RTS-01, NBS-08, and MRB-11 are all the same bug shape — **wrong answer, no error, no test failure**. Source-text assertions cannot catch that class. Every item below exists to convert a silent-miscount risk into a loud failure.

- ⬜ **PMH-01 — ~~Time-boxed to before launch~~ → ABSORBED by the 2026-07-31 scope decision; revised for the reservation header 2026-08-01** — the owner decided **all scoped items ship before production launch**, so MRB-01 lands pre-launch by definition. The original `groupId` + primary pointer shape is superseded: MRB-01 now introduces `reservations/{id}` plus nullable `Booking.reservationId`, with every new single-room and multi-room create routed through the header. The reasoning still governs: doing this before real guest money exists avoids migrating months of live booking folios, payment ledgers, and BIR-relevant exports. If MRB moves post-launch, the reservation-header cutover becomes a migration project with a rollback plan rather than a schema-only pre-launch change.
- ✅ **PMH-02 — Extract folio math into ONE shared, behavior-frozen function (gates MRB-04)** — **Shipped 2026-07-31 at v0.194.1** (commit `f2fa62b` on `refactor/pmh-02-folio-extract`, merge `0b61407` to `dev`): two helpers in `shared/utils/bookingFolio.ts` — `computeBookingFolio(input)` for the per-booking folio (replaces the `getBookingFolio` local closure in `BookingsPage.tsx`, all 8 call sites) and `computeServerFolioTotals(input)` for the server's inline `folioTotal` / `computedBalance` math (replaces the inline math in the create / confirm-with-balance transactions). **Pair with EXB-02** was the spec's recommendation — split into two PRs in practice: PMH-02 (folio) ships first; EXB-02 (add-on math) and the DSC math extraction are the next two pure-refactor PRs in the same pattern. Pure refactor with zero behavior change — the shared helpers return the same values the old code did, byte-for-byte. Pinned by 10 characterization tests in `shared/__tests__/booking-folio.test.ts` (basic shape, store-order filter criteria, persisted vs. live payments, the optimistic-update path, the full 8-call-site shape with realistic inputs, the server-side `folioTotal` + `Math.max(..., 0)` clamp, the defensive `Number(x) || 0` pattern for nullish inputs). Minimal input types (`FolioBooking`, `FolioStoreOrder`, `FolioCharge`, `FolioLivePayment`, `FolioPersistedPayment`) keep the shared utility independent of admin-app's `Booking` extension / `OnsitePayment` interface. Two pre-existing source-text tests updated in lockstep: `fin-14-incidental-charges.test.ts` (pinned the new `computeBookingFolio(` call site instead of the old `chargesTotal = charges.reduce(...)` shape) and `booking-receipt-pdf.test.ts` (end-marker updated since the deleted `getBookingPaymentsTotal` helper was used as a slice anchor). **MRB-04 is now unblocked** — the group folio can change money math in one function instead of three independent sites.
- ✅ **PMH-03 — Wire the existing emulator harness into `npm test` and extend it to behavioral tests** — **Shipped 2026-07-31 at v0.192.0** (commit `f9814dd` on `fix/pmh-03-emulator-harness`, merge `b4a4f3d` to `dev`): (1) **root `npm test` now runs the full chain including the emulator** (shared → api → admin → infra → rules); new `test:fast` script keeps the source-text-only inner loop under 30s and skips Java. (2) **`vitest.rules.config.ts` glob extended** — `firebase/tests/**/*.emulator.test.ts` joins the existing `*.rules.test.ts`; two test categories, one harness. (3) **First behavioral test shipped**: `firebase/tests/room-types-array-write.emulator.test.ts` pins the RTS-01 fix at the Firestore layer — N concurrent setDoc calls on a `roomTypes[]` array field lose all but the last write's change (the bug), a single batched setDoc with the fully-computed next array persists every type's change (the fix). Pattern is the template for the rest of PMH-05: seed via the admin context, exercise the write pattern, assert the contract. (4) **`plan/docs/CONTRIBUTING.md §Testing`** documents the three test layers (source-text / rules / emulator), what each catches, the `*.emulator.test.ts` pattern, the script inventory, the naming convention, and the Java + Firebase emulator requirement. **Java required to actually execute** — the sandbox this was authored in has no Java, so the emulator tests need to be verified on Daniel's machine with `npm test` before further pre-launch work merges. **The harness loads `firestore.rules` automatically**, so any rule change that breaks a write will surface in the emulator test too. Every block from WPM onward now has a behavioral test path; PMH-05's array-write-integrity invariant generalises the same shape to `paymentMethods[]` and `bookingSources[]`.
- ⬜ **PMH-04 — Golden-master test for Reports** — with **zero** groups in the fixture data, group-aware Reports must produce **byte-identical** output to today's implementation. Catches MRB-11 double-counting the moment it appears, and costs nothing to maintain. Apply the same shape to the CSV/XLSX exports, which are the surfaces the hotel's bookkeeping actually consumes.
- ⬜ **PMH-05 — Money + counting invariants as property tests** — (a) **balance invariant:** `group balance == sum(charges) − sum(payments)`, written in the same style as the existing rewards invariant already documented in `GOTCHAS.md` (`rewardsPoints == sum(pointsHistory.points)`) — that precedent works, copy it; (b) **unknown-key counting:** a booking source that is not in the seed `BOOKING_SOURCES` array must still be counted in Reports — one test permanently kills the NBS-08 class; (c) **array-write integrity:** a multi-item save to any `settings/hotelConfig` array field persists **every** item — the RTS-01 regression guard, generalised so it also covers `paymentMethods[]` and `bookingSources[]`.
- ⬜ **PMH-06 — Rehearse the group money lifecycle on staging via ETR** — `testRunId` tagging plus controlled reset already exists for exactly this. Create multi-room groups as TEST DATA on staging and drive the full lifecycle end to end: book → partial pay → check in → add incidental charge → check out → refund → settle after checkout. Then reset. Do this **before** MRB-04 is considered done, not after. Confirms the group folio behaves under the real transaction, real rules, and real snapshot timing rather than under mocks.

### Multi-Room Booking (MRB) — proposed 2026-07-31 · large, phased
> Owner request 2026-07-31, referencing a Booking.com availability table: guests must be able to book **multiple rooms and multiple room types in one flow**. Applies to all three create surfaces — guest `/book`, the admin New Booking modal (per NBS), and corporate `/corporate/book`.
>
> **Current model is strictly one room per booking.** `Booking` carries scalar `roomId` / `roomNumber` / `ratePerNight` / `numGuests`, and no grouping concept exists anywhere — `groupId`, `reservationId`, `groupBooking` return zero hits across `shared/`, `guest-app/server/`, and both apps. Corporate is one-room-per-booking too, so there is no precedent to extend. `roomId`/`roomNumber` read sites: **admin-app 303 · guest-app/server 169 · guest-app/src 78 · shared 13**.
>
> **Architecture revised 2026-08-01 (owner): one room-independent reservation header + N one-room booking documents.** The 2026-07-31 decision was half right: N room records are still required, and the rejected `rooms: [...]` array on one booking remains rejected because it invalidates the scalar `roomId` assumption at ~563 read sites and breaks per-room check-in, registration, ID, signature, housekeeping, room move, and checkout. What is superseded is linking those records only by `groupId` and making one designated room the financial “primary.” A cancelled room must never secretly own an active reservation's money. New shape: `reservations/{reservationId}` owns the public reference, lead booker/contact, source/corporate context, shared payment proof, discounts, group totals, and folio; each `bookings/{id}` carries `reservationId` and remains tied to exactly one physical room with its dates, occupancy, rate/add-on snapshots, registration, and lifecycle. No `isGroupPrimary` or `groupPrimaryBookingId`.
>
> **Owner decisions (2026-07-31, amended 2026-08-01):** (1) **one folio on the reservation header** — the one-bill outcome is unchanged, only its owner changes; (2) **all rooms share one date range at creation** — one stay per reservation, as in the reference screenshot; (3) **one booker at booking time**, per-room occupant names and IDs captured at check-in where registration already happens; (4) **all three create surfaces** in scope; (5) **every new single-room booking also gets a reservation header** so money, lookup, email, and reporting do not maintain separate single-room/group implementations. Legacy booking documents without `reservationId` remain self-contained and readable; pre-live TEST DATA is reset rather than migrated.
>
> **Behavior contract approved 2026-08-01:** reserve `plan/docs/DECISIONS-FEATURES.md #159` for the implementation-time decision entry (`#143` was consumed by PMH-02 after the original MRB reservation). The public experience is one reservation; room records remain independently operable. Exact adults/children/extra beds must be assigned to each room before review, but occupant identities wait until check-in. Mint one public `reservationRef` per reservation and denormalize it onto child bookings for existing admin search/display compatibility; there are no public or staff sibling refs. Lookup resolves the reservation header, then its room bookings.
>
> | Action / concept | Reservation or room scope |
> |---|---|
> | Dates at creation, payment proof, payments, folio, receipt, initial confirmation, pre-arrival reschedule | **Group** |
> | Registration/ID, room assignment or move, check-in/out, housekeeping, intercom, early arrival/departure, in-stay extension | **Room** |
> | Incidentals and store orders | Entered against a room; charged to the reservation folio |
> | Staff cancellation | Explicit choice: selected room or entire reservation |
> | Guest cancellation | Entire reservation by default; no ambiguous one-click room cancellation |
> | Partial cancellation | Remove that room's itemized charges, preserve an audit adjustment, and use the existing refund ledger for money already collected |
> | Email after a partial action | One “reservation updated” message listing the change, remaining rooms, revised total/balance, and refund/payment state |
> | Loyalty | Award once to the lead member from the final settled reservation total, never once per child booking |
>
> **Pricing and limit defaults:** a percentage voucher/member discount applies across eligible room lines; a flat voucher applies once to the reservation and is recomputed against the remaining eligible lines after a partial cancellation; mixed room-type eligibility discounts only eligible lines. Voucher usage counts once per reservation and is restored only when no active room still receives that voucher. Corporate-code `usageCap` counts rooms because it represents room allocation; cancelling an eligible room before check-in restores one use. The PEX snapshotted hold window applies uniformly to the reservation (current default 24h); MRB does not introduce an undocumented shorter large-reservation timer.
>
> **Aggregate status + reporting contract:** the reservation summary derives its main lifecycle label from non-cancelled child bookings: payment pending → `Awaiting payment`; all confirmed → `Confirmed`; a mix of confirmed/checked-in → `Partially checked in`; all active rooms checked in → `In house`; a mix containing checked-out rooms → `Partially checked out`; all active rooms checked out → `Completed`. Cancellation is a secondary count (`1 of 3 rooms cancelled`) so it never hides the operational state; only an all-cancelled reservation is `Cancelled`. Every room retains its exact status. Reservation/acquisition/cancellation counts come from reservation headers; occupancy and room-nights remain per room booking; ADR is room revenue divided by rooms sold. Availability failure preserves the cart, identifies the short room type/quantity, and creates nothing.
>
> **Sequencing: RTS first, then WPM → NBS, then MRB.** MRB-07 depends on the NBS modal rework — do not build the multi-room admin modal twice. Deliver MRB itself in phases: reservation model + server (01–05) before any UI (06–08). **See §Pre-MRB Hardening (PMH) above** — PMH-02 gates MRB-04, and PMH-01 keeps the reservation-header cutover before real guest money reaches production.
>
> **Investigation finding (verified 2026-08-01):** no reservation entity exists in `shared/`, either app, the server, or Firebase rules. The public ref is generated on a booking; lookup queries `bookings.bookingRef`; payments and charges live under `bookings/{id}`; Reports joins those subcollections by booking ID. MRB must deliberately move shared/commercial ownership instead of layering a header over unchanged writers. Already in favour: the MBP picker (`/my-booking` returning `kind: "list"`) is most of the nested-room lookup UI; the room-assignment loop generalises to N; the daily counter is already inside the transaction; Firestore's 500-document limit is far above the hotel's inventory.

- ⬜ **MRB-01 — Model: `Reservation` header + `Booking.reservationId`** — add `reservations/{id}` for the public ref, lead booker/contact, booking source/corporate context, original shared date range, payment/discount state, and group totals. Every new booking, including a one-room stay, carries server-assigned `reservationId`, denormalized `reservationRef`, room position/count, and remains one physical-room record. Shared fields on child bookings are compatibility snapshots, never independently editable sources of truth. No primary-room fields and no rooms array. Legacy null `reservationId` bookings keep today's self-contained behavior. Types: `shared/types/index.ts`; schemas: `shared/schemas/booking.ts`; rules deny client assignment/change of `reservationId`.
- ⬜ **MRB-02 — Create the reservation + assign N rooms atomically** — the client preallocates `reservationId` before shared payment/discount-proof uploads, and the server must create that exact reservation document. Generalise the candidate loop in `guest-app/server/handlers/bookings.ts` to assign N distinct rooms across the requested type mix and create their reservation header inside **one** `runTransaction`, with a guard so a room already claimed earlier in the same pass is not reassigned. Firestore's reads-before-writes rule still applies — collect every candidate read before the first write. Partial failure is all-or-nothing: if room 3 of 3 cannot be found, neither the reservation nor any child booking is written and the guest sees one clear error. Capacity validates each room's occupancy, not the reservation total. Shared proof files move to reservation-scoped Storage paths; per-room registration/ID files remain booking-scoped.
- ⬜ **MRB-03 — One reservation ref, no sibling refs** — increment the daily counter once and mint one `reservationRef` inside the create transaction. Store it canonically on the reservation and denormalize it onto every child booking so existing room-centric admin search/display remains useful. Public lookup queries the reservation first; legacy fallback still queries a self-contained booking. Email, receipt, guest UI, and staff UI show the reservation ref plus room number/position where disambiguation is needed.
- ⬜ **MRB-04 — Folio moves to the reservation header (owner decision)** — new payments and financial adjustments live under `reservations/{id}/payments` and `reservations/{id}/charges`; child-specific entries carry the affected `bookingId`. Store orders retain their room booking link and also resolve to the reservation folio. Record Payment, Verify & Record Payment, receipt PDF (itemise rooms, total once), checkout/receivables, Reports Collections, FIN reconciliation, refunds, and settlement must resolve the reservation. Partial room cancellation removes that room's itemized charges through an auditable adjustment; collected money is never silently rewritten. **This remains the highest-risk MRB item.** PMH-02 has shipped, so extend the shared behavior-frozen folio helper and preserve a legacy adapter for bookings without `reservationId`.
- ⬜ **MRB-05 — Checkout must resolve the reservation balance** — a room must not check out clean while its reservation owes money, and the unpaid-checkout reason flow (UCO) operates on the reservation balance. Room release, housekeeping-dirty, intercom resolution, and check-in/out stay per-room. Loyalty awards once to the lead member from the final settled reservation total.
- ⬜ **MRB-06 — Guest `/book` flow: quantity per room type** — Step 1 becomes a small cart: a quantity selector per available room type, a running total, and guest distribution across rooms. **Occupancy: resolved by CHD** — the adults/children split, its storage, and the per-room-type caps are specified in §Children in Booking above; land CHD first and MRB-06 inherits it. What remains MRB-specific is distributing guests across the reservation's room stays. Steps 2–4 collect one lead guest and one payment for the whole reservation.
- ⬜ **MRB-07 — Admin New Booking modal: multi-room** — **depends on NBS.** Build the multi-room selector once, on top of the reworked modal, not before it. Front desk needs this for walk-in groups, phone bookings, and OTA entry.
- ⬜ **MRB-08 — Corporate `/corporate/book`: multi-room** — arguably the strongest use case (companies book blocks). The negotiated/flat corporate rate applies per room; the corporate code's `usageCap` counts N rooms as N uses.
- ⬜ **MRB-09 — Emails fire from the reservation, not N rooms** — booking-submitted, confirmed, payment-confirmed, and check-in reminder send **one** email listing every room. Whole-reservation cancellation sends one cancellation email; a room-only cancellation or other partial action sends one reservation-updated email with the changed room, remaining rooms, revised total/balance, and refund/payment state. The receipt PDF itemises rooms and totals once.
- ⬜ **MRB-10 — Guest lookup resolves a reservation with nested rooms** — `/my-booking` first finds `reservations.reservationRef` (or verified email/token), then returns its guest-safe child room projections as one card. Reuse the MBP wire shape and privacy posture (`maskedEmail`, no guest name reflected — decisions #126/#128/#131 apply unchanged). Cancel/resend act on the reservation. Legacy bookings without `reservationId` retain today's lookup path.
- ⬜ **MRB-11 — Reports use the correct owner for each metric** — reservation, acquisition, payment, and reservation-cancellation counts come from `reservations`; occupancy, rooms sold, room-nights, room-specific cancellations, and room revenue come from child bookings; ADR is room revenue divided by rooms sold. Legacy self-contained bookings count as both one reservation and one room stay. Apply the same definitions to charts, Daily Close, CSV, XLSX, and backup/export joins.
- ⬜ **MRB-12 — Admin reservation + room affordance** — a multi-room reservation needs a visible room-count marker, derived aggregate-status label, room-stay navigator, and explicit scope on every action (`This room` / `All rooms`). Keep the per-room workspace that check-in, room moves, registration, housekeeping, and intercom depend on.
- ⬜ **MRB-13 — Cancellation: reservation vs room** — staff chooses one room or the whole reservation with targets and financial effect shown before confirmation. Cancelling one room releases only that room and routes its itemized adjustment/refund state through the reservation folio; cancelling the first-created room has no special financial consequence because no room owns the folio. Guest-facing cancellation defaults to the whole reservation and must state the room count; cancel/resend remain server-authenticated reservation actions.
- ⬜ **MRB-14 — Tests + MD sync** — server/emulator tests for atomic reservation + N-room creation (all-or-nothing partial failure, same-room-twice guard, per-room occupancy), one-room creation through the same header path, canonical/denormalized ref consistency, reservation folio + partial-cancellation adjustments, checkout balance resolution, legacy booking fallback, voucher/corporate usage, aggregate statuses, report ownership/deduplication, and one-email-per-reservation/partial-update email. MDs: `plan/features/BOOKING-FLOW.md` (reservation + multi-room cart), `plan/features/AVAILABILITY-LOCKING.md` (atomic header + N rooms), `plan/features/BOOKINGS-MANAGEMENT.md` (reservation drawer + folio), `plan/features/CORPORATE-BOOKING.md`, `plan/features/REPORTS.md` (reservations vs room stays), `plan/features/EMAIL-PDF-STORAGE.md` (one email per reservation), `plan/features/VOUCHERS.md` (reservation eligibility/use), `plan/features/SPARK-REWARDS.md` (one award), `plan/docs/TYPES.md` + `plan/docs/BACKEND.md` (`Reservation`, `reservationId`, folio paths), `plan/docs/API-ROUTES.md`, `plan/docs/SECURITY.md`, and `plan/docs/DECISIONS-FEATURES.md #159`.

### Room Type & Rates Save Defects (RTS) — reported 2026-07-31 · P1
> Owner reported two broken surfaces on 2026-07-31: **room type deletion** and **rates management cannot save**. Both are write-path defects on the same field — `settings/hotelConfig.roomTypes[]` — so they are tracked together. Code read on `dev` @ `36d4112`. All 930 admin tests pass, which is itself a finding: the suite is source-text assertions, so neither defect is behaviorally covered.
>
> **Shipped 2026-07-31 — RTS-01..02..04..07(test)** (commit `1f07946` on `fix/rates-room-type-save-and-delete`, merge `608b06c` to `dev`; v0.191.1). **RTS-03 reproduced same day on staging** — the deletion "didn't work" symptom was RTS-04 (silent failure swallow). **RTS-05 and RTS-06 are real defects in the code but were NOT the active user-visible bug**; the hotel never hit them. Both remain open below for follow-up (cheap, ~30 lines each; `ConfirmForm` for RTS-05, "loaded vs empty" check for RTS-06). The reusable lesson — never fan out per-item writes to a single Firestore array field — is now in `plan/docs/GOTCHAS.md §Firebase` and applies to `paymentMethods[]` (WPM) and `bookingSources[]` (NBS) as well as `roomTypes[]`.
>
> **Follow-up shipped 2026-08-01 — RTS-05..06** (commit `cc629b5` on `fix/rts-05-06-confirm-empty-rehydrate`, merge `c0ffeea` to `dev`; v0.200.1). The two real-but-not-active defects from the original RTS-01..04 batch ship together. RTS-05: the 3-second auto-disarm on the two-step delete confirm is replaced by a proper `Modal + ConfirmForm` (matching the RoomsPage delete flow, using the `ConfirmForm` primitive Phase 11.7 already ships with `variant="danger"` + `reasonRequired: true`). RTS-06: a `roomTypesLoaded: boolean` flag distinguishes "not loaded yet" from "loaded and legitimately empty" — the `length > 0` guard on the snapshot sync effect is dropped, so a hotel that deleted every room type no longer sees all 8 deleted types silently resurrect on the next page load. The reusable lesson about loading flags for snapshot-derived state is now in `plan/docs/GOTCHAS.md §Firebase` (alongside the no-fan-out lesson from RTS-01).

- ✅ **RTS-01 — CONFIRMED: `handleSaveRates` races itself; only one room type's rates survive** — `RatesPage.tsx` built `roomTypes.map(t => updateRoomType(t.value, {...}))` and fired them all through one `Promise.all`. `updateRoomType` in `AdminContext.tsx` did `const updated = roomTypes.map(...)` — reading `roomTypes` from its **render-time closure** — then `saveRoomTypes(updated)` wrote the **entire array** via `setDoc(..., { merge: true })`. A merge write replaces an array field wholesale, so N concurrent calls each computed "the original array with exactly one type changed" from the *same* stale snapshot, and the last write to land won. **Every other room type's edit was silently discarded.** `Promise.all` resolved clean (all N writes genuinely succeeded), so the "Rates saved" toast fired and the user watched the values snap back when the `onSnapshot` echo arrived. Matches the report exactly. **Fixed in 1f07946.**
- ✅ **RTS-02 — Fix: one batched array write, not N racing ones** — added a bulk `saveRoomTypes(types: RoomTypeEntry[]) => Promise<void>` API to `AdminContext` (exposed on the context interface + value destructure); `handleSaveRates` computes the whole matrix once and calls it once. Per-field functional state updates do not fix this on their own — the race is at the Firestore write, not in React state. Audited the other `Promise.all` + `updateRoomType` call sites before shipping: `uploadRoomTypePhoto` / `removeRoomTypePhoto` / `reorderRoomTypePhotos` all call it singly and are safe. **Fixed in 1f07946.**
- ✅ **RTS-03 — Reproduced the deletion failure and pinned the mechanism** — owner ran the staging repro on 2026-07-31: creating a throwaway room type and deleting it worked cleanly post-fix. The original "deletion doesn't work" symptom was **RTS-04** (silent failure swallow making every delete look like it didn't work), not RTS-05 or RTS-06. **Closed.**
- ✅ **RTS-04 — `saveRoomTypes` swallowed `updateSettings` failures and never rolled back optimistic state** — `updateSettings` catches its own errors, fires a "Failed to save settings" toast, and **returns `false` rather than throwing** — so `saveRoomTypes`'s own `try/catch` was dead code and its callers never learned the write failed. `deleteRoomType` therefore resolved successfully, `SettingsPage`'s `catch` saw nothing, and the row disappeared from the table until the next snapshot silently restored it. **Presents exactly as "deletion doesn't work."** Fix in 1f07946: `saveRoomTypes` now captures `previousTypes`, applies the optimistic update, checks the boolean return of `updateSettings`, and **throws + rolls back** the optimistic state on every failure path (both the `if (!success)` branch and the outer `catch`). All callers (`addRoomType` / `updateRoomType` / `deleteRoomType` / `handleDeleteRoomType` in `SettingsPage` / the photo upload + remove + reorder helpers) already wrap the call in try/catch or return a `{ success, error }` shape, so the new throw lands safely at every UI surface. **This was the user-visible mechanism** — fixing it made the staging deletion repro work clean.
- ✅ **RTS-05 — Proper delete confirm via `Modal + ConfirmForm`** — **Shipped 2026-08-01** on `fix/rts-05-06-confirm-empty-rehydrate` (v0.200.1). The 3-second auto-disarm `useEffect` on `pendingDeleteRoomType` is removed. `pendingDeleteRoomType` is now `RoomTypeEntry | null` (the full type, not the value). Both Delete buttons (mobile + desktop) call `setPendingDeleteRoomType(type)` and a new Modal opens containing a `ConfirmForm` with `variant="danger"` + `reasonRequired: true` — matching the RoomsPage delete flow. The form's onConfirm wires to `handleDeleteRoomType(pendingDeleteRoomType.value)`; onCancel + the Modal's onClose both clear the state. A blocking "cannot delete while N rooms still use this type" branch shows a different `variant="primary"` ConfirmForm (matching the RoomsPage blocked path). `isDeletingRoomType` state tracks the in-flight delete and disables the cancel during the request. 7 new source-text guards in `rts-05-06-confirm-empty-rehydrate.test.ts` pin the contract: the 3-second timer is gone, the Delete buttons just call `setPendingDeleteRoomType(type)`, the Modal uses the type's label in the title, the confirm uses `variant="danger"` + `reasonRequired`, the import is the same path as RoomsPage, and the blocking branch is wired with its own `testId`.
- ✅ **RTS-06 — `roomTypesLoaded` flag re-hydrates empty snapshots** — **Shipped 2026-08-01** on `fix/rts-05-06-confirm-empty-rehydrate` (v0.200.1). A `roomTypesLoaded: boolean` flag is added to `AdminContext` (initial: `false`, flipped to `true` inside the same effect that calls `setRoomTypes`). The `length > 0` guard on the snapshot sync effect is dropped — any array value (including `[]`) now syncs, and the flag tracks "the snapshot has been observed at least once". The initial paint still shows the `DEFAULT_ROOM_TYPES` initializer so the UI is not blank before the first snapshot. Pinned by the same test file: the flag is declared with `useState(false)`, the guard `Array.isArray(hotelConfig.roomTypes) && hotelConfig.roomTypes.length > 0` is removed, and the new guard is just `Array.isArray(hotelConfig.roomTypes)`.
- ✅ **RTS-07 — Tests + MD sync (test half shipped in 1f07946; MD sync ships in this docs PR)** — the existing 930 admin tests are source-text assertions and caught none of this. Added 8 source-text guards in `admin-app/src/__tests__/rts-01-rates-save.test.ts` (no `Promise.all` in `handleSaveRates`, `nextRoomTypes = roomTypes.map(...)` + `await saveRoomTypes(nextRoomTypes)` shape, `saveRoomTypes` captures `previousTypes`, checks the boolean return, throws on failure, has ≥2 rollback sites, exposed on both the interface and the value destructure, plus an `uploadRoomTypePhoto` singly-call guard). The pre-existing `phase-11.9-room-type-rates` test that pinned the old fan-out shape was rewritten to pin the new contract. **Behavioral emulator coverage is PMH-03's job** (the broader test-quality uplift; see §Phase 12 PMH-03) and runs in the same pre-launch batch. MDs: `plan/features/RATE-MANAGEMENT.md` (single-batch write contract), `plan/features/SETTINGS.md` Room Types cross-reference (delete guard + confirm behavior), `plan/docs/GOTCHAS.md §Firebase` (the reusable lesson). **Test half shipped in 1f07946; MD half ships in this docs PR.**


### Booking Drawer UX Refactor (BDUX) — remaining verification
> BDUX-01..08 + BDUX-05a..05n shipped 2026-07-16 (contract in archive).

- ⬜ Verify representative bookings across every status + conditional combination.
- ⬜ At 1440px, staff can understand guest/stay/payment/balance/next action without scrolling the default Overview.
- ⬜ At 375px, no horizontal scroll; primary action above safe area; modal/sheet focus + close behavior accessible.
- ⬜ Default Folio has no expanded entry form; each reachable through one labeled action.
- ⬜ Completing any Folio action leaves the user on Folio with updated Total/Paid/Balance/ledger visible.
- ⬜ Pending payment proof reachable from sticky header in one action; verified proof accessible without dominating default Folio.
- ⬜ Next valid status action reachable in one tap from any section.
- ⬜ Run admin typecheck, booking/admin regression tests, and manual visual QA across mobile/tablet/desktop before marking complete.

*(UCO-01..14 and PRC-01..19 shipped 2026-07-16 — contracts in archive.)*

### Bookings & Store Orders Filtering UX (FSO) — remaining verification
> FSO-01..18 shipped 2026-07-16 (contract in archive). Advanced filter Phase 2 pending.

- ⬜ At 375px: no horizontal scroll, quick chips operable, advanced sheet one-handed above safe area.

### Environment Test Runs & Controlled Data Reset (ETR)
> Phase 1 core shipped (ETR-01..14, ETR-S01..S15). **In progress: ETR-R (production-to-staging refresh) — foundation landed 2026-07-29 (R01 + R04 + R10 partial — server-side authorization, identity-replacement sanitization engine, audit row). Open: R02 (multiple modes — sanitized-snapshot is the only one in the foundation), R03 (reviewable preservation), R05 (file sanitization), R06 (full relational integrity), R07 (side-effect disable), R08 (post-import scan), R09 (controlled replacement with staging-reset integration — the manual import is the MVP step today).** Also open: ETR-D01..D10, ETR-15..20, ETR-21. Full spec: `plan/features/ENVIRONMENT-TEST-RESET.md`.

### Finance scope boundaries (recorded decisions — do not re-open without owner request)
- ⏸ Expenses & P&L tracking — out of scope; system is a PMS, not accounting software; exports feed external bookkeeping/BIR
- ⏸ Day-locking / night-audit snapshots — deferred at 14-room scale; payments are already append-only at the rules level, which covers the cash side; revisit if historical figures drift or staff grows

---

## Finance Lifecycle Recommendations — open items (2026-07-14)
> Source: `plan/project/AUDIT-FINANCE-LIFECYCLE-2026-07-12.md §Post-remediation recommendations`. FLR-01/02/04 fixed 2026-07-14 (detail in archive).

- ⏸ **FLR-03 — Bound Reports ledger listeners** *(deferred with trigger)* — `collectionGroup("payments"/"charges")` loads full ledger history live on every Reports visit; fine at 14 rooms, linear forever on Blaze. **Trigger: revisit at ~1 year of operation** — switch to `recordedAt`-bounded queries; all-time Receivables fall back to one-shot `getDocs`.
- 🔄 **FLR-05 — Operational handover** *(owner-facing)* — handover in `FINANCE-LIFECYCLE-HANDOVER-2026-07-14.md`. Daily Close convention + accountant VAT review + staging money-path walkthrough have explicit checklists/evidence. **Remaining:** accountant confirmation + owner sign-off before next `dev → main` milestone.

---

## Production Environment Split — cutover queue (added 2026-07-14)
> Source: `plan/project/PROD-CUTOVER-RUNBOOK.md` — demote `spark-inn-stg-7a7ad` to staging, stand up Vercel staging on `dev` at `stg.sparkinnbohol.com` / `stg-admin.sparkinnbohol.com`, cut production over to clean-slate `spark-inn-prod`. Runbook holds checklists, prod client config, secret-handling rules. PC-05 carry-over resolved: clean slate only, staff pre-provisioned — Decision #119. PC-01..PC-04 done 2026-07-14 (archive).

- ⬜ **PC-05 — Archive + data carry-over** — Full Backup XLSX + `gcloud firestore export` archive, then recreate active staff accounts in production Auth/Firestore.
- ⬜ **PC-06 — Cutover + smoke test** — freeze window, Production redeploy, preflight, end-to-end smoke booking on prod (then cancel/refund), email triggers, integrity scan, rules verification, QR spot-check, local key file deleted, first real Daily Close.

---

## INC-01 — Production Incident: rules/app version skew *(opened 2026-07-17 · resolved 2026-07-17)*

> **Root cause:** E2E-audit security rules (fixed 2026-07-17) were deployed to live `spark-inn-stg-7a7ad` (serving `sparkinnbohol.com`), but the live app is still the 2026-07-14 build — `main` is ~133 commits behind `dev` and lacks the matching client changes. New rules assume the new app.
> **Evidence (verified 2026-07-17):** deployed rules via Firebase MCP match the fixed repo versions; `git show main:...IntercomPage.tsx` still sends guest messages via client `addDoc`; `git show main:...BookingPage.tsx:716,746` still calls `getDownloadURL()` after proof/ID upload.

**Broken guest-facing flows during the incident window:**

- ✅ **INC-01a — Online-payment bookings blocked.** X-01's rules removed public read from `bookings/{id}/payment-proof/` and `discount-id/`, but the deployed app calls `getDownloadURL()` right after `uploadBytes()` → permission-denied → guest sees a permanent "Receipt upload failed. Please check your connection and try again." and cannot pass Step 3. Affects every GCash/bank booking and every Senior/PWD ID upload. Only Pay-at-Hotel bookings without a discount ID still complete.
- ✅ **INC-01b — Guest intercom dead.** G-04's rule restricts `intercoms/{room}/messages` creation to staff (guests are meant to use the new rate-limited `POST /api/intercom/send-message`), but the deployed app still writes messages client-side via `addDoc` → every guest chat message and quick request fails with permission-denied.

**Not affected / silver lining:** X-01 itself (publicly fetchable OSCA/PWD government-ID photos and payment screenshots) is **closed in production** — the deployed rules are correct; it's the app that's behind. Staff-side admin flows and Pay-at-Hotel bookings still work.

**Resolution (executed 2026-07-17):** emergency `dev → main` merge (PR #118, `2e6ca4e`) deployed to production by DK — the app now matches the deployed rules, and all 17 E2E audit fixes are live. The partial-rules-rollback and cherry-pick alternatives were rejected (would re-open X-01 / untested as a unit). Note: this merge was the incident fix, not the Phase 11 staging-milestone sign-off — the FLR-05 and client-review gates in §Phase 11 remain open.

**Close-out checklist:**
- ✅ Decide the resolution path — option 1, emergency deploy (DK, 2026-07-17)
- ✅ Deploy the fix — PR #118 merged and deployed to production
- ⬜ Verify live: a GCash test booking passes Step 3 with proof upload; a guest intercom message + quick request deliver to the admin inbox
- ⬜ Confirm no stuck bookings/guests during the breakage window (check Resend logs / booking creation rate between the 2026-07-17 rules deploy and PR #118)
- ✅ Prevention rule recorded in `plan/docs/GOTCHAS.md §Firebase` — never deploy `firebase/*.rules` ahead of the app build that matches them

---

## E2E User Journey Audit (2026-07-17) — all findings remediated

✅ All 17 findings are fixed **and merged to `dev`**: 3 HIGHs (`aae6808` — G-01 full-body Zod, C-01 RoomType-sourced conversion pricing, X-01 Storage-rule public-read removal), 8 MEDs (`cfe6581`), 6 LOWs (`9b9c85e`). All 847 tests, typecheck, builds, preflight pass. Report: `plan/docs/AUDIT-E2E-REPORT.md`.

✅ **Gate cleared 2026-07-17:** app + rules in production (PR #118 + the earlier rules deploy) — audit's NO-GO lifted. Only follow-up: re-verify X-02's white-label sweep before a second white-label client.

---

## Spark Rewards Feature Audit (2026-07-18) — fully closed 2026-07-29

Report: `plan/docs/AUDIT-SPARK-REWARDS-REPORT.md`. All 5 sections audited; 11 findings total (1 HIGH · 2 MEDIUM · 7 LOW — the report's executive summary count of "3 MEDIUM" double-counts the rules-layer integrity angle inside MED-1; the report body lists MED-1 + MED-3 only). Final go/no-go: **CONDITIONAL GO** — HIGH-1 was the only launch blocker; shipped 2026-07-25. The front-desk manual-link half of MED-3 also shipped 2026-07-29 (decision #140). The only remaining carry is the guest self-service prompt half of MED-3 (the larger of the two MED-3 paths) plus LOW-3 (theoretical); both are deliberate and have a documented path forward.

### Section 3 — Early Check-In / cross-cutting (§1 registration + stays)

- ✅ **HIGH-1 — Email-based booking match trusts unverified `email` token claims** — **Shipped 2026-07-25** (decision #133, branch `fix/spark-rewards-email-verified-gate`). `authenticateUser` (in `guest-app/server/apiRouter.ts`) now surfaces `decodedToken.email_verified` on its return shape. `handleListMemberStays` returns `403 { code: "EMAIL_NOT_VERIFIED" }` when the auth user's email isn't verified — Google sign-in tokens are always verified, so this is effectively a gate on the email/password path. `handleRegisterMember` still creates the `members/{uid}` doc but **skips `linkBookingsByEmail`** when the email isn't verified; the response carries `{ data: { emailVerified: false, linkedBookings: 0 }, warning: "Verify your email to link your past bookings." }` so the client can render a "verify your email" prompt. The router-level check in the `early-checkin-request` branch returns the same 403 before `findBooking` runs; `findBooking`'s `emailMatches` predicate is also hardened to require `user.email_verified === true` as a defensive second line. The client's `signUpWithEmail` now `void`-calls `sendEmailVerification(credential.user)` (non-blocking) immediately after `createUserWithEmailAndPassword`, and a new reusable `<EmailVerifyBanner />` component surfaces the unverified state on `My Stays` + `My Rewards` (replaces the early check-in button while unverified) + `My Profile`, with "Resend verification email" + "I already verified" actions. The auth context gains `resendVerification()` and `refreshAuthUser()`. Idempotency: a user who verifies after signup can re-call `/api/members/register` to trigger the `linkBookingsByEmail` pass — registration is idempotent (`memberNumber` is preserved). No schema change, no rules change, no migration. **Manual QA follow-up** — verify on a real test booking: (a) email/password signup sends a verification email, (b) the banner appears on `/account/profile` immediately, (c) `/account/stays` returns 403 with the "verify your email" error, (d) clicking the verification link in another tab + pressing "I already verified" promotes `user.emailVerified` to `true` and surfaces the linked stays on the next render. Tests: `guest-app/tests/api/members-register.test.ts` (7 tests total — 2 new: unverified stays-list returns `EMAIL_NOT_VERIFIED`, unverified register skips the link batch; 3 existing updated for the new `emailVerified` field). 1526/1526 tests pass. MDs: `plan/features/SPARK-REWARDS.md §Known Issues` HIGH entry closed, `plan/docs/DECISIONS-FEATURES.md #133` added, this entry.

### MEDIUMs (2)

- ✅ **MED-1 — Manual points adjustment is a client-side write, forcing rules to permit out-of-band `rewardsPoints` changes** — **Shipped 2026-07-25** (decision #134, branch `fix/spark-rewards-manual-points-server-side`). Manual adjustment was the only balance-mutation path still on the client SDK; moved to a server API route (`/api/members/adjust-points`) that mirrors `redeem-points` / `undo-redemption` / `set-active`. The client `runTransaction` path in `AdminContext.tsx:2113-2142` is removed; all balance mutations now go through the server and write a `pointsHistory` entry in the same Firestore transaction. As a result, the `members/{uid}` staff-update rule can be tightened so staff clients cannot write `rewardsPoints` directly — the server/Admin SDK is the only path. "Balance always equals sum(history)" is now provable at the rules layer, not just in app code. See `plan/features/SPARK-REWARDS.md §Known Issues` and the new "never do" rule in `plan/docs/GOTCHAS.md §Security & PII`. `plan/docs/DECISIONS-FEATURES.md #134`.
- ✅ **MED-3 — "Different email" reconciliation (build variant: front-desk manual link) — Shipped 2026-07-29** (decision #140, branch `fix/spark-rewards-med-3-link-booking` — supersedes the 2026-07-25 spec-correction-only closure on the front-desk half; see decision #135 for the original audit + spec-correction closure and the cross-reference update). New server endpoint `POST /api/members/link-booking` (`handleLinkBookingToMember` in `guest-app/server/handlers/members.ts`) — admin-only (front-desk 403), Zod-validated `{ memberUid, bookingId, reason (1..500) }`, 10/min/IP rate limit (mirrors `manual-adjust` + `set-active`). Server transaction re-reads the member + the booking, refuses cancelled bookings (400), test-run bookings (400, test-isolation invariant), and bookings already linked to a different member (409, no silent overwrite, no unlink from this surface), then sets `memberId` + `linkedByStaff` + `linkedAt` + `linkedReason` on the booking and writes an audit row under `bookings/audit/records/{bookingId}-link-{timestamp}` (mirrors the erasure audit shape from decision #49). Re-linking a booking to the same member is idempotent (no booking update, audit row still written — `alreadyLinked: true` in the response). The admin member drawer (`admin-app/src/pages/MembersPage.tsx`) gains a **Link Existing Booking** form below the points adjustment form, with the member's email surfaced in the helper copy so the staff can confirm the different-email context before linking. Toasts: success on a fresh link, info on a re-link (alreadyLinked). **Tests** — 14 server tests in `guest-app/tests/api/members-link-booking.test.ts` (auth + method + schema + lookup + booking-state + idempotency + audit row + Admin SDK posture) + 18 source-text tests in `admin-app/src/__tests__/med-3-link-booking.test.ts` pinning the route wiring, the handler contract, the AdminContext function, and the drawer form. 930/930 admin + 544/544 guest tests green, typecheck clean. **Guest self-service prompt half of MED-3 is still deferred** — see the "Deferred" entry directly below.
- ⏸ **MED-3 — "Different email" reconciliation (guest self-service half) — Still Deferred.** The guest self-service prompt ("We found bookings under a different email. Would you like to link them?") on the `/rewards` join surface / post-sign-in / post-booking confirmation is the larger of the two MED-3 paths. **Workaround** — guest can still find the booking at `/my-booking` (ref + email); staff can now also link it from the member detail drawer (the front-desk manual link above covers the common case without the guest needing to involve the front desk). `plan/docs/AUDIT-SPARK-REWARDS-REPORT.md §MED-3` and decision #135 + #140.

### LOWs (7)

- ✅ **LOW-1 — Enrollment consent disclosure inconsistent across join surfaces** — **Shipped 2026-07-25** (decision #136, branch `fix/spark-rewards-lows-batch`). The one-click "Enroll" button on `/rewards` now mirrors `ProfilePage` with a Privacy Policy + Terms of Service disclosure line below the button. All three enroll surfaces (Rewards landing one-click, `/signup` checkbox, `ProfilePage` adjacent link) now use the same `/privacy` + `/terms` copy, so the join-action consent disclosure is consistent regardless of which surface a guest uses. `plan/features/SPARK-REWARDS.md`.
- ✅ **LOW-2 — Guest-app auth persistence relies on Firebase default, not set explicitly** — **Shipped 2026-07-25** (decision #136). `guest-app/src/firebase/config.ts` now pins `setPersistence(auth, browserLocalPersistence)` at module-load. The contract is explicit; an SDK default change can no longer silently regress "stay signed in." Admin app keeps its asymmetric `browserSessionPersistence` (front-desk security stance).
- ⏸ **LOW-3 — `linkBookingsByEmail` batch not chunked to Firestore's 500-write limit** — **Deferred (theoretical only)**. A single-hotel guest with >500 same-email matches is not realistic at 14-room scale. Revisit if `linkBookingsByEmail` is ever re-pointed at a multi-property or federation-level surface. No code change.
- ✅ **LOW-4 — Divergent, dead earning formula in `shared/utils/points.ts`** — **Shipped 2026-07-25** (decision #136). The per-100-block `calculateEarnedPoints` shared helper that differed from the live `calculateCheckoutPoints` was deleted. The My Rewards "Earn N points per ₱100 spent" copy was aligned with the live proportional (fractional-₱100) crediting so a future wiring of the helper back into a "you'll earn X" estimate can't reintroduce the mismatch.
- ✅ **LOW-5 — My Stays page does not surface early check-in status** — **Shipped 2026-07-25** (decision #136). `guest-app/src/pages/StaysPage.tsx` now renders an early check-in status line per booking, mirroring `RewardsPage`. Both My Stays and My Rewards now surface the same request/approved/declined state on the relevant booking.
- ✅ **LOW-6 — Spec calls member export "CSV"; shipped export is XLSX** — **Shipped 2026-07-25** (decision #136, spec-wording correction). `plan/features/SPARK-REWARDS.md:147` reworded from "Export members list as CSV" to "Export members list via the full data backup (XLSX) export" so the spec matches the shipped artifact. The audit's "includes member data correctly" requirement is satisfied.
- ✅ **LOW-7 — Erasure placeholder email not covered by the send-skip guard** — **Shipped 2026-07-25** (decision #136). The send-skip guard in `guest-app/server/handlers/email.ts:391` now also matches the erasure placeholder (`@invalid`) in addition to the existing `@example.invalid` check. Standardized erased bookings on `@example.invalid` to keep the guard single-sourced.

### Manual QA follow-up (still open, real-device)

- **HIGH-1** — verify on a real test booking: (a) email/password signup sends a verification email, (b) the banner appears on `/account/profile` immediately, (c) `/account/stays` returns 403 with the "verify your email" error, (d) clicking the verification link + "I already verified" promotes `user.emailVerified` to `true` and surfaces the linked stays on the next render.

---

## Recently shipped (off-roadmap)

> Quick log of `feat:` / `fix:` commits that weren't on the roadmap when they shipped, so the file still reflects what's actually in the code. Format: `YYYY-MM-DD — short title — commit hash — branch.`

- **2026-07-31 — emulator harness wired into `npm test` + first behavioral test (PMH-03)** — `f9814dd` (merge `b4a4f3d`), `fix/pmh-03-emulator-harness` → `dev` (v0.192.0). Three changes: (1) root `package.json` — `test` now invokes `test:rules` (emulator) as the final step, so the full chain runs on every default test invocation; new `test:fast` script keeps the source-text-only inner loop under 30s and skips Java. (2) `vitest.rules.config.ts` — second glob `firebase/tests/**/*.emulator.test.ts` joins the existing `*.rules.test.ts`; two test categories, one harness. (3) First behavioral test: `firebase/tests/room-types-array-write.emulator.test.ts` pins the RTS-01 fix at the Firestore layer — N concurrent setDoc calls on the `roomTypes[]` array field lose all but the last write's change (the bug), a single batched setDoc with the fully-computed next array persists every type's change (the fix). The pattern (seed via admin context, exercise the write, assert the contract) is the template for PMH-05 and any future money/write-path test. `plan/docs/CONTRIBUTING.md §Testing` documents the three test layers (source-text / rules / emulator), the `*.emulator.test.ts` pattern, the script inventory, and the Java + Firebase emulator requirement. **Java required to actually execute** — Daniel verifies with `npm test` on his machine. The harness loads `firestore.rules` automatically, so any rule change that breaks a write will surface in the emulator test too.
- **2026-07-31 — rates matrix batched write + room-type delete failure propagation (RTS-01/02/04)** — `1f07946` (merge `608b06c`), `fix/rates-room-type-save-and-delete` → `dev` (v0.191.1). Rates tab "saved" but only one type's rates persisted (RTS-01: N concurrent `updateRoomType` writes raced on the shared array field — merge replaces the field wholesale, so the last write won and every other type's edit was silently dropped). Room type deletion "didn't work" — row vanished, page refresh brought it back (RTS-04: `saveRoomTypes` swallowed `updateSettings` failures and never rolled back the optimistic `setRoomTypes`). Fix: `AdminContext.saveRoomTypes(types)` bulk primitive (captures `previousTypes`, checks the boolean return of `updateSettings`, throws + rolls back on every failure path); `RatesPage.handleSaveRates` calls it once with the fully-computed next array. All callers already wrap the call in try/catch or return `{ success, error }`, so the new throw lands safely. **RTS-03 reproduced same day on staging** — the deletion "didn't work" symptom was RTS-04 (silent failure swallow), not RTS-05/06. **RTS-05 (3s auto-disarm on the two-step confirm) and RTS-06 (deleting the last type resurrects seed defaults) are real defects but NOT the active user-visible bug**; both remain open as follow-ups, cheap (~30 lines each; `ConfirmForm` for RTS-05, "loaded vs empty" check for RTS-06). New `admin-app/src/__tests__/rts-01-rates-save.test.ts` with 8 source-text guards on the new contract; pre-existing `phase-11.9-room-type-rates` test rewritten to pin the new shape. 938/938 admin + 560/560 shared + 202/202 guest + 6/6 infra tests green; typecheck clean. Docs: `plan/features/RATE-MANAGEMENT.md` (single-batch write contract on the UI + Data & Logic checklists), `plan/features/SETTINGS.md` Room Types cross-reference (delete guard + confirm behavior + single-batch write contract), `plan/docs/GOTCHAS.md §Firebase` (reusable lesson: **never fan out per-item writes to a single Firestore array field — compute the whole array and write once**; applies equally to `paymentMethods[]` per WPM, `bookingSources[]` per NBS-04, and `seasonalRateOverrides[]`).
- **2026-07-24 — payment reference number unified onto the payment ledger** — `refactor/unify-payment-reference-fields` → `dev`. Dropped the guest-entered `Booking.paymentReferenceNumber` field from the booking model + the public `/book` and `/corporate/book` forms; the canonical reference is now exclusively the `OnsitePayment.transactionReference` on the relevant `bookings/{id}/payments/{paymentId}` entry, staff-populated at verify time via the existing Verify & Record Payment / Record Payment modals. The booking drawer's inline "Original booking payment reference" edit input was removed; the sticky header now shows a read-only "Reference" cell sourced from the latest ledger entry via a shared `getLatestPaymentReference(booking)` helper (in `BookingsPage.tsx` and `BookingDrawerWorkspace.tsx`). Reports exports, dashboard pending-payment badges, the `payment-rejected` email's "Reference on file" callout, and the bookings table search all read the same source. Firestore rules' staff update allowlist for `bookings/{id}` lost the `paymentReferenceNumber` entry (the canonical reference lives on the append-only payments subcollection). 872/872 tests + 6/6 infra tests green; typecheck clean. Docs: `plan/features/BOOKING-FLOW.md` (Step 3 bullets), `plan/features/BOOKINGS-MANAGEMENT.md` (full §Payment Reference Semantics rewrite), `plan/docs/BACKEND.md` (bookings + payments subcollection rows), `plan/docs/TYPES.md` (OnsitePayment gains `transactionReference`), `plan/docs/SECURITY.md` (staff allowlist), `plan/docs/DECISIONS-FEATURES.md #124` (new decision). Branch not yet merged; awaiting pre-launch sign-off.
- **2026-07-24 — every email + QR link respects the current environment** — `aef6b26`, `fix/env-aware-emails-and-qr` → `dev`. Before this fix, every link emitted by the server (`siteUrl()` / `adminUrl()` in `email.ts`) was hardcoded to `https://www.<config.domain>`, and the admin QR generator hardcoded `https://<config.domain>/intercom/...`. The fix adds `guest-app/server/lib/siteUrl.ts` with `getServerBaseUrl()` / `getServerAdminBaseUrl()` (VERCEL_ENV-based resolution + `SITE_URL` / `ADMIN_SITE_URL` env-var override for white-label) and routes the admin QR through the existing `getApiBaseUrl()` helper. SEO canonical + OG image URLs stay hardcoded to production by design. 19 new tests (`guest-app/tests/api/site-url.test.ts`, `admin-app/src/__tests__/env-aware-qr-intercom-url.test.ts`). Docs: `plan/docs/ENV-SETUP.md` (new `SITE_URL` / `ADMIN_SITE_URL` env vars), `plan/features/EMAIL-PDF-STORAGE.md` (env-aware email note), `plan/features/QR-MANAGEMENT.md` (new "QR URL env-awareness" section + operational rule that real printable QRs must be generated from the production admin).
- **2026-07-24 — modal/drawer/sidebar backdrop now sits above any nested panel (MBZ)** — `6731aea`, `fix/modal-backdrop-z-index` → `dev`. (Logged here for the off-roadmap fix pre-MBZ-proposal timeline; the structured MBZ-01..04 entries are now ✅ in §Phase 12.)
- **2026-07-24 — booking drawer hidden behind its own backdrop (MBZ-01..04 inversion regression)** — `1507a88`, `fix/mbz-z-index-regression` → `dev`. The first MBZ attempt set every backdrop to `z-[60]` while leaving every panel at `z-50`; in a single stacking context, higher z-index wins regardless of DOM order, so every overlay's own backdrop then covered its own panel and the booking drawer disappeared behind the fade. The fix replaces the single-z-bump with a **two-tier z-index system**: tier 1 (`z-50`) for Drawer + mobile Sidebar (backdrop + panel share `z-50`, DOM order keeps panel on top of its own backdrop), tier 2 (`z-[60]`) for Modal (same pattern at the higher tier). The modal tier sits one step above the drawer/sidebar tier, so a modal that opens on top of a drawer still fades the drawer panel across the full viewport. Regression test in `admin-app/src/__tests__/modal-backdrop-z-index.test.ts` rewritten (7 tests, with explicit guards against the `z-[60]`-backdrop + `z-50`-panel inversion). `plan/admin-app/CLAUDE.md §Z-Index Scale` rewritten to document the two-tier model + anti-pattern.
- **2026-07-24 — guest registration save threw `Unsupported field value: undefined` on every submit (GCR-01 follow-up)** — `30cf918`, `fix/gcr-undefined-other-purpose` → `dev`. GCR-01 (commit `6c2273b`) introduced the purpose-of-stay dropdown; the submit handler built the `guestRegistration` object with `otherPurpose: purposeOfStay === "other" ? otherPurpose : undefined`, and Firestore's `updateDoc` rejects `undefined` as a field value — so every Save Registration click threw a hard error. The form's `onSubmit` wrapper also flips the form to the read-only summary view, so the error got masked behind a UI flicker. Fix: spread the `otherPurpose` key in only when the staff picked "Other" AND entered a reason, so the key is OMITTED (not `undefined`) from the persisted object. Mirrors the conditional `corporate` block in the public bookings-create payload. Regression test in `admin-app/src/__tests__/purpose-of-stay-registration.test.ts` now actively forbids the `: undefined` shape and pins the `...(... ? { otherPurpose } : {})` pattern.
- **2026-07-24 — registration PDF generator hung on "Preparing registration PDF..." with no upper bound (HEIC fix follow-up)** — `96abf74`, `fix/registration-pdf-outer-timeout` → `dev`. The 2026-07-24 HEIC/format fix (commit `7faf705`) bounded the image-decode step with a 5s timeout, but `printRegistrationPDF` has four OTHER awaits that were not individually bounded: `getPdfBrandLogoDataUrl` (fetch + FileReader), `getBlob` from Firebase Storage, the pre-decode FileReader in `normalizePdfImageToJpeg`, and `canvas.toDataURL` (sync but can be slow on large images). If any of these hung (slow Storage network, slow Vercel edge for the brand asset, etc.), the outer `try/catch` never fired, the `toast.error(...)` never ran, and the placeholder tab stayed open forever — exactly the "stuck on Preparing registration PDF..." symptom. Fix: wrap the body in an IIFE (`buildAndOpen`) and race it against a 20s timeout; the `setTimeout` handle is cleared in a `finally` so a fast successful run doesn't leave a dangling timer. Regression test in `admin-app/src/__tests__/pdf-generation-repair.test.ts` pins the `buildAndOpen` IIFE shape, the `Promise.race` call, the timeout message, and the `clearTimeout`-in-finally pattern.
- **2026-07-24 — Firebase Storage CORS allowlist missing staging hosts — `getBlob` blocked on every staging read (PDF outer-timeout follow-up)** — `fb1c0a3`, `fix/storage-cors-staging-hosts` → `dev`. The 2026-07-24 PDF outer-timeout fix (commit `96abf74`) closed the user-visible hang (placeholder tab closes within 20s) but the underlying root cause is CORS: the `spark-inn-stg-7a7ad` Storage bucket only had production + localhost origins in its allowlist, so when the admin app running at `stg-admin.sparkinnbohol.com` called `getBlob()` on a guest-ID photo, the browser blocked the cross-origin XHR with `No 'Access-Control-Allow-Origin' header is present on the requested resource`. The Firebase SDK retries the read, which is why it took ~20s instead of failing fast. Fix: add `https://stg.sparkinnbohol.com`, `https://stg-admin.sparkinnbohol.com`, and the `https://*.vercel.app` preview wildcard to `firebase/cors.json`. New `firebase/tests/storage-cors.test.ts` (6 tests) pins the production/staging/preview/local origin allowlist + GET/PUT methods, wired into `npm test` via the new `test:infra` script. **Operational note:** the commit updates the source of truth only — the live bucket still has the old config. Deploy with `npx tsx scripts/set-storage-cors.ts` (reads service-account creds from `guest-app/.env`). Without that deploy, the new allowlist won't reach the bucket and the PDF will still hang on staging.
- **2026-07-28 — walk-in booking modal scrollability bug MD kept in sync + regression test pinning the shipped fix** — `docs/phase-12-walkin-modal-bug-status` (pending). The walk-in modal scrollability bug reported on 2026-07-08 in `plan/bugs/walkin-booking-modal-scrollability.md` was actually fixed the same day in commit `2d99946` / `a249a3e` (`fix/walkin-booking-modal-scrollability` → `dev`), but the bug MD's status field was never updated and the fix shipped without a regression test. This commit (a) marks the bug MD as ✅ Shipped with the full resolution writeup (root cause, the six shipped changes, regression-coverage map, manual-QA follow-up checklist), and (b) adds `admin-app/src/__tests__/walkin-modal-scrollability.test.ts` (10 source-text tests) pinning the contract so a future refactor that re-introduces the `h-full max-h-full` shape, the missing `min-h-0`, the inline `style={{ maxHeight: ... }}`, the action row inside the `<form>`, or the `id`/`form` wiring failure fails CI. 912/912 admin tests green, typecheck clean. (The pre-archive ROADMAP entry for this fix lives in `plan/project/archive/ROADMAP-ARCHIVE-2026-07-17.md`; this off-roadmap entry is the post-archive pointer to the bug MD + regression test so the next reader doesn't have to dig through the archive to see the fix is locked down.)

---

## References

- **Archive (historical, non-canonical):** `plan/project/archive/ROADMAP-ARCHIVE-2026-07-17.md` — pre-compaction roadmap: Phases 0–9, 11.5/11.6 batches 1–20, Wave 1–4 decision triage, 11.7 record, PR 1/3 detail, closed audit-fix lists (FIN, FR, FL, PF, QA, NC, AUD, SA, live bugs).
- Audit reports: `plan/project/AUDIT-*.md` (historical) · `plan/docs/AUDIT-E2E-REPORT.md` (current, open MED/LOW).
- Decisions: `plan/docs/DECISIONS-ARCH.md` + `plan/docs/DECISIONS-FEATURES.md`.
- Cutover: `plan/project/PROD-CUTOVER-RUNBOOK.md` · Deploy: `plan/project/DEPLOY.md`.
