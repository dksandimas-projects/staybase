import { describe, test } from "vitest";

describe("/api/members/redeem-points", () => {
  test.todo("rejects insufficient balance");
  test.todo("updates booking total, member balance, and points history together");
  test.todo("rejects undo after check-in");
});
